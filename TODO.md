# DAAD Harvester — Preservation Work Register

> **Status baseline:** The latest local deterministic release gate reports **319 passing tests**, **254 sources**, **784 retained artifacts**, nine hash-pinned native-regeneration entries with 27 inputs and 17 outputs, complete 17-field artifact checksums, a verified public-artifact manifest, and a passing documentation-integrity check. The Pages deployment pipeline stages only manifest-approved retained bytes, verifies copied-byte SHA-256 values, and performs a post-deployment smoke check. This document is the authoritative forward roadmap; it replaces earlier audit notes that described superseded implementations and obsolete test counts.

## Preservation standard

DAAD Harvester must never label a source, container, interpreter, game database, platform, release, compiler generation, or derivative as verified unless the stored evidence supports that exact claim. A feature is complete only when it has a documented format contract, bounded implementation, deterministic negative and corruption tests, real-artifact validation where public media is available, provenance output, and a clear user-facing statement of any remaining boundary.

## Deferred external-source queue (not part of the active task plan)

- [ ] When the Z80 reverse-engineering methodology workstream is scheduled, inspect [ZornsLemma/py8dis](https://github.com/ZornsLemma/py8dis) as a potential independent static-analysis comparator. Record its pinned revision, supported input assumptions, emitted artifacts, reproducibility boundaries, and comparison results against repository-native analysis before relying on any output.
- [ ] When the historical DAAD source-catalog research workstream is scheduled, inspect [Jose Luis Cebrian’s 2023 post](https://x.com/jlcebrian/status/1700781994890678756?s=20) as a lead only. Preserve the exact cited claim and access date, locate independent corroboration, and do not promote a format, interpreter, or provenance assertion from a social-media post alone.
- [ ] When the Atari ST platform-bundle workstream is scheduled, inspect [Uto’s Atari ST and DAAD article](https://medium.com/@uto_dev/the-atari-st-and-daad-9c38ab7e6446) alongside primary manuals, retained release bytes, and independent runtime evidence. Convert only cross-validated findings into an ST profile dossier and native parser/validator requirements.
- [ ] When the PC/DOS interpreter-comparison workstream is scheduled, inspect [Utodev/PCDAAD](https://github.com/Utodev/PCDAAD) as an independent implementation and behavior comparator. Pin the revision and retain only permitted source/evidence metadata; compare its DDB, graphics, and runtime claims against native Harvester results and historic bytes before promotion.
- [ ] When the post-active MSX interpreter-comparison workstream is scheduled, inspect [nataliapc/MSX2DAAD](https://github.com/nataliapc/msx2daad) as a modern independent implementation. Keep it distinct from the active R4 historical `DAAD.MDG` and `YOURGAME.COM` investigation unless new evidence is independently verified; pin revision, file-loading model, and all comparison boundaries.
- [ ] When the ZX Spectrum interpreter-comparison workstream is scheduled, inspect [cronomantic/ZXDAAD128](https://github.com/cronomantic/ZXDAAD128) as an independent implementation/reference. Pin the revision, document machine/load assumptions and supported profiles, and use it only as a comparator until native Harvester validation and real-artifact evidence establish a claim.
- [ ] When the ZX Spectrum reverse-engineering workstream is scheduled, inspect [skoolkid/skoolkit](https://github.com/skoolkid/skoolkit) as an independent Z80 analysis/disassembly comparator. Pin the revision and each control/data classification configuration; compare its output against py8dis, architecture-aware disassemblers, retained original bytes, native extraction/parsing results, and emulator observations where applicable. Preserve agreement, disagreement, false-positive, and unknown-region records rather than selecting a preferred output without evidence.
- [ ] When the ZX DAAD catalog-and-corpus workstream is scheduled, inspect the [Spectrum Computing DAAD entry](https://spectrumcomputing.co.uk/entry/30013/ZX-Spectrum/DAAD) as catalog/discovery evidence only. Capture the accessed record, cited release facts, and available technical media separately; cross-check every claim against primary documentation, retained release bytes, independent catalogs, and multi-tool analysis before adding a corpus, format, interpreter, or platform support assertion.
- [ ] When the MSX platform-research workstream is scheduled, inventory the repositories surfaced through [Konamiman](https://github.com/Konamiman) and select only relevant, pinned MSX-DOS, filesystem, emulator, or technical-reference material. Record repository, revision, license/provenance boundary, and exact claim; compare every promoted behavior against retained bytes, primary documentation, native parsing, and at least one independent implementation.
- [ ] When the contemporary MSX corpus/discovery workstream is scheduled, inspect [xresende/msxdev](https://github.com/xresende/msxdev) for release, toolchain, and platform-context leads. Treat catalog or build metadata as discovery evidence only, preserve access/revision details, and require retained original media plus independent technical corroboration before asserting any DAAD-format or interpreter relationship.
- [ ] When multi-architecture static-analysis comparison is scheduled, evaluate [redasm-dev/redasm](https://github.com/redasm-dev/redasm) as a separately pinned analyzer. Record supported Z80/MSX target assumptions, loader configuration, output ranges, and control/data decisions; compare them against Ghidra, radare2, py8dis, SkoolKit, native evidence, and retained bytes with an explicit disagreement ledger.
- [ ] When the MSX reverse-engineering source-survey workstream is scheduled, inspect [retroabandon/msx-re](https://gitlab.com/retroabandon/msx-re) as an independent source lead. Pin the exact revision and retain cited findings separately from conclusions; verify each claimed load model, format structure, or symbol boundary through raw-byte comparison, complementary tools, primary documentation, and controlled runtime evidence where appropriate.
- [ ] When the Z80 disassembly comparison workstream is scheduled, evaluate [lvitals/z80dasm](https://github.com/lvitals/z80dasm), [sarnau/Z80DisAssembler](https://github.com/sarnau/Z80DisAssembler), and [maziac/z80dismblr](https://github.com/maziac/z80dismblr) as three independent disassembly candidates. Pin version and command/configuration for each run, compare instruction decoding and control/data partitioning against Ghidra, radare2, py8dis, SkoolKit, and original bytes, and retain every material disagreement as a reproducible analysis record rather than normalizing outputs by preference.

## Active evidence-interface correction

- [x] Build and maintain a deterministic active-backlog index that groups every unchecked work-register item by evidence input, affected subsystem, dependency, required real-artifact regression, regeneration/report impact, and atomic-delivery boundary. `scripts/build_active_backlog_index.py` deterministically regenerates `preservation_corpus/active_backlog_index.json` from every unchecked item without promoting broad requirements from child evidence; focused regression coverage protects stable IDs, sections, boundaries, and report impact.
- [ ] Add and enforce a portable agent-continuation contract that anchors the repository-owned scheduler and backlog index in Git, requires every verified bounded change to be committed and pushed to the configured remote, preserves all pending tasks and handoff state, exposes a machine-readable contract for independent agents, and replaces agent-private scheduling with the versioned project workflow without storing credentials.
- [x] Classify extensionless Commodore 64 BASIC `SYS` PRG members from validated load-address and tokenized-line structure, not source labels or filenames. Native inspection now requires the `0x0801` BASIC load address, a tokenized SYS line, ASCII decimal target, consistent next-line pointer, and terminal BASIC null link; `JABATO P.1` and `JABATO P.2` both validate as `c64-basic-sys-prg` invoking `SYS 2061`. Fingerprinting persists verified C64 media-platform evidence while preserving `is_daad_payload=false` and no interpreter identity; a malformed non-decimal SYS target is rejected. Report and public-manifest data were regenerated, and the presentation/linkage task remains active because runtime-derived DDB evidence must remain distinct from the launcher byte.
- [x] Repair every retained game-card and TUI artifact view affected by the extensionless C64 PRG classification. Static report matrix and TUI now identify the byte as a validated C64 BASIC SYS launcher, state that it is not itself a DDB, preserve a null runtime-DDB link unless a separate evidence record is explicitly available, and retain the non-correlation boundary.
- [ ] Close companion-resource support as a first-class DAAD requirement: every retained `.CHR`, `.DAT`, `.PIC`, and `.DDB` family must have a profile-scoped byte contract, native parser/validator, corruption and rejection behavior, complete checksum/provenance record, real-artifact regression, deterministic regeneration command, explicit platform applicability, and report/TUI/web explanation. No resource file may be described as understood solely from its extension, filename, or an external tool.
- [ ] Decode and validate legacy DAAD `.DAT` resource tables completely per observed profile: establish table headers, byte order, entry width, offsets, flags, media kinds, length/codec boundaries, and cross-file references to `.PIC`, audio, and companion data. Retain every unexplained entry or payload region as a profile-specific support loop until its grammar and rendering/loading semantics are evidenced.
- [x] Resolve the retained Torreoscura PCW DAT V1 directory, resource-header, and image-decoding profile. Pinned ADP `src-common/dmg.cpp:1174–1334` defines the little-endian machine-0, mode-4 header, bounded picture count, fixed 256×10-byte directory at `0x0006`, and `0x0A06` payload floor. Native inspection validates those rules, directory offsets, coordinates, flags, and declared-count consistency for `PARTE001.DAT` (20 resources) and `PARTE002.DAT` (10 resources). Each referenced resource validates its six-byte little-endian internal header: width/compression flag, height/audio flag, payload length, dimensions, and bounded payload span. Native code implements ADP’s independently inspected PCW byte-stream decompression, monochrome storage-layout reconstruction, and four-bit packed-pixel expansion for all 30 retained image resources (24 compressed and 6 raw, each 640×72 and 23,040 packed-pixel bytes). Corrupt offsets, payload overrun, and decoder failures are rejected. The corpus/report/public-manifest outputs are regenerated; `PARTE000.PIC` and cross-file DDB/PIC linkage remain active support loops.
- [ ] Resolve the standalone Torreoscura PCW `PARTE000.PIC` grammar. The retained file is exactly 16,000 bytes and has no promoted header; its size is compatible with several possible bitmap geometries but does not establish one. Independent PCW video-controller documentation describes a native 720×256 monochrome image with roller-RAM addressing, whereas 16,000 bytes equals neither that full frame nor the validated 640×72 DAT resource geometry. Do not apply the DAT decoder or claim a raw PCW screen. Obtain a documented game-specific loader path, a second independently corroborating release, or a bounded byte grammar with negative controls before implementation.
- [ ] Decode and validate legacy DAAD `.CHR` and `.PIC` resources completely per observed profile: establish glyph geometry, character/code-page mapping, pixel packing, palette/attribute behavior, picture addressing, compression, frame/plane structure, and DDB/DAT reference semantics. Require native reconstruction/rendering or exact blocking-byte records with real-artifact and corruption regressions before publishing a support claim.
- [x] Resolve the retained Torreoscura PCW ADP-writer CHR header profile. Pinned ADP `src-tools/tool_chr.cpp:562–599` writes the fixed 2,176-byte container, a padded uppercase filename stem, `CHR` tag, marker `0x02`, glyph-height byte `0x08`, and little-endian trailer constant `0x0224`; its paired loader reads the 2,048-byte glyph payload from `0x80`. Native validation now recognizes only the complete demonstrated conjunction as `adp_legacy_chr_writer`, while other 128-byte prefixes remain explicit non-promoted bytes. Real retained `PARTE001.CHR` and `PARTE002.CHR` match the profile with a `D1` stem and 256 × 8 × 8-bit glyph geometry; the corpus/report/manifest are refreshed, and the complete primary workflow passes with 312 tests. Unidentified prefix fields, custom glyph code-page semantics, cross-runtime rendering, atlas derivatives, and all PIC grammar remain active support loops.
- [x] Recognize the official R4 source-250 `PART1.CH0` companion resource as a validated legacy CHR container. Its 2,176 bytes are exact-hash identical to retained `PARTE001.CHR` (SHA-256 `715c0a6e717e9475d80cf5a6c2afbb7bff76dcdc235435bc51cebe02cbc6c6ba`) and satisfy the already validated ADP-writer header profile: `D1` stem, `CHR` tag, writer marker `0x02`, glyph height `0x08`, and 2,048-byte payload at `0x80`. Native inspection now routes `.CH0` through the same bounded parser; a real-artifact regression asserts exact duplicate identity and validated 256 × 8 × 8-bit geometry. This does not equate other CH0-like, FNT, SDG, or arbitrary font extensions with the profile.
- [ ] Build a platform-bundle evidence model for every retained DAAD release. For ZX Spectrum, Amstrad CPC, C64, Plus/4, MSX, PCW, Atari ST, Amiga, and IBM PC/DOS, enumerate each DDB’s associated interpreters, launchers, loaders, fonts, graphics, resource tables, audio, executable wrappers, tape/disk/cartridge media, compression, and platform-specific metadata. Every relationship must state whether it is byte-measured, structurally validated, runtime-observed, catalog-only, or unresolved; identical extensions across targets must never be assumed semantically equivalent.
- [ ] For every platform bundle, implement deterministic cross-file reference validation between DDBs and their companion resources where the profile grammar establishes one. Persist validated references, absent/optional resources, unresolved references, and corruption outcomes in the corpus/report schema; add profile-scoped real-artifact regressions and reject source-label-only bundle claims.
- [ ] Publish decoded-media derivatives only after native profile verification. For every supported image or audio resource, generate a deterministic browser-safe preview derivative from the retained original bytes, record the exact decoder profile/version, original and derived paths, complete checksums, dimensions/duration/codec metadata, provenance, validation status, and regeneration command. Original bytes remain separately downloadable; a failed, partial, heuristic, or unsupported decode must remain visible as a non-playable/non-renderable evidence record rather than receive a fabricated preview.
- [ ] Add accessible image and audio evidence views to the generated web report and equivalent TUI inspector. Each verified image preview and audio playback control must be reachable from its artifact, game, source, platform bundle, and checksum deep links; disclose the native decoder, profile, rendered/payload boundaries, original download, derived-preview checksum, and unresolved companion relationships. The default view must never autoplay media, hide the original byte record, infer audio/image semantics from an extension, or show a preview without verified decoder evidence.
- [ ] Publish verified font and character-set evidence as first-class companion resources. For every natively validated CHR, FNT, CH0, SINTAC, or platform-specific font profile, deterministically render a glyph atlas and per-glyph inspection view from the original bytes; record glyph geometry, character index/code-page mapping, bitmap/plane layout, header semantics, decoder profile/version, original and derived checksums, provenance, and DDB/runtime/bundle relationship. Unknown header fields, width tables, encodings, or target-specific rendering behavior must remain explicit rather than being normalized or guessed.
- [ ] Resolve the two official R4 source-250 896-byte `DAAD.FNT` variants as separate byte profiles. `depth2_f0f7416a_DAAD.FNT` is all `E5` bytes and remains a retained non-font claim; `depth2_dcd3ab68_DAAD.FNT` is a populated legacy candidate beginning with a `DAAD`/`FNT`-like record. Neither satisfies ADP/PCDAAD’s independently observed 6,672-byte `JSJ SINTAC FNT3` profile. Acquire a same-generation sample or loader/writer evidence, establish field boundaries, glyph and proportional-width semantics, corruption behavior, and real-artifact regression before native decoding or publication. The exact inventory is recorded in `reverse_engineering/derived/fnt/r4_fnt_evidence_inventory.md`.
- [x] Validate the official source-250 CPC `DAAD.FNT` container without overclaiming its glyph grammar. The populated 896-byte member exactly matches the official classic CPC tape-loader font, passes a bounded AMSDOS binary-header check (type `0x02`, load address `0x9378`, matching 768-byte logical/real/stored payload sizes, and checksum), and is natively recognized as `daad-cpc-loader-fnt`. A header-byte corruption rejects; the separate all-`E5` member remains an explicit unrecognized FNT profile. The complete glyph payload grammar, character mapping, rendering, and all noncanonical FNT variants remain active tasks.
- [ ] Add accessible font and glyph views to the web report and TUI. Each verified glyph atlas must provide artifact/game/platform/checksum deep links, original-byte download, derived-preview checksum, keyboard-accessible glyph navigation, rendering-profile disclosure, and explicit distinction between custom game glyphs, system/runtime fonts, and merely retained opaque font candidates. The interface must not substitute a browser font, render a guessed glyph ordering, or display an unvalidated asset as a decoded character set.
- [ ] Publish profile-scoped DDB decompilation evidence in the web report and TUI. For every decompiled artifact, expose stable readable IR/source output, byte spans and section/provenance links, profile/version/platform, exact unrecognized or blocked constructs, compiler/recompiler inputs and outputs, all checksum comparisons, and byte-identical or bounded-difference status. A visual decompilation view must not turn an unsupported semantic region, unverified third-party output, or non-byte-identical recompilation into a claim of complete source recovery.
- [ ] Publish verified interpreter-analysis evidence in the web report and TUI. For every retained interpreter binary, expose immutable original-byte identity, architecture, load/base assumptions, analyzer/tool version and configuration, raw disassembly, derived decompilation or pseudocode where available, symbols, entry points, control-flow exports, output hashes, and links to exact comparison inputs. Each view must label generated analysis as derived output rather than recovered source and distinguish exact-hash profile identity from filename, bundle, or behavioral evidence.
- [ ] Add accessible interpreter-analysis comparison views. A user must be able to navigate from an interpreter profile, platform, game bundle, or analysis artifact to the outputs of multiple compatible analyzers, their shared/differing code ranges, known symbols, tool failure/rejection states, and cross-check evidence against runtime behavior and DDB loading. The web and TUI must never flatten disagreement into one authoritative pseudocode listing or imply equivalence between binaries that differ by hash, load model, or architecture.
- [ ] Gate interpreter-analysis publication in the primary workflow and CI. Each published analysis must regenerate from a committed original or explicitly authorized derived input with fixed architecture/base/tool configuration, verify original and output hashes, retain failure logs, reject unsupported tool/profile combinations explicitly, and validate every web/TUI deep link and provenance label. Any analysis lacking reproducible inputs, an architecture/load declaration, or a comparison boundary must remain unavailable for display rather than silently omitted or overstated.
- [ ] Redesign the web evidence explorer around progressive, relationship-first navigation. It must offer readable project orientation, platform and game/port entry points, persistent evidence filters, full-by-default bundle trees, artifact/resource previews, analysis readers, checksum/provenance panels, and stable shareable routes that preserve the selected section and filter state. The visual hierarchy must distinguish original bytes, decoded derivatives, decompilation outputs, structural evidence, runtime observations, unresolved blockers, and catalog-only facts without hiding any retained record behind summary cards.
- [ ] Redesign the TUI to provide functional parity with the web evidence explorer while remaining terminal-safe. It must support keyboard-first navigation among game bundles, resources, original/derived/blocked evidence, media and font previews where terminal-capable, decompilation and interpreter-analysis readers, complete checksum/provenance inspection, search/filter state, deep-link-equivalent artifact identifiers, and clear recovery paths. The TUI must not reduce, fabricate, or silently omit evidence solely because a graphical preview is unavailable.
- [ ] Add interface-quality and accessibility regressions for web and TUI evidence workflows. Tests must exercise direct game/source/artifact/checksum/resource/analysis routes, keyboard navigation, focus order, filter persistence, mobile and narrow-terminal layouts, readable contrast, original-versus-derived labelling, unavailable/blocked states, download boundaries, and error recovery. The CI build must reject UI/schema/report drift that makes a validated evidence record inaccessible or a non-claim appear as a supported feature.
- [x] Correct the resource-explorer first-viewport layout observed against real report data. The initial wide-table implementation compressed native evidence and publication-boundary text into unreadable narrow columns at desktop width, forcing horizontal scrolling before the user could understand a record. It now uses responsive evidence cards that retain semantic table headers for assistive technology while exposing role, original retained artifact, parser status, publication boundary, and keyboard-accessible detail action in the default viewport. A browser check against the staged 730-artifact report confirmed the two-column desktop layout is legible without horizontal movement; narrow layouts collapse to one card column.
- [ ] Gate decoded-media and decompilation publication in the primary workflow and CI. The gate must regenerate every derived preview and decompilation view from committed originals and native code, verify complete checksums and manifest membership, reject stale/missing/out-of-bounds derivatives or UI links, confirm accessible web/TUI navigation, and pin real-artifact plus corruption/negative controls per profile.
- [x] Resolve static-report log nondeterminism observed during the interface increment. Reproduced failure: although mutable live logs were removed from the regeneration-manifest inputs, `report_export.py` still read their last lines into `report_data.json`, causing `regenerate_preservation_report.py --check` to fail after another run. The exporter now publishes a fixed, explicit boundary explaining that live operational and game log tails are excluded from the deterministic static report; the viewer retains this disclosure without treating local telemetry as portable evidence. A regression mutates both live logs under a fixed report timestamp and proves byte-identical output. The report and contract hash were refreshed, the manifest verifier passes all 9 native entries, and `python scripts/run_primary_workflow.py` passed all gates with 314 tests.
- [ ] Enforce open-world resource discovery for every retained platform bundle. Every extracted byte, embedded region, filesystem member, tape block, disk extent, executable segment, archive entry, and cross-file reference must enter a deterministic inventory with observed name, byte range, complete checksums, parent lineage, candidate role, platform/profile context, parser outcome, and evidence state. Unknown, extensionless, encrypted, compressed, or structurally anomalous resources must create a retained support loop with a reproducer; they may not disappear into generic “other” storage or be omitted because their family was not anticipated.
- [ ] Build a complete resource relationship graph per release and platform. It must include DDBs, interpreters, loaders, fonts, graphics, audio, text/vocabulary data, dictionaries, object/location tables, overlays, save-state/configuration data, protection metadata, executable segments, media wrappers, and every future family discovered by inspection. Relationships must distinguish measured reference, same-bundle adjacency, source/catalog assertion, derived recovery, and unresolved candidate; web, TUI, report, manifest, and CI views must expose the graph without inventing semantic links.
- [ ] Add a deterministic unknown-resource review gate. CI must fail if a newly retained member lacks a stable inventory record, complete checksums, parent/bundle provenance, a parser result or explicit rejection, and a next-action/reproducer state. The gate must summarize newly observed extensions, signatures, embedded regions, and profile deviations so that expanding the corpus automatically expands the research queue rather than silently lowering coverage.
- [ ] Audit and correct game-card lineage for retained containers, extracted members, derived recoveries, DDB payloads, and support assets. A card must not present a provenance-backed platform as `unknown`, must not imply interpreter correlation for an unrelated byte, and must explain the exact evidence boundary for every non-correlatable artifact.
- [ ] Add a responsive evidence legend and per-artifact explanation that defines container, extracted member, derived recovery, DDB, interpreter, media support asset, and image/resource payload. The interface must explain terms such as `PIC`, show parent/child relationships, and make game-ready payloads distinct from source media and auxiliary bytes.
- [ ] Make the game-port explorer complete by default across every retained technical medium. Expanded cards must show all associated D64, DSK, TAP, TZX, ZIP, PRG, DDB, image/resource, and derived-recovery records in a deterministic order; any compact mode must be an explicit optional reduction, never the default evidence view.
- [ ] Enforce the evidence-interface contract in the primary workflow and CI across all surfaces: report schema/lineage fields, generated report data, authored documentation links and terminology, the production web build and shared complete-view routes, mobile layout regressions, and the TUI’s equivalent artifact/source/format explanation. A green build must reject missing format legend text, compact-by-default game evidence, a source-backed medium displayed as unexplained `unknown`, or a schema/documentation/TUI drift.
- [ ] Establish deterministic ADP interoperability: audit `jlcebrian/ADP` input/output formats and platform coverage from committed source and primary documentation; compare it to native Harvester capability per format; package only reproducible permitted integration assets; publish a versioned capability matrix in docs, web, and TUI; and gate all compatibility claims with fixtures, byte/provenance boundaries, and CI checks. ADP output remains independent comparison evidence unless a repository-native validator proves the specific claim.
- [ ] Correctly model ADP as an independent DAAD interpreter and toolchain, not a DAAD derivative or extension. Document its accepted, extracted, generated, converted, and packaged formats per pinned upstream revision; acquire and measure available game releases in those formats; learn each format with repository-native parsers; and retain ADP operations only as independently versioned comparison evidence.
- [ ] Publish a deterministic DAAD-format download pack. For every authorized retained game/release/platform/media variant, create a browser-visible package record and a direct individual download with exact bytes, complete checksums, provenance, technical-medium/format explanation, release relationship, and optional independently verified ADP compatibility observation. The pack must be generated from committed manifests, never assembled by hand or inferred from filenames.
- [ ] Implement a scientific format-generation and comparison loop for every DAAD/ADP-relevant technical medium: document byte structure and source inputs, generate the format deterministically with repository-native code, validate it structurally, compare it to available authentic release bytes and parser observations, and record byte-identical equality or the exact justified difference. A format cannot be labelled generated/verified merely because an external tool accepts it; every unimplemented generator or unexplained difference must retain its reproducer and research state.
- [ ] For every ADP toolchain medium, study and cross-document both the pinned ADP reader/writer source and independent primary format specifications before implementation. The resulting dossier must identify the exact source files/functions, byte grammar, generator inputs, validation predicates, target/media constraints, known ADP boundaries, native Harvester comparison, real release fixtures, and test command; no format support claim may rest solely on tool behavior or a filename extension.
- [ ] Implement all promoted DAAD/ADP format generators as repository-native deterministic code and run them in the primary workflow and CI. Export each generation’s inputs, output paths, complete checksums, structural validation, authentic-release comparison result, and blocker/reproducer state into the static report; render the same evidence in the web and TUI; and publish generated bytes only through manifest-controlled public package records.
- [ ] Comprehensively document everything evidence-backed that can be learned from the pinned ADP source and releases: architecture, modules, machine profiles, database readers/writers, compiler path, disk/media path, graphics and font paths, command surface, test suite, bundled fixtures, package layouts, format grammars, compatibility differences, errors, and unresolved questions. Every dossier claim must cite exact source files/functions and, where available, an independent primary specification and a retained release fixture.
- [ ] Enforce multi-evidence comparison for every promoted ADP/DAAD format and compatibility claim. Cross-check pinned ADP source and tool output against retained interpreter disassemblies/decompilations, primary documentation, exact profile hashes, native Harvester parser/generator results, authentic release bytes, deterministic regressions, and emulator observations where applicable. Persist every agreement and disagreement with inputs, commands, versions, outputs, complete digests, evidence grades, and a reproducer; no single tool or document is authoritative in isolation.
- [ ] Add accessible authentic game identity visuals to the complete web game explorer. Every game card/detail must expose a clearly labelled original cover or logo with a provenance field, taken only from a retained asset or a registered public-source asset. Generated illustrative replacements are prohibited. If an authentic logo or cover is low quality, permit only a conservatively restored derivative that preserves the original content and links to its exact source. Preserve alt text, download/source boundaries, responsive layout, and CI checks that reject silent placeholder substitution, unregistered artwork, missing provenance, or an undisclosed restoration.
- [ ] Expand the canonical game and release catalog across the entire documented DAAD ecosystem: historic commercial titles, modern releases, contemporary revisions, platform ports, compiler/runtime evolutions, and named derivatives. Every addition requires a primary or registered evidence URL, explicit release/platform/version boundary, source/corpus acquisition result, and a clear distinction between catalog knowledge and byte-measured evidence.
- [ ] Build an authentic visual registry for every documented DAAD game/release rather than only the initial classic corpus. Each retained or public-source cover/logo must have a title/release/platform relationship, original-source URL or retained identifier, SHA-256, content type, access date, alt text, and restoration declaration; missing artwork stays explicitly absent until an authentic source is registered.

## Self-contained regeneration requirement

- [ ] Audit every promoted preservation claim and classify its regeneration path as **repository-native**, **declared Python/package dependency**, or **external independent validator**. A claim may not require an external GUI application, emulator, proprietary tool, or undeclared local resource for its primary regeneration.
- [ ] Add deterministic repository commands and CI-safe tests for every primary extraction, structural validation, byte comparison, report, and evidence artifact. Retained media and committed reference fixtures must be sufficient to reproduce the expected hashes and evidence tables after a documented environment setup.
- [ ] Reclassify VICE, Ghidra, radare2, c64tapedecode, and comparable programs as optional cross-validation evidence only. Preserve their command logs and results, but implement and test an equivalent repository-native primary path before any support claim is promoted.
- [ ] Make all emulator-derived state claims explicitly reproducible from retained snapshots or native parsed inputs with a committed extraction script; where live emulation remains necessary to acquire a new state, record it as an acquisition method rather than a regeneration prerequisite.
- [ ] Publish the self-contained regeneration requirement verbatim in the project policy, README, contributor guidance, corpus/evidence contracts, format dossiers, reverse-engineering methodology, and release checklist. Every document that introduces an evidence level or support claim must link to this requirement.
- [ ] Add an automated policy check that rejects new promoted-evidence records lacking a native regeneration command, committed input manifest, declared dependencies, output-hash assertions, and an explicit classification for every external validator.
- [ ] Require a self-contained-regeneration attestation in every commit-level audit and release gate. CI must fail if a required primary result invokes an undeclared executable, network endpoint, external emulator, GUI program, or non-committed input.
- [ ] Audit existing reports, screenshots, recordings, static-site artifacts, binary analyses, decompilations, media extractions, DDB outputs, and emulator observations. Replace non-self-contained primary paths or downgrade them to optional corroboration until a native regeneration path is committed and tested.
- [ ] Enforce state-manifest coherence after every corpus mutation: an acquisition, unpack, fingerprint, synthesis, library refresh, or report refresh must either regenerate and verify all affected hash-pinned outputs in the same release gate or leave the manifest intentionally failing; no stale `state.db` or derived-output hash may be silently promoted.
- [ ] Audit interpreter correlation boundaries across every artifact family. Non-DDB identity claims must arise from the artifact’s own exact bytes or documented filename evidence; DDB bundle correlation must be target-platform-filtered; fresh scans must delete stale revocable interpreter claims before recording replacements; and cross-platform contamination cases require retained regressions.
- [ ] Enforce a no-LLM-runtime rule for all preservation-critical execution paths: discovery replay from retained inputs, parsing, extraction, fingerprinting, decompilation/recompilation, byte comparison, evidence generation, report generation, and CI verification must be deterministic code with declared dependencies. Any LLM-assisted development activity must remain optional and may never be a hidden operational dependency.
- [ ] Publish and test a contributor-independent continuation workflow: a clean clone must be able to install declared dependencies, execute the complete offline verification gate, identify every active blocker and its reproducer, add a profile through the retained-sample/contract/regression/manifest sequence, and produce a reviewable evidence delta without relying on private context, model memory, or undocumented workstation configuration.
- [ ] Publish a repository-root-indexed, prompt-free clone-to-contribution guide for humans and agents. It must contain the exact bootstrap commands, offline verification transcript, document-reading order, state/corpus vocabulary, active-blocker handoff format, profile-implementation checklist, evidence/manifest update sequence, atomic-commit criteria, rollback/recovery boundary, and a documentation-gate regression proving that a fresh clone can discover it without external instructions.
- [ ] Define and test the clean-clone installation contract for Linux, macOS, Windows, and Termux: minimum system packages, supported Python versions, virtual-environment setup, declared dependency installation, optional external-validator separation, offline verification command, and actionable failure diagnostics. The primary preservation path must not rely on undeclared host executables or a preconfigured developer environment.
- [ ] Implement and test one deterministic cross-host primary automation entry point for Linux, macOS, Windows, and Termux. It must orchestrate offline corpus/resource/manifest verification, native parsing and fingerprinting, deterministic synthesis/library/report regeneration, byte/hash comparison, and actionable nonzero failure output without a GUI, emulator, LLM, network endpoint, or undeclared host executable. Platform-specific emulation remains an explicitly non-primary acquisition or corroboration step.
- [ ] Implement and test cross-host, noninteractive adapters for every non-primary external validator and acquisition workflow where the tool is available. Each adapter must declare capability and installation status, use a fixed configuration and execution bound, capture logs/outputs/hashes, compare against the native evidence path, and emit an actionable explicit `unavailable` diagnostic on unsupported hosts without blocking the deterministic primary workflow. No optional workflow may require GUI interaction, hidden manual steps, or an LLM.
- [ ] Build an auditable CI toolchain matrix that executes every used or planned external emulator, disassembler, decompiler, compiler, converter, and comparator in headless/batch mode against retained real artifacts. Each lane must provision pinned tool/firmware versions, retain configuration and bounded logs/outputs, compare its result with the native path, publish a host-capability status, and make incompatibility explicit rather than silently skipping the tool. The native primary gate remains independently reproducible, while the extended matrix is automated on every compatible runner.
- [ ] Enforce local/CI primary-gate parity. CI and a clean local clone must invoke the same repository launcher and report the same verifier set, committed input identities, manifest/resource/corpus counts, static-report hash, documentation result, and test result. The parity record must declare supported interpreter/OS versions and explicit harmless environment differences; any unexplained output, count, hash, or command divergence is a reproducibility defect with a retained failure log, corrective change, and regression.
- [x] Restore and enforce CI-generated GitHub Pages delivery at `https://boolforge.github.io/DAAD-harvester/`: build the committed static report portal from verified report evidence, configure the repository Pages source/action correctly, publish on the intended branch event, and run a post-deployment public smoke check that rejects HTTP 404, an incorrect base path, stale report data, or missing documentation/report navigation. The public portal is a release artifact, not a local preview.
- [x] Turn the GitHub Pages portal and TUI into evidence-driven working interfaces rather than summary-only dashboards. They explain the project and its preservation boundary at entry, surface the CI-generated ScummVM detection header and its deterministic provenance, group retained titles by separate catalog/source/measured platform layers, expose source/artifact/interpreter/DDB evidence and complete checksums, distinguish source links from published bytes, support search/filter/detail navigation, and carry real-evidence regressions.
- [x] Add a clear public orientation before the web report’s technical metrics. It states what DAAD Harvester preserves, why catalog/source/retained-artifact/measured-structure/interpreter claims are separate, the nine target platforms and technical media scope, how to inspect or download a specific evidence record, and the current project boundaries; keyboard-accessible paths serve readers, preservers, and implementers.
- [x] Ship a complete game/port/checksum and ScummVM-handoff explorer in both consumers. The report exports a title matrix that keeps catalog platform, source platform, and retained measured-artifact platform separate, and publishes source-associated artifacts, evidence state, DDB/interpreter fields, size, exact SHA-256, detection provenance, a bounded header preview, and conditional header download. The Pages viewer and TUI render the matrix with search/filter/detail/copy actions and clear unavailable/unknown states.
- [x] Build accessible entity-level exploration and deep linking in both consumers. The web report provides stable shareable routes for project sections, games, sources, artifacts, checksum anchors, and detection handoff; restores views from URLs; persists safe filters in query state; and exposes keyboard focus. The TUI provides matching selectable game/port, artifact-detail, and detection-handoff views with visible boundaries and deterministic navigation.
- [x] Publish authorized retained bytes through a deterministic Pages artifact manifest. Every currently retained artifact has an opaque approved public path, file size, complete 17-field checksum suite, provenance, evidence state, and detail/download route. CI stages only manifest-approved paths, recomputes each copied byte digest, rejects traversal/substitution, and smoke-tests the deployed manifest plus a staged artifact.
- [x] Make checksum and download coverage strictly platform-, medium-, and variant-neutral for every currently retained game-related byte across the nine target platforms. The report, web, TUI, public manifest, CI staging, and verification preserve separate identity, all 17 digests, provenance, evidence state, and availability; project-facing contracts use exact recorded technical media classifications.
- [x] Expose every recorded checksum for every retained artifact across the report, Pages viewer, and TUI. The 17-field suite has algorithm labels and artifact scope, the web view offers copyable values and stable checksum links, and absence is explicit rather than derived or silently omitted.
- [x] Enforce complete deterministic checksum generation and backfill. Ingestion, extraction, and re-fingerprinting compute the required 17-field suite from every retained byte; the repository-native verification command re-reads retained files, detects stored-digest mismatches, and fails the primary workflow on missing evidence.
- [x] Add a UI evidence contract and tests for public/TUI numbers, platform associations, download links, checksum records, detection entries, and unknown states. The interfaces state their boundaries and do not infer a game port, interpreter version, downloadable artifact, or ScummVM compatibility beyond deterministic report evidence.
- [ ] Maintain a repository-wide requirements, evidence, and improvement traceability system. It must map every project objective and implementation claim—targets, formats, profiles, interpreters, derivatives, corpus acquisition, reverse engineering, decompiler/recompiler, external-tool automation, CI, report/TUI behavior, ScummVM handoff, blockers, and recovery—to its authoritative documentation, retained inputs, deterministic command, generated outputs, tests, manifest record, public representation, owner boundary, and non-claim. The documentation map and CI link gate must expose omissions rather than allowing a requirement, evidence record, or improvement decision to become orphaned.
- [ ] Make documentation continuity a permanent change-gate rule. Every future code, corpus, schema, toolchain, CI, report, TUI, decompiler, or ScummVM-handoff contribution must add or update its plain-language objective, scope/non-claim, evidence inputs, data contract, responsible source modules, positive/rejection tests, deterministic regeneration command, expected outputs/hashes, manifest/report impact, user-visible behavior, blocker/recovery state, and continuation instruction. The documentation checker and review checklist must reject a contribution that leaves future humans or agents unable to understand, verify, improve, or safely extend it from a clean clone.
- [ ] Enforce human-and-agent continuation parity. A clean clone must expose one documented reading order, machine-readable work/evidence state, deterministic command plan, structured pass/fail diagnostics, and bounded change protocol that lead both a human and an agent from project orientation to the same independently checkable action. No step may depend on interpreting a private conversation, remembering an unavailable prompt, inferring an undocumented convention, or accessing a personal workstation.
- [x] Enforce frequent atomic remote delivery. Once a coherent change has passed focused verification, separate it from unrelated work, commit only its code/evidence/tests/docs/generated outputs and required manifest updates, and push it promptly to the configured remote. Each commit must be independently understandable and reproducible from a clean clone; transient logs, half-implemented changes, and unrelated corpus mutations must not be bundled into a delivery.
- [x] Release the verified report/TUI evidence-explorer increment as its own atomic remote commit before continuing the separate checksum-backfill implementation. Include only the deterministic report/schema output, Pages and TUI explorer behavior, focused regressions, required documentation, manifest repin, and work-register updates; exclude timestamped logs and incomplete backfill code.
- [x] Split the current delivery into multiple atomic commits and pushes: first the tested report-export/schema contract and regenerated output/manifest; then the tested Pages/TUI evidence consumers and their regressions; then the separately verified checksum-generation/backfill, documentation, and CI gate. Each is independently buildable and explainable from its own diff.
- [x] Publish and enforce the atomic remote-delivery policy in contributor-facing documentation: define the maximum unit of validated local work, required focused/full checks, commit-scoping rules, immediate push step, remote/CI confirmation, and recovery when a check or push fails. Link it from root and continuation navigation so no contributor can treat long-lived unpushed validated work as acceptable.
- [x] Strengthen the delivery methodology against oversized commits: define layer-specific commit boundaries, require staged file/stat review before every commit, require documentation/control-plane changes to ship independently when they do not alter executable behavior, and require a hard stop/split when a diff contains more than one independently testable claim. Record prior oversized interface delivery as a process defect without rewriting published history.
- [ ] Complete a documentation-coverage audit for every relevant preservation concern: source and authorization provenance, retained media, profile/format contracts, parser/loader behavior, interpreter and DDB evidence, reverse-engineering outputs, decompiler/recompiler rules, deterministic commands, generated outputs, tests, manifests, active blockers, failure/recovery paths, contributor decisions, and final integration criteria. The documentation index must provide a direct, maintained path to each concern and CI must reject missing required navigation links.

## Completed foundation

- [x] Canonical official target vocabulary: ZX Spectrum, Amstrad CPC, Commodore 64, Commodore Plus/4, MSX, Amstrad PCW, Atari ST, Amiga, and IBM PC/DOS.
- [x] Platform-neutral source, artifact, and version-evidence persistence, including catalog-only source status and normalized source roles.
- [x] Modern DRC V2/V3 structural DDB validation across all canonical targets.
- [x] Historical compact V1/V2 structural DDB validation derived from the MSX2DAAD interpreter header and loader contract.
- [x] Exact interpreter identity through same-platform official SHA-256 profiles, with qualified runtime filename evidence kept separate.
- [x] Initial native media adapters and provenance for supported archive, tape, disk, filesystem, and compressed-image families.
- [x] Current all-platform discovery adapters, including direct-media versus catalog-only source contracts.
- [x] A first all-platform audit run, bounded real public-media validation, and an honest regenerated TUI recording.
- [x] Existing GitHub Actions unit-test and CLI-smoke workflow.

## Critical work in progress

### 0. Granular documentation architecture

- [x] Replace broad topic monographs with a focused-module tree: one technical responsibility per file, with directory indexes and bidirectional cross-references.
- [x] Separate conceptual narrative, byte schemas, state/flow diagrams, platform dossiers, source records, reproducible artifact manifests, and dated audit results into distinct documents.
- [x] Add a documentation migration map that identifies each legacy broad document, its destination modules, and its retained index role.
- [x] Add permanent automated local-link, focused-header/reference, and Mermaid-declaration checks before publication; source-led claims remain routed through the relevant ledger.

### 1. Complete format and archive coverage

- [x] Publish `docs/formats/FORMAT_CAPABILITY_MATRIX.md` listing every input family, extension, magic, platform ambiguity, wrapper, expected member model, implementation owner, test corpus, and real-artifact result.
- [x] Validate DMS archive and every track through CRC-16/ARC plus additive checksum; decode NOCOMP, SIMPLE, QUICK, MEDIUM, DEEP, HEAVY1, and HEAVY2 with deterministic mode fixtures and bounded failure paths.
- [x] Validate typed TZX/CDT v1.20 block streams, including all standardized data, control-flow, metadata, and extension boundaries, while retaining control evidence rather than replaying tape control graphs.
- [x] Extract bounded FAT12/FAT16 filesystems, including validated VFAT short/long-name association and directory chains; validate DOS MZ header structure; inspect DMS, STX/Pasti, and SPS/IPF preservation evidence.
- [x] Extract FFS files spanning validated Amiga extension-header chains without silently truncating continuation blocks.
- [ ] Replace any “limited,” heuristic, or filename-dependent path with documented native parsing or a deliberately rejected unsupported state. Do not silently fall back to blind copying.
- [ ] Cover the complete required family of DAAD-relevant containers and media: TAP/TZX/CDT; +3DOS; CPC standard/extended DSK; C64/Plus4 D64, T64, PRG, P00; MSX/PCW/DOS FAT12/FAT16 DSK; MSX CAS and ROM; Atari ST ST, MSA, STX/IPF evidence; Amiga ADF, ADZ, DMS, LHA/LZH; DOS COM/EXE; and nested ZIP/7z/RAR/LHA/ARJ/cab/gzip/tar where legally and technically applicable.
- [ ] Add strict bounds, checksum/geometry validation, corruption cases, recursive provenance, decompression limits, and negative controls for every parser.
- [ ] Add public real-artifact audit fixtures or reproducible acquisition manifests where redistribution is not permitted.
- [ ] Implement native Commodore raw-TAP V0/V1/V2 pulse-stream decoding with exact declared-length validation, overflow semantics, conventional ROM-loader block recovery, byte-level provenance, corruption fixtures, and an explicit evidence path for custom-loader streams that cannot use conventional framing.
- [ ] Reconstruct the C64 KERNAL software-servo reader used by the retained *Jabato* Side A later stream from the preserved `read.s` implementation: model `cmp0` adaptation, synchronization, duplicate-pass/error handling, pointer setup, and checksums; reproduce every byte of the verified `$3880`–`$99C2` Part 1 DDB from original pulses; and add block-level runtime/ADP identity regressions before claiming complete physical recovery.

### 2. Versions, interpreters, and derivatives

- [x] Create `docs/versions/` with a cited chronology of original DAAD, historical V1/V2 database generations, DAAD V2 releases, DRC V2/V3 output, and explicitly documented compatibility boundaries.
- [x] Inventory registered official interpreter binaries across all nine canonical platforms, including measured SHA-256 anchors, language/variant metadata where present, filename aliases, and source-controlled distribution provenance.
- [x] Document and classify derivatives/extensions separately from original DAAD: DRC, DAAD Ready!, Maluva, MSX2DAAD, PCDAAD, UnDAAD, and compatibility boundaries. “Derivative” names its supported format and deviation; it is not assumed equivalent.
- [x] Extend fingerprints only from actual format/interpreter specifications, with structural, corruption, and profile-correlation regressions that persist decision evidence.
- [ ] Investigate historical multi-part commercial layouts using real retained artifacts; keep unresolved layouts as explicit archaeology targets, never guessed DDBs.

### 3. Sources and metadata

- [x] Maintain a cited source register under `docs/sources/` for the catalog, archive, API, public release-page, format, and interpreter sources used by the project.
- [x] Record source accessibility, robots/terms boundary, API/schema, direct-media contract, platform coverage, known failure modes, and last validated result in the source register and audit notes.
- [ ] Re-validate every adapter live and use the outcome to repair selectors, rate limits, direct-media admission, catalog-only handling, and provenance fields.
- [ ] Expand the evidence catalog conservatively with source URLs for title/platform/release facts and keep binary-version facts distinct.

### 4. TUI and visual evidence

- [x] Redesign the Rich TUI around legibility, terminal-safe responsive geometry, clearer hierarchy, detailed artifact inspection, and real operational feedback.
- [x] Add selectable platform-inspired nostalgia themes that preserve accessibility: Spectrum, CPC, C64, Plus/4, MSX, PCW, Atari ST, Amiga, DOS, and a neutral forensic theme.
- [x] Replace the compact GIF with an extended, readable, authentic interaction recording backed by a real retained audit database; it shows evidence, selection, inspector, theme, filter, queue, metrics, and pause states without invented detections.
- [x] Document capture dimensions, aspect-ratio rationale, theme, real input database, and reproducible recording command.

### 5. HTML reports, static library index, and publication

- [x] Implement deterministic static-report export from persisted outputs, including summary, source ledger, logs, platform/version/detection results, detection table preview/download state, and a library manifest index with safe relative links.
- [x] Build a static report viewer with no invented data and clear loading/error states.
- [x] Add GitHub Actions jobs for unit tests, static analysis, generated-report verification, static-site build, and GitHub Pages deployment.
- [x] Configure and verify GitHub Pages at `https://boolforge.github.io/DAAD-harvester/` after successful deployment run `32235035851`.

### 6. Deep audit and release gate

- [x] Run the complete deterministic suite, corruption regressions, CLI report smoke test, static analysis, documentation-link checks, and report build.
- [x] Run a fresh bounded public-media audit through discovery, fetch, native unpack, fingerprint, organization, and report export; preserve the `174`-source, five-download, 37-artifact, zero-verified-DDB outcome and source failure in `docs/audits/2026-08-19_FINAL_LIVE_AUDIT.md`.
- [x] Update `README.md`, this roadmap, modular docs, and the generated report truthfully from final evidence.
- [x] Publish in small, reviewable commits to `main`; verify `HEAD == origin/main` after every milestone.
- [x] Record the modular-documentation audit, report build, CLI/report smoke test, and remaining native-archive release gate in `docs/audits/2026-08-19_DOCUMENTATION_EXPANSION_AUDIT.md`.

### 7. Authorization-gated reverse-engineering corpus

- [ ] Replace restrictive handling statuses with the institutional-authority directive: all acquired original bytes, extracted members, third-party materials, tool outputs, decompilations, and execution records are retained, analyzed, run where safe, and published; provenance fields remain evidentiary rather than blocking.
- [ ] Complete every retained-corpus objective end-to-end—acquisition, execution, extraction, analysis, comparison, documentation, publication, regression, and audit—while preserving original/derived distinctions and reproducibility records.
- [x] Record per-artifact authorization, license, source URL, acquisition date, checksum, platform/architecture, and redistribution status for the retained official interpreter profile corpus.
- [x] Ingest the user-documented authorization record by reference into each official original-binary manifest and record its scope without placing sensitive permission correspondence in generated reports.
- [ ] Record the user-documented European/international preservation-agreement reference as a scope-level provenance record linked from every retained interpreter and game-corpus manifest, without exposing sensitive correspondence.
- [ ] Apply the user-confirmed universal authorization scope to all retained corpus classes, while preserving individual source URL, acquisition, hash, platform, lineage, and analysis-tool records for every committed item.
- [x] Create a machine-readable inventory that distinguishes original binary, public original source, public derivative source, disassembly, decompiler output, symbol map, and analytical notes.
- [x] Define reproducible, architecture-specific workflows for Z80, 6502/8501, 68000, and 8086-class DAAD artifacts, including tool version pins, raw-mode base assumptions, command logs, and one successful exact-hash pilot per family.
- [x] Analyze every retained official exact-hash interpreter profile whose provenance/authorization gate is satisfied; preserve immutable originals separately from derived output and never label generated pseudocode as recovered source.
- [ ] Cross-check each derived interpretation with independent tools, known DDB/runtime behavior, checksums, entry-point mappings, and reproducible test fixtures before documentation claims are promoted.
- [ ] Discover public third-party disassemblies/decompilations by platform and version; retain their provenance and verify input-byte hashes, architecture/load assumptions, and claimed ranges against locally generated analysis before use.
- [ ] Retain the confirmed relevant `jlcebrian/ADP` public implementation at a pinned revision; record its license, stated platform/version scope, source/binary composition, and independent comparison outcome before incorporating its claims.
- [ ] Commit every accepted third-party comparison artifact, raw local disassembly, decompiler output, symbol/control-flow export, tool configuration, command log, and output hash in the self-contained reverse-engineering corpus.
- [ ] Retain comparative inputs in immutable `third_party/` paths and generated local outputs in separate tool/version-qualified paths; no documentation-only reference may substitute for an accepted artifact.
- [ ] Add focused reverse-engineering dossiers, schema/provenance contracts, audit records, and CI-safe integrity checks without redistributing restricted original binaries or proprietary-source equivalents.

### 8. All-platform retained game corpus and regression suite

- [ ] Apply the global completion rule to every parser, media format, loader, runtime variant, DDB structure, companion asset, and decompiler construct: an unsupported observation must enter a retained-sample research, implementation, differential-check, and regression loop until verified support exists; no terminal unsupported omission is permitted.
- [ ] Enforce the global conflict and issue lifecycle across the entire project: every conflict, discrepancy, failed hypothesis, corrupted or ambiguous input, failed gate, implementation defect, external-source disagreement, and rejected candidate rule must receive an in-repository reproducible record when observed; its record must name evidence, byte ranges or reproducers where applicable, affected profile/artifact, next experiment, and status. When fixed, update the same record with the cause, implementation and commit references, regression evidence, regenerated outputs, and verification command; no issue may disappear merely because a later commit happens to pass.
- [x] Enforce American English across all project-authored code, documentation, test names and assertions, generated evidence, scripts, commit messages, contributor instructions, and user-facing progress text. The clean-clone validator audits 428 authored text files and is part of the primary workflow; the policy, contributor guidance, deterministic check, and documentation-gate markers preserve the rule against unapproved authored-language drift.
- [x] Refine the authored-language validator boundary: project-authored prose and code remain American English, while external sources, archival materials, original code, webpages, official terminology, authentic game titles, and historical documentation may remain in any original language. The verified evidence exclusions are `preservation_corpus/`, `reverse_engineering/public_sources/`, and `reverse_engineering/public_implementations/`; they preserve provenance and never authorize non-English project prose.
- [x] Resolve the authored-language validator self-scan defect: the full primary workflow initially failed because `scripts/check_american_english.py` detected its own deliberate diacritic-marker regex as non-English content. The checker now excludes only its implementation file from its text-input set; it still validates 428 other authored text files. `python scripts/run_primary_workflow.py` then passed all corpus, report, documentation, language, static-analysis, and 301 regression checks. The resolution commit records the repair.
- [ ] Enforce blocker closure: every temporary blocker record must culminate in a verified implementation, real-artifact regression, refreshed corpus output, and evidence-backed support claim. A measured but unresolved state is an investigation checkpoint only and may never be accepted as the final result for any platform, format, loader, interpreter, DDB structure, derivative, or game variant.
- [ ] For every blocker, perform deep external research before selecting a solution: inspect primary documentation and source where available, multiple independent preservation archives/catalogs, public implementations and tools, platform-specific technical references, and comparable artifact variants. Retain source URLs, downloaded/hashed research materials where permitted, conclusions, counter-evidence, and byte-level validation against the authorized corpus; then implement, test, and iterate until verified closure.
- [ ] When an authorized source endpoint is captcha-protected, rate-limited, or otherwise non-automatable, retain the exact access outcome and continue acquisition through independent accessible archives, mirrors, catalog-linked variants, and primary releases. The blocked endpoint must remain a provenance record, never a reason to halt comparison or closure work.
- [ ] Treat release-scene, crack, dump, trainer, and archival tags as provenance metadata rather than game semantics. Acquire and compare alternate/original variants whenever possible, identify byte-level loader/intro/interpreter modifications, and validate all conclusions against retained artifacts before modeling runtime behavior.
- [ ] Make every cracked, scene-modified, trained, fixed, translated, compilation, and archival release fully functional: preserve and execute its exact loader/intro path, recover its game data and embedded runtime behavior, distinguish modifications from the underlying game, and add real-artifact regressions plus emulator evidence until that variant reaches verified support.
- [ ] Apply this closure rule universally to every DAAD system, software and hardware platform, game, release, compiler/runtime version, historical or modern derivative, interpreter, media/container form, compression/protection scheme, loader, encoding, companion resource, and game-specific fork. No category, artifact, or variation is exempt from complete verified treatment.
- [ ] Build the complete native DDB decoder grammar for every observed DAAD ecosystem profile. The grammar must cover modern DRC V2/V3, historical V1/V2, PAWS, documented compiler/runtime extensions, wrappers, target byte order/base-address conventions, CondAct opcode/operand/indirection rules, text/token tables, process serialization, alignment, deduplication, external vectors, and every retained divergent layout. Every grammar rule requires a profile tuple, source/specification/runtime evidence, retained bytes, corruption regression, and explicit decoder acceptance/rejection behavior; an extension or opaque byte range remains an active closure blocker until decoded.
- [ ] Continue legacy V2 DOS `BLANK.DDB` section closure after its header, process pointers, offset tables, and XOR text records: establish source-backed grammars and real-artifact regressions for vocabulary records, token-block boundaries/expansion, object location/word/attribute tables, connection records, header extension/external data, and all remaining exact opaque ranges; then expand the same closure process to retained V1 C64, V2 ZX, and V2 Amiga profiles without generalizing an unverified rule.
- [x] Resolve retained Amiga V2 `PART1.DDB` residual byte range `0x080B`–`0x080C`: the original record incorrectly called it token-adjacent because the generic pointer-owner hint labeled the gap `legacy_token_block_payload_pending_grammar`. The native ledger proved that `0x080B` follows `TextNode(0x07FD, 0x080B)` and immediately precedes `OffsetTableNode(0x080C, 0x088A, system_messages_table)`; decoded token nodes are earlier. Pinned ADP `src-tools/dc_main.cpp:2415–2448` writes raw `0x00` when an XOR-text message payload ends at an odd byte boundary and then writes its target-endian offset table. `AlignmentPaddingNode` now represents that byte only when its strict source-backed predicates hold. The retained Amiga V2 `PART1.DDB` regression pins all four observed boundaries—`0x080B` before system messages, `0x088B` before messages, `0x089D` before object names, and `0x099F` before location descriptions—then requires all 17 digest fields and byte-identical recompile output. `scripts/verify_ddb_round_trips.py --write` refreshed the visible opaque-range ledger, and `python scripts/run_primary_workflow.py` passed all gates with 301 tests. The resolution commit records the implementation.
- [x] Resolve retained DOS V2 `BLANK.DDB` byte range `0x08C3`–`0x08C4`: the native ledger identifies raw `0x00` immediately after `ConnectionListNode(0x08C0, 0x08C3)` and immediately before `OffsetTableNode(0x08C4, 0x08CA, connections_table)`. Pinned ADP `src-tools/dc_main.cpp:2450–2471` emits verb/destination pairs, raw `0xFF`, a raw zero when the complete connection payload ends at an odd offset, and then the target-endian connection pointer table. The retained DOS artifact has a three-byte final connection list, while retained Amiga V2 independently corroborates the same rule at `0x09AD`–`0x09AE`. `AlignmentPaddingNode` now types each byte only when the exact connection-list parity and adjacent table predicates hold; a retained DOS mutation changing `0x08C3` to nonzero remains opaque and recompiles byte-identically. `scripts/verify_ddb_round_trips.py --write` regenerated the affected opaque-range ledger, and `python scripts/run_primary_workflow.py` passed all gates with 301 tests. The resolution commit records the implementation.
- [x] Resolve retained DOS V2 object-attribute alignment bytes `BLANK.DDB:0x08CD`–`0x08CE` and `SPANISH.DDB:0x078B`–`0x078C`: each is raw `0x00` immediately after an odd-length typed `object_attributes_table` and before the typed V2 `extended_object_attributes_table`. Pinned ADP `src-tools/dc_main.cpp:2493–2516,2716–2725` writes one attribute byte per object, then explicitly appends zero when the accumulated output length is odd before serializing the two-byte-per-object extended attributes. The native alignment predicate now requires that exact attribute-table parity and immediately adjacent extended-table boundary. Both retained DOS artifacts pin the expected node, while nonzero mutations remain opaque and recompile byte-identically. `scripts/verify_ddb_round_trips.py --write` refreshed the ledger, and `python scripts/run_primary_workflow.py` passed all gates with 302 tests. The resolution commit records the implementation.
- [x] Resolve retained DOS V2 process-section markers `BLANK.DDB:0x08D1`–`0x08D2` and `SPANISH.DDB:0x078F`–`0x0790`: each raw `0xFF` immediately precedes the first referenced `CondActStreamNode` in the serialized process section. Pinned ADP `src-tools/dc_main.cpp:2529–2584` begins `AppendProcesses()` with raw `0xFF`, then emits CondAct code, process-entry lists, and the process pointer table. Retained probes independently find the exact marker followed by streams referenced at `0x08FA` and `0x07B8`, respectively. `ProcessSectionMarkerNode` now types only that exact raw byte before the first decoded CondAct stream; non-`0xFF` mutations remain opaque and recompile byte-identically. `scripts/verify_ddb_round_trips.py --write` refreshed the ledger, and `python scripts/run_primary_workflow.py` passed all gates with 302 tests. The resolution commit records the implementation.
- [x] Resolve retained DOS V2 process-layout bytes following the process-section marker: `AppendProcesses()` aligns an odd-length CondAct-code group with one raw zero before that process’s entry list, then emits the entry-list terminator as two raw zero bytes. The native ledger pins the alignment nodes at `BLANK.DDB:0x08F9,0x0945,0x0971,0x0981` and `SPANISH.DDB:0x07B7,0x0803,0x082F,0x083F,0x08F5`; `ProcessEntryNode` now owns the complete two-byte terminator and rejects a malformed second byte. The first focused regression falsely included the one-byte vocabulary terminator through a generic `is_terminator` attribute lookup; it now filters `ProcessEntryNode` explicitly. The first full workflow also exposed a stale evidence-test assumption that every record must remain `structurally_bounded`; the contract now accepts either `structurally_bounded` with visible opaque ranges or `semantically_decoded` with none, while retaining byte-identity and all 17 digest assertions. `scripts/verify_ddb_round_trips.py --write` regenerated the ledger, and `python scripts/run_primary_workflow.py` passed all gates with 302 tests. The resolution commit records the implementation.
- [x] Resolve retained legacy V2 external-vector tables `0x22`–`0x3C`: the existing `HeaderExtensionNode` previously owned only the first two-byte external-data field. Pinned DRC `drb.php:121–123,1778–1779,2037–2068` independently defines a thirteen-word target-endian external-vector table: vectors 0–2 are EXTERN, SFX, and INT, and vectors 3–12 are user pointers. Pinned ADP reads the primary external-data pointer at `0x22`, recognizes the external PSG pointer at `0x28`, and allocates the corresponding 24 bytes in its PC/ST/Amiga V2 compiler path. `ExternalVectorTableNode` now owns the complete table only when every word precedes the earliest pointer-defined section. Retained DOS `BLANK.DDB` and `SPANISH.DDB`, ZX Side 2, and both Amiga records satisfy that predicate with thirteen zero words; a mutated vector word remains typed and recompiles byte-identically. `scripts/verify_ddb_round_trips.py --write` refreshed evidence, and `python scripts/run_primary_workflow.py` passed all gates with 302 tests. The resolution commit records the implementation.
- [x] Resolve retained embedded ZX V2 `CODE__embedded_002400.ddb` range `0x3C`–`0x7A`: the verified external-vector table owns `0x22`–`0x3C`, while the embedded ZX artifact has a structurally distinct pre-section payload before its earliest pointer-defined section at `0x7A`. The initial inventory incorrectly described the whole tail as 62 zero bytes; the direct probe corrected this: `0x3C`–`0x43` is zero, while `0x44`–`0x79` contains nonzero bytes beginning `1E 1E 1E 1E 00 00 00 01`. This payload is absent from the sibling ZX Side 2 DDB. The DAAD V2 manual documents CTL as the fixed first data section and valid location for non-relocatable code or data, establishing section-level ownership but not a finer semantic subtype. `ControlSectionNode` now owns this exact legacy V2 ZX interval only after a complete external-vector table and before the first pointer-defined section; a mutation at `0x44` remains losslessly typed and recompiles byte-identically. The focused test also restored an unrelated missing `ObjectTableNode` import. `scripts/verify_ddb_round_trips.py --write` regenerated evidence, and `python scripts/run_primary_workflow.py` passed all gates with 302 tests. The resolution commit records the implementation.
- [x] Resolve the Torreoscura PCW corpus acquisition and DSK extraction lifecycle. The original Computer Emuzone candidate remained public-403 inaccessible and was not bypassed. A bounded Zona F.I. publisher-page adapter instead retained source 253’s authorized PCW archive, including `TO.DSK` and `TO2.DSK` (each a 194,816-byte `EXTENDED CPC DSK` medium labeled `CP/M Box - Habisoft`, with 40 single-sided tracks). Initial generic and PCW-format assumptions failed reproducibly: the physical descriptor order is interleaved (`0x41,0x46,0x42,0x47,0x43,0x48,0x44,0x49,0x45`), and generic CP/M block/origin trials did not recover a valid payload. Independent read-only `cpmtools` evidence rejected the images under `pcw` but listed Side 1’s `PARTE000.PIC`, `PARTE001.CHR`, `PARTE001.DAT`, `PARTE001.DDB`, and `TO.COM`, plus Side 2’s `PARTE002.CHR`, `PARTE002.DAT`, and `PARTE002.DDB`, under the documented `cpcsys` layout (40 tracks, 9 sectors/track, 512-byte sectors, 1 KiB blocks, 64 directory entries, two boot tracks). The native extractor now sorts each track by sector identifier, evaluates the documented data and system filesystem origins, and maps directories and allocation blocks relative to the selected origin. The native parser additionally accepts only zero-filled CP/M final-record padding at the exact 128-byte rounded boundary. Real-artifact regression asserts Side 2 extraction and verified `PARTE002.DDB` recognition as legacy V2 PCW with a 14,817-byte declared payload; source-scoped re-unpack and fingerprint also record `PARTE001.DDB` and `PARTE002.DDB` as verified PCW V2 payloads. `scripts/regenerate_preservation_report.py`, `scripts/build_public_artifact_manifest.py`, and `scripts/verify_regeneration_manifest.py` refreshed and verified the 27-input/17-output self-contained contract. `python scripts/run_primary_workflow.py` passed all gates with 305 tests. The resolution commit records the native implementation and refreshed corpus evidence.
- [x] Resolve the retained C64 V1 direct-artifact profile mismatch: `preservation_corpus/derived/commodore_loader/jabato_ass_part1_post_mirar.ddb` (SHA-256 `7ffbee6ca3e614011b30261a74022d199ee3345843a0525e92dc9cb5b7bdb5e6`) validates with native target base `0x3880`. The survey table’s `0x0` field was its **source offset**, not the omitted base address; the parser and JSON survey key retain the actual base. A pinned legacy V1 C64 `0x3880` profile regression now requires decompile→recompile byte equality and all 17 canonical digests, distinguishing raw stored bytes from target-memory pointer semantics.
- [x] Resolve the retained ZX V2 direct-artifact profile mismatch: `preservation_corpus/extracted/depth3_25a67864_CODE__embedded_002400.ddb` (SHA-256 `7d7b26973b9c36a6dca4e804e2c4dbfccda663985f6052080ac85151bb1386ab`) rejects the file-relative trial because its parser-measured target base is `0x8400`. A pinned legacy V2 ZX `0x8400` regression and generated evidence record now require decompile→recompile byte equality and all 17 canonical digests, distinguishing extracted-member storage from target-memory pointer semantics.
- [x] Resolve the legacy synthetic-fixture versus object-word-table conflict: the shared nonzero-object fixture retains zero optional location/word/attribute pointers accepted by the parser’s structural fixture contract, while populated retained artifacts use source-backed tables. Pinned ADP `AppendObjectWords` establishes two-byte noun/adjective pairs per object; its loader’s one-byte-per-object check is therefore an acceptance minimum, not the full semantic width. The native IR now skips zero optional tables, decodes populated two-byte object-word rows, preserves real-artifact byte ownership, and retains the sparse fixture as an explicit zero-pointer boundary.
- [x] Correct the retained `legacy-v2-dos-little-raw-blank-r4` token-boundary probe defect. The first reported overlap at `0x0386` occurred only at candidate token index `256`, beyond ADP’s documented `n <= 255` scan. The checked reproducer now records both the rejected unbounded overlap and the bounded no-overlap result; the false profile blocker is closed. The complete profile-specific token grammar remains an active part of the global native DDB decoder requirement.
- [ ] Build the matching complete native DDB encoder from the lossless profile-scoped intermediate representation. It must reproduce all preserved representation choices—not only equivalent game semantics—including original byte ordering, header/pointer forms, wrapper checksums, padding/alignment, stream sharing, indirection lowering, and target/subtarget exceptions. No external compiler, emulator, current time, locale, filesystem metadata, unordered traversal, or LLM may be a primary dependency.
- [ ] Close DDB support profile by profile with retained-artifact decompile → recompile → byte-for-byte comparison. For every mismatch, retain first/last differing bytes, contiguous ranges, owning IR nodes, source/profile evidence, and a minimal reproducer; research and implement the missing grammar until equality is achieved. Publish verified profile records with all 17 digests and comparison evidence in the primary workflow, CI, static report, Pages viewer, TUI, manifests, and documentation. Structural parsing, opaque pass-through, an external compiler result, or runtime observation may not be represented as round-trip completion.
- [ ] Maintain linked, platform-specific format-profile dossiers for every recognized and newly observed geometry, filesystem, disk/tape/container variant, boot convention, loader, executable/load model, encoding, and companion package. Each dossier must identify retained samples and hashes; primary and independent references; exact acceptance contract; corruption/rejection contract; profile applicability boundaries; unresolved but active variants; real-artifact regressions; deterministic regeneration command; and cross-links to the capability matrix, evidence model, coverage ledger, and report. A profile proven on one platform must never be silently generalized to another.
- [ ] Maintain an equal-priority execution and evidence matrix for ZX Spectrum, Amstrad CPC, C64, Plus/4, MSX, Amstrad PCW, Atari ST, Amiga, and IBM PC/DOS, including all known software/runtime derivatives; no platform-specific investigation may be mistaken for a platform priority or permit another platform to fall outside active coverage tracking.
- [ ] Apply the institutional preservation authorization globally to supporting runtime resources required for reproducibility—including firmware, system ROMs, emulator dependencies, compilers, and reference media—while retaining each resource’s origin URL, acquisition timestamp, authorization context, version, SHA-256, and role in the evidence chain.
- [ ] Persist the universal institutional authorization directive verbatim in the project policy, corpus manifests, acquisition/reverse-engineering records, and CI-verifiable documentation: all required artifacts and tools may be acquired, retained, executed, analyzed, disassembled, decompiled, recompiled, compared, and published for the project’s scientific preservation mission, without category- or platform-specific exceptions.
- [ ] Build deterministic emulator-backed reconstruction workflows for every target whose loaders or protected media cannot be resolved statically; retain command scripts, bounded snapshots or RAM ranges, runtime configuration, execution limits, and byte-level comparisons as corpus evidence.
- [ ] Treat missing supporting firmware or a runtime dependency as a named acquisition/provenance task, not a terminal blocker: find, retain, hash, validate, and regression-test the resource under the same institutional authorization and global completion rule.
- [ ] Close the retained C64 *Jabato vs. Imperio: Libertad* tape loop: reconcile both original raw pulse streams, their controlled title/credits/loading paths, and active runtime representations; reproduce the recovered Side A Part 1 and Side B Part 2 ranges from pulses alone, compare both originals against the ASS D64 variant, implement the resulting parser/loader support, and add real-artifact regressions before promotion.
- [ ] Complete official DAAD R4 source 250’s MSX `MSX.DSK` lifecycle. The native retained-sample parser and regression establish an MSX-DOS FAT12 2DD profile (`EB FE 90`, 512-byte sectors, two sectors per cluster, two FATs, 112 root entries, 1,440 sectors, `0xF9`, three sectors per FAT, nine sectors/track, two heads) and recover five hash-pinned members: `MSXDOS.SYS`, `COMMAND.COM`, `DAAD.MDG`, `DAAD.Z80`, and `YOURGAME.COM`; a malformed geometry regression also exists. The append-only re-unpack now materializes their corpus rows, complete checksums, lineage, report records, and public-manifest entries without deleting earlier source-250 descendants. The remaining work is to classify each member’s target/runtime behavior without filename-only claims, complete MDG and launcher grammars, validate cross-file references, and register the final MSX bundle graph in the self-contained regeneration lifecycle.
- [ ] Decode the official R4 MSX `DAAD.MDG` companion-resource grammar natively. The R4 2,105-byte member is now independently exact-hash matched to the official classic `Interpreters/MSX/DAAD.MDG` template, whose `xREADME.TXT` calls it an empty graphics database with a standard character set. This establishes a canonical-template identity, not its complete internal grammar. Determine container fields, glyph geometry/code-page, graphics or font payload boundaries, compression, and loader references by comparing independent retained samples and source code. Add real-artifact, corruption, cross-file-reference, and deterministic glyph-preview regressions before publishing any font or image derivative.
- [x] Resolve the observed R4 MSX MDG profile contradiction before borrowing the public build-script layout. The materialized source-250 `DAAD.MDG` is 2,105 bytes, while the retained public script’s `skip=2266,count=34` operation requires at least 2,300 bytes; its documented 218-byte/central-charset/34-byte split therefore cannot be applied to this R4 byte sequence. Independent same-generation evidence is now available: the R4 bytes exactly equal the official classic `Interpreters/MSX/DAAD.MDG` file (2,105 bytes, SHA-256 `c588b0e7cbdbd3a591085cd233d471c7a37fed85a88085ced8a560a42a759f06`), whose accompanying `xREADME.TXT` identifies it as an empty graphics database with the standard character set. The discrepancy is therefore a documented generation/profile divergence; both profiles remain separate. Full grammar decoding, field semantics, and font rendering remain active tasks and may not be inferred from the exact template identity.
- [x] Recognize and persist bounded R4 MSX companion-resource evidence. Native inspection accepts `DAAD.MDG` only when both its 2,105-byte size and canonical SHA-256 match the independently corroborated official classic template; same-size mutations and different-size MDG files remain explicit unrecognized profiles. It also recognizes the 559-byte `YOURGAME.COM` raw-COM profile only when its measured entry bytes, filename table (`DAAD.MDG`, `PART1.DDB`, `DAAD.Z80`, `LOADPIC.SC2`), and three FCB service-call patterns agree; a corrupted table rejects. Real-artifact regressions pin both outcomes, source 250 was re-unpacked against `preservation_corpus`, and report/state contract hashes were refreshed. This does not claim a generic MDG parser, complete COM grammar, or successful runtime execution.
- [ ] Complete the official R4 MSX `DAAD.Z80` interpreter analysis with redundant architecture-aware methods. Its materialized SHA-256 already exactly matches the retained English `daad-msx-msxedi-official` profile; the currently retained public `DMSXIEF3.BIN` and `DMSXISF3.BIN` build assets have different hashes, so the build script proves a convention rather than substituting an original byte identity. Preserve all originals and complete digests; establish load address, entry points, DDB/MDG loading behavior, and the precise relation of every distinct public interpreter sample. Publish only reproducible disassembly/decompilation outputs with tool configuration, byte ranges, and disagreement boundaries; the existing raw-base analysis remains unverified runtime semantics.
- [ ] Reverse and validate the official R4 MSX `YOURGAME.COM` launcher and its relation to `MSXDOS.SYS`, `COMMAND.COM`, `DAAD.Z80`, and `DAAD.MDG`. Establish its MSX-DOS executable/load contract, command-line or default-file behavior, memory/entry assumptions, and error boundaries from native byte analysis plus independent retained evidence. Add real-artifact and malformed-header regressions before identifying it as a functional game launcher.
- [ ] Build a source-250 MSX bundle graph linking the materialized FAT12 members through measured filesystem membership, public-build-script roles, native parser evidence, exact interpreter identity where available, and unresolved runtime relations. Persist the graph in corpus/report/TUI/web data with separate categories for measured, build-script-derived, runtime-observed, and unresolved edges; never infer a semantic link solely from a shared DAAD filename.
- [ ] Audit the complete source-250 official R4 re-unpack materialization as an intentional multi-platform corpus expansion. The current retained ledger has 164 depth-two members. A deterministic first partition found 104 without a native media signature and 39 opaque program images, alongside resource families such as `CH0`, `FNT`, `SDG`, `P3F`, `RSC`, `ACC`, extensionless C64 loaders, and platform package files; `PART1.CH0` is now closed only because its bytes exactly duplicate a validated CHR artifact. Verify every remaining child’s source-250 lineage, complete checksums, container/member relation, platform evidence, parser/rejection state, and report visibility. Divide each family into bounded platform-bundle closure tasks with real bytes and reproducible negative controls; do not treat shared extensions, same names, or source-archive adjacency as a semantic claim.
- [x] Repair the source-scoped re-unpack preservation defect observed during the source-250 MSX refresh. Reproduced failure: `reunpack_retained_source()` called `clear_derived_artifacts()` and deleted each old extracted path before recursively re-parsing the same root, contradicting its append/deduplicate contract. The R4 refresh removed 17 previously committed depth-two bytes and replaced same-named members with different hashes, proving that one source can contain multiple distinct release/package members sharing a filename. The committed corpus baseline was restored before implementation. Re-unpack now preserves every existing descendant and relies on existing source/name/depth/hash deduplication to append only genuinely new byte identities. A regression simulates two divergent parser recoveries from one retained root and proves both earlier and later rows plus their bytes survive. `python scripts/run_primary_workflow.py` passes all gates with 315 tests.
- [x] Preserve authentic extracted corpus bytes through Git commit checks. The append-only source-250 materialization introduced original CP/M configuration/key files whose retained bytes include trailing spaces; `git diff --check` correctly flagged text whitespace but that transformation must never be applied to preservation artifacts. `.gitattributes` now marks every `preservation_corpus/extracted/**` path as binary, preventing EOL or whitespace normalization while preserving deterministic checksum verification for the actual bytes. Authored-text style gates remain separate and continue to inspect project-authored files only.
- [x] Correct the source-250 re-scan command boundary observed during the MSX companion-resource refresh. A first source-scoped command used the CLI default `output/` directory and therefore found zero retained roots; it did not mutate the committed corpus. The completed command explicitly targets `--output-dir preservation_corpus`, then re-unpacks source 250 and fingerprints the same committed SQLite ledger. Future corpus-refresh instructions must name the output directory, preventing a visually successful but non-persistent re-scan.
- [ ] Treat the currently verified MSX-DOS 1DD/2DD 8-/9-sector FAT12 profiles as an expandable evidence set—not a terminal geometry whitelist. Every additional MSX FAT/media geometry must enter retained-sample acquisition, primary-reference research, explicit acceptance contract, malformed-image rejection tests, real-artifact regression, and self-contained regeneration before promotion; no coherent historical geometry may be silently rejected or omitted.
- [ ] Resolve official DAAD R4 source 250’s PCW `PCWSDI.BIN` and `PCWEDI.BIN` interpreter/package structures: establish load model and target semantics using redundant analysis, preserve byte-range evidence, implement native recognition/extraction where applicable, and add real-artifact regressions.
- [ ] Inventory and classify every Atari ST and IBM PC/DOS package/member in official DAAD R4 by platform, executable/load format, interpreter lineage, and DDB/companion relationship; retain the evidence matrix and implement any missing native media support until no R4 target member remains undifferentiated.
- [ ] Convert the source-250 official DAAD R4 bundle into hash-pinned repository-native regeneration entries for its promoted MSX, PCW, Atari ST, and DOS evidence, with all third-party emulators/disassemblers retained only as acquisition or independent validation aids.
- [ ] Close the ADP-to-original C64 *Jabato* evidence loop: retain fixture provenance and hashes, preserve the verified byte-identical `JABATO-C64 1.DDB` to Side A RAM `$3880` and `JABATO-C64 2.DDB` to Side B RAM `$3880` comparisons, and convert every accepted relation into deterministic extraction and regression checks without treating ADP as a physical tape-loader substitute.
- [x] Retain acquisition batch 001 as committed originals, extracts, source database, command logs, classified library, coverage ledger, structural-candidate evidence, and CI hash verifier; the first slice covers Amiga, C64, CPC, Plus/4, and ZX.
- [x] Resolve the retained Amiga Chichén Itzá OFS ADF loop: extract all 19 files, preserve equal-byte members with distinct names, verify `PART1.DDB` and `PART2.DDB` as legacy V2 Amiga DDBs, and refresh stale descendants through a tested `--reunpack-source` workflow.
- [ ] Define an artifact-level authorization/retention gate for DAAD game downloads that is independent from interpreter authorization and records source terms, rights evidence, download URL, timestamps, and SHA-256.
- [ ] Run Harvester discovery/acquisition across ZX Spectrum, CPC, C64, Plus/4, MSX, PCW, Atari ST, Amiga, and IBM PC/DOS sources; preserve every candidate and outcome in a resumable source ledger.
- [ ] Compare every retained original/extracted game member and executable neighbor against official interpreter hashes and structural runtime evidence; retain every differing or unmatched interpreter candidate as a new immutable variant for separate analysis.
- [ ] Commit accepted game originals under a platform/game/source hierarchy and all safe extracted members under a separate lineage-preserving tree, with original-to-member checksum and parser evidence manifests.
- [ ] Convert accepted real artifacts into regression fixtures/manifests for media inspection, recursive unpacking, DDB validation, interpreter correlation, organization, and static-report output without fabricating detections.
- [ ] Publish platform coverage, failures, unavailable items, rights-restricted records, extraction statuses, and test use in dated audit documents and the report corpus.
- [ ] Produce a source-by-source coverage ledger proving which reachable catalogs, queries, pages, and downloads were exhausted; record inaccessible, missing, duplicate, and unresolved historical items as explicit gaps rather than asserting unverifiable universal completeness.

### 9. DAAD game decompiler research and differential implementation

- [ ] Inventory every reachable public DAAD game decompiler, decompiler-like tool, compiler inverse, source recovery project, and format reader; retain complete pinned source/release inputs and document exact platform/version/format scope.
- [ ] Build a modular DAAD game/DDB preservation decompiler, comparable in purpose to UnDAAD but broader and evidence-linked; recover vocabulary, messages, locations, objects, connections, processes, condacts, flags, attributes, graphics/media references, and DSF/SCE-like output with byte-range lineage, confidence, ambiguity, and tool/version metadata.
- [ ] Implement platform-aware profiles for ZX Spectrum, CPC, C64, Plus/4, MSX, PCW, Atari ST, Amiga, and IBM PC/DOS, separating shared DDB semantics from platform loader/media/runtime differences.
- [ ] Replace any single-axis version classification with a multi-axis evidence profile covering DDB structure, compiler lineage, runtime/interpreter lineage, target/load model, media/loader package, graphics/sound companions, encoding/language, derivative/fork family, and game-specific variation.
- [ ] Differentially compare the new decompiler with every retained public implementation/decompiler and independently reproduce disagreements against original bytes, traces, and known game fixtures.
- [ ] Add exhaustive real-artifact regression tests, deterministic output fixtures, source-map/evidence manifests, documentation, and report integration before promoting any recovered interpretation beyond its evidence level.
- [ ] Keep the DAAD game/DDB decompiler explicitly separate from interpreter-binary disassembly/decompilation while linking both evidence layers through runtime version, media lineage, and measured execution behavior.
- [ ] Enforce corpus-complete decompiler coverage: every discovered DAAD-ecosystem artifact must have a manifest, multi-axis profile, decompilation output or explicit blocker record, output hashes, and regression status; newly acquired artifacts automatically join the required coverage set.

### 10. Universal decompile–recompile round-trip verification

- [ ] Define a lossless, version-neutral intermediate representation for every measured DDB byte range, including header fields, section ordering, padding, reserved bytes, compiler-specific layout choices, load address, and companion/loader provenance.
- [ ] Implement a compiler for that representation which can emit each verified historical and modern DAAD/derivative target layout without normalizing away source-byte distinctions.
- [ ] Require `original bytes → decompile → IR → recompile → byte-for-byte comparison` for every retained DDB and every newly acquired DDB candidate; persist input/output SHA-256 values, differing byte ranges, tool versions, target/load model, and deterministic reproducer commands.
- [ ] When a round trip differs, automatically classify the discrepancy as unknown structure, semantic ambiguity, compiler-layout choice, companion/load-model dependency, or implementation defect; retain the mismatch and iterate until either byte identity is achieved or the exact blocking construct receives a dedicated support loop.
- [ ] Add higher-order differential checks: decompile/recompile cycles across supported profiles, semantic process/condact traces where executable evidence is safe, independently derived interpreter behavior, and comparisons against retained public tools such as UnDAAD and ADP.
- [ ] Make byte identity, or a documented and actively iterated byte-range blocker, a mandatory promotion gate for every decompiler/compiler profile. No variant may be treated as complete solely because it produces plausible text output.

### 11. Future ScummVM DAAD engine

- [ ] After corpus coverage, native-media support, DDB decompiler/compiler round-trip evidence, and interpreter/runtime profiles are mature, conduct a separate ScummVM feasibility and architecture study for a universal DAAD engine.
- [ ] Research and retain the current ScummVM engine-development documentation, coding standards, comment conventions, build/test requirements, review expectations, merged-engine histories, and comparable parser/VM engine patterns before writing ScummVM-facing code.
- [ ] Map the DAAD preservation intermediate representation, platform load models, interpreter behavior evidence, graphics/audio companions, and detector metadata to ScummVM subsystems without importing uncertain reverse-engineering claims as runtime behavior.
- [ ] Build the eventual engine through upstream-quality tests, detector fixtures, save/load validation, cross-platform execution evidence, reviewable commits, and feedback-driven alignment with current ScummVM project practices.

### 12. Final emulator-equivalence and ScummVM integration gate

- [ ] Preserve execution order: do not begin native ScummVM engine source implementation until exhaustive all-platform acquisition, format/loader support, game-embedded interpreter analysis, full decompiler/compiler coverage with round-trip evidence, original-runtime differential evidence, and the corpus-completeness audit have passed their explicit gates.
- [ ] Implement the universal DAAD engine directly as a native ScummVM engine, using ScummVM subsystems, coding conventions, detector model, build system, test infrastructure, and review standards from its first production source file; no standalone engine may substitute for this deliverable.
- [ ] Define a platform-neutral behavioral evidence format that records deterministic input scripts, frame/cycle boundaries, DDB/runtime state, flags, locations, object state, messages, graphics, audio events, disk/tape/loader transitions, save states, screenshots, and checksums for every retained executable game path.
- [ ] Run the universal DAAD engine and each corresponding original target runtime under controlled emulators with the same scripted inputs. Compare observable behavior and captured state at every defined checkpoint across all historical and modern DAAD versions, derivatives, platforms, target/load models, and game-specific forks.
- [ ] Treat every behavioral discrepancy—including a single byte, state transition, parser result, timing-sensitive load, graphic cell, text encoding difference, audio event, or save/load divergence—as a retained reproducible defect record. Diagnose, correct, retest, and repeat until equivalence is demonstrated or the exact condition is represented by an active support loop.
- [ ] Execute massive iterative differential testing over the full authorized corpus, including repeated decompile/recompile checks, interpreter comparisons, emulator snapshots, controlled interaction paths, malformed-but-retained structures, and cross-tool disagreement cases. Preserve all scripts, captures, hashes, expected results, and failure deltas.
- [ ] Require a final independent audit of source, tests, corpus manifests, provenance, reverse-engineering claims, security boundaries, build reproducibility, behavioral-equivalence records, and unresolved loops before declaring the universal engine ready for ScummVM-facing work.
- [ ] Only after the final audit passes, create and maintain a ScummVM fork; integrate the DAAD engine through its current coding, architecture, detector, build, test, documentation, review, and contribution practices. Run the complete equivalence suite again in the fork and iterate on every discrepancy before proposing any upstream-quality integration milestone.
