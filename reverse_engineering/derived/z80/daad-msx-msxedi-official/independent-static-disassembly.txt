; z80dasm 1.1.6
; command line: z80dasm -a -g 0 /home/ubuntu/DAAD-harvester/reverse_engineering/artifacts/original/msx/daad-msx-msxedi-official/MSXEDI.Z80

	org	00000h

	jp 0cf3ah		;0000
	ret nc			;0003
	jr nz,$+1		;0004
	rst 38h			;0006
	rst 38h			;0007
	ret p			;0008
	rst 38h			;0009
	ret			;000a
	nop			;000b
	nop			;000c
	jp 0cedfh		;000d
	ret			;0010
	nop			;0011
	nop			;0012
	jp 0cee4h		;0013
	jp 0bdc6h		;0016
	jp 0c845h		;0019
	jp 0c907h		;001c
	jp 0ca54h		;001f
	jp 0c7edh		;0022
	jp 0c7f9h		;0025
	jp 0b02dh		;0028
	ld bc,0dd0fh		;002b
	set 7,a		;002e
	add a,0e1h		;0030
	dec hl			;0032
	dec hl			;0033
	dec hl			;0034
	dec hl			;0035
	ld (0d011h),hl		;0036
	ld a,d			;0039
	ld (0cfd4h),a		;003a
	ld ix,0d0d0h		;003d
	res 6,(ix-001h)		;0041
	ld a,(0cfd8h)		;0045
	or a			;0048
	jr nz,$+10		;0049
	ld a,008h		;004b
	call 0c845h		;004d
	call 0ccadh		;0050
	call 0b078h		;0053
	call 0ca54h		;0056
	call 0c7edh		;0059
	res 2,(ix-001h)		;005c
	call 0ca93h		;0060
	ld hl,0cf3ah		;0063
	ld (hl),00dh		;0066
	ld (0cfb3h),hl		;0068
	ld (ix-007h),078h		;006b
	ld hl,(0cfd7h)		;006f
	ld a,h			;0072
	or a			;0073
	jp z,0b0cah		;0074
	jp (hl)			;0077
	call 0c905h		;0078
	ld a,(0cfd4h)		;007b
	ld hl,0b0beh		;007e
	bit 0,(ix-001h)		;0081
	jr nz,$+19		;0085
	ld hl,0b0adh		;0087
	cp 065h		;008a
	jr z,$+30		;008c
	ld hl,0b0b3h		;008e
	cp 066h		;0091
	jr z,$+23		;0093
	ld hl,0b0b7h		;0095
	ld e,a			;0098
	push de			;0099
	call 0bd9dh		;009a
	pop de			;009d
	call 0bd4ch		;009e
	bit 0,(ix-001h)		;00a1
	res 0,(ix-001h)		;00a5
	ret			;00a9
	jp 0bd9dh		;00aa
	cp l			;00ad
	xor l			;00ae
	cp d			;00af
	cp (hl)			;00b0
	or h			;00b1
	push af			;00b2
	or (hl)			;00b3
	ret nc			;00b4
	or b			;00b5
	rst 18h			;00b6
	cp d			;00b7
	adc a,l			;00b8
	adc a,l			;00b9
	sub b			;00ba
	adc a,l			;00bb
	rst 18h			;00bc
	push af			;00bd
	cp b			;00be
	sbc a,(hl)			;00bf
	sub d			;00c0
	sbc a,d			;00c1
	rst 18h			;00c2
	cp d			;00c3
	adc a,l			;00c4
	adc a,l			;00c5
	sub b			;00c6
	adc a,l			;00c7
	rst 18h			;00c8
	push af			;00c9
	ld sp,(0cfd5h)		;00ca
	call 0ceefh		;00ce
	ld a,002h		;00d1
	call 0005fh		;00d3
	ld a,(0b02bh)		;00d6
	ld (0f3ebh),a		;00d9
	ld (0f3eah),a		;00dc
	ld a,(0b02ch)		;00df
	ld (0f3e9h),a		;00e2
	call 00062h		;00e5
	call 0cefah		;00e8
	ld de,0d024h		;00eb
	ld a,008h		;00ee
	ld hl,0d014h		;00f0
	ld bc,00010h		;00f3
	ldir		;00f6
	dec a			;00f8
	jr nz,$-9		;00f9
	ld (0d0b6h),a		;00fb
	ld de,0d0b7h		;00fe
	ld hl,0d014h		;0101
	ld bc,00010h		;0104
	ldir		;0107
	ld a,(0d0bbh)		;0109
	ld e,a			;010c
	ld d,000h		;010d
	ld (0cfbbh),de		;010f
	ld hl,0d0d0h		;0113
	push hl			;0116
	pop ix		;0117
	xor a			;0119
	ld b,a			;011a
	ld (hl),a			;011b
	inc hl			;011c
	djnz $-2		;011d
	set 7,(ix+01dh)		;011f
	call 0b12dh		;0123
	jp nz,0b05ch		;0126
	call 0ceefh		;0129
	rst 0			;012c
	ld (0d00dh),sp		;012d
	xor a			;0131
	ld (0d010h),a		;0132
	call 0bd0ah		;0135
	xor a			;0138
	push af			;0139
	ld (0d013h),a		;013a
	ld (0d00fh),a		;013d
	xor a			;0140
	cp (hl)			;0141
	jr nz,$+26		;0142
	pop de			;0144
	ld a,d			;0145
	or a			;0146
	jr z,$+7		;0147
	pop hl			;0149
	pop bc			;014a
	jp 0b5e8h		;014b
	ld hl,0d00fh		;014e
	cp (hl)			;0151
	ret z			;0152
	ld a,e			;0153
	ld (0d010h),a		;0154
	dec (hl)			;0157
	pop bc			;0158
	pop hl			;0159
	jr $+34		;015a
	ld a,(hl)			;015c
	inc hl			;015d
	cp 0ffh		;015e
	jr z,$+7		;0160
	cp (ix+021h)		;0162
	jr nz,$+12		;0165
	ld a,(hl)			;0167
	cp 0ffh		;0168
	jr z,$+13		;016a
	cp (ix+022h)		;016c
	jr z,$+8		;016f
	inc hl			;0171
	inc hl			;0172
	inc hl			;0173
	jp 0b140h		;0174
	inc hl			;0177
	ld c,(hl)			;0178
	inc hl			;0179
	ld b,(hl)			;017a
	inc hl			;017b
	ld a,(bc)			;017c
	cp 0ffh		;017d
	jr z,$-63		;017f
	ld (0d0cbh),a		;0181
	push hl			;0184
	ld d,000h		;0185
	sla a		;0187
	ld e,a			;0189
	inc bc			;018a
	ld a,(bc)			;018b
	ld hl,0b788h		;018c
	add hl,de			;018f
	ld e,(hl)			;0190
	inc hl			;0191
	ld d,(hl)			;0192
	push de			;0193
	ld d,000h		;0194
	ld e,a			;0196
	bit 7,(ix-005h)		;0197
	jr z,$+8		;019b
	ld hl,0d0d0h		;019d
	add hl,de			;01a0
	ld a,(hl)			;01a1
	ld e,a			;01a2
	ld hl,0d1d0h		;01a3
	add hl,de			;01a6
	push hl			;01a7
	ld hl,0d0d0h		;01a8
	add hl,de			;01ab
	pop de			;01ac
	ret			;01ad
	cp (ix+026h)		;01ae
	jr z,$+99		;01b1
	jr $+14		;01b3
	cp (ix+026h)		;01b5
	jr nz,$+92		;01b8
	jr $+7		;01ba
	cp (ix+026h)		;01bc
	jr c,$+85		;01bf
	pop hl			;01c1
	jp 0b140h		;01c2
	cp (ix+026h)		;01c5
	jr c,$-7		;01c8
	jr z,$-9		;01ca
	jr $+72		;01cc
	ld a,(de)			;01ce
	cp (ix+026h)		;01cf
	jr z,$+66		;01d2
	cp 0fdh		;01d4
	jr nc,$+62		;01d6
	jr $-23		;01d8
	ld a,(de)			;01da
	cp (ix+026h)		;01db
	jr z,$-29		;01de
	cp 0fdh		;01e0
	jr nc,$-33		;01e2
	jr $+48		;01e4
	ld a,(de)			;01e6
	cp 0fdh		;01e7
	jr z,$+43		;01e9
	jr $-42		;01eb
	ld a,(de)			;01ed
	cp 0fdh		;01ee
	jr nz,$+36		;01f0
	jr $-49		;01f2
	ld a,(de)			;01f4
	cp 0feh		;01f5
	jr nz,$-54		;01f7
	jr $+27		;01f9
	ld a,(de)			;01fb
	cp 0feh		;01fc
	jr z,$-61		;01fe
	jr $+20		;0200
	call 0bd29h		;0202
	cp h			;0205
	jr nc,$+14		;0206
	jr $-71		;0208
	ld a,(hl)			;020a
	or a			;020b
	jr nz,$-75		;020c
	jr $+6		;020e
	ld a,(hl)			;0210
	or a			;0211
	jr z,$-81		;0212
	inc bc			;0214
	pop hl			;0215
	jp 0b17ch		;0216
	inc bc			;0219
	ld a,(bc)			;021a
	cp (hl)			;021b
	jr nz,$-91		;021c
	jr $-10		;021e
	call 0b50dh		;0220
	jr $-8		;0223
	inc bc			;0225
	ld a,(bc)			;0226
	cp (hl)			;0227
	jr z,$-103		;0228
	jr $-22		;022a
	call 0b50dh		;022c
	jr $-8		;022f
	call 0b50dh		;0231
	jr $+4		;0234
	inc bc			;0236
	ld a,(bc)			;0237
	cp (hl)			;0238
	jr c,$-37		;0239
	jr $-122		;023b
	call 0b50dh		;023d
	jr $+4		;0240
	inc bc			;0242
	ld a,(bc)			;0243
	cp (hl)			;0244
	jp c,0b1c1h		;0245
	jp z,0b1c1h		;0248
	jp 0b214h		;024b
	cp (ix+023h)		;024e
	jr z,$-61		;0251
	jp 0b1c1h		;0253
	cp (ix+024h)		;0256
	jr $-8		;0259
	cp (ix+02bh)		;025b
	jr $-13		;025e
	cp (ix+02ch)		;0260
	jr $-18		;0263
	cp (ix+02dh)		;0265
	jr $-23		;0268
	ld l,00ch		;026a
	push bc			;026c
	call 0bfefh		;026d
	pop bc			;0270
	ld a,(hl)			;0271
	ld l,01eh		;0272
	call 0bd1ah		;0274
	cpl			;0277
	cp (hl)			;0278
	jp nz,0b1c1h		;0279
	jp 0b457h		;027c
	ld l,00dh		;027f
	call 0bfefh		;0281
	ld a,(hl)			;0284
	ld l,01fh		;0285
	call 0bd1ah		;0287
	cpl			;028a
	sub (hl)			;028b
	jr z,$+13		;028c
	xor a			;028e
	ld (0d0f6h),a		;028f
	ld sp,(0d00dh)		;0292
	jp 0b131h		;0296
	ld sp,(0d00dh)		;0299
	or a			;029d
	ret			;029e
	res 2,(ix-001h)		;029f
	call 0bd94h		;02a3
	pop hl			;02a6
	set 4,(ix-001h)		;02a7
	jp 0b144h		;02ab
	ld l,00fh		;02ae
	jr $-13		;02b0
	push bc			;02b2
	call 0c7cfh		;02b3
	pop bc			;02b6
	jp 0b457h		;02b7
	inc bc			;02ba
	call 0b00ah		;02bb
	jp 0b456h		;02be
	inc bc			;02c1
	call 0b00dh		;02c2
	jp 0b456h		;02c5
	push bc			;02c8
	call 0c885h		;02c9
	pop bc			;02cc
	jp 0b457h		;02cd
	ld (ix+001h),000h		;02d0
	ld hl,0d1d0h		;02d4
	ld a,(hl)			;02d7
	cp 0ffh		;02d8
	jp z,0b457h		;02da
	cp 0fdh		;02dd
	jr c,$+6		;02df
	ld a,(0d0f6h)		;02e1
	ld (hl),a			;02e4
	inc hl			;02e5
	jr $-15		;02e6
	push bc			;02e8
	ld a,(0d0f6h)		;02e9
	ld bc,0fefdh		;02ec
	call 0b952h		;02ef
	pop bc			;02f2
	ld l,01ah		;02f3
	jp z,0b384h		;02f5
	dec bc			;02f8
	call 0b99fh		;02f9
	ld a,(de)			;02fc
	ld l,019h		;02fd
	cp 0fdh		;02ff
	jr nc,$+74		;0301
	inc l			;0303
	cp (ix+026h)		;0304
	jr nz,$+129		;0307
	ld a,(0d103h)		;0309
	call 0b8cbh		;030c
	call 0b8a5h		;030f
	ld l,02bh		;0312
	add a,h			;0314
	jr c,$+115		;0315
	ld hl,0d104h		;0317
	cp (hl)			;031a
	jr z,$+6		;031b
	ld l,02bh		;031d
	jr nc,$+105		;031f
	ld l,024h		;0321
	ld h,01bh		;0323
	push hl			;0325
	ld a,(0d0d1h)		;0326
	ld hl,0d0f5h		;0329
	cp (hl)			;032c
	jr nc,$+15		;032d
	ex de,hl			;032f
	ld (hl),0feh		;0330
	inc (ix+001h)		;0332
	pop hl			;0335
	call 0bd94h		;0336
	jp 0b456h		;0339
	pop hl			;033c
	ld l,h			;033d
	ld a,(0d013h)		;033e
	or a			;0341
	jp z,0b29fh		;0342
	ld a,(00103h)		;0345
	ld (0d013h),a		;0348
	jp 0b29fh		;034b
	call 0b943h		;034e
	jr z,$+51		;0351
	dec bc			;0353
	call 0b99fh		;0354
	ld a,(de)			;0357
	cp 0fdh		;0358
	ld l,018h		;035a
	jr z,$-17		;035c
	ld l,031h		;035e
	cp (ix+026h)		;0360
	jr z,$-24		;0363
	ld l,01ch		;0365
	cp 0feh		;0367
	jr nz,$-30		;0369
	ld a,(0d0f6h)		;036b
	ld (de),a			;036e
	dec (ix+001h)		;036f
	ld l,027h		;0372
	jr $-62		;0374
	push bc			;0376
	ld a,0fdh		;0377
	ld b,0feh		;0379
	call 0b94fh		;037b
	pop bc			;037e
	dec bc			;037f
	jr nz,$+14		;0380
	ld l,017h		;0382
	jr c,$+4		;0384
	ld l,008h		;0386
	jp 0b29fh		;0388
	call 0b99fh		;038b
	ld a,(de)			;038e
	ld l,032h		;038f
	cp 0feh		;0391
	jr z,$-11		;0393
	cp (ix+026h)		;0395
	jr z,$-16		;0398
	ld l,017h		;039a
	cp 0fdh		;039c
	jr nz,$-22		;039e
	call 0b926h		;03a0
	inc l			;03a3
	jr nc,$-28		;03a4
	ld l,026h		;03a6
	ld h,02ah		;03a8
	jp 0b325h		;03aa
	push bc			;03ad
	ld b,0fdh		;03ae
	call 0b94dh		;03b0
	pop bc			;03b3
	ld l,01ch		;03b4
	jr z,$-50		;03b6
	dec bc			;03b8
	call 0b99fh		;03b9
	ld a,(de)			;03bc
	ld l,031h		;03bd
	cp (ix+026h)		;03bf
	jr z,$-58		;03c2
	ld l,01dh		;03c4
	cp 0fdh		;03c6
	jr z,$-64		;03c8
	dec l			;03ca
	jr c,$-67		;03cb
	call 0b926h		;03cd
	jr nc,$-72		;03d0
	ex de,hl			;03d2
	ld (hl),0fdh		;03d3
	dec (ix+001h)		;03d5
	ld l,025h		;03d8
	jp 0b336h		;03da
	ld e,a			;03dd
	ld d,000h		;03de
	push bc			;03e0
	call 0c809h		;03e1
	pop bc			;03e4
	jr $+113		;03e5
	ld (0d0f6h),a		;03e7
	jr $+108		;03ea
	ld de,(0010eh)		;03ec
	ld hl,00104h		;03f0
	jr $+22		;03f3
	set 6,(ix-001h)		;03f5
	ld de,(00110h)		;03f9
	ld hl,00105h		;03fd
	jr $+9		;0400
	ld de,(00112h)		;0402
	ld hl,00106h		;0406
	cp (hl)			;0409
	jr nc,$+17		;040a
	ex de,hl			;040c
	ld d,000h		;040d
	ld e,a			;040f
	add hl,de			;0410
	add hl,de			;0411
	ld e,(hl)			;0412
	inc hl			;0413
	ld d,(hl)			;0414
	ex de,hl			;0415
	call 0bd9dh		;0416
	jr $+61		;0419
	ld d,007h		;041b
	jp 0b02dh		;041d
	call 0b99fh		;0420
	ex de,hl			;0423
	ld a,(hl)			;0424
	cp 0feh		;0425
	ld (hl),0fch		;0427
	jr nz,$+45		;0429
	dec (ix+001h)		;042b
	jr $+40		;042e
	call 0b99fh		;0430
	ld a,(de)			;0433
	cp 0feh		;0434
	ld a,(0d0f6h)		;0436
	ld (de),a			;0439
	jr $-17		;043a
	inc bc			;043c
	ld a,(bc)			;043d
	call 0b99fh		;043e
	ld l,a			;0441
	ld h,000h		;0442
	ld a,(de)			;0444
	push de			;0445
	ld de,0d1d0h		;0446
	add hl,de			;0449
	ld d,(hl)			;044a
	ld (hl),a			;044b
	pop hl			;044c
	ld (hl),d			;044d
	jr $+8		;044e
	ld (hl),0ffh		;0450
	jr $+4		;0452
	ld (hl),000h		;0454
	inc bc			;0456
	pop hl			;0457
	set 4,(ix-001h)		;0458
	jp 0b17ch		;045c
	inc bc			;045f
	ld d,a			;0460
	ld a,(bc)			;0461
	add a,(hl)			;0462
	jr nc,$+36		;0463
	ld a,0ffh		;0465
	jr $+32		;0467
	inc bc			;0469
	ld a,(bc)			;046a
	ld d,a			;046b
	ld a,(hl)			;046c
	sub d			;046d
	jr nc,$+3		;046e
	xor a			;0470
	ld (hl),a			;0471
	jr $-28		;0472
	inc bc			;0474
	push af			;0475
	ld a,(bc)			;0476
	push hl			;0477
	ld e,a			;0478
	ld d,000h		;0479
	ld hl,0d0d0h		;047b
	add hl,de			;047e
	ld e,(hl)			;047f
	pop hl			;0480
	pop af			;0481
	jr $+7		;0482
	inc bc			;0484
	ld d,a			;0485
	ld a,(bc)			;0486
	ld e,a			;0487
	ld a,d			;0488
	cp 026h		;0489
	jr nz,$+14		;048b
	ld a,(00104h)		;048d
	cp e			;0490
	ld d,001h		;0491
	jp c,0b02dh		;0493
	jp z,0b02dh		;0496
	ld (hl),e			;0499
	jr $-68		;049a
	call 0c905h		;049c
	jr $-72		;049f
	ld e,(hl)			;04a1
	inc hl			;04a2
	ld d,(hl)			;04a3
	call 0bd4eh		;04a4
	jr $-81		;04a7
	ld e,(hl)			;04a9
	call 0bd4ch		;04aa
	jr $-87		;04ad
	inc bc			;04af
	ex de,hl			;04b0
	ld a,(bc)			;04b1
	call 0b91fh		;04b2
	cp (hl)			;04b5
	jp nz,0b1c1h		;04b6
	jp 0b214h		;04b9
	inc bc			;04bc
	ex de,hl			;04bd
	ld a,(bc)			;04be
	call 0b91fh		;04bf
	cp (hl)			;04c2
	jp nz,0b214h		;04c3
	jp 0b1c1h		;04c6
	call 0b99fh		;04c9
	inc bc			;04cc
	ex de,hl			;04cd
	ld a,(bc)			;04ce
	call 0b91fh		;04cf
	push af			;04d2
	ld a,(hl)			;04d3
	jr $+31		;04d4
	call 0b91fh		;04d6
	push af			;04d9
	ld a,(0d103h)		;04da
	jr $+19		;04dd
	ld a,(hl)			;04df
	cp 0ffh		;04e0
	jr nz,$+8		;04e2
	ld d,002h		;04e4
	jp 0b02dh		;04e6
	ld a,(de)			;04e9
	push af			;04ea
	inc bc			;04eb
	ld a,(bc)			;04ec
	call 0b99fh		;04ed
	call 0b93ah		;04f0
	cp 0feh		;04f3
	jr nz,$+5		;04f5
	dec (ix+001h)		;04f7
	pop af			;04fa
	cp 0feh		;04fb
	jr nz,$+5		;04fd
	inc (ix+001h)		;04ff
	ld (hl),a			;0502
	jp 0b456h		;0503
	ex de,hl			;0506
	call 0b50eh		;0507
	jp 0b487h		;050a
	ex de,hl			;050d
	inc bc			;050e
	ld a,(bc)			;050f
	push bc			;0510
	ld c,a			;0511
	ld b,000h		;0512
	ld hl,0d0d0h		;0514
	add hl,bc			;0517
	ld a,(de)			;0518
	ld e,a			;0519
	ld d,c			;051a
	pop bc			;051b
	ret			;051c
	call 0b50dh		;051d
	jp 0b462h		;0520
	call 0b50dh		;0523
	jp 0b46bh		;0526
	ld d,(ix+026h)		;0529
	set 1,(ix-001h)		;052c
	call 0ba1ch		;0530
	jp 0b457h		;0533
	push bc			;0536
	or a			;0537
	jr nz,$+7		;0538
	call 0bacch		;053a
	jr $+5		;053d
	call 0bac0h		;053f
	pop bc			;0542
	jp c,0b214h		;0543
	set 4,(ix-001h)		;0546
	jp 0b1c1h		;054a
	call 0b91fh		;054d
	ld d,a			;0550
	res 1,(ix-001h)		;0551
	call 0ba1ch		;0555
	jp 0b456h		;0558
	inc bc			;055b
	ld hl,00107h		;055c
	cp (hl)			;055f
	jr nc,$+40		;0560
	or a			;0562
	jr z,$+37		;0563
	call 0bd0ah		;0565
	push af			;0568
	ld a,(0d00fh)		;0569
	inc a			;056c
	ld (0d00fh),a		;056d
	cp 00ah		;0570
	jp z,0b58dh		;0572
	pop af			;0575
	push bc			;0576
	ld de,(0d010h)		;0577
	ld d,000h		;057b
	push de			;057d
	ld (0d010h),a		;057e
	res 4,(ix-001h)		;0581
	jp 0b140h		;0585
	ld d,006h		;0588
	jp 0b02dh		;058a
	pop af			;058d
	ld d,003h		;058e
	jp 0b02dh		;0590
	pop hl			;0593
	ld a,(0d010h)		;0594
	call 0bd0ah		;0597
	jp 0b140h		;059a
	ld (0d100h),a		;059d
	inc bc			;05a0
	ld a,(bc)			;05a1
	ld e,a			;05a2
	ld d,0f8h		;05a3
	jr $+28		;05a5
	inc bc			;05a7
	jp 0b456h		;05a8
	ld (0d0f5h),a		;05ab
	inc bc			;05ae
	ld a,(bc)			;05af
	ld (0d104h),a		;05b0
	jp 0b456h		;05b3
	ld (0d0f9h),a		;05b6
	inc bc			;05b9
	ld a,(bc)			;05ba
	rlca			;05bb
	rlca			;05bc
	rlca			;05bd
	ld e,a			;05be
	ld d,0c7h		;05bf
	ld a,(0d101h)		;05c1
	xor e			;05c4
	and d			;05c5
	xor e			;05c6
	ld (0d101h),a		;05c7
	jp 0b456h		;05ca
	res 2,(ix-001h)		;05cd
	jp 0b457h		;05d1
	call 0b91fh		;05d4
	ld (0d102h),a		;05d7
	ld a,(0d013h)		;05da
	or a			;05dd
	ld d,004h		;05de
	jp nz,0b02dh		;05e0
	pop hl			;05e3
	res 4,(ix-001h)		;05e4
	push bc			;05e8
	push hl			;05e9
	ld de,0ff00h		;05ea
	push de			;05ed
	push hl			;05ee
	push bc			;05ef
	ld a,(0d013h)		;05f0
	ld e,a			;05f3
	ld a,(0d102h)		;05f4
	call 0b9f4h		;05f7
	jr nc,$+41		;05fa
	ld (0d0f2h),a		;05fc
	cp (ix+02ch)		;05ff
	inc hl			;0602
	ld a,(hl)			;0603
	ld (0d0f3h),a		;0604
	jr nz,$+15		;0607
	ld a,(0d0fdh)		;0609
	cp 0ffh		;060c
	jr z,$+5		;060e
	cp (hl)			;0610
	jr nz,$+5		;0611
	pop hl			;0613
	jr $-32		;0614
	ld a,e			;0616
	ld (0d013h),a		;0617
	dec a			;061a
	call 0b99fh		;061b
	pop de			;061e
	pop bc			;061f
	jp 0b456h		;0620
	xor a			;0623
	ld (0d013h),a		;0624
	pop bc			;0627
	pop hl			;0628
	pop hl			;0629
	pop hl			;062a
	pop hl			;062b
	jp 0b144h		;062c
	push af			;062f
	call 0b50eh		;0630
	pop af			;0633
	push hl			;0634
	call 0b8cbh		;0635
	ld a,h			;0638
	pop hl			;0639
	jp 0b487h		;063a
	ld d,a			;063d
	call 0b8a5h		;063e
	jp 0b487h		;0641
	push hl			;0644
	call 0bd29h		;0645
	ld d,a			;0648
	ld a,h			;0649
	pop hl			;064a
	jp 0b487h		;064b
	ld (0cfd9h),a		;064e
	call 0b943h		;0651
	jp z,0b384h		;0654
	jr $+10		;0657
	call 0b99fh		;0659
	inc bc			;065c
	ld a,(bc)			;065d
	ld (0cfd9h),a		;065e
	ld a,(de)			;0661
	cp 0fdh		;0662
	ld l,018h		;0664
	jp z,0b29fh		;0666
	ld l,031h		;0669
	cp (ix+026h)		;066b
	jp z,0b29fh		;066e
	cp 0feh		;0671
	ld l,01ch		;0673
	jp nz,0b29fh		;0675
	ld a,(0cfd9h)		;0678
	ld (de),a			;067b
	dec (ix+001h)		;067c
	ld l,02ch		;067f
	call 0b6d9h		;0681
	jp 0b336h		;0684
	ld (0cfd9h),a		;0687
	push bc			;068a
	ld bc,0fefdh		;068b
	call 0b952h		;068e
	jr nz,$+9		;0691
	ld a,(0d0f6h)		;0693
	ld b,a			;0696
	call 0b94fh		;0697
	pop bc			;069a
	jr nz,$+10		;069b
	ld a,(0cfd9h)		;069d
	jr c,$+49		;06a0
	jp 0b386h		;06a2
	ld a,(0cfd9h)		;06a5
	jr $+7		;06a8
	call 0b99fh		;06aa
	inc bc			;06ad
	ld a,(bc)			;06ae
	ld h,a			;06af
	ld a,(de)			;06b0
	ld l,019h		;06b1
	cp 0fdh		;06b3
	jp nc,0b29fh		;06b5
	cp h			;06b8
	jr nz,$+16		;06b9
	ld a,h			;06bb
	push de			;06bc
	call 0b93ah		;06bd
	pop de			;06c0
	cp 0fdh		;06c1
	jp nc,0b321h		;06c3
	jp 0b309h		;06c6
	ld l,02dh		;06c9
	cp (ix+026h)		;06cb
	ld a,h			;06ce
	jr z,$+4		;06cf
	ld l,034h		;06d1
	call 0b6d9h		;06d3
	jp 0b29fh		;06d6
	push af			;06d9
	call 0bd94h		;06da
	pop af			;06dd
	set 4,(ix-003h)		;06de
	call 0bcceh		;06e2
	ld l,033h		;06e5
	ret			;06e7
	push bc			;06e8
	ld b,0fdh		;06e9
	call 0b94dh		;06eb
	pop bc			;06ee
	jp 0b457h		;06ef
	push bc			;06f2
	ld hl,(00118h)		;06f3
	ld de,0d1d0h		;06f6
	ld a,(hl)			;06f9
	ldi		;06fa
	cp 0feh		;06fc
	jr c,$-5		;06fe
	jr nz,$-18		;0700
	inc (ix+001h)		;0702
	jr $-12		;0705
	res 4,(ix-001h)		;0707
	pop hl			;070b
	jp 0b144h		;070c
	push bc			;070f
	ld a,(hl)			;0710
	push hl			;0711
	ld hl,00104h		;0712
	cp (hl)			;0715
	call c,0baa6h		;0716
	pop hl			;0719
	pop bc			;071a
	jp nc,0b1c1h		;071b
	ld (hl),a			;071e
	jp 0b456h		;071f
	bit 4,(ix-001h)		;0722
	jp nz,0b215h		;0726
	jp 0b1c1h		;0729
	bit 4,(ix-001h)		;072c
	jp nz,0b1c1h		;0730
	jp 0b215h		;0733
	ld h,000h		;0736
	ld l,a			;0738
	sla l		;0739
	jr nc,$+3		;073b
	dec h			;073d
	add hl,hl			;073e
	pop de			;073f
	add hl,de			;0740
	jp 0b140h		;0741
	cp 0ffh		;0744
	jr z,$+5		;0746
	ld (0d0f1h),a		;0748
	inc bc			;074b
	ld a,(bc)			;074c
	cp 0ffh		;074d
	jr z,$+5		;074f
	ld (0d0f2h),a		;0751
	jp 0b456h		;0754
	call 0b99fh		;0757
	jp 0b456h		;075a
	ld a,020h		;075d
	call 0c907h		;075f
	jp 0b457h		;0762
	call 0b888h		;0765
	jp nz,0b214h		;0768
	jp 0b1c1h		;076b
	call 0b888h		;076e
	jp nz,0b1c1h		;0771
	jp 0b214h		;0774
	ld l,a			;0777
	inc bc			;0778
	ld a,(bc)			;0779
	inc bc			;077a
	ld h,a			;077b
	call 0b782h		;077c
	jp 0b457h		;077f
	jp (hl)			;0782
	ld d,005h		;0783
	jp 0b02dh		;0785
	xor (hl)			;0788
	or c			;0789
	or l			;078a
	or c			;078b
	cp h			;078c
	or c			;078d
	push bc			;078e
	or c			;078f
	adc a,0b1h		;0790
	jp c,0e6b1h		;0792
	or c			;0795
	cpir		;0796
	call p,0fbb1h		;0798
	or c			;079b
	ld (bc),a			;079c
	or d			;079d
	ld a,(bc)			;079e
	or d			;079f
	djnz $-76		;07a0
	add hl,de			;07a2
	or d			;07a3
	ld (hl),0b2h		;07a4
	ld b,d			;07a6
	or d			;07a7
	ld c,(hl)			;07a8
	or d			;07a9
	ld d,(hl)			;07aa
	or d			;07ab
	pop bc			;07ac
	or d			;07ad
	call pe,06ab3h		;07ae
	or d			;07b1
	ld a,a			;07b2
	or d			;07b3
	and (hl)			;07b4
	or d			;07b5
	xor (hl)			;07b6
	or d			;07b7
	or d			;07b8
	or d			;07b9
	ld d,e			;07ba
	cp a			;07bb
	ld (hl),h			;07bc
	cp a			;07bd
	and c			;07be
	or h			;07bf
	call m,0c8bdh		;07c0
	or d			;07c3
	ret nc			;07c4
	or d			;07c5
	ret pe			;07c6
	or d			;07c7
	ld c,(hl)			;07c8
	or e			;07c9
	xor l			;07ca
	or e			;07cb
	halt			;07cc
	or e			;07cd
	defb 0ddh,0b3h,044h	;illegal sequence		;07ce
	or a			;07d1
	rst 20h			;07d2
	or e			;07d3
	push af			;07d4
	or e			;07d5
	adc a,e			;07d6
	or e			;07d7
	ld sp,hl			;07d8
	or d			;07d9
	ld d,h			;07da
	or e			;07db
	cp c			;07dc
	or e			;07dd
	jr nz,$-74		;07de
	jr nc,$-74		;07e0
	inc a			;07e2
	or h			;07e3
	ret			;07e4
	or h			;07e5
	ld d,b			;07e6
	or h			;07e7
	ld d,h			;07e8
	or h			;07e9
	ld e,a			;07ea
	or h			;07eb
	ld l,c			;07ec
	or h			;07ed
	add a,h			;07ee
	or h			;07ef
	sbc a,h			;07f0
	or h			;07f1
	xor c			;07f2
	or h			;07f3
	ld (bc),a			;07f4
	or h			;07f5
	xor a			;07f6
	or h			;07f7
	ld d,a			;07f8
	or a			;07f9
	ld e,l			;07fa
	or a			;07fb
	ld h,l			;07fc
	or a			;07fd
	ld l,(hl)			;07fe
	or a			;07ff
	add hl,hl			;0800
	or l			;0801
	cp d			;0802
	or d			;0803
	add hl,de			;0804
	cp a			;0805
	add hl,hl			;0806
	cp a			;0807
	call z,0d0beh		;0808
	cp (hl)			;080b
	rst 10h			;080c
	cp (hl)			;080d
	pop af			;080e
	cp (hl)			;080f
	ld e,e			;0810
	or d			;0811
	ld h,b			;0812
	or d			;0813
	ld h,l			;0814
	or d			;0815
	dec e			;0816
	or l			;0817
	inc hl			;0818
	or l			;0819
	ld (hl),0b5h		;081a
	ld c,l			;081c
	or l			;081d
	ld e,e			;081e
	or l			;081f
	jr nz,$-76		;0820
	ld sp,hl			;0822
	or e			;0823
	ld a,l			;0824
	cp (hl)			;0825
	dec h			;0826
	or d			;0827
	inc l			;0828
	or d			;0829
	ret c			;082a
	cp l			;082b
	sub l			;082c
	cp (hl)			;082d
	sbc a,l			;082e
	or l			;082f
	rst 20h			;0830
	cp l			;0831
	call nc,0a7b5h		;0832
	or l			;0835
	and a			;0836
	or l			;0837
	cp h			;0838
	or h			;0839
	cpl			;083a
	or (hl)			;083b
	ld e,c			;083c
	or (hl)			;083d
	xor d			;083e
	or (hl)			;083f
	call 0abb5h		;0840
	or l			;0843
	dec a			;0844
	or (hl)			;0845
	ld b,h			;0846
	or (hl)			;0847
	or (hl)			;0848
	or l			;0849
	ld (036beh),hl		;084a
	cp (hl)			;084d
	ld e,d			;084e
	cp (hl)			;084f
	ret pe			;0850
	or (hl)			;0851
	ld (hl),a			;0852
	or a			;0853
	sub 0b4h		;0854
	rlca			;0856
	or a			;0857
	ld c,(hl)			;0858
	or (hl)			;0859
	add a,a			;085a
	or (hl)			;085b
	rrca			;085c
	or a			;085d
	or c			;085e
	cp (hl)			;085f
	sub e			;0860
	or l			;0861
	add a,l			;0862
	cp (hl)			;0863
	sbc a,c			;0864
	or d			;0865
	ld sp,hl			;0866
	cp (hl)			;0867
	dec a			;0868
	or d			;0869
	ld sp,022b2h		;086a
	or a			;086d
	inc l			;086e
	or a			;086f
	ld (hl),0b7h		;0870
	sub d			;0872
	or d			;0873
	ld c,l			;0874
	cp (hl)			;0875
	rlca			;0876
	or l			;0877
	add a,e			;0878
	or a			;0879
	jp (hl)			;087a
	or h			;087b
	add a,e			;087c
	or a			;087d
	rst 18h			;087e
	or h			;087f
	add a,e			;0880
	or a			;0881
	ld b,0b5h		;0882
	ld (hl),h			;0884
	or h			;0885
	jp p,067b6h		;0886
	rrca			;0889
	rrca			;088a
	rrca			;088b
	and 01fh		;088c
	ld l,a			;088e
	ld a,03bh		;088f
	sub l			;0891
	ld (0b8a2h),a		;0892
	ld a,h			;0895
	rlca			;0896
	rlca			;0897
	rlca			;0898
	and 038h		;0899
	or 046h		;089b
	ld (0b8a3h),a		;089d
	defb 0ddh,0cbh,000h,000h	;rlc (ix+000h) & ld b,(ix+000h)		;08a0
	ret			;08a4
	xor a			;08a5
	push hl			;08a6
	push de			;08a7
	push bc			;08a8
	ld bc,(00102h)		;08a9
	ld c,a			;08ad
	ld de,0d1d0h		;08ae
	push af			;08b1
	ld a,(de)			;08b2
	cp 0fdh		;08b3
	ld l,c			;08b5
	ld h,000h		;08b6
	call nc,0b8cch		;08b8
	pop af			;08bb
	add a,h			;08bc
	jr nc,$+6		;08bd
	ld a,0ffh		;08bf
	jr $+6		;08c1
	inc c			;08c3
	inc de			;08c4
	djnz $-20		;08c5
	pop bc			;08c7
	pop de			;08c8
	pop hl			;08c9
	ret			;08ca
	ld l,a			;08cb
	ex de,hl			;08cc
	xor a			;08cd
	ld d,a			;08ce
	ld (0cfdbh),a		;08cf
	call 0b8d8h		;08d2
	ex de,hl			;08d5
	ld a,l			;08d6
	ret			;08d7
	push hl			;08d8
	push de			;08d9
	push bc			;08da
	call 0b932h		;08db
	and 07fh		;08de
	add a,0c0h		;08e0
	res 6,a		;08e2
	res 7,a		;08e4
	jr nc,$+45		;08e6
	and 03fh		;08e8
	jr z,$+40		;08ea
	ld c,a			;08ec
	ld a,(0cfdbh)		;08ed
	inc a			;08f0
	ld (0cfdbh),a		;08f1
	cp 00ah		;08f4
	jr z,$+23		;08f6
	ld a,c			;08f8
	ld bc,(00102h)		;08f9
	ld c,e			;08fd
	ld e,000h		;08fe
	ld d,a			;0900
	ld hl,0d1d0h		;0901
	ld a,(hl)			;0904
	cp c			;0905
	call z,0b8d8h		;0906
	inc hl			;0909
	inc e			;090a
	djnz $-7		;090b
	ld hl,0cfdbh		;090d
	dec (hl)			;0910
	ld a,d			;0911
	scf			;0912
	pop bc			;0913
	pop de			;0914
	pop hl			;0915
	push af			;0916
	add a,d			;0917
	ld d,a			;0918
	jr nc,$+4		;0919
	ld d,0ffh		;091b
	pop af			;091d
	ret			;091e
	cp 0ffh		;091f
	ret nz			;0921
	ld a,(0d0f6h)		;0922
	ret			;0925
	push de			;0926
	ld e,(ix+033h)		;0927
	call 0b932h		;092a
	pop de			;092d
	ld l,028h		;092e
	rlca			;0930
	ret			;0931
	ld d,000h		;0932
	ld hl,(0011ch)		;0934
	add hl,de			;0937
	ld a,(hl)			;0938
	ret			;0939
	ld e,a			;093a
	ld d,000h		;093b
	ld hl,0d1d0h		;093d
	add hl,de			;0940
	ld a,(hl)			;0941
	ret			;0942
	push bc			;0943
	ld b,0fdh		;0944
	call 0b94dh		;0946
	pop bc			;0949
	ld l,01ch		;094a
	ret			;094c
	ld a,0feh		;094d
	ld c,(ix+026h)		;094f
	ld (0d103h),a		;0952
	ld a,(0d0f2h)		;0955
	inc a			;0958
	scf			;0959
	jr z,$+30		;095a
	push bc			;095c
	ld b,(ix+033h)		;095d
	call 0b984h		;0960
	jr c,$+25		;0963
	pop bc			;0965
	call 0b984h		;0966
	jr c,$+20		;0969
	ld b,c			;096b
	call 0b984h		;096c
	jr c,$+14		;096f
	ld b,0ffh		;0971
	call 0b984h		;0973
	jr c,$+8		;0976
	ld a,0ffh		;0978
	jr $+37		;097a
	pop bc			;097c
	or a			;097d
	dec hl			;097e
	ex de,hl			;097f
	dec hl			;0980
	ld a,l			;0981
	jr $+29		;0982
	ld e,000h		;0984
	call 0b9f5h		;0986
	ret nc			;0989
	call 0b992h		;098a
	pop hl			;098d
	jr nz,$-8		;098e
	scf			;0990
	ret			;0991
	cp (ix+022h)		;0992
	ret nz			;0995
	ld a,(0d0f3h)		;0996
	cp 0ffh		;0999
	ret z			;099b
	inc hl			;099c
	cp (hl)			;099d
	ret			;099e
	push af			;099f
	push hl			;09a0
	push de			;09a1
	ld hl,00000h		;09a2
	ld (0d106h),hl		;09a5
	ld (0d108h),hl		;09a8
	ld (0d10ah),hl		;09ab
	ld e,a			;09ae
	ld (0d103h),a		;09af
	cp 0ffh		;09b2
	jr z,$+51		;09b4
	ld hl,00103h		;09b6
	cp (hl)			;09b9
	jr nc,$+49		;09ba
	call 0b932h		;09bc
	rla			;09bf
	rr (ix+039h)		;09c0
	rla			;09c4
	rr (ix+038h)		;09c5
	call 0b93dh		;09c9
	ld (0d106h),a		;09cc
	ex de,hl			;09cf
	call 0b8cch		;09d0
	ld (ix+037h),h		;09d3
	ld h,000h		;09d6
	ex de,hl			;09d8
	ld hl,(0011eh)		;09d9
	add hl,de			;09dc
	add hl,de			;09dd
	ld a,(hl)			;09de
	ld (0d10bh),a		;09df
	inc hl			;09e2
	ld a,(hl)			;09e3
	ld (0d10ah),a		;09e4
	pop de			;09e7
	pop hl			;09e8
	pop af			;09e9
	ret			;09ea
	pop de			;09eb
	pop hl			;09ec
	pop af			;09ed
	pop af			;09ee
	ld d,000h		;09ef
	jp 0b02dh		;09f1
	ld b,a			;09f4
	ld hl,0d1d0h		;09f5
	ld d,000h		;09f8
	add hl,de			;09fa
	ld a,(00103h)		;09fb
	cp e			;09fe
	ret z			;09ff
	ld a,b			;0a00
	inc de			;0a01
	cp 0ffh		;0a02
	jr z,$+3		;0a04
	cp (hl)			;0a06
	inc hl			;0a07
	jr nz,$-13		;0a08
	ex (sp),hl			;0a0a
	push hl			;0a0b
	push de			;0a0c
	dec e			;0a0d
	ld hl,(0011ah)		;0a0e
	add hl,de			;0a11
	add hl,de			;0a12
	ld a,(hl)			;0a13
	cp 0ffh		;0a14
	pop de			;0a16
	ret c			;0a17
	pop hl			;0a18
	ex (sp),hl			;0a19
	jr $-31		;0a1a
	push bc			;0a1c
	ld bc,00000h		;0a1d
	push bc			;0a20
	res 7,(ix+035h)		;0a21
	res 4,(ix-003h)		;0a25
	ld hl,0d1d0h		;0a29
	add hl,bc			;0a2c
	ld a,(hl)			;0a2d
	cp 0ffh		;0a2e
	jr z,$+76		;0a30
	cp d			;0a32
	jr nz,$+45		;0a33
	ld h,c			;0a35
	ld l,000h		;0a36
	ex (sp),hl			;0a38
	push hl			;0a39
	pop af			;0a3a
	bit 7,(ix+035h)		;0a3b
	jr nz,$+30		;0a3f
	set 7,(ix+035h)		;0a41
	bit 1,(ix-001h)		;0a45
	jr z,$+14		;0a49
	ld l,001h		;0a4b
	call 0bd94h		;0a4d
	bit 6,(ix+035h)		;0a50
	call z,0c905h		;0a54
	ex (sp),hl			;0a57
	ld l,001h		;0a58
	ex (sp),hl			;0a5a
	jr $+5		;0a5b
	call 0ba63h		;0a5d
	inc bc			;0a60
	jr $-56		;0a61
	bit 6,(ix+035h)		;0a63
	jr nz,$+9		;0a67
	ld l,a			;0a69
	call 0bd13h		;0a6a
	jp 0bd99h		;0a6d
	jr c,$+9		;0a70
	push af			;0a72
	ld l,02eh		;0a73
	call 0bd94h		;0a75
	pop af			;0a78
	jp 0bcceh		;0a79
	pop af			;0a7c
	pop bc			;0a7d
	bit 7,(ix+035h)		;0a7e
	jr z,$+26		;0a82
	bit 6,(ix+035h)		;0a84
	jr z,$-37		;0a88
	jr c,$+10		;0a8a
	push af			;0a8c
	ld l,02fh		;0a8d
	call 0bd94h		;0a8f
	pop af			;0a92
	scf			;0a93
	call 0ba63h		;0a94
	ld l,030h		;0a97
	jp 0bd94h		;0a99
	bit 1,(ix-001h)		;0a9c
	ret nz			;0aa0
	ld l,035h		;0aa1
	jp 0bd94h		;0aa3
	ld hl,(00114h)		;0aa6
	ld c,a			;0aa9
	ld b,000h		;0aaa
	add hl,bc			;0aac
	add hl,bc			;0aad
	ld e,(hl)			;0aae
	inc hl			;0aaf
	ld d,(hl)			;0ab0
	ex de,hl			;0ab1
	ld a,(hl)			;0ab2
	cp 0ffh		;0ab3
	ret z			;0ab5
	cp (ix+021h)		;0ab6
	inc hl			;0ab9
	ld a,(hl)			;0aba
	scf			;0abb
	ret z			;0abc
	inc hl			;0abd
	jr $-12		;0abe
	ld hl,(0d00bh)		;0ac0
	ld a,l			;0ac3
	or h			;0ac4
	jr z,$+101		;0ac5
	ld d,001h		;0ac7
	xor a			;0ac9
	jr $+32		;0aca
	bit 2,(ix-001h)		;0acc
	jr z,$+8		;0ad0
	ld hl,(0d009h)		;0ad2
	ld a,(hl)			;0ad5
	cp 00dh		;0ad6
	push af			;0ad8
	scf			;0ad9
	call z,0bfc5h		;0ada
	jr nc,$+41		;0add
	ld (0d009h),hl		;0adf
	pop af			;0ae2
	ld de,00000h		;0ae3
	ld (0d00bh),de		;0ae6
	ld (ix-002h),d		;0aea
	ld de,0ffffh		;0aed
	ld a,e			;0af0
	jr nz,$+8		;0af1
	ld (0d0f1h),a		;0af3
	ld (0d0f4h),a		;0af6
	ld (0d0f2h),de		;0af9
	ld (0d0fbh),de		;0afd
	ld (0d0fdh),a		;0b01
	jr $+10		;0b04
	pop af			;0b06
	set 7,(ix+031h)		;0b07
	jr $+31		;0b0b
	inc hl			;0b0d
	call 0bcb3h		;0b0e
	cp 022h		;0b11
	jr z,$+23		;0b13
	call 0bcbbh		;0b15
	jr c,$+25		;0b18
	cp 00dh		;0b1a
	jr nz,$-15		;0b1c
	bit 2,(ix-001h)		;0b1e
	jr z,$+8		;0b22
	res 2,(ix-001h)		;0b24
	jr $-92		;0b28
	res 2,(ix-001h)		;0b2a
	scf			;0b2e
	ret			;0b2f
	pop hl			;0b30
	call 0bc63h		;0b31
	or a			;0b34
	jp nz,0bbb7h		;0b35
	call 0bcbah		;0b38
	jr c,$-10		;0b3b
	cp 022h		;0b3d
	jr nz,$+35		;0b3f
	bit 0,(ix-002h)		;0b41
	jr nz,$+29		;0b45
	ld de,(0d00bh)		;0b47
	ld a,d			;0b4b
	or e			;0b4c
	jr nz,$-35		;0b4d
	inc hl			;0b4f
	ld (0d00bh),hl		;0b50
	ld a,(hl)			;0b53
	cp 00dh		;0b54
	jr z,$-44		;0b56
	inc hl			;0b58
	cp 022h		;0b59
	jr nz,$-8		;0b5b
	call 0bcb3h		;0b5d
	jr $-40		;0b60
	set 2,(ix-001h)		;0b62
	bit 0,(ix-002h)		;0b66
	jr nz,$+7		;0b6a
	ld (0d009h),hl		;0b6c
	jr $+5		;0b6f
	ld (0d00bh),hl		;0b71
	ld a,(hl)			;0b74
	cp 00dh		;0b75
	jr nz,$+6		;0b77
	res 2,(ix-001h)		;0b79
	bit 1,(ix-002h)		;0b7d
	jr nz,$+17		;0b81
	ld a,(0d0f2h)		;0b83
	cp 0ffh		;0b86
	jr z,$-94		;0b88
	cp 028h		;0b8a
	ret nc			;0b8c
	ld (0d0f1h),a		;0b8d
	or a			;0b90
	ret			;0b91
	bit 5,(ix-002h)		;0b92
	jr nz,$+7		;0b96
	ld a,0ffh		;0b98
	ld (0d0f4h),a		;0b9a
	ld de,(0d0fch)		;0b9d
	call 0bba8h		;0ba1
	ld de,(0d0f2h)		;0ba4
	ld a,e			;0ba8
	cp 0ffh		;0ba9
	jr z,$+10		;0bab
	cp 032h		;0bad
	jr c,$+6		;0baf
	ld (0d0feh),de		;0bb1
	xor a			;0bb5
	ret			;0bb6
	ld e,a			;0bb7
	ld a,(bc)			;0bb8
	cp 005h		;0bb9
	jr z,$-89		;0bbb
	or a			;0bbd
	jr nz,$+19		;0bbe
	bit 1,(ix-002h)		;0bc0
	jp nz,0bb31h		;0bc4
	ld (ix+021h),e		;0bc7
	set 1,(ix-002h)		;0bca
	jp 0bb31h		;0bce
	cp 001h		;0bd1
	jr nz,$+19		;0bd3
	bit 5,(ix-002h)		;0bd5
	jp nz,0bb31h		;0bd9
	ld (ix+024h),e		;0bdc
	set 5,(ix-002h)		;0bdf
	jp 0bb31h		;0be3
	cp 004h		;0be6
	jr nz,$+16		;0be8
	ld a,(0d0fbh)		;0bea
	cp 0ffh		;0bed
	jp nz,0bb31h		;0bef
	ld (ix+02bh),e		;0bf2
	jp 0bb31h		;0bf5
	bit 3,(ix-002h)		;0bf8
	jp nz,0bb31h		;0bfc
	push hl			;0bff
	ld hl,0d0f2h		;0c00
	bit 2,(ix-002h)		;0c03
	jr z,$+5		;0c07
	ld hl,0d0fch		;0c09
	cp 006h		;0c0c
	jr z,$+68		;0c0e
	cp 002h		;0c10
	jr nz,$+48		;0c12
	bit 2,(ix-002h)		;0c14
	jr z,$+30		;0c18
	ld a,(0d0f2h)		;0c1a
	cp e			;0c1d
	jr nz,$+20		;0c1e
	inc hl			;0c20
	ld a,(hl)			;0c21
	dec hl			;0c22
	cp 0ffh		;0c23
	jr z,$+18		;0c25
	cp (ix+023h)		;0c27
	jr z,$+13		;0c2a
	ld a,(0d0f3h)		;0c2c
	inc a			;0c2f
	jr z,$+7		;0c30
	set 3,(ix-002h)		;0c32
	ld (hl),e			;0c36
	set 2,(ix-002h)		;0c37
	res 7,(ix-002h)		;0c3b
	jp 0bb30h		;0c3f
	bit 7,(ix-002h)		;0c42
	jp nz,0bb30h		;0c46
	inc hl			;0c49
	ld (hl),e			;0c4a
	set 7,(ix-002h)		;0c4b
	jp 0bb30h		;0c4f
	ld a,(0d0feh)		;0c52
	cp 0ffh		;0c55
	jp z,0bb30h		;0c57
	ld e,a			;0c5a
	inc hl			;0c5b
	ld a,(0d0ffh)		;0c5c
	ld (hl),a			;0c5f
	dec hl			;0c60
	jr $-77		;0c61
	ld a,0dfh		;0c63
	ld de,0cfd4h		;0c65
	ld b,005h		;0c68
	dec de			;0c6a
	ld (de),a			;0c6b
	djnz $-2		;0c6c
	ld b,005h		;0c6e
	call 0bcbah		;0c70
	jr nc,$+15		;0c73
	cpl			;0c75
	ld (de),a			;0c76
	inc de			;0c77
	inc hl			;0c78
	djnz $-9		;0c79
	call 0bcbah		;0c7b
	inc hl			;0c7e
	jr c,$-4		;0c7f
	dec hl			;0c81
	call 0bcb3h		;0c82
	ld bc,(00116h)		;0c85
	push hl			;0c89
	ld hl,0cfcfh		;0c8a
	res 5,(ix-001h)		;0c8d
	ld d,005h		;0c91
	ld a,(bc)			;0c93
	or a			;0c94
	jr nz,$+4		;0c95
	pop hl			;0c97
	ret			;0c98
	cp (hl)			;0c99
	jr z,$+6		;0c9a
	set 5,(ix-001h)		;0c9c
	inc bc			;0ca0
	inc hl			;0ca1
	dec d			;0ca2
	jr nz,$-16		;0ca3
	bit 5,(ix-001h)		;0ca5
	jr nz,$+6		;0ca9
	ld a,(bc)			;0cab
	inc bc			;0cac
	pop hl			;0cad
	ret			;0cae
	inc bc			;0caf
	inc bc			;0cb0
	jr $-39		;0cb1
	ld a,(hl)			;0cb3
	cp 020h		;0cb4
	ret nz			;0cb6
	inc hl			;0cb7
	jr $-5		;0cb8
	ld a,(hl)			;0cba
	call 0bcc7h		;0cbb
	ccf			;0cbe
	ret c			;0cbf
	cp 041h		;0cc0
	ccf			;0cc2
	ret nc			;0cc3
	cp 05bh		;0cc4
	ret			;0cc6
	cp 030h		;0cc7
	ret c			;0cc9
	cp 03ah		;0cca
	ccf			;0ccc
	ret			;0ccd
	push hl			;0cce
	ld hl,00103h		;0ccf
	cp (hl)			;0cd2
	jr nc,$+53		;0cd3
	call 0bd12h		;0cd5
	call 0bcfah		;0cd8
	jr z,$-3		;0cdb
	bit 4,(ix-003h)		;0cdd
	jr nz,$+6		;0ce1
	and 0dfh		;0ce3
	jr $+12		;0ce5
	call 0bcfah		;0ce7
	jr nz,$-3		;0cea
	call 0bcfah		;0cec
	jr z,$-3		;0cef
	cpl			;0cf1
	call 0c907h		;0cf2
	call 0bcfah		;0cf5
	jr $-7		;0cf8
	ld a,(hl)			;0cfa
	cp 0f5h		;0cfb
	jr z,$+10		;0cfd
	cp 0d1h		;0cff
	jr z,$+6		;0d01
	inc hl			;0d03
	cp 0dfh		;0d04
	ret			;0d06
	pop hl			;0d07
	pop hl			;0d08
	ret			;0d09
	ld l,a			;0d0a
	push de			;0d0b
	ld de,(0010ah)		;0d0c
	jr $+15		;0d10
	ld l,a			;0d12
	push de			;0d13
	ld de,(0010ch)		;0d14
	jr $+7		;0d18
	push de			;0d1a
	ld de,(00112h)		;0d1b
	ld h,000h		;0d1f
	add hl,hl			;0d21
	add hl,de			;0d22
	ld e,(hl)			;0d23
	inc hl			;0d24
	ld d,(hl)			;0d25
	ex de,hl			;0d26
	pop de			;0d27
	ret			;0d28
	push af			;0d29
	push bc			;0d2a
	ld bc,00865h		;0d2b
	ld hl,00000h		;0d2e
	ld de,(0d0b4h)		;0d31
	inc e			;0d35
	ld d,h			;0d36
	srl c		;0d37
	jr nc,$+3		;0d39
	add hl,de			;0d3b
	sla e		;0d3c
	rl d		;0d3e
	djnz $-9		;0d40
	ld (0d0b4h),hl		;0d42
	ld a,h			;0d45
	or a			;0d46
	jr z,$-28		;0d47
	pop bc			;0d49
	pop af			;0d4a
	ret			;0d4b
	ld d,000h		;0d4c
	push hl			;0d4e
	push de			;0d4f
	res 1,(ix-003h)		;0d50
	ex de,hl			;0d54
	ld de,02710h		;0d55
	call 0bd76h		;0d58
	ld de,003e8h		;0d5b
	call 0bd76h		;0d5e
	ld de,00064h		;0d61
	call 0bd76h		;0d64
	ld de,0000ah		;0d67
	call 0bd76h		;0d6a
	ld a,l			;0d6d
	add a,030h		;0d6e
	call 0c907h		;0d70
	pop de			;0d73
	pop hl			;0d74
	ret			;0d75
	xor a			;0d76
	inc a			;0d77
	or a			;0d78
	sbc hl,de		;0d79
	jr nc,$-4		;0d7b
	add hl,de			;0d7d
	dec a			;0d7e
	jr nz,$+7		;0d7f
	bit 1,(ix-003h)		;0d81
	ret z			;0d85
	set 1,(ix-003h)		;0d86
	add a,030h		;0d8a
	jp 0c907h		;0d8c
	call 0bd1ah		;0d8f
	jr $+7		;0d92
	call 0bd1ah		;0d94
	jr $+6		;0d97
	set 6,(ix-001h)		;0d99
	ld a,(hl)			;0d9d
	cpl			;0d9e
	cp 00ah		;0d9f
	jr z,$+25		;0da1
	cp (ix-006h)		;0da3
	jr nz,$+14		;0da6
	set 4,(ix-003h)		;0da8
	ld a,(0d103h)		;0dac
	call 0bcceh		;0daf
	jr $+5		;0db2
	call 0c907h		;0db4
	inc hl			;0db7
	jr $-27		;0db8
	bit 6,(ix-001h)		;0dba
	ret z			;0dbe
	res 6,(ix-001h)		;0dbf
	jp 0c905h		;0dc3
	ex (sp),hl			;0dc6
	ld (0bdd5h),de		;0dc7
	ld e,(hl)			;0dcb
	inc hl			;0dcc
	ld d,(hl)			;0dcd
	inc hl			;0dce
	ex de,hl			;0dcf
	add hl,de			;0dd0
	ex de,hl			;0dd1
	ex (sp),hl			;0dd2
	push de			;0dd3
	ld de,00000h		;0dd4
	ret			;0dd7
	and 007h		;0dd8
	ld e,a			;0dda
	ld a,(0d0c6h)		;0ddb
	and 0f8h		;0dde
	or e			;0de0
	ld (0d0c6h),a		;0de1
	jp 0b456h		;0de4
	ld hl,0affdh		;0de7
	cp (hl)			;0dea
	jr nc,$+14		;0deb
	ld (0cfdah),a		;0ded
	ld e,a			;0df0
	call 0c2b4h		;0df1
	bit 7,(hl)		;0df4
	jp nz,0b214h		;0df6
	jp 0b1c1h		;0df9
	res 7,(ix-004h)		;0dfc
	or a			;0e00
	jr z,$+6		;0e01
	set 7,(ix-004h)		;0e03
	push bc			;0e07
	call 0ca54h		;0e08
	ld a,(0d0c6h)		;0e0b
	push af			;0e0e
	res 6,(ix-00ah)		;0e0f
	ld a,(0cfdah)		;0e13
	ld e,a			;0e16
	call 0c225h		;0e17
	pop af			;0e1a
	ld (0d0c6h),a		;0e1b
	pop bc			;0e1e
	jp 0b456h		;0e1f
	push bc			;0e22
	call 0ca54h		;0e23
	res 5,(ix-00ah)		;0e26
	res 7,(ix-00ah)		;0e2a
	ld hl,(0d0c4h)		;0e2e
	ld (0d0b9h),hl		;0e31
	jr $+21		;0e34
	push bc			;0e36
	call 0ca54h		;0e37
	res 5,(ix-00ah)		;0e3a
	res 7,(ix-00ah)		;0e3e
	ld de,(0d0b9h)		;0e42
	call 0c8aeh		;0e46
	pop bc			;0e49
	jp 0b457h		;0e4a
	push bc			;0e4d
	push af			;0e4e
	call 0ca54h		;0e4f
	ld a,(0d0c5h)		;0e52
	ld d,a			;0e55
	pop af			;0e56
	ld e,a			;0e57
	jr $+17		;0e58
	ld d,a			;0e5a
	inc bc			;0e5b
	ld a,(bc)			;0e5c
	ld e,a			;0e5d
	push bc			;0e5e
	push de			;0e5f
	call 0ca54h		;0e60
	pop de			;0e63
	ld a,(0d0c2h)		;0e64
	add a,d			;0e67
	ld d,a			;0e68
	ld a,(0d0c3h)		;0e69
	add a,e			;0e6c
	ld e,a			;0e6d
	call 0c8aeh		;0e6e
	res 5,(ix-00ah)		;0e71
	res 7,(ix-00ah)		;0e75
	pop bc			;0e79
	jp 0b456h		;0e7a
	push bc			;0e7d
	call 0c845h		;0e7e
	pop bc			;0e81
	jp 0b456h		;0e82
	dec bc			;0e85
	push bc			;0e86
	call 0ca54h		;0e87
	ld a,(0d0c0h)		;0e8a
	srl a		;0e8d
	ld h,a			;0e8f
	ld a,015h		;0e90
	sub h			;0e92
	jr $+25		;0e93
	push af			;0e95
	push bc			;0e96
	call 0ca54h		;0e97
	pop bc			;0e9a
	pop af			;0e9b
	cp 018h		;0e9c
	jr c,$+3		;0e9e
	xor a			;0ea0
	ld (0d0c2h),a		;0ea1
	inc bc			;0ea4
	push bc			;0ea5
	ld a,(bc)			;0ea6
	cp 02ah		;0ea7
	jr c,$+3		;0ea9
	xor a			;0eab
	ld (0d0c3h),a		;0eac
	jr $+19		;0eaf
	push af			;0eb1
	push bc			;0eb2
	call 0ca54h		;0eb3
	pop bc			;0eb6
	pop af			;0eb7
	dec a			;0eb8
	ld (0d0c1h),a		;0eb9
	inc bc			;0ebc
	push bc			;0ebd
	ld a,(bc)			;0ebe
	ld (0d0c0h),a		;0ebf
	call 0c8e1h		;0ec2
	call 0c895h		;0ec5
	pop bc			;0ec8
	jp 0b456h		;0ec9
	inc bc			;0ecc
	jp 0b456h		;0ecd
	call 0bfb9h		;0ed0
	ld e,0f0h		;0ed3
	jr $+13		;0ed5
	call 0bfb9h		;0ed7
	rlca			;0eda
	rlca			;0edb
	rlca			;0edc
	rlca			;0edd
	and 0f0h		;0ede
	ld e,00fh		;0ee0
	ld d,a			;0ee2
	ld a,(0d0bbh)		;0ee3
	and e			;0ee6
	or d			;0ee7
	ld (0d0bbh),a		;0ee8
	ld (0cfbbh),a		;0eeb
	jp 0b456h		;0eee
	and 00fh		;0ef1
	call 0cd7eh		;0ef3
	jp 0b456h		;0ef6
	call 0ceefh		;0ef9
	call 0009ch		;0efc
	jr z,$+20		;0eff
	call 0009fh		;0f01
	ld (0d10ch),a		;0f04
	call 0cefah		;0f07
	ld a,(0d0c1h)		;0f0a
	call 0c828h		;0f0d
	jp 0b215h		;0f10
	call 0cefah		;0f13
	jp 0b1c1h		;0f16
	ld de,0d2d0h		;0f19
	set 0,(ix-003h)		;0f1c
	push bc			;0f20
	ld hl,0d0d0h		;0f21
	ld bc,00200h		;0f24
	jr $+38		;0f27
	bit 0,(ix-003h)		;0f29
	jr nz,$+9		;0f2d
	res 2,(ix-001h)		;0f2f
	jp 0b1c1h		;0f33
	inc bc			;0f36
	push bc			;0f37
	ld hl,0d2d0h		;0f38
	ld de,0d0d0h		;0f3b
	ld c,a			;0f3e
	ld b,000h		;0f3f
	inc bc			;0f41
	ldir		;0f42
	ld hl,0d3d0h		;0f44
	ld de,0d1d0h		;0f47
	ld bc,00100h		;0f4a
	ldir		;0f4d
	pop bc			;0f4f
	jp 0b457h		;0f50
	push bc			;0f53
	ld hl,(0cfd7h)		;0f54
	push hl			;0f57
	ld hl,0bf6ch		;0f58
	ld (0cfd7h),hl		;0f5b
	call 0bf83h		;0f5e
	ld l,03dh		;0f61
	call 0bd94h		;0f63
	call 0c7edh		;0f66
	call 0cd8ah		;0f69
	pop hl			;0f6c
	ld (0cfd7h),hl		;0f6d
	pop bc			;0f70
	jp 0b456h		;0f71
	push bc			;0f74
	call 0bf83h		;0f75
	call 0cdech		;0f78
	res 2,(ix-001h)		;0f7b
	pop bc			;0f7f
	jp 0b456h		;0f80
	ld l,03ch		;0f83
	call 0bfefh		;0f85
	ld de,0bfb1h		;0f88
	ld bc,00008h		;0f8b
	ld a,(hl)			;0f8e
	cp 00dh		;0f8f
	jr z,$+13		;0f91
	cp 020h		;0f93
	jr z,$+9		;0f95
	ldi		;0f97
	jp pe,0bf8eh		;0f99
	jr $+9		;0f9c
	ex de,hl			;0f9e
	ld (hl),020h		;0f9f
	inc hl			;0fa1
	dec c			;0fa2
	jr nz,$-4		;0fa3
	ld iy,0bfaah		;0fa5
	ret			;0fa9
	ld d,h			;0faa
	ld c,d			;0fab
	ld b,a			;0fac
	nop			;0fad
	ld (bc),a			;0fae
	ret nc			;0faf
	ret nc			;0fb0
	ld b,h			;0fb1
	ld b,c			;0fb2
	ld b,c			;0fb3
	ld b,h			;0fb4
	ld d,(hl)			;0fb5
	ld sp,0302eh		;0fb6
	and 00fh		;0fb9
	ld e,a			;0fbb
	ld d,000h		;0fbc
	ld hl,(0aff9h)		;0fbe
	inc hl			;0fc1
	add hl,de			;0fc2
	ld a,(hl)			;0fc3
	ret			;0fc4
	res 4,(ix-002h)		;0fc5
	ld a,(0d0fah)		;0fc9
	or a			;0fcc
	jr z,$+11		;0fcd
	ld l,a			;0fcf
	ld a,(00106h)		;0fd0
	cp l			;0fd3
	jr z,$+4		;0fd4
	jr nc,$+37		;0fd6
	call 0bd29h		;0fd8
	ld a,h			;0fdb
	ld l,002h		;0fdc
	cp 01eh		;0fde
	jr c,$+27		;0fe0
	inc l			;0fe2
	cp 03ch		;0fe3
	jr c,$+22		;0fe5
	inc l			;0fe7
	cp 05ah		;0fe8
	jr c,$+17		;0fea
	inc l			;0fec
	jr $+14		;0fed
	res 2,(ix-001h)		;0fef
	set 4,(ix-002h)		;0ff3
	ld (ix-007h),00ch		;0ff7
	push hl			;0ffb
	ld a,(0d0b6h)		;0ffc
	ld (0d008h),a		;0fff
	ld a,(0d0f9h)		;1002
	and 007h		;1005
	call nz,0c845h		;1007
	pop hl			;100a
	call 0bd94h		;100b
	call 0ca54h		;100e
	res 6,(ix-00ah)		;1011
	ld a,080h		;1015
	ld (0d0beh),a		;1017
	ld l,021h		;101a
	call 0bd94h		;101c
	call 0c1fdh		;101f
	call 0cc22h		;1022
	ld a,(0d0c4h)		;1025
	ld (0d007h),a		;1028
	bit 4,(ix-002h)		;102b
	jr nz,$+25		;102f
	ld a,(0d101h)		;1031
	rlca			;1034
	jr nc,$+19		;1035
	rlca			;1037
	jr nc,$+16		;1038
	rlca			;103a
	jr nc,$+13		;103b
	ld hl,0cf3ah		;103d
	call 0c1b9h		;1040
	call 0c1c8h		;1043
	jr $+23		;1046
	xor a			;1048
	ld (0cfb5h),a		;1049
	ld hl,0cf3ah		;104c
	ld a,00dh		;104f
	ld (hl),a			;1051
	ld (0cfb3h),hl		;1052
	jr $+8		;1055
	bit 0,(ix+031h)		;1057
	jr nz,$+36		;105b
	bit 4,(ix-002h)		;105d
	jr nz,$+30		;1061
	ld a,(0d100h)		;1063
	or a			;1066
	jr z,$+24		;1067
	call 0c7f9h		;1069
	jr c,$+22		;106c
	ld hl,(0cfb3h)		;106e
	call 0c1b9h		;1071
	call 0c1deh		;1074
	ld a,(0d101h)		;1077
	or 0c0h		;107a
	jp 0c0fch		;107c
	call 0c7edh		;107f
	cp 008h		;1082
	jp z,0c17ah		;1084
	cp 01dh		;1087
	jp z,0c150h		;1089
	cp 01ch		;108c
	jp z,0c169h		;108e
	cp 00dh		;1091
	jr nz,$+11		;1093
	ld a,(0cfb5h)		;1095
	or a			;1098
	jp nz,0c0eah		;1099
	jr $-69		;109c
	ld e,a			;109e
	ld a,(0cfb5h)		;109f
	cp (ix-007h)		;10a2
	jp z,0c057h		;10a5
	ld a,e			;10a8
	cp 020h		;10a9
	jp c,0c057h		;10ab
	cp 05bh		;10ae
	jr c,$+12		;10b0
	cp 061h		;10b2
	jp c,0c057h		;10b4
	cp 07bh		;10b7
	jp nc,0c057h		;10b9
	ld hl,(0cfb3h)		;10bc
	push hl			;10bf
	ld a,00dh		;10c0
	ld b,0ffh		;10c2
	cpir		;10c4
	pop bc			;10c6
	ld a,e			;10c7
	push hl			;10c8
	pop de			;10c9
	or a			;10ca
	sbc hl,bc		;10cb
	push hl			;10cd
	pop bc			;10ce
	push de			;10cf
	pop hl			;10d0
	dec hl			;10d1
	lddr		;10d2
	ld hl,0cfb5h		;10d4
	inc (hl)			;10d7
	ld hl,(0cfb3h)		;10d8
	ld (hl),a			;10db
	inc hl			;10dc
	ld (0cfb3h),hl		;10dd
	dec hl			;10e0
	call 0c1b9h		;10e1
	call 0c1c8h		;10e4
	jp 0c057h		;10e7
	ld hl,(0cfb3h)		;10ea
	call 0c1b9h		;10ed
	call 0c1deh		;10f0
	call 0c905h		;10f3
	ld a,(0d101h)		;10f6
	and 03fh		;10f9
	scf			;10fb
	ld (0d101h),a		;10fc
	push af			;10ff
	call 0cc32h		;1100
	bit 3,(ix+031h)		;1103
	call nz,0c885h		;1107
	set 6,(ix-00ah)		;110a
	ld a,(0d008h)		;110e
	call 0c845h		;1111
	pop af			;1114
	push af			;1115
	jr nc,$+49		;1116
	bit 4,(ix+031h)		;1118
	ld l,021h		;111c
	call nz,0bd94h		;111e
	ld hl,0cf3ah		;1121
	ld a,(hl)			;1124
	cp 00dh		;1125
	jr z,$+25		;1127
	push af			;1129
	bit 4,(ix+031h)		;112a
	call nz,0c907h		;112e
	pop af			;1131
	cp 061h		;1132
	jr c,$+9		;1134
	cp 07bh		;1136
	jr nc,$+5		;1138
	and 0dfh		;113a
	ld (hl),a			;113c
	inc hl			;113d
	jr $-26		;113e
	bit 4,(ix+031h)		;1140
	call nz,0c905h		;1144
	pop af			;1147
	ld (ix-007h),078h		;1148
	ld hl,0cf3ah		;114c
	ret			;114f
	ld de,(0cfb3h)		;1150
	ld hl,0cf3ah		;1154
	or a			;1157
	sbc hl,de		;1158
	jr z,$+12		;115a
	dec de			;115c
	ld (0cfb3h),de		;115d
	ld a,008h		;1161
	call 0caa2h		;1163
	jp 0c057h		;1166
	ld hl,(0cfb3h)		;1169
	ld a,(hl)			;116c
	cp 00dh		;116d
	jr z,$-9		;116f
	inc hl			;1171
	ld (0cfb3h),hl		;1172
	call 0caa2h		;1175
	jr $-18		;1178
	ld de,(0cfb3h)		;117a
	ld hl,0cf3ah		;117e
	or a			;1181
	sbc hl,de		;1182
	jr z,$-30		;1184
	push de			;1186
	pop hl			;1187
	ld a,00dh		;1188
	ld b,0ffh		;118a
	cpir		;118c
	or a			;118e
	sbc hl,de		;118f
	push hl			;1191
	pop bc			;1192
	push de			;1193
	pop hl			;1194
	dec de			;1195
	ldir		;1196
	ld a,008h		;1198
	call 0caa2h		;119a
	ld hl,0cfb5h		;119d
	dec (hl)			;11a0
	ld hl,(0cfb3h)		;11a1
	dec hl			;11a4
	ld (0cfb3h),hl		;11a5
	call 0c1b9h		;11a8
	push hl			;11ab
	ld a,020h		;11ac
	call 0caa2h		;11ae
	pop hl			;11b1
	inc hl			;11b2
	call 0c1c8h		;11b3
	jp 0c057h		;11b6
	call 0cc32h		;11b9
	ld a,(hl)			;11bc
	cp 00dh		;11bd
	jp z,0cc22h		;11bf
	call 0caa2h		;11c2
	inc hl			;11c5
	jr $-10		;11c6
	ld de,(0cfb3h)		;11c8
	xor a			;11cc
	sbc hl,de		;11cd
	ret z			;11cf
	call 0cc32h		;11d0
	ld b,l			;11d3
	ld a,008h		;11d4
	call 0caa2h		;11d6
	djnz $-5		;11d9
	jp 0cc22h		;11db
	ld de,0cf3ah		;11de
	or a			;11e1
	sbc hl,de		;11e2
	ld a,(0d007h)		;11e4
	add a,l			;11e7
	sub (ix-00dh)		;11e8
	ld l,(ix-00fh)		;11eb
	sub (ix-010h)		;11ee
	jr c,$+8		;11f1
	dec l			;11f3
	jr nz,$-6		;11f4
	ld l,(ix-00fh)		;11f6
	ld a,l			;11f9
	jp 0c828h		;11fa
	ld de,(0d0c0h)		;11fd
	ld d,000h		;1201
	ld h,d			;1203
	ld l,d			;1204
	ld bc,(0d0c0h)		;1205
	inc b			;1209
	add hl,de			;120a
	djnz $-1		;120b
	ld a,(0d0c3h)		;120d
	inc a			;1210
	sub (ix-00dh)		;1211
	ld e,a			;1214
	or a			;1215
	sbc hl,de		;1216
	ld e,(ix-007h)		;1218
	push hl			;121b
	or a			;121c
	sbc hl,de		;121d
	pop hl			;121f
	ret nc			;1220
	ld (ix-007h),l		;1221
	ret			;1224
	bit 5,(ix+01dh)		;1225
	ret nz			;1229
	call 0c2b4h		;122a
	bit 7,(hl)		;122d
	jp z,0c28bh		;122f
	call 0c254h		;1232
	bit 3,(ix+01dh)		;1235
	call nz,0cd7eh		;1239
	call 0c885h		;123c
	bit 7,(ix-004h)		;123f
	call z,0c28bh		;1243
	bit 4,(ix+01dh)		;1246
	call nz,0c7edh		;124a
	ld a,(0cfbdh)		;124d
	call 0cd7eh		;1250
	ret			;1253
	ld a,(0cfb8h)		;1254
	and 00fh		;1257
	ld (0cfb8h),a		;1259
	ld a,(0f3ebh)		;125c
	ld (0cfbdh),a		;125f
	inc hl			;1262
	ld a,(hl)			;1263
	push af			;1264
	inc hl			;1265
	ld a,(hl)			;1266
	ld (0d0c2h),a		;1267
	inc hl			;126a
	ld a,(hl)			;126b
	ld (0d0c3h),a		;126c
	inc hl			;126f
	ld a,(hl)			;1270
	dec a			;1271
	ld (0d0c1h),a		;1272
	inc hl			;1275
	ld a,(hl)			;1276
	ld (0d0c0h),a		;1277
	push de			;127a
	call 0c8e1h		;127b
	pop de			;127e
	pop af			;127f
	ld l,a			;1280
	ld h,000h		;1281
	ld (0cfbbh),hl		;1283
	ld l,h			;1286
	ld (0cfb9h),hl		;1287
	ret			;128a
	push ix		;128b
	push iy		;128d
	ld d,000h		;128f
	ld hl,(0aff1h)		;1291
	ex de,hl			;1294
	add hl,hl			;1295
	add hl,de			;1296
	ld e,(hl)			;1297
	inc hl			;1298
	ld d,(hl)			;1299
	push de			;129a
	pop ix		;129b
	ld iy,0cfb6h		;129d
	ld a,000h		;12a1
	ld (0c7c9h),a		;12a3
	ld (0c7cah),a		;12a6
	ld (0cfb7h),a		;12a9
	call 0c2cbh		;12ac
	pop iy		;12af
	pop ix		;12b1
	ret			;12b3
	ld hl,(0aff3h)		;12b4
	ld d,000h		;12b7
	add hl,de			;12b9
	add hl,de			;12ba
	add hl,de			;12bb
	add hl,de			;12bc
	add hl,de			;12bd
	add hl,de			;12be
	ret			;12bf
	ld e,003h		;12c0
	jp 0c2c7h		;12c2
	ld e,001h		;12c5
	ld d,000h		;12c7
	add ix,de		;12c9
	ld a,(ix+000h)		;12cb
	and 007h		;12ce
	sla a		;12d0
	ld hl,0c2e2h		;12d2
	ld e,a			;12d5
	ld d,000h		;12d6
	add hl,de			;12d8
	ld (0c7c7h),ix		;12d9
	ld a,(hl)			;12dd
	inc hl			;12de
	ld h,(hl)			;12df
	ld l,a			;12e0
	jp (hl)			;12e1
	jp p,003c2h		;12e2
	jp 0c351h		;12e5
	xor 0c3h		;12e8
	ld b,d			;12ea
	call nz,0c4ceh		;12eb
	jp po,009c4h		;12ee
	push bc			;12f1
	ld a,(ix+000h)		;12f2
	call 0c33ch		;12f5
	ld b,(ix+002h)		;12f8
	ld c,(ix+001h)		;12fb
	call 0c769h		;12fe
	jr $+52		;1301
	ld a,(ix+000h)		;1303
	and 018h		;1306
	cp 018h		;1308
	jr nz,$+12		;130a
	call 0c525h		;130c
	ld (0cfb9h),bc		;130f
	jp 0c2c0h		;1313
	call 0c33ch		;1316
	call 0c553h		;1319
	ld b,d			;131c
	ld c,e			;131d
	ld e,001h		;131e
	bit 6,(iy+000h)		;1320
	jr z,$+4		;1324
	ld e,0ffh		;1326
	ld d,001h		;1328
	bit 7,(iy+000h)		;132a
	jr z,$+4		;132e
	ld d,0ffh		;1330
	call 0c725h		;1332
	xor a			;1335
	call 0c346h		;1336
	jp 0c2c0h		;1339
	rrca			;133c
	rrca			;133d
	rrca			;133e
	and 003h		;133f
	or a			;1341
	add a,002h		;1342
	and 005h		;1344
	ld e,a			;1346
	ld a,(0cfb8h)		;1347
	and 0fah		;134a
	or e			;134c
	ld (0cfb8h),a		;134d
	ret			;1350
	ld a,(ix+000h)		;1351
	cp 012h		;1354
	jp z,0c398h		;1356
	call 0c525h		;1359
	bit 5,(ix+000h)		;135c
	jr nz,$+24		;1360
	xor a			;1362
	ld hl,0c370h		;1363
	ld e,l			;1366
	ld d,h			;1367
	call 0c592h		;1368
	ld e,003h		;136b
	jp 0c2c7h		;136d
	rst 38h			;1370
	rst 38h			;1371
	rst 38h			;1372
	rst 38h			;1373
	rst 38h			;1374
	rst 38h			;1375
	rst 38h			;1376
	rst 38h			;1377
	ld a,(ix+003h)		;1378
	ld l,a			;137b
	ld h,000h		;137c
	add hl,hl			;137e
	add hl,hl			;137f
	add hl,hl			;1380
	ld de,(0aff7h)		;1381
	add hl,de			;1385
	ld e,l			;1386
	ld d,h			;1387
	xor a			;1388
	bit 4,(ix+000h)		;1389
	jr z,$+3		;138d
	dec a			;138f
	call 0c592h		;1390
	ld e,004h		;1393
	jp 0c2c7h		;1395
	ld de,(0cfbbh)		;1398
	ld a,(ix+002h)		;139c
	inc a			;139f
	sla a		;13a0
	sla a		;13a2
	sla a		;13a4
	ld b,a			;13a6
	ld c,000h		;13a7
	ld a,c			;13a9
	add a,(ix+004h)		;13aa
	or 020h		;13ad
	ld h,a			;13af
	ld a,(ix+003h)		;13b0
	sla a		;13b3
	sla a		;13b5
	sla a		;13b7
	ld l,a			;13b9
	call 0cd71h		;13ba
	push bc			;13bd
	push hl			;13be
	ld hl,0d4d9h		;13bf
	in a,(098h)		;13c2
	xor e			;13c4
	and d			;13c5
	xor e			;13c6
	ld (hl),a			;13c7
	inc hl			;13c8
	djnz $-7		;13c9
	pop hl			;13cb
	pop bc			;13cc
	push bc			;13cd
	call 0cd62h		;13ce
	ld hl,0d4d9h		;13d1
	ld a,(hl)			;13d4
	out (098h),a		;13d5
	nop			;13d7
	nop			;13d8
	nop			;13d9
	inc hl			;13da
	djnz $-7		;13db
	pop bc			;13dd
	ld a,c			;13de
	cp (ix+001h)		;13df
	jr z,$+6		;13e2
	inc c			;13e4
	jp 0c3a9h		;13e5
	ld de,00005h		;13e8
	jp 0c2c7h		;13eb
	ld a,(0cfb7h)		;13ee
	cp 00ah		;13f1
	jr nz,$+18		;13f3
	ld a,000h		;13f5
	ld (0cfb7h),a		;13f7
	ld (0c7c9h),a		;13fa
	ld (0c7cah),a		;13fd
	ld a,068h		;1400
	jp 0b03ah		;1402
	or a			;1405
	jr nz,$+6		;1406
	ld (0c7c5h),ix		;1408
	inc (iy+001h)		;140c
	push ix		;140f
	ld bc,(0c7c9h)		;1411
	ld a,c			;1415
	or b			;1416
	push af			;1417
	ld a,(ix+000h)		;1418
	rrca			;141b
	rrca			;141c
	rrca			;141d
	and 007h		;141e
	ld (0c7c9h),a		;1420
	ld a,(0c7cah)		;1423
	xor (ix+000h)		;1426
	and 0c0h		;1429
	ld (0c7cah),a		;142b
	ld h,000h		;142e
	ld l,(ix+001h)		;1430
	add hl,hl			;1433
	ld de,(0aff1h)		;1434
	add hl,de			;1438
	ld e,(hl)			;1439
	inc hl			;143a
	ld d,(hl)			;143b
	push de			;143c
	pop ix		;143d
	jp 0c2cbh		;143f
	ld a,(ix+000h)		;1442
	call 0c33ch		;1445
	ld a,(ix+001h)		;1448
	ld l,a			;144b
	ld h,000h		;144c
	add hl,hl			;144e
	add hl,hl			;144f
	add hl,hl			;1450
	ld de,(0aff7h)		;1451
	add hl,de			;1455
	ex de,hl			;1456
	ld a,(ix+003h)		;1457
	ld h,a			;145a
	ld a,(ix+002h)		;145b
	sla a		;145e
	sla a		;1460
	sla a		;1462
	ld l,a			;1464
	ld a,(0cfb8h)		;1465
	ld b,0ffh		;1468
	rra			;146a
	jr c,$+3		;146b
	inc b			;146d
	rra			;146e
	rra			;146f
	sbc a,a			;1470
	ld c,a			;1471
	call 0cd71h		;1472
	push hl			;1475
	ld a,008h		;1476
	ld hl,0d4d9h		;1478
	ex de,hl			;147b
	ex af,af'			;147c
	in a,(098h)		;147d
	and b			;147f
	xor (hl)			;1480
	xor c			;1481
	ld (de),a			;1482
	inc hl			;1483
	inc de			;1484
	ex af,af'			;1485
	dec a			;1486
	jr nz,$-11		;1487
	pop hl			;1489
	push hl			;148a
	call 0cd62h		;148b
	ld hl,0d4d9h		;148e
	ld b,008h		;1491
	ld a,(hl)			;1493
	out (098h),a		;1494
	nop			;1496
	nop			;1497
	nop			;1498
	inc hl			;1499
	djnz $-7		;149a
	pop hl			;149c
	set 5,h		;149d
	push hl			;149f
	ld de,(0cfbbh)		;14a0
	call 0cd71h		;14a4
	ld hl,0d4d9h		;14a7
	ld b,008h		;14aa
	in a,(098h)		;14ac
	xor e			;14ae
	and d			;14af
	xor e			;14b0
	ld (hl),a			;14b1
	inc hl			;14b2
	djnz $-7		;14b3
	pop hl			;14b5
	pop bc			;14b6
	push bc			;14b7
	call 0cd62h		;14b8
	ld hl,0d4d9h		;14bb
	ld b,008h		;14be
	ld a,(hl)			;14c0
	out (098h),a		;14c1
	nop			;14c3
	nop			;14c4
	nop			;14c5
	inc hl			;14c6
	djnz $-7		;14c7
	ld e,004h		;14c9
	jp 0c2c7h		;14cb
	ld hl,0cfbbh		;14ce
	ld a,(ix+000h)		;14d1
	srl a		;14d4
	srl a		;14d6
	srl a		;14d8
	ld b,00fh		;14da
	bit 4,a		;14dc
	jr z,$+15		;14de
	jr $+19		;14e0
	ld hl,0cfbbh		;14e2
	ld a,(ix+000h)		;14e5
	rlca			;14e8
	ld b,0f0h		;14e9
	jr c,$+8		;14eb
	call 0c502h		;14ed
	xor a			;14f0
	jr $+8		;14f1
	ld a,(hl)			;14f3
	call 0c502h		;14f4
	ld a,0ffh		;14f7
	call 0c502h		;14f9
	ld de,00001h		;14fc
	jp 0c2c7h		;14ff
	xor (hl)			;1502
	and b			;1503
	xor (hl)			;1504
	ld (hl),a			;1505
	inc hl			;1506
	ld a,b			;1507
	ret			;1508
	ld a,(0cfb7h)		;1509
	or a			;150c
	ret z			;150d
	pop bc			;150e
	ld a,b			;150f
	and 007h		;1510
	ld (0c7c9h),a		;1512
	ld a,b			;1515
	and 0c0h		;1516
	ld (0c7cah),a		;1518
	pop ix		;151b
	dec (iy+001h)		;151d
	ld e,002h		;1520
	jp 0c2c7h		;1522
	call 0c553h		;1525
	ld bc,(0cfb9h)		;1528
	ld a,c			;152c
	bit 6,(iy+000h)		;152d
	jr z,$+6		;1531
	sub e			;1533
	jp 0c538h		;1534
	add a,e			;1537
	ld c,a			;1538
	ld a,b			;1539
	bit 7,(iy+000h)		;153a
	jr z,$+10		;153e
	sub d			;1540
	jr nc,$+16		;1541
	sub 050h		;1543
	jp 0c551h		;1545
	add a,d			;1548
	jr c,$+6		;1549
	cp 0b0h		;154b
	jr c,$+4		;154d
	sub 0b0h		;154f
	ld b,a			;1551
	ret			;1552
	ld a,(0c7c9h)		;1553
	and a			;1556
	jp nz,0c563h		;1557
	ld e,(ix+001h)		;155a
	ld d,(ix+002h)		;155d
	jp 0c574h		;1560
	ld d,000h		;1563
	ld e,(ix+002h)		;1565
	call 0c57eh		;1568
	ld c,l			;156b
	ld e,(ix+001h)		;156c
	call 0c57eh		;156f
	ld d,c			;1572
	ld e,l			;1573
	ld a,(0c7cah)		;1574
	xor (ix+000h)		;1577
	ld (0cfb6h),a		;157a
	ret			;157d
	ld hl,00000h		;157e
	ld b,a			;1581
	add hl,de			;1582
	djnz $-1		;1583
	srl h		;1585
	rr l		;1587
	srl h		;1589
	rr l		;158b
	srl h		;158d
	rr l		;158f
	ret			;1591
	push iy		;1592
	push ix		;1594
	ld (0c7cdh),sp		;1596
	ld ix,0d4d1h		;159a
	push ix		;159e
	ld (0c7cbh),bc		;15a0
	ld c,a			;15a4
	ld b,008h		;15a5
	ld a,(de)			;15a7
	or (hl)			;15a8
	xor c			;15a9
	inc hl			;15aa
	inc de			;15ab
	ld (ix+000h),a		;15ac
	inc ix		;15af
	djnz $-10		;15b1
	pop ix		;15b3
	ld hl,0d4d9h		;15b5
	ld (hl),0ffh		;15b8
	inc hl			;15ba
	ld b,0b0h		;15bb
	ld (hl),000h		;15bd
	inc hl			;15bf
	djnz $-3		;15c0
	ld (hl),0ffh		;15c2
	ld bc,(0c7cbh)		;15c4
	ld d,000h		;15c8
	call 0c710h		;15ca
	call 0cd71h		;15cd
	in a,(098h)		;15d0
	and e			;15d2
	jp nz,0c707h		;15d3
	ld a,01eh		;15d6
	ex af,af'			;15d8
	ld hl,0ffffh		;15d9
	push hl			;15dc
	ld a,d			;15dd
	and 0fch		;15de
	ld d,a			;15e0
	call 0c710h		;15e1
	ld a,b			;15e4
	bit 7,d		;15e5
	jr z,$+6		;15e7
	inc b			;15e9
	jp 0c5eeh		;15ea
	dec b			;15ed
	push bc			;15ee
	exx			;15ef
	ld l,a			;15f0
	ld h,000h		;15f1
	ld de,0d4dah		;15f3
	add hl,de			;15f6
	push hl			;15f7
	pop iy		;15f8
	and 007h		;15fa
	ld (0c601h),a		;15fc
	ld d,(ix+000h)		;15ff
	pop bc			;1602
	ld a,b			;1603
	cp 0b0h		;1604
	call c,0c710h		;1606
	exx			;1609
	bit 7,d		;160a
	exx			;160c
	jr z,$+8		;160d
	ld c,(iy+001h)		;160f
	jp 0c618h		;1612
	ld c,(iy-001h)		;1615
	exx			;1618
	srl e		;1619
	jr nc,$+22		;161b
	ld e,080h		;161d
	inc hl			;161f
	inc hl			;1620
	inc hl			;1621
	inc hl			;1622
	inc hl			;1623
	inc hl			;1624
	inc hl			;1625
	inc hl			;1626
	exx			;1627
	inc hl			;1628
	inc hl			;1629
	inc hl			;162a
	inc hl			;162b
	inc hl			;162c
	inc hl			;162d
	inc hl			;162e
	inc hl			;162f
	exx			;1630
	inc c			;1631
	jr z,$+10		;1632
	call 0cd71h		;1634
	in a,(098h)		;1637
	and e			;1639
	jr z,$-33		;163a
	dec c			;163c
	ld (iy+000h),c		;163d
	jp 0c69eh		;1640
	call 0cd71h		;1643
	in a,(098h)		;1646
	ld (0d4d0h),a		;1648
	and e			;164b
	jp nz,0c6edh		;164c
	call 0cd62h		;164f
	exx			;1652
	ld a,d			;1653
	exx			;1654
	and e			;1655
	or (ix-001h)		;1656
	out (098h),a		;1659
	push de			;165b
	call 0c792h		;165c
	pop de			;165f
	ld a,c			;1660
	or a			;1661
	jp z,0c6edh		;1662
	bit 1,d		;1665
	jr nz,$+54		;1667
	exx			;1669
	ld e,a			;166a
	ld a,c			;166b
	or a			;166c
	jp z,0c67ch		;166d
	cp e			;1670
	jp c,0c67ch		;1671
	exx			;1674
	ld a,d			;1675
	or 003h		;1676
	ld d,a			;1678
	jp 0c69dh		;1679
	call 0cd71h		;167c
	in a,(098h)		;167f
	exx			;1681
	and e			;1682
	jr nz,$+3		;1683
	scf			;1685
	bit 0,d		;1686
	jp nz,0c698h		;1688
	jr nc,$+18		;168b
	ex af,af'			;168d
	dec a			;168e
	jr z,$+103		;168f
	ex af,af'			;1691
	push bc			;1692
	set 0,d		;1693
	jp 0c69dh		;1695
	jp c,0c69dh		;1698
	res 0,d		;169b
	dec c			;169d
	sla e		;169e
	jp nc,0c643h		;16a0
	ld e,001h		;16a3
	dec hl			;16a5
	dec hl			;16a6
	dec hl			;16a7
	dec hl			;16a8
	dec hl			;16a9
	dec hl			;16aa
	dec hl			;16ab
	dec hl			;16ac
	call 0cd71h		;16ad
	in a,(098h)		;16b0
	exx			;16b2
	dec hl			;16b3
	dec hl			;16b4
	dec hl			;16b5
	dec hl			;16b6
	dec hl			;16b7
	dec hl			;16b8
	dec hl			;16b9
	dec hl			;16ba
	or a			;16bb
	jr nz,$+45		;16bc
	exx			;16be
	bit 1,d		;16bf
	exx			;16c1
	jr nz,$+16		;16c2
	call 0cd71h		;16c4
	in a,(098h)		;16c7
	or a			;16c9
	jr nz,$+31		;16ca
	exx			;16cc
	bit 0,d		;16cd
	exx			;16cf
	jr z,$+25		;16d0
	exx			;16d2
	call 0cd62h		;16d3
	exx			;16d6
	ld a,d			;16d7
	exx			;16d8
	out (098h),a		;16d9
	push de			;16db
	call 0c792h		;16dc
	pop de			;16df
	ld a,c			;16e0
	sub 008h		;16e1
	ld c,a			;16e3
	jr c,$+9		;16e4
	jp 0c6a5h		;16e6
	exx			;16e9
	jp 0c643h		;16ea
	ex af,af'			;16ed
	inc a			;16ee
	ex af,af'			;16ef
	pop bc			;16f0
	ld a,b			;16f1
	inc a			;16f2
	jp nz,0c5ddh		;16f3
	ld bc,(0c7cbh)		;16f6
	bit 7,d		;16fa
	jr nz,$+11		;16fc
	set 7,d		;16fe
	inc b			;1700
	ld a,b			;1701
	cp 0b0h		;1702
	jp c,0c5cah		;1704
	ld sp,(0c7cdh)		;1707
	pop ix		;170b
	pop iy		;170d
	ret			;170f
	push bc			;1710
	call 0c7aah		;1711
	ld bc,0c71dh		;1714
	add a,c			;1717
	ld c,a			;1718
	ld a,(bc)			;1719
	ld e,a			;171a
	pop bc			;171b
	ret			;171c
	add a,b			;171d
	ld b,b			;171e
	jr nz,$+18		;171f
	ex af,af'			;1721
	inc b			;1722
	ld (bc),a			;1723
	ld bc,0b879h		;1724
	jr nc,$+8		;1727
	ld l,c			;1729
	push de			;172a
	xor a			;172b
	ld e,a			;172c
	jr $+9		;172d
	or c			;172f
	ret z			;1730
	ld l,b			;1731
	ld b,c			;1732
	push de			;1733
	ld d,000h		;1734
	ld h,b			;1736
	ld a,b			;1737
	rra			;1738
	add a,l			;1739
	jr c,$+5		;173a
	cp h			;173c
	jr c,$+9		;173d
	sub h			;173f
	ld c,a			;1740
	exx			;1741
	pop bc			;1742
	push bc			;1743
	jr $+6		;1744
	ld c,a			;1746
	push de			;1747
	exx			;1748
	pop bc			;1749
	ld hl,(0cfb9h)		;174a
	ld a,b			;174d
	add a,h			;174e
	ld b,a			;174f
	ld a,c			;1750
	inc a			;1751
	add a,l			;1752
	jr c,$+15		;1753
	jr z,$+15		;1755
	dec a			;1757
	ld c,a			;1758
	call 0c769h		;1759
	exx			;175c
	ld a,c			;175d
	djnz $-37		;175e
	pop de			;1760
	ret			;1761
	jr z,$-11		;1762
	ld a,067h		;1764
	jp 0b03ah		;1766
	ld (0cfb9h),bc		;1769
	call 0c7aah		;176d
	ld b,a			;1770
	inc b			;1771
	ld a,0feh		;1772
	rrca			;1774
	djnz $-1		;1775
	ld b,a			;1777
	call 0cd71h		;1778
	in a,(098h)		;177b
	ld c,(iy+002h)		;177d
	bit 0,c		;1780
	jr nz,$+3		;1782
	and b			;1784
	bit 2,c		;1785
	jr nz,$+4		;1787
	xor b			;1789
	cpl			;178a
	ld b,a			;178b
	call 0cd62h		;178c
	ld a,b			;178f
	out (098h),a		;1790
	set 5,h		;1792
	ld de,(0cfbbh)		;1794
	call 0cd71h		;1798
	in a,(098h)		;179b
	xor e			;179d
	and d			;179e
	xor e			;179f
	push af			;17a0
	call 0cd62h		;17a1
	pop af			;17a4
	out (098h),a		;17a5
	res 5,h		;17a7
	ret			;17a9
	ld a,0afh		;17aa
	sub b			;17ac
	jp c,0c764h		;17ad
	ld b,a			;17b0
	and 007h		;17b1
	ld l,a			;17b3
	ld a,c			;17b4
	and 0f8h		;17b5
	or l			;17b7
	ld l,a			;17b8
	ld a,b			;17b9
	srl a		;17ba
	srl a		;17bc
	srl a		;17be
	ld h,a			;17c0
	ld a,c			;17c1
	and 007h		;17c2
	ret			;17c4
	nop			;17c5
	nop			;17c6
	nop			;17c7
	nop			;17c8
	nop			;17c9
	nop			;17ca
	nop			;17cb
	nop			;17cc
	nop			;17cd
	nop			;17ce
	call 0ca54h		;17cf
	ld l,010h		;17d2
	call 0bd94h		;17d4
	call 0ca54h		;17d7
	ld a,(0d0c1h)		;17da
	inc a			;17dd
	call 0c828h		;17de
	bit 2,(ix+031h)		;17e1
	jr z,$+8		;17e5
	ld a,(0d100h)		;17e7
	or a			;17ea
	jr nz,$+14		;17eb
	push de			;17ed
	call 0ceefh		;17ee
	halt			;17f1
	call 0009ch		;17f2
	jr z,$-3		;17f5
	jr $+38		;17f7
	push de			;17f9
	ld d,(ix+030h)		;17fa
	ld e,000h		;17fd
	srl d		;17ff
	rr e		;1801
	srl d		;1803
	rr e		;1805
	jr $+3		;1807
	push de			;1809
	call 0ceefh		;180a
	halt			;180d
	call 0009ch		;180e
	jr nz,$+12		;1811
	call 0cf38h		;1813
	dec de			;1816
	ld a,d			;1817
	or e			;1818
	jr nz,$-11		;1819
	jr $+6		;181b
	call 0009fh		;181d
	scf			;1820
	push af			;1821
	call 0cefah		;1822
	pop af			;1825
	pop de			;1826
	ret			;1827
	ld (0d0beh),a		;1828
	push ix		;182b
	push bc			;182d
	ld ix,0d024h		;182e
	ld de,00010h		;1832
	ld b,008h		;1835
	ld a,(ix+00ah)		;1837
	ld (ix+007h),a		;183a
	add ix,de		;183d
	djnz $-8		;183f
	pop bc			;1841
	pop ix		;1842
	ret			;1844
	cp (ix-01ah)		;1845
	ret z			;1848
	push af			;1849
	call 0ca54h		;184a
	ld a,(0d0b6h)		;184d
	call 0c874h		;1850
	ex de,hl			;1853
	ld hl,0d0b7h		;1854
	ld bc,00010h		;1857
	ldir		;185a
	pop af			;185c
	push af			;185d
	ld (0d0b6h),a		;185e
	call 0c874h		;1861
	ld de,0d0b7h		;1864
	ld bc,00010h		;1867
	ldir		;186a
	ld a,(0d0bbh)		;186c
	ld (0cfbbh),a		;186f
	pop af			;1872
	ret			;1873
	ld hl,0d024h		;1874
	rlca			;1877
	rlca			;1878
	rlca			;1879
	rlca			;187a
	ld e,a			;187b
	ld d,000h		;187c
	add hl,de			;187e
	ret			;187f
	bit 6,(ix+01dh)		;1880
	ret nz			;1884
	push bc			;1885
	push hl			;1886
	push de			;1887
	call 0ca93h		;1888
	call 0c895h		;188b
	call 0ccadh		;188e
	pop de			;1891
	pop hl			;1892
	pop bc			;1893
	ret			;1894
	ld a,(0d0c2h)		;1895
	ld (0d0c5h),a		;1898
	call 0cd5eh		;189b
	ld (0d0bch),hl		;189e
	ld a,(0d0c3h)		;18a1
	ld (0d0c4h),a		;18a4
	ld a,(0d0c1h)		;18a7
	ld (0d0beh),a		;18aa
	ret			;18ad
	ld a,d			;18ae
	cp (ix-00eh)		;18af
	jr c,$+27		;18b2
	ld a,(0d0c2h)		;18b4
	add a,(ix-00fh)		;18b7
	cp d			;18ba
	jr c,$+18		;18bb
	ld a,e			;18bd
	cp (ix-00dh)		;18be
	jr c,$+12		;18c1
	ld a,(0d0c3h)		;18c3
	add a,(ix-010h)		;18c6
	dec a			;18c9
	cp e			;18ca
	jr nc,$+8		;18cb
	ld e,(ix-00dh)		;18cd
	ld d,(ix-00eh)		;18d0
	ld (ix-00ch),e		;18d3
	ld a,d			;18d6
	ld (0d0c5h),a		;18d7
	call 0cd5eh		;18da
	ld (0d0bch),hl		;18dd
	ret			;18e0
	ld a,(0d0c2h)		;18e1
	add a,(ix-00fh)		;18e4
	cp 018h		;18e7
	jr c,$+10		;18e9
	ld a,017h		;18eb
	sub (ix-00eh)		;18ed
	ld (0d0c1h),a		;18f0
	ld a,(0d0c3h)		;18f3
	add a,(ix-010h)		;18f6
	cp 02bh		;18f9
	ret c			;18fb
	ld a,02ah		;18fc
	sub (ix-00dh)		;18fe
	ld (0d0c0h),a		;1901
	ret			;1904
	ld a,00dh		;1905
	push ix		;1907
	ld ix,0d0d0h		;1909
	cp 080h		;190d
	jr c,$+37		;190f
	bit 2,(ix-003h)		;1911
	jr z,$+31		;1915
	push hl			;1917
	ld hl,(00108h)		;1918
	sub 07fh		;191b
	bit 7,(hl)		;191d
	inc hl			;191f
	jr z,$-3		;1920
	dec a			;1922
	jr nz,$-6		;1923
	ld a,(hl)			;1925
	add a,080h		;1926
	jr c,$+13		;1928
	add a,080h		;192a
	call 0c932h		;192c
	inc hl			;192f
	jr $-11		;1930
	push ix		;1932
	push hl			;1934
	push de			;1935
	push bc			;1936
	cp 00dh		;1937
	jr z,$+76		;1939
	cp 010h		;193b
	jr c,$+92		;193d
	bit 0,(ix-00ah)		;193f
	jr z,$+4		;1943
	or 080h		;1945
	bit 6,(ix-00ah)		;1947
	jr z,$+53		;194b
	cp 020h		;194d
	jr z,$+34		;194f
	cp 0a0h		;1951
	jr z,$+30		;1953
	push af			;1955
	ld a,(0cfdch)		;1956
	cp 028h		;1959
	call z,0ca54h		;195b
	pop af			;195e
	res 5,(ix-00ah)		;195f
	ld hl,(0d005h)		;1963
	ld (hl),a			;1966
	inc hl			;1967
	ld (0d005h),hl		;1968
	ld hl,0cfdch		;196b
	inc (hl)			;196e
	jr $+36		;196f
	bit 5,(ix-00ah)		;1971
	jr nz,$+30		;1975
	push af			;1977
	call 0ca54h		;1978
	pop af			;197b
	bit 5,(ix-00ah)		;197c
	call z,0ca2ch		;1980
	jr $+16		;1983
	call 0ca54h		;1985
	bit 7,(ix-00ah)		;1988
	res 7,(ix-00ah)		;198c
	call z,0c9c7h		;1990
	pop bc			;1993
	pop de			;1994
	pop hl			;1995
	pop ix		;1996
	ret			;1998
	cp 00fh		;1999
	jr nz,$+8		;199b
	res 0,(ix-00ah)		;199d
	jr $-14		;19a1
	cp 00eh		;19a3
	jr nz,$+8		;19a5
	set 0,(ix-00ah)		;19a7
	jr $-24		;19ab
	push af			;19ad
	call 0ca54h		;19ae
	pop af			;19b1
	cp 00bh		;19b2
	jr nz,$+7		;19b4
	call 0c885h		;19b6
	jr $-38		;19b9
	cp 00ch		;19bb
	jr nz,$-42		;19bd
	ld a,(0d0c1h)		;19bf
	call 0c7deh		;19c2
	jr $-50		;19c5
	ld a,00dh		;19c7
	call 0caa2h		;19c9
	ld hl,0d0beh		;19cc
	dec (hl)			;19cf
	ret nz			;19d0
	ld a,(0d0c1h)		;19d1
	ld (hl),a			;19d4
	bit 6,(ix-004h)		;19d5
	ret nz			;19d9
	bit 1,(ix-00ah)		;19da
	ret nz			;19de
	call 0c828h		;19df
	ld l,020h		;19e2
	call 0bd1ah		;19e4
	ld a,(0d0c6h)		;19e7
	push af			;19ea
	ld a,002h		;19eb
	ld (0d0c6h),a		;19ed
	ld a,(hl)			;19f0
	cpl			;19f1
	cp 00ah		;19f2
	jr z,$+8		;19f4
	inc hl			;19f6
	call 0c907h		;19f7
	jr $-10		;19fa
	pop af			;19fc
	ld (0d0c6h),a		;19fd
	bit 1,(ix+031h)		;1a00
	jr z,$+13		;1a04
	ld a,(0d100h)		;1a06
	or a			;1a09
	jr z,$+7		;1a0a
	call 0c7f9h		;1a0c
	jr $+5		;1a0f
	call 0c7edh		;1a11
	ld a,(0d0c4h)		;1a14
	cp (ix-00dh)		;1a17
	ret z			;1a1a
	ld a,008h		;1a1b
	call 0caa2h		;1a1d
	ld a,020h		;1a20
	call 0caa2h		;1a22
	ld a,008h		;1a25
	call 0caa2h		;1a27
	jr $-22		;1a2a
	ld e,a			;1a2c
	push de			;1a2d
	call 0caa2h		;1a2e
	pop de			;1a31
	res 7,(ix-00ah)		;1a32
	ld a,(0d0c4h)		;1a36
	cp (ix-00dh)		;1a39
	ret nz			;1a3c
	ld a,e			;1a3d
	cp 020h		;1a3e
	jr nz,$+12		;1a40
	bit 6,(ix-00ah)		;1a42
	jr z,$+6		;1a46
	set 5,(ix-00ah)		;1a48
	call 0c9cch		;1a4c
	set 7,(ix-00ah)		;1a4f
	ret			;1a53
	bit 6,(ix-00ah)		;1a54
	ret z			;1a58
	ld a,(0cfdch)		;1a59
	or a			;1a5c
	ret z			;1a5d
	ld a,(0d0c3h)		;1a5e
	add a,(ix-010h)		;1a61
	ld hl,0d0c4h		;1a64
	sub (hl)			;1a67
	ld hl,0cfdch		;1a68
	cp (hl)			;1a6b
	jr z,$+15		;1a6c
	jr nc,$+17		;1a6e
	ld a,(0d0c4h)		;1a70
	cp (ix-00dh)		;1a73
	call nz,0c9c7h		;1a76
	jr $+6		;1a79
	set 5,(ix-00ah)		;1a7b
	ld bc,(0cfdbh)		;1a7f
	ld hl,0cfddh		;1a83
	ld a,(hl)			;1a86
	push hl			;1a87
	push bc			;1a88
	call 0ca2ch		;1a89
	pop bc			;1a8c
	pop hl			;1a8d
	inc hl			;1a8e
	djnz $-9		;1a8f
	jr $+6		;1a91
	res 5,(ix-00ah)		;1a93
	ld hl,0cfddh		;1a97
	ld (0d005h),hl		;1a9a
	xor a			;1a9d
	ld (0cfdch),a		;1a9e
	ret			;1aa1
	push hl			;1aa2
	push de			;1aa3
	push bc			;1aa4
	push af			;1aa5
	call 0cabfh		;1aa6
	pop af			;1aa9
	cp 008h		;1aaa
	call z,0cad1h		;1aac
	cp 00dh		;1aaf
	call z,0cafch		;1ab1
	or a			;1ab4
	call nz,0cb2dh		;1ab5
	call 0cabfh		;1ab8
	pop bc			;1abb
	pop de			;1abc
	pop hl			;1abd
	ret			;1abe
	bit 3,(ix-00ah)		;1abf
	ret z			;1ac3
	set 4,(ix-00ah)		;1ac4
	ld a,05fh		;1ac8
	call 0cb2dh		;1aca
	res 4,(ix-00ah)		;1acd
	ld hl,0d0c4h		;1ad1
	ld a,(0d0c3h)		;1ad4
	cp (hl)			;1ad7
	jr z,$+5		;1ad8
	dec (hl)			;1ada
	jr $+31		;1adb
	ld hl,0d0c2h		;1add
	ld a,(0d0c5h)		;1ae0
	cp (hl)			;1ae3
	jr z,$+22		;1ae4
	dec a			;1ae6
	ld (0d0c5h),a		;1ae7
	call 0cd5eh		;1aea
	ld (0d0bch),hl		;1aed
	ld a,(0d0c3h)		;1af0
	add a,(ix-010h)		;1af3
	dec a			;1af6
	ld (0d0c4h),a		;1af7
	xor a			;1afa
	ret			;1afb
	ld a,(0d0c3h)		;1afc
	add a,(ix-010h)		;1aff
	cp (ix-00ch)		;1b02
	jr z,$+9		;1b05
	ld a,020h		;1b07
	call 0cb2dh		;1b09
	jr $-16		;1b0c
	ld hl,0d0c5h		;1b0e
	ld a,(0d0c2h)		;1b11
	add a,(ix-00fh)		;1b14
	cp (hl)			;1b17
	jr z,$+10		;1b18
	inc (hl)			;1b1a
	ld a,(hl)			;1b1b
	call 0cd5eh		;1b1c
	ld (0d0bch),hl		;1b1f
	call z,0cc3ch		;1b22
	ld a,(0d0c3h)		;1b25
	ld (0d0c4h),a		;1b28
	xor a			;1b2b
	ret			;1b2c
	push af			;1b2d
	ld a,(0d0c3h)		;1b2e
	add a,(ix-010h)		;1b31
	cp (ix-00ch)		;1b34
	jp nz,0cb3dh		;1b37
	call 0cafch		;1b3a
	ld a,(0d0c4h)		;1b3d
	call 0cd4fh		;1b40
	and 003h		;1b43
	bit 1,a		;1b45
	jr z,$+3		;1b47
	inc hl			;1b49
	sla l		;1b4a
	rl h		;1b4c
	sla l		;1b4e
	rl h		;1b50
	sla l		;1b52
	rl h		;1b54
	ld de,(0d0bch)		;1b56
	add hl,de			;1b5a
	push af			;1b5b
	set 7,(ix-003h)		;1b5c
	call 0cd71h		;1b60
	ld b,010h		;1b63
	ld de,0cfbfh		;1b65
	in a,(098h)		;1b68
	nop			;1b6a
	nop			;1b6b
	nop			;1b6c
	ld (de),a			;1b6d
	inc de			;1b6e
	djnz $-7		;1b6f
	res 7,(ix-003h)		;1b71
	pop af			;1b75
	ex (sp),hl			;1b76
	push hl			;1b77
	rlca			;1b78
	ld d,000h		;1b79
	ld e,a			;1b7b
	ld bc,0ffffh		;1b7c
	bit 4,(ix-00ah)		;1b7f
	jr nz,$+9		;1b83
	ld hl,0cc1ah		;1b85
	add hl,de			;1b88
	ld c,(hl)			;1b89
	inc hl			;1b8a
	ld b,(hl)			;1b8b
	rlc e		;1b8c
	rlc e		;1b8e
	ld hl,0cbfah		;1b90
	add hl,de			;1b93
	ld (0cbabh),hl		;1b94
	ld de,(0aff7h)		;1b97
	pop af			;1b9b
	ld l,a			;1b9c
	ld h,000h		;1b9d
	add hl,hl			;1b9f
	add hl,hl			;1ba0
	add hl,hl			;1ba1
	add hl,de			;1ba2
	ld de,0cfbfh		;1ba3
	ld a,008h		;1ba6
	ex af,af'			;1ba8
	push hl			;1ba9
	call 0cbfah		;1baa
	ld a,(de)			;1bad
	and b			;1bae
	xor h			;1baf
	ld (de),a			;1bb0
	inc de			;1bb1
	inc de			;1bb2
	inc de			;1bb3
	inc de			;1bb4
	inc de			;1bb5
	inc de			;1bb6
	inc de			;1bb7
	inc de			;1bb8
	ld a,(de)			;1bb9
	and c			;1bba
	xor l			;1bbb
	ld (de),a			;1bbc
	dec de			;1bbd
	dec de			;1bbe
	dec de			;1bbf
	dec de			;1bc0
	dec de			;1bc1
	dec de			;1bc2
	dec de			;1bc3
	pop hl			;1bc4
	inc hl			;1bc5
	ex af,af'			;1bc6
	dec a			;1bc7
	jr nz,$-32		;1bc8
	pop hl			;1bca
	set 7,(ix-003h)		;1bcb
	call 0cd62h		;1bcf
	ld de,0cfbfh		;1bd2
	ld b,010h		;1bd5
	ld a,(de)			;1bd7
	out (098h),a		;1bd8
	nop			;1bda
	nop			;1bdb
	nop			;1bdc
	inc de			;1bdd
	djnz $-7		;1bde
	set 5,h		;1be0
	call 0cd62h		;1be2
	ld a,(0cfbbh)		;1be5
	ld b,010h		;1be8
	out (098h),a		;1bea
	nop			;1bec
	nop			;1bed
	nop			;1bee
	djnz $-5		;1bef
	res 7,(ix-003h)		;1bf1
	ld hl,0d0c4h		;1bf5
	inc (hl)			;1bf8
	ret			;1bf9
	ld h,(hl)			;1bfa
	ld l,000h		;1bfb
	ret			;1bfd
	nop			;1bfe
	nop			;1bff
	nop			;1c00
	nop			;1c01
	ld l,(hl)			;1c02
	ld h,000h		;1c03
	add hl,hl			;1c05
	add hl,hl			;1c06
	ret			;1c07
	nop			;1c08
	nop			;1c09
	ld l,(hl)			;1c0a
	ld h,000h		;1c0b
	add hl,hl			;1c0d
	add hl,hl			;1c0e
	add hl,hl			;1c0f
	add hl,hl			;1c10
	ret			;1c11
	ld l,(hl)			;1c12
	ld h,000h		;1c13
	srl l		;1c15
	srl l		;1c17
	ret			;1c19
	rst 38h			;1c1a
	inc bc			;1c1b
	rrca			;1c1c
	call m,0f03fh		;1c1d
	ret nz			;1c20
	rst 38h			;1c21
	push hl			;1c22
	bit 3,(ix-00ah)		;1c23
	jr nz,$+19		;1c27
	set 3,(ix-00ah)		;1c29
	call 0cac4h		;1c2d
	jr $+10		;1c30
	push hl			;1c32
	call 0cabfh		;1c33
	res 3,(ix-00ah)		;1c36
	pop hl			;1c3a
	ret			;1c3b
	call 0ccf4h		;1c3c
	ld a,(0d0c1h)		;1c3f
	ld c,(ix-00eh)		;1c42
	or a			;1c45
	jr z,$+99		;1c46
	ld b,a			;1c48
	ld a,(0d0bfh)		;1c49
	sla a		;1c4c
	sla a		;1c4e
	sla a		;1c50
	set 7,(ix-003h)		;1c52
	push bc			;1c56
	push af			;1c57
	ld a,c			;1c58
	inc a			;1c59
	call 0cd2fh		;1c5a
	call 0cd71h		;1c5d
	pop af			;1c60
	push af			;1c61
	ld b,a			;1c62
	ld de,0d4d9h		;1c63
	in a,(098h)		;1c66
	nop			;1c68
	nop			;1c69
	nop			;1c6a
	ld (de),a			;1c6b
	inc de			;1c6c
	djnz $-7		;1c6d
	set 5,h		;1c6f
	call 0cd71h		;1c71
	pop af			;1c74
	push af			;1c75
	ld b,a			;1c76
	in a,(098h)		;1c77
	ld (de),a			;1c79
	inc de			;1c7a
	djnz $-4		;1c7b
	ld a,c			;1c7d
	call 0cd2fh		;1c7e
	call 0cd62h		;1c81
	pop af			;1c84
	push af			;1c85
	ld b,a			;1c86
	ld de,0d4d9h		;1c87
	ld a,(de)			;1c8a
	out (098h),a		;1c8b
	nop			;1c8d
	nop			;1c8e
	nop			;1c8f
	inc de			;1c90
	djnz $-7		;1c91
	set 5,h		;1c93
	call 0cd62h		;1c95
	pop af			;1c98
	push af			;1c99
	ld b,a			;1c9a
	ld a,(de)			;1c9b
	out (098h),a		;1c9c
	nop			;1c9e
	nop			;1c9f
	nop			;1ca0
	inc de			;1ca1
	djnz $-7		;1ca2
	pop af			;1ca4
	pop bc			;1ca5
	inc c			;1ca6
	djnz $-81		;1ca7
	ld b,001h		;1ca9
	jr $+9		;1cab
	ld b,(ix-00fh)		;1cad
	inc b			;1cb0
	ld c,(ix-00eh)		;1cb1
	call 0ccf4h		;1cb4
	ld (0d0bfh),a		;1cb7
	sla a		;1cba
	sla a		;1cbc
	sla a		;1cbe
	set 7,(ix-003h)		;1cc0
	push bc			;1cc4
	push af			;1cc5
	ld a,c			;1cc6
	call 0cd2fh		;1cc7
	call 0cd62h		;1cca
	pop af			;1ccd
	push af			;1cce
	ld b,a			;1ccf
	xor a			;1cd0
	out (098h),a		;1cd1
	nop			;1cd3
	nop			;1cd4
	nop			;1cd5
	djnz $-5		;1cd6
	set 5,h		;1cd8
	call 0cd62h		;1cda
	pop af			;1cdd
	push af			;1cde
	ld b,a			;1cdf
	ld a,(0cfbbh)		;1ce0
	out (098h),a		;1ce3
	nop			;1ce5
	nop			;1ce6
	nop			;1ce7
	djnz $-5		;1ce8
	pop af			;1cea
	pop bc			;1ceb
	inc c			;1cec
	djnz $-41		;1ced
	res 7,(ix-003h)		;1cef
	ret			;1cf3
	push bc			;1cf4
	push hl			;1cf5
	ld a,(0d0c0h)		;1cf6
	ld b,a			;1cf9
	srl a		;1cfa
	srl a		;1cfc
	ld c,a			;1cfe
	add a,a			;1cff
	add a,c			;1d00
	ld c,a			;1d01
	ld a,(0d0c3h)		;1d02
	and 003h		;1d05
	sla a		;1d07
	sla a		;1d09
	ld e,a			;1d0b
	ld a,b			;1d0c
	and 003h		;1d0d
	add a,e			;1d0f
	ld e,a			;1d10
	ld d,000h		;1d11
	ld hl,0cd1fh		;1d13
	add hl,de			;1d16
	ld a,(hl)			;1d17
	add a,c			;1d18
	ld (0d0bfh),a		;1d19
	pop hl			;1d1c
	pop bc			;1d1d
	ret			;1d1e
	nop			;1d1f
	ld bc,00302h		;1d20
	ld bc,00302h		;1d23
	inc bc			;1d26
	ld bc,00202h		;1d27
	inc bc			;1d2a
	ld bc,00201h		;1d2b
	inc bc			;1d2e
	call 0cd5eh		;1d2f
	push hl			;1d32
	ld a,(0d0c3h)		;1d33
	call 0cd4fh		;1d36
	and 003h		;1d39
	jr z,$+5		;1d3b
	dec a			;1d3d
	ld e,a			;1d3e
	add hl,de			;1d3f
	sla l		;1d40
	rl h		;1d42
	sla l		;1d44
	rl h		;1d46
	sla l		;1d48
	rl h		;1d4a
	pop de			;1d4c
	add hl,de			;1d4d
	ret			;1d4e
	ld e,a			;1d4f
	srl e		;1d50
	srl e		;1d52
	ld d,000h		;1d54
	ld hl,00000h		;1d56
	add hl,de			;1d59
	add hl,de			;1d5a
	add hl,de			;1d5b
	ret			;1d5c
	ret			;1d5d
	ld h,a			;1d5e
	ld l,000h		;1d5f
	ret			;1d61
	ld a,l			;1d62
	di			;1d63
	out (099h),a		;1d64
	ld a,h			;1d66
	and 03fh		;1d67
	or 040h		;1d69
	out (099h),a		;1d6b
	ei			;1d6d
	ex (sp),hl			;1d6e
	ex (sp),hl			;1d6f
	ret			;1d70
	ld a,l			;1d71
	di			;1d72
	out (099h),a		;1d73
	ld a,h			;1d75
	and 03fh		;1d76
	out (099h),a		;1d78
	ei			;1d7a
	ex (sp),hl			;1d7b
	ex (sp),hl			;1d7c
	ret			;1d7d
	ld (0f3ebh),a		;1d7e
	call 0ceefh		;1d81
	call 00062h		;1d84
	jp 0cefah		;1d87
	push iy		;1d8a
	call 0ceefh		;1d8c
	di			;1d8f
	pop de			;1d90
	ld bc,0000fh		;1d91
	xor a			;1d94
	call 0cdach		;1d95
	ld c,(iy+003h)		;1d98
	ld b,(iy+004h)		;1d9b
	ld e,(iy+005h)		;1d9e
	ld d,(iy+006h)		;1da1
	ld a,0ffh		;1da4
	call 0cdach		;1da6
	jp 0cefah		;1da9
	push bc			;1dac
	push de			;1dad
	push af			;1dae
	call 000eah		;1daf
	pop af			;1db2
	call 000edh		;1db3
	ld hl,00000h		;1db6
	pop de			;1db9
	pop bc			;1dba
	push bc			;1dbb
	push de			;1dbc
	ld a,(de)			;1dbd
	ld e,a			;1dbe
	ld d,000h		;1dbf
	add hl,de			;1dc1
	push hl			;1dc2
	call 000edh		;1dc3
	call 000b7h		;1dc6
	jr c,$+24		;1dc9
	pop hl			;1dcb
	pop de			;1dcc
	pop bc			;1dcd
	inc de			;1dce
	dec bc			;1dcf
	ld a,b			;1dd0
	or c			;1dd1
	jr nz,$-23		;1dd2
	ld a,l			;1dd4
	call 000edh		;1dd5
	call 000f0h		;1dd8
	ret			;1ddb
	call 000e7h		;1ddc
	jr $+5		;1ddf
	call 000f0h		;1de1
	call 0cefah		;1de4
	ld a,065h		;1de7
	jp 0b03ah		;1de9
	call 0ceefh		;1dec
	di			;1def
	ld de,0d6d9h		;1df0
	ld bc,0000fh		;1df3
	xor a			;1df6
	call 0ce6eh		;1df7
	jr nz,$-10		;1dfa
	set 6,(ix-004h)		;1dfc
	ld hl,0cecdh		;1e00
	ld b,007h		;1e03
	call 0cec5h		;1e05
	ld hl,0d6e0h		;1e08
	ld b,008h		;1e0b
	call 0cec5h		;1e0d
	call 0c905h		;1e10
	res 6,(ix-004h)		;1e13
	push iy		;1e17
	pop hl			;1e19
	ld de,00007h		;1e1a
	add hl,de			;1e1d
	ld de,0d6e0h		;1e1e
	ld b,008h		;1e21
	ld a,(de)			;1e23
	cp (hl)			;1e24
	inc hl			;1e25
	inc de			;1e26
	jr nz,$-55		;1e27
	djnz $-6		;1e29
	set 6,(ix-004h)		;1e2b
	ld hl,0ced4h		;1e2f
	ld b,00bh		;1e32
	call 0cec5h		;1e34
	res 6,(ix-004h)		;1e37
	ld a,(iy+003h)		;1e3b
	ld b,(iy+004h)		;1e3e
	ld c,a			;1e41
	or b			;1e42
	jr nz,$+8		;1e43
	ld bc,(0d6dch)		;1e45
	jr $+12		;1e49
	ld hl,(0d6dch)		;1e4b
	or a			;1e4e
	sbc hl,bc		;1e4f
	jr nc,$+4		;1e51
	ld c,l			;1e53
	ld b,h			;1e54
	ld a,(iy+005h)		;1e55
	ld d,(iy+006h)		;1e58
	ld e,a			;1e5b
	or d			;1e5c
	jr nz,$+6		;1e5d
	ld de,(0d6deh)		;1e5f
	ld a,0ffh		;1e63
	call 0ce6eh		;1e65
	jp nz,0cebdh		;1e68
	jp 0cefah		;1e6b
	push de			;1e6e
	push bc			;1e6f
	push af			;1e70
	call 000e1h		;1e71
	jp c,0cddch		;1e74
	call 000e4h		;1e77
	jp c,0cebah		;1e7a
	ld l,a			;1e7d
	pop af			;1e7e
	cp l			;1e7f
	ld hl,00000h		;1e80
	pop bc			;1e83
	pop de			;1e84
	jr z,$+9		;1e85
	call 000e7h		;1e87
	ld a,0ffh		;1e8a
	or a			;1e8c
	ret			;1e8d
	push bc			;1e8e
	push de			;1e8f
	push hl			;1e90
	call 000e4h		;1e91
	jp c,0cebah		;1e94
	pop hl			;1e97
	pop de			;1e98
	ld (de),a			;1e99
	ld c,a			;1e9a
	ld b,000h		;1e9b
	add hl,bc			;1e9d
	call 000b7h		;1e9e
	jp c,0cddch		;1ea1
	pop bc			;1ea4
	inc de			;1ea5
	dec bc			;1ea6
	ld a,b			;1ea7
	or c			;1ea8
	jr nz,$-27		;1ea9
	push hl			;1eab
	call 000e4h		;1eac
	jp c,0cebah		;1eaf
	call 000e7h		;1eb2
	pop hl			;1eb5
	cp l			;1eb6
	jr nz,$+6		;1eb7
	ret			;1eb9
	call 000e7h		;1eba
	call 0cefah		;1ebd
	ld a,066h		;1ec0
	jp 0b03ah		;1ec2
	ld a,(hl)			;1ec5
	call 0c907h		;1ec6
	inc hl			;1ec9
	djnz $-5		;1eca
	ret			;1ecc
	ld b,(hl)			;1ecd
	ld l,a			;1ece
	ld (hl),l			;1ecf
	ld l,(hl)			;1ed0
	ld h,h			;1ed1
	ld a,(04c20h)		;1ed2
	ld l,a			;1ed5
	ld h,c			;1ed6
	ld h,h			;1ed7
	ld l,c			;1ed8
	ld l,(hl)			;1ed9
	ld h,a			;1eda
	ld l,02eh		;1edb
	ld l,00dh		;1edd
	ld e,a			;1edf
	ld a,(bc)			;1ee0
	cp 00eh		;1ee1
	ret nc			;1ee3
	push af			;1ee4
	call 0ceefh		;1ee5
	pop af			;1ee8
	call 00093h		;1ee9
	jp 0cefah		;1eec
	di			;1eef
	ld a,(0b008h)		;1ef0
	out (0a8h),a		;1ef3
	ld (0cfbeh),a		;1ef5
	ei			;1ef8
	ret			;1ef9
	di			;1efa
	ld a,(0b009h)		;1efb
	out (0a8h),a		;1efe
	ld (0cfbeh),a		;1f00
	ei			;1f03
	ret			;1f04
	di			;1f05
	push af			;1f06
	push hl			;1f07
	push ix		;1f08
	ld a,(0cfbeh)		;1f0a
	push af			;1f0d
	ld a,(0b009h)		;1f0e
	out (0a8h),a		;1f11
	ld (0cfbeh),a		;1f13
	ld hl,(0b011h)		;1f16
	ld ix,0d0d0h		;1f19
	call 0b010h		;1f1d
	ld a,(0b008h)		;1f20
	out (0a8h),a		;1f23
	ld (0cfbeh),a		;1f25
	nop			;1f28
	nop			;1f29
	rst 38h			;1f2a
	di			;1f2b
	pop af			;1f2c
	out (0a8h),a		;1f2d
	ld (0cfbeh),a		;1f2f
	pop ix		;1f32
	pop hl			;1f34
	pop af			;1f35
	ei			;1f36
	ret			;1f37
	halt			;1f38
	ret			;1f39
	di			;1f3a
	ld a,(0b009h)		;1f3b
	out (0a8h),a		;1f3e
	ld (0cfbeh),a		;1f40
	ld b,000h		;1f43
	ld hl,0d900h		;1f45
	ld a,0d8h		;1f48
	ld (hl),a			;1f4a
	inc hl			;1f4b
	djnz $-2		;1f4c
	ld (hl),a			;1f4e
	ld a,0d9h		;1f4f
	ld i,a		;1f51
	ld a,0c3h		;1f53
	ld (0d8d8h),a		;1f55
	ld hl,0cf05h		;1f58
	ld (0d8d9h),hl		;1f5b
	ld hl,00122h		;1f5e
	ld de,0b00ah		;1f61
	ld a,003h		;1f64
	ex af,af'			;1f66
	ld c,(hl)			;1f67
	inc hl			;1f68
	ld b,(hl)			;1f69
	inc hl			;1f6a
	ld a,b			;1f6b
	or c			;1f6c
	jr z,$+14		;1f6d
	ex de,hl			;1f6f
	ld a,0c3h		;1f70
	ld (hl),a			;1f72
	inc hl			;1f73
	ld (hl),c			;1f74
	inc hl			;1f75
	ld (hl),b			;1f76
	inc hl			;1f77
	ex de,hl			;1f78
	jr $+5		;1f79
	inc de			;1f7b
	inc de			;1f7c
	inc de			;1f7d
	ex af,af'			;1f7e
	dec a			;1f7f
	jr nz,$-26		;1f80
	ld a,(00102h)		;1f82
	ld (0d0cah),a		;1f85
	ld (0cfd5h),sp		;1f88
	ld ix,0d0d0h		;1f8c
	im 2		;1f90
	ei			;1f92
	res 2,(ix-003h)		;1f93
	ld hl,(00108h)		;1f97
	ld a,l			;1f9a
	or h			;1f9b
	jp z,0b05ch		;1f9c
	set 2,(ix-003h)		;1f9f
	jp 0b05ch		;1fa3
	rst 38h			;1fa6
	rst 38h			;1fa7
	rst 38h			;1fa8
	rst 38h			;1fa9
	rst 38h			;1faa
	rst 38h			;1fab
	rst 38h			;1fac
	rst 38h			;1fad
	rst 38h			;1fae
	rst 38h			;1faf
	rst 38h			;1fb0
	rst 38h			;1fb1
	rst 38h			;1fb2
	ld a,(000cfh)		;1fb3
	nop			;1fb6
	nop			;1fb7
	nop			;1fb8
	nop			;1fb9
	nop			;1fba
	pop af			;1fbb
	nop			;1fbc
	nop			;1fbd
	nop			;1fbe
	rst 38h			;1fbf
	rst 38h			;1fc0
	rst 38h			;1fc1
	rst 38h			;1fc2
	rst 38h			;1fc3
	rst 38h			;1fc4
	rst 38h			;1fc5
	rst 38h			;1fc6
	rst 38h			;1fc7
	rst 38h			;1fc8
	rst 38h			;1fc9
	rst 38h			;1fca
	rst 38h			;1fcb
	rst 38h			;1fcc
	rst 38h			;1fcd
	rst 38h			;1fce
	rst 38h			;1fcf
	rst 38h			;1fd0
	rst 38h			;1fd1
	rst 38h			;1fd2
	rst 38h			;1fd3
	nop			;1fd4
	nop			;1fd5
	nop			;1fd6
	nop			;1fd7
	nop			;1fd8
	nop			;1fd9
	nop			;1fda
	nop			;1fdb
	nop			;1fdc
	rst 38h			;1fdd
	rst 38h			;1fde
	rst 38h			;1fdf
	rst 38h			;1fe0
	rst 38h			;1fe1
	rst 38h			;1fe2
	rst 38h			;1fe3
	rst 38h			;1fe4
	rst 38h			;1fe5
	rst 38h			;1fe6
	rst 38h			;1fe7
	rst 38h			;1fe8
	rst 38h			;1fe9
	rst 38h			;1fea
	rst 38h			;1feb
	rst 38h			;1fec
	rst 38h			;1fed
	rst 38h			;1fee
	rst 38h			;1fef
	rst 38h			;1ff0
	rst 38h			;1ff1
	rst 38h			;1ff2
	rst 38h			;1ff3
	rst 38h			;1ff4
	rst 38h			;1ff5
	rst 38h			;1ff6
	rst 38h			;1ff7
	rst 38h			;1ff8
	rst 38h			;1ff9
	rst 38h			;1ffa
	rst 38h			;1ffb
	rst 38h			;1ffc
	rst 38h			;1ffd
	rst 38h			;1ffe
	rst 38h			;1fff
	rst 38h			;2000
	rst 38h			;2001
	rst 38h			;2002
	rst 38h			;2003
	rst 38h			;2004
	defb 0ddh,0cfh,000h	;illegal sequence		;2005
	nop			;2008
	nop			;2009
	nop			;200a
	nop			;200b
	nop			;200c
	nop			;200d
	nop			;200e
	nop			;200f
	nop			;2010
	nop			;2011
	nop			;2012
	nop			;2013
	nop			;2014
	nop			;2015
	nop			;2016
	nop			;2017
	pop af			;2018
	nop			;2019
	nop			;201a
	rla			;201b
	jr nz,$+44		;201c
	rla			;201e
	nop			;201f
	nop			;2020
	nop			;2021
	nop			;2022
	ld b,b			;2023
	nop			;2024
	nop			;2025
	nop			;2026
	nop			;2027
	pop af			;2028
	nop			;2029
	nop			;202a
	rla			;202b
	jr nz,$+44		;202c
	rla			;202e
	nop			;202f
	nop			;2030
	nop			;2031
	nop			;2032
	ld b,b			;2033
	nop			;2034
	nop			;2035
	nop			;2036
	nop			;2037
	pop af			;2038
	nop			;2039
	nop			;203a
	rla			;203b
	jr nz,$+44		;203c
	rla			;203e
	nop			;203f
	nop			;2040
	nop			;2041
	nop			;2042
	ld b,b			;2043
	nop			;2044
	nop			;2045
	nop			;2046
	nop			;2047
	pop af			;2048
	nop			;2049
	nop			;204a
	rla			;204b
	jr nz,$+44		;204c
	rla			;204e
	nop			;204f
	nop			;2050
	nop			;2051
	nop			;2052
	ld b,b			;2053
	nop			;2054
	nop			;2055
	nop			;2056
	nop			;2057
	pop af			;2058
	nop			;2059
	nop			;205a
	rla			;205b
	jr nz,$+44		;205c
	rla			;205e
	nop			;205f
	nop			;2060
	nop			;2061
	nop			;2062
	ld b,b			;2063
	nop			;2064
	nop			;2065
	nop			;2066
	nop			;2067
	pop af			;2068
	nop			;2069
	nop			;206a
	rla			;206b
	jr nz,$+44		;206c
	rla			;206e
	nop			;206f
	nop			;2070
	nop			;2071
	nop			;2072
	ld b,b			;2073
	nop			;2074
	nop			;2075
	nop			;2076
	nop			;2077
	pop af			;2078
	nop			;2079
	nop			;207a
	rla			;207b
	jr nz,$+44		;207c
	rla			;207e
	nop			;207f
	nop			;2080
	nop			;2081
	nop			;2082
	ld b,b			;2083
	nop			;2084
	nop			;2085
	nop			;2086
	nop			;2087
	pop af			;2088
	nop			;2089
	nop			;208a
	rla			;208b
	jr nz,$+44		;208c
	rla			;208e
	nop			;208f
	nop			;2090
	nop			;2091
	nop			;2092
	ld b,b			;2093
	nop			;2094
	nop			;2095
	nop			;2096
	nop			;2097
	pop af			;2098
	nop			;2099
	nop			;209a
	rla			;209b
	jr nz,$+44		;209c
	rla			;209e
	nop			;209f
	nop			;20a0
	nop			;20a1
	nop			;20a2
	ld b,b			;20a3
	nop			;20a4
	nop			;20a5
	nop			;20a6
	nop			;20a7
	ld sp,hl			;20a8
	nop			;20a9
	nop			;20aa
	inc bc			;20ab
	dec c			;20ac
	djnz $+5		;20ad
	nop			;20af
	nop			;20b0
	nop			;20b1
	nop			;20b2
	ld b,b			;20b3
	nop			;20b4
	nop			;20b5
	nop			;20b6
	rst 38h			;20b7
	rst 38h			;20b8
	nop			;20b9
	nop			;20ba
	pop af			;20bb
	nop			;20bc
	nop			;20bd
	rla			;20be
	jr nz,$+44		;20bf
	rla			;20c1
	nop			;20c2
	nop			;20c3
	nop			;20c4
	nop			;20c5
	ld b,b			;20c6
	nop			;20c7
	nop			;20c8
	ld a,b			;20c9
	nop			;20ca
	nop			;20cb
	nop			;20cc
	nop			;20cd
	nop			;20ce
	nop			;20cf

[stderr]
Warning: Code might not be 8080 compatible!
