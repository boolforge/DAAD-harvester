; z80dasm 1.1.6
; command line: z80dasm -a -g 0 /home/ubuntu/DAAD-harvester/reverse_engineering/artifacts/original/msx/daad-msx-msxsdi-official/MSXSDI.Z80

	org	00000h

	jp 0cfcfh		;0000
	ld h,a			;0003
	ld hl,0ffffh		;0004
	rst 38h			;0007
	ret p			;0008
	rst 38h			;0009
	ret			;000a
	nop			;000b
	nop			;000c
	jp 0cf74h		;000d
	ret			;0010
	nop			;0011
	nop			;0012
	jp 0cf79h		;0013
	jp 0be26h		;0016
	jp 0c8d8h		;0019
	jp 0c99ah		;001c
	jp 0cae7h		;001f
	jp 0c880h		;0022
	jp 0c88ch		;0025
	jp 0b02dh		;0028
	ld bc,0dd0fh		;002b
	set 7,a		;002e
	add a,0e1h		;0030
	dec hl			;0032
	dec hl			;0033
	dec hl			;0034
	dec hl			;0035
	ld (0d0a6h),hl		;0036
	ld a,d			;0039
	ld (0d069h),a		;003a
	ld ix,0d167h		;003d
	res 6,(ix-001h)		;0041
	ld a,(0d06dh)		;0045
	or a			;0048
	jr nz,$+10		;0049
	ld a,008h		;004b
	call 0c8d8h		;004d
	call 0cd40h		;0050
	call 0b078h		;0053
	call 0cae7h		;0056
	call 0c880h		;0059
	res 2,(ix-001h)		;005c
	call 0cb26h		;0060
	ld hl,0cfcfh		;0063
	ld (hl),00dh		;0066
	ld (0d048h),hl		;0068
	ld (ix-007h),078h		;006b
	ld hl,(0d06ch)		;006f
	ld a,h			;0072
	or a			;0073
	jp z,0b0cah		;0074
	jp (hl)			;0077
	call 0c998h		;0078
	ld a,(0d069h)		;007b
	ld hl,0b0beh		;007e
	bit 0,(ix-001h)		;0081
	jr nz,$+19		;0085
	ld hl,0b0adh		;0087
	cp 065h		;008a
	jr z,$+30		;008c
	ld hl,0b0b3h		;008e
	cp 066h		;0091
	jr z,$+23		;0093
	ld hl,0b0b7h		;0095
	ld e,a			;0098
	push de			;0099
	call 0bdf1h		;009a
	pop de			;009d
	call 0bda0h		;009e
	bit 0,(ix-001h)		;00a1
	res 0,(ix-001h)		;00a5
	ret			;00a9
	jp 0bdf1h		;00aa
	cp l			;00ad
	xor l			;00ae
	cp d			;00af
	cp (hl)			;00b0
	or h			;00b1
	push af			;00b2
	or (hl)			;00b3
	ret nc			;00b4
	or b			;00b5
	rst 18h			;00b6
	cp d			;00b7
	adc a,l			;00b8
	adc a,l			;00b9
	sub b			;00ba
	adc a,l			;00bb
	rst 18h			;00bc
	push af			;00bd
	cp b			;00be
	sbc a,(hl)			;00bf
	sub d			;00c0
	sbc a,d			;00c1
	rst 18h			;00c2
	cp d			;00c3
	adc a,l			;00c4
	adc a,l			;00c5
	sub b			;00c6
	adc a,l			;00c7
	rst 18h			;00c8
	push af			;00c9
	ld sp,(0d06ah)		;00ca
	call 0cf84h		;00ce
	ld a,002h		;00d1
	call 0005fh		;00d3
	ld a,(0b02bh)		;00d6
	ld (0f3ebh),a		;00d9
	ld (0f3eah),a		;00dc
	ld a,(0b02ch)		;00df
	ld (0f3e9h),a		;00e2
	call 00062h		;00e5
	call 0cf8fh		;00e8
	ld de,0d0bbh		;00eb
	ld a,008h		;00ee
	ld hl,0d0abh		;00f0
	ld bc,00010h		;00f3
	ldir		;00f6
	dec a			;00f8
	jr nz,$-9		;00f9
	ld (0d14dh),a		;00fb
	ld de,0d14eh		;00fe
	ld hl,0d0abh		;0101
	ld bc,00010h		;0104
	ldir		;0107
	ld a,(0d152h)		;0109
	ld e,a			;010c
	ld d,000h		;010d
	ld (0d050h),de		;010f
	ld hl,0d167h		;0113
	push hl			;0116
	pop ix		;0117
	xor a			;0119
	ld b,a			;011a
	ld (hl),a			;011b
	inc hl			;011c
	djnz $-2		;011d
	set 7,(ix+01dh)		;011f
	call 0b12dh		;0123
	jp nz,0b05ch		;0126
	call 0cf84h		;0129
	rst 0			;012c
	ld (0d0a2h),sp		;012d
	xor a			;0131
	ld (0d0a5h),a		;0132
	call 0bd5eh		;0135
	xor a			;0138
	push af			;0139
	ld (0d0a8h),a		;013a
	ld (0d0a4h),a		;013d
	xor a			;0140
	cp (hl)			;0141
	jr nz,$+26		;0142
	pop de			;0144
	ld a,d			;0145
	or a			;0146
	jr z,$+7		;0147
	pop hl			;0149
	pop bc			;014a
	jp 0b5e8h		;014b
	ld hl,0d0a4h		;014e
	cp (hl)			;0151
	ret z			;0152
	ld a,e			;0153
	ld (0d0a5h),a		;0154
	dec (hl)			;0157
	pop bc			;0158
	pop hl			;0159
	jr $+34		;015a
	ld a,(hl)			;015c
	inc hl			;015d
	cp 0ffh		;015e
	jr z,$+7		;0160
	cp (ix+021h)		;0162
	jr nz,$+12		;0165
	ld a,(hl)			;0167
	cp 0ffh		;0168
	jr z,$+13		;016a
	cp (ix+022h)		;016c
	jr z,$+8		;016f
	inc hl			;0171
	inc hl			;0172
	inc hl			;0173
	jp 0b140h		;0174
	inc hl			;0177
	ld c,(hl)			;0178
	inc hl			;0179
	ld b,(hl)			;017a
	inc hl			;017b
	ld a,(bc)			;017c
	cp 0ffh		;017d
	jr z,$-63		;017f
	ld (0d162h),a		;0181
	push hl			;0184
	ld d,000h		;0185
	sla a		;0187
	ld e,a			;0189
	inc bc			;018a
	ld a,(bc)			;018b
	ld hl,0b78ch		;018c
	add hl,de			;018f
	ld e,(hl)			;0190
	inc hl			;0191
	ld d,(hl)			;0192
	push de			;0193
	ld d,000h		;0194
	ld e,a			;0196
	bit 7,(ix-005h)		;0197
	jr z,$+8		;019b
	ld hl,0d167h		;019d
	add hl,de			;01a0
	ld a,(hl)			;01a1
	ld e,a			;01a2
	ld hl,0d267h		;01a3
	add hl,de			;01a6
	push hl			;01a7
	ld hl,0d167h		;01a8
	add hl,de			;01ab
	pop de			;01ac
	ret			;01ad
	cp (ix+026h)		;01ae
	jr z,$+99		;01b1
	jr $+14		;01b3
	cp (ix+026h)		;01b5
	jr nz,$+92		;01b8
	jr $+7		;01ba
	cp (ix+026h)		;01bc
	jr c,$+85		;01bf
	pop hl			;01c1
	jp 0b140h		;01c2
	cp (ix+026h)		;01c5
	jr c,$-7		;01c8
	jr z,$-9		;01ca
	jr $+72		;01cc
	ld a,(de)			;01ce
	cp (ix+026h)		;01cf
	jr z,$+66		;01d2
	cp 0fdh		;01d4
	jr nc,$+62		;01d6
	jr $-23		;01d8
	ld a,(de)			;01da
	cp (ix+026h)		;01db
	jr z,$-29		;01de
	cp 0fdh		;01e0
	jr nc,$-33		;01e2
	jr $+48		;01e4
	ld a,(de)			;01e6
	cp 0fdh		;01e7
	jr z,$+43		;01e9
	jr $-42		;01eb
	ld a,(de)			;01ed
	cp 0fdh		;01ee
	jr nz,$+36		;01f0
	jr $-49		;01f2
	ld a,(de)			;01f4
	cp 0feh		;01f5
	jr nz,$-54		;01f7
	jr $+27		;01f9
	ld a,(de)			;01fb
	cp 0feh		;01fc
	jr z,$-61		;01fe
	jr $+20		;0200
	call 0bd7dh		;0202
	cp h			;0205
	jr nc,$+14		;0206
	jr $-71		;0208
	ld a,(hl)			;020a
	or a			;020b
	jr nz,$-75		;020c
	jr $+6		;020e
	ld a,(hl)			;0210
	or a			;0211
	jr z,$-81		;0212
	inc bc			;0214
	pop hl			;0215
	jp 0b17ch		;0216
	inc bc			;0219
	ld a,(bc)			;021a
	cp (hl)			;021b
	jr nz,$-91		;021c
	jr $-10		;021e
	call 0b50dh		;0220
	jr $-8		;0223
	inc bc			;0225
	ld a,(bc)			;0226
	cp (hl)			;0227
	jr z,$-103		;0228
	jr $-22		;022a
	call 0b50dh		;022c
	jr $-8		;022f
	call 0b50dh		;0231
	jr $+4		;0234
	inc bc			;0236
	ld a,(bc)			;0237
	cp (hl)			;0238
	jr c,$-37		;0239
	jr $-122		;023b
	call 0b50dh		;023d
	jr $+4		;0240
	inc bc			;0242
	ld a,(bc)			;0243
	cp (hl)			;0244
	jp c,0b1c1h		;0245
	jp z,0b1c1h		;0248
	jp 0b214h		;024b
	cp (ix+023h)		;024e
	jr z,$-61		;0251
	jp 0b1c1h		;0253
	cp (ix+024h)		;0256
	jr $-8		;0259
	cp (ix+02bh)		;025b
	jr $-13		;025e
	cp (ix+02ch)		;0260
	jr $-18		;0263
	cp (ix+02dh)		;0265
	jr $-23		;0268
	ld l,00ch		;026a
	push bc			;026c
	call 0c04fh		;026d
	pop bc			;0270
	ld a,(hl)			;0271
	ld l,01eh		;0272
	call 0bd6eh		;0274
	cpl			;0277
	cp (hl)			;0278
	jp nz,0b1c1h		;0279
	jp 0b457h		;027c
	ld l,00dh		;027f
	call 0c04fh		;0281
	ld a,(hl)			;0284
	ld l,01fh		;0285
	call 0bd6eh		;0287
	cpl			;028a
	sub (hl)			;028b
	jr z,$+13		;028c
	xor a			;028e
	ld (0d18dh),a		;028f
	ld sp,(0d0a2h)		;0292
	jp 0b131h		;0296
	ld sp,(0d0a2h)		;0299
	or a			;029d
	ret			;029e
	res 2,(ix-001h)		;029f
	call 0bde8h		;02a3
	pop hl			;02a6
	set 4,(ix-001h)		;02a7
	jp 0b144h		;02ab
	ld l,00fh		;02ae
	jr $-13		;02b0
	push bc			;02b2
	call 0c862h		;02b3
	pop bc			;02b6
	jp 0b457h		;02b7
	inc bc			;02ba
	call 0b00ah		;02bb
	jp 0b456h		;02be
	inc bc			;02c1
	call 0b00dh		;02c2
	jp 0b456h		;02c5
	push bc			;02c8
	call 0c918h		;02c9
	pop bc			;02cc
	jp 0b457h		;02cd
	ld (ix+001h),000h		;02d0
	ld hl,0d267h		;02d4
	ld a,(hl)			;02d7
	cp 0ffh		;02d8
	jp z,0b457h		;02da
	cp 0fdh		;02dd
	jr c,$+6		;02df
	ld a,(0d18dh)		;02e1
	ld (hl),a			;02e4
	inc hl			;02e5
	jr $-15		;02e6
	push bc			;02e8
	ld a,(0d18dh)		;02e9
	ld bc,0fefdh		;02ec
	call 0b956h		;02ef
	pop bc			;02f2
	ld l,01ah		;02f3
	jp z,0b384h		;02f5
	dec bc			;02f8
	call 0b9a3h		;02f9
	ld a,(de)			;02fc
	ld l,019h		;02fd
	cp 0fdh		;02ff
	jr nc,$+74		;0301
	inc l			;0303
	cp (ix+026h)		;0304
	jr nz,$+129		;0307
	ld a,(0d19ah)		;0309
	call 0b8cfh		;030c
	call 0b8a9h		;030f
	ld l,02bh		;0312
	add a,h			;0314
	jr c,$+115		;0315
	ld hl,0d19bh		;0317
	cp (hl)			;031a
	jr z,$+6		;031b
	ld l,02bh		;031d
	jr nc,$+105		;031f
	ld l,024h		;0321
	ld h,01bh		;0323
	push hl			;0325
	ld a,(0d168h)		;0326
	ld hl,0d18ch		;0329
	cp (hl)			;032c
	jr nc,$+15		;032d
	ex de,hl			;032f
	ld (hl),0feh		;0330
	inc (ix+001h)		;0332
	pop hl			;0335
	call 0bde8h		;0336
	jp 0b456h		;0339
	pop hl			;033c
	ld l,h			;033d
	ld a,(0d0a8h)		;033e
	or a			;0341
	jp z,0b29fh		;0342
	ld a,(00103h)		;0345
	ld (0d0a8h),a		;0348
	jp 0b29fh		;034b
	call 0b947h		;034e
	jr z,$+51		;0351
	dec bc			;0353
	call 0b9a3h		;0354
	ld a,(de)			;0357
	cp 0fdh		;0358
	ld l,018h		;035a
	jr z,$-17		;035c
	ld l,031h		;035e
	cp (ix+026h)		;0360
	jr z,$-24		;0363
	ld l,01ch		;0365
	cp 0feh		;0367
	jr nz,$-30		;0369
	ld a,(0d18dh)		;036b
	ld (de),a			;036e
	dec (ix+001h)		;036f
	ld l,027h		;0372
	jr $-62		;0374
	push bc			;0376
	ld a,0fdh		;0377
	ld b,0feh		;0379
	call 0b953h		;037b
	pop bc			;037e
	dec bc			;037f
	jr nz,$+14		;0380
	ld l,017h		;0382
	jr c,$+4		;0384
	ld l,008h		;0386
	jp 0b29fh		;0388
	call 0b9a3h		;038b
	ld a,(de)			;038e
	ld l,032h		;038f
	cp 0feh		;0391
	jr z,$-11		;0393
	cp (ix+026h)		;0395
	jr z,$-16		;0398
	ld l,017h		;039a
	cp 0fdh		;039c
	jr nz,$-22		;039e
	call 0b92ah		;03a0
	inc l			;03a3
	jr nc,$-28		;03a4
	ld l,026h		;03a6
	ld h,02ah		;03a8
	jp 0b325h		;03aa
	push bc			;03ad
	ld b,0fdh		;03ae
	call 0b951h		;03b0
	pop bc			;03b3
	ld l,01ch		;03b4
	jr z,$-50		;03b6
	dec bc			;03b8
	call 0b9a3h		;03b9
	ld a,(de)			;03bc
	ld l,031h		;03bd
	cp (ix+026h)		;03bf
	jr z,$-58		;03c2
	ld l,01dh		;03c4
	cp 0fdh		;03c6
	jr z,$-64		;03c8
	dec l			;03ca
	jr c,$-67		;03cb
	call 0b92ah		;03cd
	jr nc,$-72		;03d0
	ex de,hl			;03d2
	ld (hl),0fdh		;03d3
	dec (ix+001h)		;03d5
	ld l,025h		;03d8
	jp 0b336h		;03da
	ld e,a			;03dd
	ld d,000h		;03de
	push bc			;03e0
	call 0c89ch		;03e1
	pop bc			;03e4
	jr $+113		;03e5
	ld (0d18dh),a		;03e7
	jr $+108		;03ea
	ld de,(0010eh)		;03ec
	ld hl,00104h		;03f0
	jr $+22		;03f3
	set 6,(ix-001h)		;03f5
	ld de,(00110h)		;03f9
	ld hl,00105h		;03fd
	jr $+9		;0400
	ld de,(00112h)		;0402
	ld hl,00106h		;0406
	cp (hl)			;0409
	jr nc,$+17		;040a
	ex de,hl			;040c
	ld d,000h		;040d
	ld e,a			;040f
	add hl,de			;0410
	add hl,de			;0411
	ld e,(hl)			;0412
	inc hl			;0413
	ld d,(hl)			;0414
	ex de,hl			;0415
	call 0bdf1h		;0416
	jr $+61		;0419
	ld d,007h		;041b
	jp 0b02dh		;041d
	call 0b9a3h		;0420
	ex de,hl			;0423
	ld a,(hl)			;0424
	cp 0feh		;0425
	ld (hl),0fch		;0427
	jr nz,$+45		;0429
	dec (ix+001h)		;042b
	jr $+40		;042e
	call 0b9a3h		;0430
	ld a,(de)			;0433
	cp 0feh		;0434
	ld a,(0d18dh)		;0436
	ld (de),a			;0439
	jr $-17		;043a
	inc bc			;043c
	ld a,(bc)			;043d
	call 0b9a3h		;043e
	ld l,a			;0441
	ld h,000h		;0442
	ld a,(de)			;0444
	push de			;0445
	ld de,0d267h		;0446
	add hl,de			;0449
	ld d,(hl)			;044a
	ld (hl),a			;044b
	pop hl			;044c
	ld (hl),d			;044d
	jr $+8		;044e
	ld (hl),0ffh		;0450
	jr $+4		;0452
	ld (hl),000h		;0454
	inc bc			;0456
	pop hl			;0457
	set 4,(ix-001h)		;0458
	jp 0b17ch		;045c
	inc bc			;045f
	ld d,a			;0460
	ld a,(bc)			;0461
	add a,(hl)			;0462
	jr nc,$+36		;0463
	ld a,0ffh		;0465
	jr $+32		;0467
	inc bc			;0469
	ld a,(bc)			;046a
	ld d,a			;046b
	ld a,(hl)			;046c
	sub d			;046d
	jr nc,$+3		;046e
	xor a			;0470
	ld (hl),a			;0471
	jr $-28		;0472
	inc bc			;0474
	push af			;0475
	ld a,(bc)			;0476
	push hl			;0477
	ld e,a			;0478
	ld d,000h		;0479
	ld hl,0d167h		;047b
	add hl,de			;047e
	ld e,(hl)			;047f
	pop hl			;0480
	pop af			;0481
	jr $+7		;0482
	inc bc			;0484
	ld d,a			;0485
	ld a,(bc)			;0486
	ld e,a			;0487
	ld a,d			;0488
	cp 026h		;0489
	jr nz,$+14		;048b
	ld a,(00104h)		;048d
	cp e			;0490
	ld d,001h		;0491
	jp c,0b02dh		;0493
	jp z,0b02dh		;0496
	ld (hl),e			;0499
	jr $-68		;049a
	call 0c998h		;049c
	jr $-72		;049f
	ld e,(hl)			;04a1
	inc hl			;04a2
	ld d,(hl)			;04a3
	call 0bda2h		;04a4
	jr $-81		;04a7
	ld e,(hl)			;04a9
	call 0bda0h		;04aa
	jr $-87		;04ad
	inc bc			;04af
	ex de,hl			;04b0
	ld a,(bc)			;04b1
	call 0b923h		;04b2
	cp (hl)			;04b5
	jp nz,0b1c1h		;04b6
	jp 0b214h		;04b9
	inc bc			;04bc
	ex de,hl			;04bd
	ld a,(bc)			;04be
	call 0b923h		;04bf
	cp (hl)			;04c2
	jp nz,0b214h		;04c3
	jp 0b1c1h		;04c6
	call 0b9a3h		;04c9
	inc bc			;04cc
	ex de,hl			;04cd
	ld a,(bc)			;04ce
	call 0b923h		;04cf
	push af			;04d2
	ld a,(hl)			;04d3
	jr $+31		;04d4
	call 0b923h		;04d6
	push af			;04d9
	ld a,(0d19ah)		;04da
	jr $+19		;04dd
	ld a,(hl)			;04df
	cp 0ffh		;04e0
	jr nz,$+8		;04e2
	ld d,002h		;04e4
	jp 0b02dh		;04e6
	ld a,(de)			;04e9
	push af			;04ea
	inc bc			;04eb
	ld a,(bc)			;04ec
	call 0b9a3h		;04ed
	call 0b93eh		;04f0
	cp 0feh		;04f3
	jr nz,$+5		;04f5
	dec (ix+001h)		;04f7
	pop af			;04fa
	cp 0feh		;04fb
	jr nz,$+5		;04fd
	inc (ix+001h)		;04ff
	ld (hl),a			;0502
	jp 0b456h		;0503
	ex de,hl			;0506
	call 0b50eh		;0507
	jp 0b487h		;050a
	ex de,hl			;050d
	inc bc			;050e
	ld a,(bc)			;050f
	push bc			;0510
	ld c,a			;0511
	ld b,000h		;0512
	ld hl,0d167h		;0514
	add hl,bc			;0517
	ld a,(de)			;0518
	ld e,a			;0519
	ld d,c			;051a
	pop bc			;051b
	ret			;051c
	call 0b50dh		;051d
	jp 0b462h		;0520
	call 0b50dh		;0523
	jp 0b46bh		;0526
	ld d,(ix+026h)		;0529
	set 1,(ix-001h)		;052c
	call 0ba20h		;0530
	jp 0b457h		;0533
	push bc			;0536
	or a			;0537
	jr nz,$+7		;0538
	call 0bad4h		;053a
	jr $+5		;053d
	call 0bac8h		;053f
	pop bc			;0542
	jp c,0b214h		;0543
	set 4,(ix-001h)		;0546
	jp 0b1c1h		;054a
	call 0b923h		;054d
	ld d,a			;0550
	res 1,(ix-001h)		;0551
	call 0ba20h		;0555
	jp 0b456h		;0558
	inc bc			;055b
	ld hl,00107h		;055c
	cp (hl)			;055f
	jr nc,$+40		;0560
	or a			;0562
	jr z,$+37		;0563
	call 0bd5eh		;0565
	push af			;0568
	ld a,(0d0a4h)		;0569
	inc a			;056c
	ld (0d0a4h),a		;056d
	cp 00ah		;0570
	jp z,0b58dh		;0572
	pop af			;0575
	push bc			;0576
	ld de,(0d0a5h)		;0577
	ld d,000h		;057b
	push de			;057d
	ld (0d0a5h),a		;057e
	res 4,(ix-001h)		;0581
	jp 0b140h		;0585
	ld d,006h		;0588
	jp 0b02dh		;058a
	pop af			;058d
	ld d,003h		;058e
	jp 0b02dh		;0590
	pop hl			;0593
	ld a,(0d0a5h)		;0594
	call 0bd5eh		;0597
	jp 0b140h		;059a
	ld (0d197h),a		;059d
	inc bc			;05a0
	ld a,(bc)			;05a1
	ld e,a			;05a2
	ld d,0f8h		;05a3
	jr $+28		;05a5
	inc bc			;05a7
	jp 0b456h		;05a8
	ld (0d18ch),a		;05ab
	inc bc			;05ae
	ld a,(bc)			;05af
	ld (0d19bh),a		;05b0
	jp 0b456h		;05b3
	ld (0d190h),a		;05b6
	inc bc			;05b9
	ld a,(bc)			;05ba
	rlca			;05bb
	rlca			;05bc
	rlca			;05bd
	ld e,a			;05be
	ld d,0c7h		;05bf
	ld a,(0d198h)		;05c1
	xor e			;05c4
	and d			;05c5
	xor e			;05c6
	ld (0d198h),a		;05c7
	jp 0b456h		;05ca
	res 2,(ix-001h)		;05cd
	jp 0b457h		;05d1
	call 0b923h		;05d4
	ld (0d199h),a		;05d7
	ld a,(0d0a8h)		;05da
	or a			;05dd
	ld d,004h		;05de
	jp nz,0b02dh		;05e0
	pop hl			;05e3
	res 4,(ix-001h)		;05e4
	push bc			;05e8
	push hl			;05e9
	ld de,0ff00h		;05ea
	push de			;05ed
	push hl			;05ee
	push bc			;05ef
	ld a,(0d0a8h)		;05f0
	ld e,a			;05f3
	ld a,(0d199h)		;05f4
	call 0b9f8h		;05f7
	jr nc,$+41		;05fa
	ld (0d189h),a		;05fc
	cp (ix+02ch)		;05ff
	inc hl			;0602
	ld a,(hl)			;0603
	ld (0d18ah),a		;0604
	jr nz,$+15		;0607
	ld a,(0d194h)		;0609
	cp 0ffh		;060c
	jr z,$+5		;060e
	cp (hl)			;0610
	jr nz,$+5		;0611
	pop hl			;0613
	jr $-32		;0614
	ld a,e			;0616
	ld (0d0a8h),a		;0617
	dec a			;061a
	call 0b9a3h		;061b
	pop de			;061e
	pop bc			;061f
	jp 0b456h		;0620
	xor a			;0623
	ld (0d0a8h),a		;0624
	pop bc			;0627
	pop hl			;0628
	pop hl			;0629
	pop hl			;062a
	pop hl			;062b
	jp 0b144h		;062c
	push af			;062f
	call 0b50eh		;0630
	pop af			;0633
	push hl			;0634
	call 0b8cfh		;0635
	ld a,h			;0638
	pop hl			;0639
	jp 0b487h		;063a
	ld d,a			;063d
	call 0b8a9h		;063e
	jp 0b487h		;0641
	push hl			;0644
	call 0bd7dh		;0645
	ld d,a			;0648
	ld a,h			;0649
	pop hl			;064a
	jp 0b487h		;064b
	ld (0d06eh),a		;064e
	call 0b947h		;0651
	jp z,0b384h		;0654
	jr $+10		;0657
	call 0b9a3h		;0659
	inc bc			;065c
	ld a,(bc)			;065d
	ld (0d06eh),a		;065e
	ld a,(de)			;0661
	cp 0fdh		;0662
	ld l,018h		;0664
	jp z,0b29fh		;0666
	ld l,031h		;0669
	cp (ix+026h)		;066b
	jp z,0b29fh		;066e
	cp 0feh		;0671
	ld l,01ch		;0673
	jp nz,0b29fh		;0675
	ld a,(0d06eh)		;0678
	ld (de),a			;067b
	dec (ix+001h)		;067c
	ld l,02ch		;067f
	call 0b6d9h		;0681
	jp 0b336h		;0684
	ld (0d06eh),a		;0687
	push bc			;068a
	ld bc,0fefdh		;068b
	call 0b956h		;068e
	jr nz,$+9		;0691
	ld a,(0d18dh)		;0693
	ld b,a			;0696
	call 0b953h		;0697
	pop bc			;069a
	jr nz,$+10		;069b
	ld a,(0d06eh)		;069d
	jr c,$+49		;06a0
	jp 0b386h		;06a2
	ld a,(0d06eh)		;06a5
	jr $+7		;06a8
	call 0b9a3h		;06aa
	inc bc			;06ad
	ld a,(bc)			;06ae
	ld h,a			;06af
	ld a,(de)			;06b0
	ld l,019h		;06b1
	cp 0fdh		;06b3
	jp nc,0b29fh		;06b5
	cp h			;06b8
	jr nz,$+16		;06b9
	ld a,h			;06bb
	push de			;06bc
	call 0b93eh		;06bd
	pop de			;06c0
	cp 0fdh		;06c1
	jp nc,0b321h		;06c3
	jp 0b309h		;06c6
	ld l,02dh		;06c9
	cp (ix+026h)		;06cb
	ld a,h			;06ce
	jr z,$+4		;06cf
	ld l,034h		;06d1
	call 0b6d9h		;06d3
	jp 0b29fh		;06d6
	push af			;06d9
	call 0bde8h		;06da
	pop af			;06dd
	set 4,(ix-003h)		;06de
	set 5,(ix-003h)		;06e2
	call 0bcfdh		;06e6
	ld l,033h		;06e9
	ret			;06eb
	push bc			;06ec
	ld b,0fdh		;06ed
	call 0b951h		;06ef
	pop bc			;06f2
	jp 0b457h		;06f3
	push bc			;06f6
	ld hl,(00118h)		;06f7
	ld de,0d267h		;06fa
	ld a,(hl)			;06fd
	ldi		;06fe
	cp 0feh		;0700
	jr c,$-5		;0702
	jr nz,$-18		;0704
	inc (ix+001h)		;0706
	jr $-12		;0709
	res 4,(ix-001h)		;070b
	pop hl			;070f
	jp 0b144h		;0710
	push bc			;0713
	ld a,(hl)			;0714
	push hl			;0715
	ld hl,00104h		;0716
	cp (hl)			;0719
	call c,0baaeh		;071a
	pop hl			;071d
	pop bc			;071e
	jp nc,0b1c1h		;071f
	ld (hl),a			;0722
	jp 0b456h		;0723
	bit 4,(ix-001h)		;0726
	jp nz,0b215h		;072a
	jp 0b1c1h		;072d
	bit 4,(ix-001h)		;0730
	jp nz,0b1c1h		;0734
	jp 0b215h		;0737
	ld h,000h		;073a
	ld l,a			;073c
	sla l		;073d
	jr nc,$+3		;073f
	dec h			;0741
	add hl,hl			;0742
	pop de			;0743
	add hl,de			;0744
	jp 0b140h		;0745
	cp 0ffh		;0748
	jr z,$+5		;074a
	ld (0d188h),a		;074c
	inc bc			;074f
	ld a,(bc)			;0750
	cp 0ffh		;0751
	jr z,$+5		;0753
	ld (0d189h),a		;0755
	jp 0b456h		;0758
	call 0b9a3h		;075b
	jp 0b456h		;075e
	ld a,020h		;0761
	call 0c99ah		;0763
	jp 0b457h		;0766
	call 0b88ch		;0769
	jp nz,0b214h		;076c
	jp 0b1c1h		;076f
	call 0b88ch		;0772
	jp nz,0b1c1h		;0775
	jp 0b214h		;0778
	ld l,a			;077b
	inc bc			;077c
	ld a,(bc)			;077d
	inc bc			;077e
	ld h,a			;077f
	call 0b786h		;0780
	jp 0b457h		;0783
	jp (hl)			;0786
	ld d,005h		;0787
	jp 0b02dh		;0789
	xor (hl)			;078c
	or c			;078d
	or l			;078e
	or c			;078f
	cp h			;0790
	or c			;0791
	push bc			;0792
	or c			;0793
	adc a,0b1h		;0794
	jp c,0e6b1h		;0796
	or c			;0799
	cpir		;079a
	call p,0fbb1h		;079c
	or c			;079f
	ld (bc),a			;07a0
	or d			;07a1
	ld a,(bc)			;07a2
	or d			;07a3
	djnz $-76		;07a4
	add hl,de			;07a6
	or d			;07a7
	ld (hl),0b2h		;07a8
	ld b,d			;07aa
	or d			;07ab
	ld c,(hl)			;07ac
	or d			;07ad
	ld d,(hl)			;07ae
	or d			;07af
	pop bc			;07b0
	or d			;07b1
	call pe,06ab3h		;07b2
	or d			;07b5
	ld a,a			;07b6
	or d			;07b7
	and (hl)			;07b8
	or d			;07b9
	xor (hl)			;07ba
	or d			;07bb
	or d			;07bc
	or d			;07bd
	or e			;07be
	cp a			;07bf
	call nc,0a1bfh		;07c0
	or h			;07c3
	ld e,h			;07c4
	cp (hl)			;07c5
	ret z			;07c6
	or d			;07c7
	ret nc			;07c8
	or d			;07c9
	ret pe			;07ca
	or d			;07cb
	ld c,(hl)			;07cc
	or e			;07cd
	xor l			;07ce
	or e			;07cf
	halt			;07d0
	or e			;07d1
	defb 0ddh,0b3h,048h	;illegal sequence		;07d2
	or a			;07d5
	rst 20h			;07d6
	or e			;07d7
	push af			;07d8
	or e			;07d9
	adc a,e			;07da
	or e			;07db
	ld sp,hl			;07dc
	or d			;07dd
	ld d,h			;07de
	or e			;07df
	cp c			;07e0
	or e			;07e1
	jr nz,$-74		;07e2
	jr nc,$-74		;07e4
	inc a			;07e6
	or h			;07e7
	ret			;07e8
	or h			;07e9
	ld d,b			;07ea
	or h			;07eb
	ld d,h			;07ec
	or h			;07ed
	ld e,a			;07ee
	or h			;07ef
	ld l,c			;07f0
	or h			;07f1
	add a,h			;07f2
	or h			;07f3
	sbc a,h			;07f4
	or h			;07f5
	xor c			;07f6
	or h			;07f7
	ld (bc),a			;07f8
	or h			;07f9
	xor a			;07fa
	or h			;07fb
	ld e,e			;07fc
	or a			;07fd
	ld h,c			;07fe
	or a			;07ff
	ld l,c			;0800
	or a			;0801
	ld (hl),d			;0802
	or a			;0803
	add hl,hl			;0804
	or l			;0805
	cp d			;0806
	or d			;0807
	ld a,c			;0808
	cp a			;0809
	adc a,c			;080a
	cp a			;080b
	inc l			;080c
	cp a			;080d
	jr nc,$-63		;080e
	scf			;0810
	cp a			;0811
	ld d,c			;0812
	cp a			;0813
	ld e,e			;0814
	or d			;0815
	ld h,b			;0816
	or d			;0817
	ld h,l			;0818
	or d			;0819
	dec e			;081a
	or l			;081b
	inc hl			;081c
	or l			;081d
	ld (hl),0b5h		;081e
	ld c,l			;0820
	or l			;0821
	ld e,e			;0822
	or l			;0823
	jr nz,$-76		;0824
	ld sp,hl			;0826
	or e			;0827
	cp (ix+025h)		;0828
	or d			;082b
	inc l			;082c
	or d			;082d
	jr c,$-64		;082e
	push af			;0830
	cp (hl)			;0831
	sbc a,l			;0832
	or l			;0833
	ld b,a			;0834
	cp (hl)			;0835
	call nc,0a7b5h		;0836
	or l			;0839
	and a			;083a
	or l			;083b
	cp h			;083c
	or h			;083d
	cpl			;083e
	or (hl)			;083f
	ld e,c			;0840
	or (hl)			;0841
	xor d			;0842
	or (hl)			;0843
	call 0abb5h		;0844
	or l			;0847
	dec a			;0848
	or (hl)			;0849
	ld b,h			;084a
	or (hl)			;084b
	or (hl)			;084c
	or l			;084d
	add a,d			;084e
	cp (hl)			;084f
	sub (hl)			;0850
	cp (hl)			;0851
	cp d			;0852
	cp (hl)			;0853
	call pe,07bb6h		;0854
	or a			;0857
	sub 0b4h		;0858
	dec bc			;085a
	or a			;085b
	ld c,(hl)			;085c
	or (hl)			;085d
	add a,a			;085e
	or (hl)			;085f
	inc de			;0860
	or a			;0861
	ld de,093bfh		;0862
	or l			;0865
	push hl			;0866
	cp (hl)			;0867
	sbc a,c			;0868
	or d			;0869
	ld e,c			;086a
	cp a			;086b
	dec a			;086c
	or d			;086d
	ld sp,026b2h		;086e
	or a			;0871
	jr nc,$-71		;0872
	ld a,(092b7h)		;0874
	or d			;0877
	xor l			;0878
	cp (hl)			;0879
	rlca			;087a
	or l			;087b
	add a,a			;087c
	or a			;087d
	jp (hl)			;087e
	or h			;087f
	add a,a			;0880
	or a			;0881
	rst 18h			;0882
	or h			;0883
	add a,a			;0884
	or a			;0885
	ld b,0b5h		;0886
	ld (hl),h			;0888
	or h			;0889
	or 0b6h		;088a
	ld h,a			;088c
	rrca			;088d
	rrca			;088e
	rrca			;088f
	and 01fh		;0890
	ld l,a			;0892
	ld a,03bh		;0893
	sub l			;0895
	ld (0b8a6h),a		;0896
	ld a,h			;0899
	rlca			;089a
	rlca			;089b
	rlca			;089c
	and 038h		;089d
	or 046h		;089f
	ld (0b8a7h),a		;08a1
	defb 0ddh,0cbh,000h,000h	;rlc (ix+000h) & ld b,(ix+000h)		;08a4
	ret			;08a8
	xor a			;08a9
	push hl			;08aa
	push de			;08ab
	push bc			;08ac
	ld bc,(00102h)		;08ad
	ld c,a			;08b1
	ld de,0d267h		;08b2
	push af			;08b5
	ld a,(de)			;08b6
	cp 0fdh		;08b7
	ld l,c			;08b9
	ld h,000h		;08ba
	call nc,0b8d0h		;08bc
	pop af			;08bf
	add a,h			;08c0
	jr nc,$+6		;08c1
	ld a,0ffh		;08c3
	jr $+6		;08c5
	inc c			;08c7
	inc de			;08c8
	djnz $-20		;08c9
	pop bc			;08cb
	pop de			;08cc
	pop hl			;08cd
	ret			;08ce
	ld l,a			;08cf
	ex de,hl			;08d0
	xor a			;08d1
	ld d,a			;08d2
	ld (0d070h),a		;08d3
	call 0b8dch		;08d6
	ex de,hl			;08d9
	ld a,l			;08da
	ret			;08db
	push hl			;08dc
	push de			;08dd
	push bc			;08de
	call 0b936h		;08df
	and 07fh		;08e2
	add a,0c0h		;08e4
	res 6,a		;08e6
	res 7,a		;08e8
	jr nc,$+45		;08ea
	and 03fh		;08ec
	jr z,$+40		;08ee
	ld c,a			;08f0
	ld a,(0d070h)		;08f1
	inc a			;08f4
	ld (0d070h),a		;08f5
	cp 00ah		;08f8
	jr z,$+23		;08fa
	ld a,c			;08fc
	ld bc,(00102h)		;08fd
	ld c,e			;0901
	ld e,000h		;0902
	ld d,a			;0904
	ld hl,0d267h		;0905
	ld a,(hl)			;0908
	cp c			;0909
	call z,0b8dch		;090a
	inc hl			;090d
	inc e			;090e
	djnz $-7		;090f
	ld hl,0d070h		;0911
	dec (hl)			;0914
	ld a,d			;0915
	scf			;0916
	pop bc			;0917
	pop de			;0918
	pop hl			;0919
	push af			;091a
	add a,d			;091b
	ld d,a			;091c
	jr nc,$+4		;091d
	ld d,0ffh		;091f
	pop af			;0921
	ret			;0922
	cp 0ffh		;0923
	ret nz			;0925
	ld a,(0d18dh)		;0926
	ret			;0929
	push de			;092a
	ld e,(ix+033h)		;092b
	call 0b936h		;092e
	pop de			;0931
	ld l,028h		;0932
	rlca			;0934
	ret			;0935
	ld d,000h		;0936
	ld hl,(0011ch)		;0938
	add hl,de			;093b
	ld a,(hl)			;093c
	ret			;093d
	ld e,a			;093e
	ld d,000h		;093f
	ld hl,0d267h		;0941
	add hl,de			;0944
	ld a,(hl)			;0945
	ret			;0946
	push bc			;0947
	ld b,0fdh		;0948
	call 0b951h		;094a
	pop bc			;094d
	ld l,01ch		;094e
	ret			;0950
	ld a,0feh		;0951
	ld c,(ix+026h)		;0953
	ld (0d19ah),a		;0956
	ld a,(0d189h)		;0959
	inc a			;095c
	scf			;095d
	jr z,$+30		;095e
	push bc			;0960
	ld b,(ix+033h)		;0961
	call 0b988h		;0964
	jr c,$+25		;0967
	pop bc			;0969
	call 0b988h		;096a
	jr c,$+20		;096d
	ld b,c			;096f
	call 0b988h		;0970
	jr c,$+14		;0973
	ld b,0ffh		;0975
	call 0b988h		;0977
	jr c,$+8		;097a
	ld a,0ffh		;097c
	jr $+37		;097e
	pop bc			;0980
	or a			;0981
	dec hl			;0982
	ex de,hl			;0983
	dec hl			;0984
	ld a,l			;0985
	jr $+29		;0986
	ld e,000h		;0988
	call 0b9f9h		;098a
	ret nc			;098d
	call 0b996h		;098e
	pop hl			;0991
	jr nz,$-8		;0992
	scf			;0994
	ret			;0995
	cp (ix+022h)		;0996
	ret nz			;0999
	ld a,(0d18ah)		;099a
	cp 0ffh		;099d
	ret z			;099f
	inc hl			;09a0
	cp (hl)			;09a1
	ret			;09a2
	push af			;09a3
	push hl			;09a4
	push de			;09a5
	ld hl,00000h		;09a6
	ld (0d19dh),hl		;09a9
	ld (0d19fh),hl		;09ac
	ld (0d1a1h),hl		;09af
	ld e,a			;09b2
	ld (0d19ah),a		;09b3
	cp 0ffh		;09b6
	jr z,$+51		;09b8
	ld hl,00103h		;09ba
	cp (hl)			;09bd
	jr nc,$+49		;09be
	call 0b936h		;09c0
	rla			;09c3
	rr (ix+039h)		;09c4
	rla			;09c8
	rr (ix+038h)		;09c9
	call 0b941h		;09cd
	ld (0d19dh),a		;09d0
	ex de,hl			;09d3
	call 0b8d0h		;09d4
	ld (ix+037h),h		;09d7
	ld h,000h		;09da
	ex de,hl			;09dc
	ld hl,(0011eh)		;09dd
	add hl,de			;09e0
	add hl,de			;09e1
	ld a,(hl)			;09e2
	ld (0d1a2h),a		;09e3
	inc hl			;09e6
	ld a,(hl)			;09e7
	ld (0d1a1h),a		;09e8
	pop de			;09eb
	pop hl			;09ec
	pop af			;09ed
	ret			;09ee
	pop de			;09ef
	pop hl			;09f0
	pop af			;09f1
	pop af			;09f2
	ld d,000h		;09f3
	jp 0b02dh		;09f5
	ld b,a			;09f8
	ld hl,0d267h		;09f9
	ld d,000h		;09fc
	add hl,de			;09fe
	ld a,(00103h)		;09ff
	cp e			;0a02
	ret z			;0a03
	ld a,b			;0a04
	inc de			;0a05
	cp 0ffh		;0a06
	jr z,$+3		;0a08
	cp (hl)			;0a0a
	inc hl			;0a0b
	jr nz,$-13		;0a0c
	ex (sp),hl			;0a0e
	push hl			;0a0f
	push de			;0a10
	dec e			;0a11
	ld hl,(0011ah)		;0a12
	add hl,de			;0a15
	add hl,de			;0a16
	ld a,(hl)			;0a17
	cp 0ffh		;0a18
	pop de			;0a1a
	ret c			;0a1b
	pop hl			;0a1c
	ex (sp),hl			;0a1d
	jr $-31		;0a1e
	push bc			;0a20
	ld bc,00000h		;0a21
	push bc			;0a24
	res 7,(ix+035h)		;0a25
	res 4,(ix-003h)		;0a29
	set 5,(ix-003h)		;0a2d
	ld hl,0d267h		;0a31
	add hl,bc			;0a34
	ld a,(hl)			;0a35
	cp 0ffh		;0a36
	jr z,$+76		;0a38
	cp d			;0a3a
	jr nz,$+45		;0a3b
	ld h,c			;0a3d
	ld l,000h		;0a3e
	ex (sp),hl			;0a40
	push hl			;0a41
	pop af			;0a42
	bit 7,(ix+035h)		;0a43
	jr nz,$+30		;0a47
	set 7,(ix+035h)		;0a49
	bit 1,(ix-001h)		;0a4d
	jr z,$+14		;0a51
	ld l,001h		;0a53
	call 0bde8h		;0a55
	bit 6,(ix+035h)		;0a58
	call z,0c998h		;0a5c
	ex (sp),hl			;0a5f
	ld l,001h		;0a60
	ex (sp),hl			;0a62
	jr $+5		;0a63
	call 0ba6bh		;0a65
	inc bc			;0a68
	jr $-56		;0a69
	bit 6,(ix+035h)		;0a6b
	jr nz,$+9		;0a6f
	ld l,a			;0a71
	call 0bd67h		;0a72
	jp 0bdedh		;0a75
	jr c,$+9		;0a78
	push af			;0a7a
	ld l,02eh		;0a7b
	call 0bde8h		;0a7d
	pop af			;0a80
	jp 0bcfdh		;0a81
	pop af			;0a84
	pop bc			;0a85
	bit 7,(ix+035h)		;0a86
	jr z,$+26		;0a8a
	bit 6,(ix+035h)		;0a8c
	jr z,$-37		;0a90
	jr c,$+10		;0a92
	push af			;0a94
	ld l,02fh		;0a95
	call 0bde8h		;0a97
	pop af			;0a9a
	scf			;0a9b
	call 0ba6bh		;0a9c
	ld l,030h		;0a9f
	jp 0bde8h		;0aa1
	bit 1,(ix-001h)		;0aa4
	ret nz			;0aa8
	ld l,035h		;0aa9
	jp 0bde8h		;0aab
	ld hl,(00114h)		;0aae
	ld c,a			;0ab1
	ld b,000h		;0ab2
	add hl,bc			;0ab4
	add hl,bc			;0ab5
	ld e,(hl)			;0ab6
	inc hl			;0ab7
	ld d,(hl)			;0ab8
	ex de,hl			;0ab9
	ld a,(hl)			;0aba
	cp 0ffh		;0abb
	ret z			;0abd
	cp (ix+021h)		;0abe
	inc hl			;0ac1
	ld a,(hl)			;0ac2
	scf			;0ac3
	ret z			;0ac4
	inc hl			;0ac5
	jr $-12		;0ac6
	ld hl,(0d0a0h)		;0ac8
	ld a,l			;0acb
	or h			;0acc
	jr z,$+101		;0acd
	ld d,001h		;0acf
	xor a			;0ad1
	jr $+32		;0ad2
	bit 2,(ix-001h)		;0ad4
	jr z,$+8		;0ad8
	ld hl,(0d09eh)		;0ada
	ld a,(hl)			;0add
	cp 00dh		;0ade
	push af			;0ae0
	scf			;0ae1
	call z,0c025h		;0ae2
	jr nc,$+41		;0ae5
	ld (0d09eh),hl		;0ae7
	pop af			;0aea
	ld de,00000h		;0aeb
	ld (0d0a0h),de		;0aee
	ld (ix-002h),d		;0af2
	ld de,0ffffh		;0af5
	ld a,e			;0af8
	jr nz,$+8		;0af9
	ld (0d188h),a		;0afb
	ld (0d18bh),a		;0afe
	ld (0d189h),de		;0b01
	ld (0d192h),de		;0b05
	ld (0d194h),a		;0b09
	jr $+10		;0b0c
	pop af			;0b0e
	set 7,(ix+031h)		;0b0f
	jr $+31		;0b13
	inc hl			;0b15
	call 0bcdbh		;0b16
	cp 022h		;0b19
	jr z,$+23		;0b1b
	call 0bce3h		;0b1d
	jr c,$+25		;0b20
	cp 00dh		;0b22
	jr nz,$-15		;0b24
	bit 2,(ix-001h)		;0b26
	jr z,$+8		;0b2a
	res 2,(ix-001h)		;0b2c
	jr $-92		;0b30
	res 2,(ix-001h)		;0b32
	scf			;0b36
	ret			;0b37
	pop hl			;0b38
	call 0bc88h		;0b39
	or a			;0b3c
	jp nz,0bbbfh		;0b3d
	call 0bce2h		;0b40
	jr c,$-10		;0b43
	cp 022h		;0b45
	jr nz,$+35		;0b47
	bit 0,(ix-002h)		;0b49
	jr nz,$+29		;0b4d
	ld de,(0d0a0h)		;0b4f
	ld a,d			;0b53
	or e			;0b54
	jr nz,$-35		;0b55
	inc hl			;0b57
	ld (0d0a0h),hl		;0b58
	ld a,(hl)			;0b5b
	cp 00dh		;0b5c
	jr z,$-44		;0b5e
	inc hl			;0b60
	cp 022h		;0b61
	jr nz,$-8		;0b63
	call 0bcdbh		;0b65
	jr $-40		;0b68
	set 2,(ix-001h)		;0b6a
	bit 0,(ix-002h)		;0b6e
	jr nz,$+7		;0b72
	ld (0d09eh),hl		;0b74
	jr $+5		;0b77
	ld (0d0a0h),hl		;0b79
	ld a,(hl)			;0b7c
	cp 00dh		;0b7d
	jr nz,$+6		;0b7f
	res 2,(ix-001h)		;0b81
	bit 1,(ix-002h)		;0b85
	jr nz,$+17		;0b89
	ld a,(0d189h)		;0b8b
	cp 0ffh		;0b8e
	jr z,$-94		;0b90
	cp 028h		;0b92
	ret nc			;0b94
	ld (0d188h),a		;0b95
	or a			;0b98
	ret			;0b99
	bit 5,(ix-002h)		;0b9a
	jr nz,$+7		;0b9e
	ld a,0ffh		;0ba0
	ld (0d18bh),a		;0ba2
	ld de,(0d193h)		;0ba5
	call 0bbb0h		;0ba9
	ld de,(0d189h)		;0bac
	ld a,e			;0bb0
	cp 0ffh		;0bb1
	jr z,$+10		;0bb3
	cp 032h		;0bb5
	jr c,$+6		;0bb7
	ld (0d195h),de		;0bb9
	xor a			;0bbd
	ret			;0bbe
	ld e,a			;0bbf
	ld a,(bc)			;0bc0
	cp 005h		;0bc1
	jr z,$-89		;0bc3
	or a			;0bc5
	jr nz,$+48		;0bc6
	bit 1,(ix-002h)		;0bc8
	jp nz,0bb39h		;0bcc
	ld (ix+021h),e		;0bcf
	set 1,(ix-002h)		;0bd2
	push hl			;0bd6
	ld hl,(0d0a9h)		;0bd7
	dec hl			;0bda
	ld a,(hl)			;0bdb
	cp 053h		;0bdc
	jr nz,$+4		;0bde
	dec hl			;0be0
	ld a,(hl)			;0be1
	cp 041h		;0be2
	jr z,$+6		;0be4
	cp 04fh		;0be6
	jr nz,$+8		;0be8
	dec hl			;0bea
	ld a,(hl)			;0beb
	cp 04ch		;0bec
	ld a,006h		;0bee
	pop hl			;0bf0
	jr z,$+44		;0bf1
	jp 0bb39h		;0bf3
	cp 001h		;0bf6
	jr nz,$+19		;0bf8
	bit 5,(ix-002h)		;0bfa
	jp nz,0bb39h		;0bfe
	ld (ix+024h),e		;0c01
	set 5,(ix-002h)		;0c04
	jp 0bb39h		;0c08
	cp 004h		;0c0b
	jr nz,$+16		;0c0d
	ld a,(0d192h)		;0c0f
	cp 0ffh		;0c12
	jp nz,0bb39h		;0c14
	ld (ix+02bh),e		;0c17
	jp 0bb39h		;0c1a
	push hl			;0c1d
	ld hl,0d165h		;0c1e
	bit 2,(hl)		;0c21
	jr nz,$+35		;0c23
	cp 003h		;0c25
	jr z,$+28		;0c27
	cp 002h		;0c29
	jr z,$+18		;0c2b
	ld a,(0d195h)		;0c2d
	cp 0ffh		;0c30
	jr z,$+17		;0c32
	ld e,a			;0c34
	ld a,(0d196h)		;0c35
	ld (0d18ah),a		;0c38
	set 6,(hl)		;0c3b
	ld a,e			;0c3d
	ld (0d189h),a		;0c3e
	set 2,(hl)		;0c41
	jp 0bb38h		;0c43
	bit 6,(hl)		;0c46
	jr nz,$+13		;0c48
	set 6,(hl)		;0c4a
	cp 003h		;0c4c
	jr nz,$+7		;0c4e
	ld (ix+023h),e		;0c50
	jr $-16		;0c53
	bit 3,(hl)		;0c55
	jr nz,$+34		;0c57
	cp 003h		;0c59
	jr z,$-24		;0c5b
	cp 002h		;0c5d
	jr z,$+18		;0c5f
	ld a,(0d195h)		;0c61
	cp 0ffh		;0c64
	jr z,$-35		;0c66
	ld e,a			;0c68
	ld a,(0d196h)		;0c69
	ld (0d194h),a		;0c6c
	set 7,(hl)		;0c6f
	ld a,e			;0c71
	ld (0d193h),a		;0c72
	set 3,(hl)		;0c75
	jr $-52		;0c77
	bit 7,(hl)		;0c79
	jr nz,$-56		;0c7b
	set 7,(hl)		;0c7d
	cp 003h		;0c7f
	jr nz,$-62		;0c81
	ld (ix+02dh),e		;0c83
	jr $-67		;0c86
	ld a,0dfh		;0c88
	ld de,0d069h		;0c8a
	ld b,005h		;0c8d
	dec de			;0c8f
	ld (de),a			;0c90
	djnz $-2		;0c91
	ld b,005h		;0c93
	call 0bce2h		;0c95
	jr nc,$+15		;0c98
	cpl			;0c9a
	ld (de),a			;0c9b
	inc de			;0c9c
	inc hl			;0c9d
	djnz $-9		;0c9e
	call 0bce2h		;0ca0
	inc hl			;0ca3
	jr c,$-4		;0ca4
	dec hl			;0ca6
	ld (0d0a9h),hl		;0ca7
	call 0bcdbh		;0caa
	ld bc,(00116h)		;0cad
	push hl			;0cb1
	ld hl,0d064h		;0cb2
	res 5,(ix-001h)		;0cb5
	ld d,005h		;0cb9
	ld a,(bc)			;0cbb
	or a			;0cbc
	jr nz,$+4		;0cbd
	pop hl			;0cbf
	ret			;0cc0
	cp (hl)			;0cc1
	jr z,$+6		;0cc2
	set 5,(ix-001h)		;0cc4
	inc bc			;0cc8
	inc hl			;0cc9
	dec d			;0cca
	jr nz,$-16		;0ccb
	bit 5,(ix-001h)		;0ccd
	jr nz,$+6		;0cd1
	ld a,(bc)			;0cd3
	inc bc			;0cd4
	pop hl			;0cd5
	ret			;0cd6
	inc bc			;0cd7
	inc bc			;0cd8
	jr $-39		;0cd9
	ld a,(hl)			;0cdb
	cp 020h		;0cdc
	ret nz			;0cde
	inc hl			;0cdf
	jr $-5		;0ce0
	ld a,(hl)			;0ce2
	call 0bcf6h		;0ce3
	ccf			;0ce6
	ret c			;0ce7
	cp 015h		;0ce8
	ccf			;0cea
	ret nc			;0ceb
	cp 020h		;0cec
	ret c			;0cee
	cp 041h		;0cef
	ccf			;0cf1
	ret nc			;0cf2
	cp 05bh		;0cf3
	ret			;0cf5
	cp 030h		;0cf6
	ret c			;0cf8
	cp 03ah		;0cf9
	ccf			;0cfb
	ret			;0cfc
	push hl			;0cfd
	ld hl,00103h		;0cfe
	cp (hl)			;0d01
	jr nc,$+90		;0d02
	call 0bd66h		;0d04
	call 0bd4eh		;0d07
	jr z,$-3		;0d0a
	bit 4,(ix-003h)		;0d0c
	jr nz,$+6		;0d10
	and 0dfh		;0d12
	jr $+49		;0d14
	cp 0aah		;0d16
	jr z,$+6		;0d18
	cp 08ah		;0d1a
	jr nz,$+41		;0d1c
	call 0bd4eh		;0d1e
	call 0bd4eh		;0d21
	jr nz,$+26		;0d24
	push af			;0d26
	ld a,065h		;0d27
	call 0bd33h		;0d29
	ld a,06ch		;0d2c
	call 0c99ah		;0d2e
	jr $+19		;0d31
	bit 5,(ix-003h)		;0d33
	jr nz,$+4		;0d37
	and 0dfh		;0d39
	jp 0c99ah		;0d3b
	push af			;0d3e
	ld a,06ch		;0d3f
	call 0bd33h		;0d41
	pop af			;0d44
	cpl			;0d45
	call 0c99ah		;0d46
	call 0bd4eh		;0d49
	jr $-7		;0d4c
	ld a,(hl)			;0d4e
	cp 0f5h		;0d4f
	jr z,$+10		;0d51
	cp 0d1h		;0d53
	jr z,$+6		;0d55
	inc hl			;0d57
	cp 0dfh		;0d58
	ret			;0d5a
	pop hl			;0d5b
	pop hl			;0d5c
	ret			;0d5d
	ld l,a			;0d5e
	push de			;0d5f
	ld de,(0010ah)		;0d60
	jr $+15		;0d64
	ld l,a			;0d66
	push de			;0d67
	ld de,(0010ch)		;0d68
	jr $+7		;0d6c
	push de			;0d6e
	ld de,(00112h)		;0d6f
	ld h,000h		;0d73
	add hl,hl			;0d75
	add hl,de			;0d76
	ld e,(hl)			;0d77
	inc hl			;0d78
	ld d,(hl)			;0d79
	ex de,hl			;0d7a
	pop de			;0d7b
	ret			;0d7c
	push af			;0d7d
	push bc			;0d7e
	ld bc,00865h		;0d7f
	ld hl,00000h		;0d82
	ld de,(0d14bh)		;0d85
	inc e			;0d89
	ld d,h			;0d8a
	srl c		;0d8b
	jr nc,$+3		;0d8d
	add hl,de			;0d8f
	sla e		;0d90
	rl d		;0d92
	djnz $-9		;0d94
	ld (0d14bh),hl		;0d96
	ld a,h			;0d99
	or a			;0d9a
	jr z,$-28		;0d9b
	pop bc			;0d9d
	pop af			;0d9e
	ret			;0d9f
	ld d,000h		;0da0
	push hl			;0da2
	push de			;0da3
	res 1,(ix-003h)		;0da4
	ex de,hl			;0da8
	ld de,02710h		;0da9
	call 0bdcah		;0dac
	ld de,003e8h		;0daf
	call 0bdcah		;0db2
	ld de,00064h		;0db5
	call 0bdcah		;0db8
	ld de,0000ah		;0dbb
	call 0bdcah		;0dbe
	ld a,l			;0dc1
	add a,030h		;0dc2
	call 0c99ah		;0dc4
	pop de			;0dc7
	pop hl			;0dc8
	ret			;0dc9
	xor a			;0dca
	inc a			;0dcb
	or a			;0dcc
	sbc hl,de		;0dcd
	jr nc,$-4		;0dcf
	add hl,de			;0dd1
	dec a			;0dd2
	jr nz,$+7		;0dd3
	bit 1,(ix-003h)		;0dd5
	ret z			;0dd9
	set 1,(ix-003h)		;0dda
	add a,030h		;0dde
	jp 0c99ah		;0de0
	call 0bd6eh		;0de3
	jr $+7		;0de6
	call 0bd6eh		;0de8
	jr $+6		;0deb
	set 6,(ix-001h)		;0ded
	ld a,(hl)			;0df1
	cpl			;0df2
	cp 00ah		;0df3
	jr z,$+37		;0df5
	set 5,(ix-003h)		;0df7
	cp (ix-006h)		;0dfb
	jr z,$+10		;0dfe
	cp 040h		;0e00
	jr nz,$+18		;0e02
	res 5,(ix-003h)		;0e04
	set 4,(ix-003h)		;0e08
	ld a,(0d19ah)		;0e0c
	call 0bcfdh		;0e0f
	jr $+5		;0e12
	call 0c99ah		;0e14
	inc hl			;0e17
	jr $-39		;0e18
	bit 6,(ix-001h)		;0e1a
	ret z			;0e1e
	res 6,(ix-001h)		;0e1f
	jp 0c998h		;0e23
	ex (sp),hl			;0e26
	ld (0be35h),de		;0e27
	ld e,(hl)			;0e2b
	inc hl			;0e2c
	ld d,(hl)			;0e2d
	inc hl			;0e2e
	ex de,hl			;0e2f
	add hl,de			;0e30
	ex de,hl			;0e31
	ex (sp),hl			;0e32
	push de			;0e33
	ld de,00000h		;0e34
	ret			;0e37
	and 007h		;0e38
	ld e,a			;0e3a
	ld a,(0d15dh)		;0e3b
	and 0f8h		;0e3e
	or e			;0e40
	ld (0d15dh),a		;0e41
	jp 0b456h		;0e44
	ld hl,0affdh		;0e47
	cp (hl)			;0e4a
	jr nc,$+14		;0e4b
	ld (0d06fh),a		;0e4d
	ld e,a			;0e50
	call 0c347h		;0e51
	bit 7,(hl)		;0e54
	jp nz,0b214h		;0e56
	jp 0b1c1h		;0e59
	res 7,(ix-004h)		;0e5c
	or a			;0e60
	jr z,$+6		;0e61
	set 7,(ix-004h)		;0e63
	push bc			;0e67
	call 0cae7h		;0e68
	ld a,(0d15dh)		;0e6b
	push af			;0e6e
	res 6,(ix-00ah)		;0e6f
	ld a,(0d06fh)		;0e73
	ld e,a			;0e76
	call 0c2b8h		;0e77
	pop af			;0e7a
	ld (0d15dh),a		;0e7b
	pop bc			;0e7e
	jp 0b456h		;0e7f
	push bc			;0e82
	call 0cae7h		;0e83
	res 5,(ix-00ah)		;0e86
	res 7,(ix-00ah)		;0e8a
	ld hl,(0d15bh)		;0e8e
	ld (0d150h),hl		;0e91
	jr $+21		;0e94
	push bc			;0e96
	call 0cae7h		;0e97
	res 5,(ix-00ah)		;0e9a
	res 7,(ix-00ah)		;0e9e
	ld de,(0d150h)		;0ea2
	call 0c941h		;0ea6
	pop bc			;0ea9
	jp 0b457h		;0eaa
	push bc			;0ead
	push af			;0eae
	call 0cae7h		;0eaf
	ld a,(0d15ch)		;0eb2
	ld d,a			;0eb5
	pop af			;0eb6
	ld e,a			;0eb7
	jr $+17		;0eb8
	ld d,a			;0eba
	inc bc			;0ebb
	ld a,(bc)			;0ebc
	ld e,a			;0ebd
	push bc			;0ebe
	push de			;0ebf
	call 0cae7h		;0ec0
	pop de			;0ec3
	ld a,(0d159h)		;0ec4
	add a,d			;0ec7
	ld d,a			;0ec8
	ld a,(0d15ah)		;0ec9
	add a,e			;0ecc
	ld e,a			;0ecd
	call 0c941h		;0ece
	res 5,(ix-00ah)		;0ed1
	res 7,(ix-00ah)		;0ed5
	pop bc			;0ed9
	jp 0b456h		;0eda
	push bc			;0edd
	call 0c8d8h		;0ede
	pop bc			;0ee1
	jp 0b456h		;0ee2
	dec bc			;0ee5
	push bc			;0ee6
	call 0cae7h		;0ee7
	ld a,(0d157h)		;0eea
	srl a		;0eed
	ld h,a			;0eef
	ld a,015h		;0ef0
	sub h			;0ef2
	jr $+25		;0ef3
	push af			;0ef5
	push bc			;0ef6
	call 0cae7h		;0ef7
	pop bc			;0efa
	pop af			;0efb
	cp 018h		;0efc
	jr c,$+3		;0efe
	xor a			;0f00
	ld (0d159h),a		;0f01
	inc bc			;0f04
	push bc			;0f05
	ld a,(bc)			;0f06
	cp 02ah		;0f07
	jr c,$+3		;0f09
	xor a			;0f0b
	ld (0d15ah),a		;0f0c
	jr $+19		;0f0f
	push af			;0f11
	push bc			;0f12
	call 0cae7h		;0f13
	pop bc			;0f16
	pop af			;0f17
	dec a			;0f18
	ld (0d158h),a		;0f19
	inc bc			;0f1c
	push bc			;0f1d
	ld a,(bc)			;0f1e
	ld (0d157h),a		;0f1f
	call 0c974h		;0f22
	call 0c928h		;0f25
	pop bc			;0f28
	jp 0b456h		;0f29
	inc bc			;0f2c
	jp 0b456h		;0f2d
	call 0c019h		;0f30
	ld e,0f0h		;0f33
	jr $+13		;0f35
	call 0c019h		;0f37
	rlca			;0f3a
	rlca			;0f3b
	rlca			;0f3c
	rlca			;0f3d
	and 0f0h		;0f3e
	ld e,00fh		;0f40
	ld d,a			;0f42
	ld a,(0d152h)		;0f43
	and e			;0f46
	or d			;0f47
	ld (0d152h),a		;0f48
	ld (0d050h),a		;0f4b
	jp 0b456h		;0f4e
	and 00fh		;0f51
	call 0ce11h		;0f53
	jp 0b456h		;0f56
	call 0cf84h		;0f59
	call 0009ch		;0f5c
	jr z,$+20		;0f5f
	call 0009fh		;0f61
	ld (0d1a3h),a		;0f64
	call 0cf8fh		;0f67
	ld a,(0d158h)		;0f6a
	call 0c8bbh		;0f6d
	jp 0b215h		;0f70
	call 0cf8fh		;0f73
	jp 0b1c1h		;0f76
	ld de,0d367h		;0f79
	set 0,(ix-003h)		;0f7c
	push bc			;0f80
	ld hl,0d167h		;0f81
	ld bc,00200h		;0f84
	jr $+38		;0f87
	bit 0,(ix-003h)		;0f89
	jr nz,$+9		;0f8d
	res 2,(ix-001h)		;0f8f
	jp 0b1c1h		;0f93
	inc bc			;0f96
	push bc			;0f97
	ld hl,0d367h		;0f98
	ld de,0d167h		;0f9b
	ld c,a			;0f9e
	ld b,000h		;0f9f
	inc bc			;0fa1
	ldir		;0fa2
	ld hl,0d467h		;0fa4
	ld de,0d267h		;0fa7
	ld bc,00100h		;0faa
	ldir		;0fad
	pop bc			;0faf
	jp 0b457h		;0fb0
	push bc			;0fb3
	ld hl,(0d06ch)		;0fb4
	push hl			;0fb7
	ld hl,0bfcch		;0fb8
	ld (0d06ch),hl		;0fbb
	call 0bfe3h		;0fbe
	ld l,03dh		;0fc1
	call 0bde8h		;0fc3
	call 0c880h		;0fc6
	call 0ce1dh		;0fc9
	pop hl			;0fcc
	ld (0d06ch),hl		;0fcd
	pop bc			;0fd0
	jp 0b456h		;0fd1
	push bc			;0fd4
	call 0bfe3h		;0fd5
	call 0ce7fh		;0fd8
	res 2,(ix-001h)		;0fdb
	pop bc			;0fdf
	jp 0b456h		;0fe0
	ld l,03ch		;0fe3
	call 0c04fh		;0fe5
	ld de,0c011h		;0fe8
	ld bc,00008h		;0feb
	ld a,(hl)			;0fee
	cp 00dh		;0fef
	jr z,$+13		;0ff1
	cp 020h		;0ff3
	jr z,$+9		;0ff5
	ldi		;0ff7
	jp pe,0bfeeh		;0ff9
	jr $+9		;0ffc
	ex de,hl			;0ffe
	ld (hl),020h		;0fff
	inc hl			;1001
	dec c			;1002
	jr nz,$-4		;1003
	ld iy,0c00ah		;1005
	ret			;1009
	ld d,h			;100a
	ld c,d			;100b
	ld b,a			;100c
	nop			;100d
	ld (bc),a			;100e
	ld h,a			;100f
	pop de			;1010
	ld b,h			;1011
	ld b,c			;1012
	ld b,c			;1013
	ld b,h			;1014
	ld d,(hl)			;1015
	ld sp,0302eh		;1016
	and 00fh		;1019
	ld e,a			;101b
	ld d,000h		;101c
	ld hl,(0aff9h)		;101e
	inc hl			;1021
	add hl,de			;1022
	ld a,(hl)			;1023
	ret			;1024
	res 4,(ix-002h)		;1025
	ld a,(0d191h)		;1029
	or a			;102c
	jr z,$+11		;102d
	ld l,a			;102f
	ld a,(00106h)		;1030
	cp l			;1033
	jr z,$+4		;1034
	jr nc,$+37		;1036
	call 0bd7dh		;1038
	ld a,h			;103b
	ld l,002h		;103c
	cp 01eh		;103e
	jr c,$+27		;1040
	inc l			;1042
	cp 03ch		;1043
	jr c,$+22		;1045
	inc l			;1047
	cp 05ah		;1048
	jr c,$+17		;104a
	inc l			;104c
	jr $+14		;104d
	res 2,(ix-001h)		;104f
	set 4,(ix-002h)		;1053
	ld (ix-007h),00ch		;1057
	push hl			;105b
	ld a,(0d14dh)		;105c
	ld (0d09dh),a		;105f
	ld a,(0d190h)		;1062
	and 007h		;1065
	call nz,0c8d8h		;1067
	pop hl			;106a
	call 0bde8h		;106b
	call 0cae7h		;106e
	res 6,(ix-00ah)		;1071
	res 3,(ix-003h)		;1075
	ld a,080h		;1079
	ld (0d155h),a		;107b
	ld l,021h		;107e
	call 0bde8h		;1080
	call 0c290h		;1083
	call 0ccb5h		;1086
	ld a,(0d15bh)		;1089
	ld (0d09ch),a		;108c
	bit 4,(ix-002h)		;108f
	jr nz,$+25		;1093
	ld a,(0d198h)		;1095
	rlca			;1098
	jr nc,$+19		;1099
	rlca			;109b
	jr nc,$+16		;109c
	rlca			;109e
	jr nc,$+13		;109f
	ld hl,0cfcfh		;10a1
	call 0c24ch		;10a4
	call 0c25bh		;10a7
	jr $+23		;10aa
	xor a			;10ac
	ld (0d04ah),a		;10ad
	ld hl,0cfcfh		;10b0
	ld a,00dh		;10b3
	ld (hl),a			;10b5
	ld (0d048h),hl		;10b6
	jr $+8		;10b9
	bit 0,(ix+031h)		;10bb
	jr nz,$+36		;10bf
	bit 4,(ix-002h)		;10c1
	jr nz,$+30		;10c5
	ld a,(0d197h)		;10c7
	or a			;10ca
	jr z,$+24		;10cb
	call 0c88ch		;10cd
	jr c,$+22		;10d0
	ld hl,(0d048h)		;10d2
	call 0c24ch		;10d5
	call 0c271h		;10d8
	ld a,(0d198h)		;10db
	or 0c0h		;10de
	jp 0c183h		;10e0
	call 0c880h		;10e3
	cp 008h		;10e6
	jp z,0c20dh		;10e8
	cp 01dh		;10eb
	jp z,0c1e3h		;10ed
	cp 01ch		;10f0
	jp z,0c1fch		;10f2
	cp 00dh		;10f5
	jr nz,$+11		;10f7
	ld a,(0d04ah)		;10f9
	or a			;10fc
	jp nz,0c171h		;10fd
	jr $-69		;1100
	ld e,a			;1102
	ld hl,0c111h		;1103
	ld a,(hl)			;1106
	inc hl			;1107
	or a			;1108
	jr z,$+29		;1109
	cp e			;110b
	jr z,$+25		;110c
	inc hl			;110e
	jr $-9		;110f
	and h			;1111
	ld a,(de)			;1112
	and l			;1113
	dec de			;1114
	and b			;1115
	dec d			;1116
	add a,d			;1117
	ld d,0a1h		;1118
	rla			;111a
	and d			;111b
	jr $-91		;111c
	add hl,de			;111e
	or a			;111f
	ld e,0b6h		;1120
	rra			;1122
	nop			;1123
	nop			;1124
	ld e,(hl)			;1125
	ld a,(0d04ah)		;1126
	cp (ix-007h)		;1129
	jp z,0c0bbh		;112c
	ld a,e			;112f
	cp 011h		;1130
	jp c,0c0bbh		;1132
	cp 05bh		;1135
	jr c,$+12		;1137
	cp 061h		;1139
	jp c,0c0bbh		;113b
	cp 07bh		;113e
	jp nc,0c0bbh		;1140
	ld hl,(0d048h)		;1143
	push hl			;1146
	ld a,00dh		;1147
	ld b,0ffh		;1149
	cpir		;114b
	pop bc			;114d
	ld a,e			;114e
	push hl			;114f
	pop de			;1150
	or a			;1151
	sbc hl,bc		;1152
	push hl			;1154
	pop bc			;1155
	push de			;1156
	pop hl			;1157
	dec hl			;1158
	lddr		;1159
	ld hl,0d04ah		;115b
	inc (hl)			;115e
	ld hl,(0d048h)		;115f
	ld (hl),a			;1162
	inc hl			;1163
	ld (0d048h),hl		;1164
	dec hl			;1167
	call 0c24ch		;1168
	call 0c25bh		;116b
	jp 0c0bbh		;116e
	ld hl,(0d048h)		;1171
	call 0c24ch		;1174
	call 0c271h		;1177
	call 0c998h		;117a
	ld a,(0d198h)		;117d
	and 03fh		;1180
	scf			;1182
	ld (0d198h),a		;1183
	push af			;1186
	call 0ccc5h		;1187
	bit 3,(ix+031h)		;118a
	call nz,0c918h		;118e
	set 6,(ix-00ah)		;1191
	ld a,(0d09dh)		;1195
	call 0c8d8h		;1198
	pop af			;119b
	push af			;119c
	jr nc,$+61		;119d
	bit 4,(ix+031h)		;119f
	ld l,021h		;11a3
	call nz,0bde8h		;11a5
	ld hl,0cfcfh		;11a8
	ld a,(hl)			;11ab
	cp 00dh		;11ac
	jr z,$+37		;11ae
	push af			;11b0
	bit 4,(ix+031h)		;11b1
	call nz,0c99ah		;11b5
	pop af			;11b8
	cp 01ah		;11b9
	jr c,$+10		;11bb
	cp 020h		;11bd
	jr nc,$+6		;11bf
	or 001h		;11c1
	jr $+12		;11c3
	cp 061h		;11c5
	jr c,$+9		;11c7
	cp 07bh		;11c9
	jr nc,$+5		;11cb
	and 0dfh		;11cd
	ld (hl),a			;11cf
	inc hl			;11d0
	jr $-38		;11d1
	bit 4,(ix+031h)		;11d3
	call nz,0c998h		;11d7
	pop af			;11da
	ld (ix-007h),078h		;11db
	ld hl,0cfcfh		;11df
	ret			;11e2
	ld de,(0d048h)		;11e3
	ld hl,0cfcfh		;11e7
	or a			;11ea
	sbc hl,de		;11eb
	jr z,$+12		;11ed
	dec de			;11ef
	ld (0d048h),de		;11f0
	ld a,008h		;11f4
	call 0cb35h		;11f6
	jp 0c0bbh		;11f9
	ld hl,(0d048h)		;11fc
	ld a,(hl)			;11ff
	cp 00dh		;1200
	jr z,$-9		;1202
	inc hl			;1204
	ld (0d048h),hl		;1205
	call 0cb35h		;1208
	jr $-18		;120b
	ld de,(0d048h)		;120d
	ld hl,0cfcfh		;1211
	or a			;1214
	sbc hl,de		;1215
	jr z,$-30		;1217
	push de			;1219
	pop hl			;121a
	ld a,00dh		;121b
	ld b,0ffh		;121d
	cpir		;121f
	or a			;1221
	sbc hl,de		;1222
	push hl			;1224
	pop bc			;1225
	push de			;1226
	pop hl			;1227
	dec de			;1228
	ldir		;1229
	ld a,008h		;122b
	call 0cb35h		;122d
	ld hl,0d04ah		;1230
	dec (hl)			;1233
	ld hl,(0d048h)		;1234
	dec hl			;1237
	ld (0d048h),hl		;1238
	call 0c24ch		;123b
	push hl			;123e
	ld a,020h		;123f
	call 0cb35h		;1241
	pop hl			;1244
	inc hl			;1245
	call 0c25bh		;1246
	jp 0c0bbh		;1249
	call 0ccc5h		;124c
	ld a,(hl)			;124f
	cp 00dh		;1250
	jp z,0ccb5h		;1252
	call 0cb35h		;1255
	inc hl			;1258
	jr $-10		;1259
	ld de,(0d048h)		;125b
	xor a			;125f
	sbc hl,de		;1260
	ret z			;1262
	call 0ccc5h		;1263
	ld b,l			;1266
	ld a,008h		;1267
	call 0cb35h		;1269
	djnz $-5		;126c
	jp 0ccb5h		;126e
	ld de,0cfcfh		;1271
	or a			;1274
	sbc hl,de		;1275
	ld a,(0d09ch)		;1277
	add a,l			;127a
	sub (ix-00dh)		;127b
	ld l,(ix-00fh)		;127e
	sub (ix-010h)		;1281
	jr c,$+8		;1284
	dec l			;1286
	jr nz,$-6		;1287
	ld l,(ix-00fh)		;1289
	ld a,l			;128c
	jp 0c8bbh		;128d
	ld de,(0d157h)		;1290
	ld d,000h		;1294
	ld h,d			;1296
	ld l,d			;1297
	ld bc,(0d157h)		;1298
	inc b			;129c
	add hl,de			;129d
	djnz $-1		;129e
	ld a,(0d15ah)		;12a0
	inc a			;12a3
	sub (ix-00dh)		;12a4
	ld e,a			;12a7
	or a			;12a8
	sbc hl,de		;12a9
	ld e,(ix-007h)		;12ab
	push hl			;12ae
	or a			;12af
	sbc hl,de		;12b0
	pop hl			;12b2
	ret nc			;12b3
	ld (ix-007h),l		;12b4
	ret			;12b7
	bit 5,(ix+01dh)		;12b8
	ret nz			;12bc
	call 0c347h		;12bd
	bit 7,(hl)		;12c0
	jp z,0c31eh		;12c2
	call 0c2e7h		;12c5
	bit 3,(ix+01dh)		;12c8
	call nz,0ce11h		;12cc
	call 0c918h		;12cf
	bit 7,(ix-004h)		;12d2
	call z,0c31eh		;12d6
	bit 4,(ix+01dh)		;12d9
	call nz,0c880h		;12dd
	ld a,(0d052h)		;12e0
	call 0ce11h		;12e3
	ret			;12e6
	ld a,(0d04dh)		;12e7
	and 00fh		;12ea
	ld (0d04dh),a		;12ec
	ld a,(0f3ebh)		;12ef
	ld (0d052h),a		;12f2
	inc hl			;12f5
	ld a,(hl)			;12f6
	push af			;12f7
	inc hl			;12f8
	ld a,(hl)			;12f9
	ld (0d159h),a		;12fa
	inc hl			;12fd
	ld a,(hl)			;12fe
	ld (0d15ah),a		;12ff
	inc hl			;1302
	ld a,(hl)			;1303
	dec a			;1304
	ld (0d158h),a		;1305
	inc hl			;1308
	ld a,(hl)			;1309
	ld (0d157h),a		;130a
	push de			;130d
	call 0c974h		;130e
	pop de			;1311
	pop af			;1312
	ld l,a			;1313
	ld h,000h		;1314
	ld (0d050h),hl		;1316
	ld l,h			;1319
	ld (0d04eh),hl		;131a
	ret			;131d
	push ix		;131e
	push iy		;1320
	ld d,000h		;1322
	ld hl,(0aff1h)		;1324
	ex de,hl			;1327
	add hl,hl			;1328
	add hl,de			;1329
	ld e,(hl)			;132a
	inc hl			;132b
	ld d,(hl)			;132c
	push de			;132d
	pop ix		;132e
	ld iy,0d04bh		;1330
	ld a,000h		;1334
	ld (0c85ch),a		;1336
	ld (0c85dh),a		;1339
	ld (0d04ch),a		;133c
	call 0c35eh		;133f
	pop iy		;1342
	pop ix		;1344
	ret			;1346
	ld hl,(0aff3h)		;1347
	ld d,000h		;134a
	add hl,de			;134c
	add hl,de			;134d
	add hl,de			;134e
	add hl,de			;134f
	add hl,de			;1350
	add hl,de			;1351
	ret			;1352
	ld e,003h		;1353
	jp 0c35ah		;1355
	ld e,001h		;1358
	ld d,000h		;135a
	add ix,de		;135c
	ld a,(ix+000h)		;135e
	and 007h		;1361
	sla a		;1363
	ld hl,0c375h		;1365
	ld e,a			;1368
	ld d,000h		;1369
	add hl,de			;136b
	ld (0c85ah),ix		;136c
	ld a,(hl)			;1370
	inc hl			;1371
	ld h,(hl)			;1372
	ld l,a			;1373
	jp (hl)			;1374
	add a,l			;1375
	jp 0c396h		;1376
	call po,081c3h		;1379
	call nz,0c4d5h		;137c
	ld h,c			;137f
	push bc			;1380
	ld (hl),l			;1381
	push bc			;1382
	sbc a,h			;1383
	push bc			;1384
	ld a,(ix+000h)		;1385
	call 0c3cfh		;1388
	ld b,(ix+002h)		;138b
	ld c,(ix+001h)		;138e
	call 0c7fch		;1391
	jr $+52		;1394
	ld a,(ix+000h)		;1396
	and 018h		;1399
	cp 018h		;139b
	jr nz,$+12		;139d
	call 0c5b8h		;139f
	ld (0d04eh),bc		;13a2
	jp 0c353h		;13a6
	call 0c3cfh		;13a9
	call 0c5e6h		;13ac
	ld b,d			;13af
	ld c,e			;13b0
	ld e,001h		;13b1
	bit 6,(iy+000h)		;13b3
	jr z,$+4		;13b7
	ld e,0ffh		;13b9
	ld d,001h		;13bb
	bit 7,(iy+000h)		;13bd
	jr z,$+4		;13c1
	ld d,0ffh		;13c3
	call 0c7b8h		;13c5
	xor a			;13c8
	call 0c3d9h		;13c9
	jp 0c353h		;13cc
	rrca			;13cf
	rrca			;13d0
	rrca			;13d1
	and 003h		;13d2
	or a			;13d4
	add a,002h		;13d5
	and 005h		;13d7
	ld e,a			;13d9
	ld a,(0d04dh)		;13da
	and 0fah		;13dd
	or e			;13df
	ld (0d04dh),a		;13e0
	ret			;13e3
	ld a,(ix+000h)		;13e4
	cp 012h		;13e7
	jp z,0c42bh		;13e9
	call 0c5b8h		;13ec
	bit 5,(ix+000h)		;13ef
	jr nz,$+24		;13f3
	xor a			;13f5
	ld hl,0c403h		;13f6
	ld e,l			;13f9
	ld d,h			;13fa
	call 0c625h		;13fb
	ld e,003h		;13fe
	jp 0c35ah		;1400
	rst 38h			;1403
	rst 38h			;1404
	rst 38h			;1405
	rst 38h			;1406
	rst 38h			;1407
	rst 38h			;1408
	rst 38h			;1409
	rst 38h			;140a
	ld a,(ix+003h)		;140b
	ld l,a			;140e
	ld h,000h		;140f
	add hl,hl			;1411
	add hl,hl			;1412
	add hl,hl			;1413
	ld de,(0aff7h)		;1414
	add hl,de			;1418
	ld e,l			;1419
	ld d,h			;141a
	xor a			;141b
	bit 4,(ix+000h)		;141c
	jr z,$+3		;1420
	dec a			;1422
	call 0c625h		;1423
	ld e,004h		;1426
	jp 0c35ah		;1428
	ld de,(0d050h)		;142b
	ld a,(ix+002h)		;142f
	inc a			;1432
	sla a		;1433
	sla a		;1435
	sla a		;1437
	ld b,a			;1439
	ld c,000h		;143a
	ld a,c			;143c
	add a,(ix+004h)		;143d
	or 020h		;1440
	ld h,a			;1442
	ld a,(ix+003h)		;1443
	sla a		;1446
	sla a		;1448
	sla a		;144a
	ld l,a			;144c
	call 0ce04h		;144d
	push bc			;1450
	push hl			;1451
	ld hl,0d570h		;1452
	in a,(098h)		;1455
	xor e			;1457
	and d			;1458
	xor e			;1459
	ld (hl),a			;145a
	inc hl			;145b
	djnz $-7		;145c
	pop hl			;145e
	pop bc			;145f
	push bc			;1460
	call 0cdf5h		;1461
	ld hl,0d570h		;1464
	ld a,(hl)			;1467
	out (098h),a		;1468
	nop			;146a
	nop			;146b
	nop			;146c
	inc hl			;146d
	djnz $-7		;146e
	pop bc			;1470
	ld a,c			;1471
	cp (ix+001h)		;1472
	jr z,$+6		;1475
	inc c			;1477
	jp 0c43ch		;1478
	ld de,00005h		;147b
	jp 0c35ah		;147e
	ld a,(0d04ch)		;1481
	cp 00ah		;1484
	jr nz,$+18		;1486
	ld a,000h		;1488
	ld (0d04ch),a		;148a
	ld (0c85ch),a		;148d
	ld (0c85dh),a		;1490
	ld a,068h		;1493
	jp 0b03ah		;1495
	or a			;1498
	jr nz,$+6		;1499
	ld (0c858h),ix		;149b
	inc (iy+001h)		;149f
	push ix		;14a2
	ld bc,(0c85ch)		;14a4
	ld a,c			;14a8
	or b			;14a9
	push af			;14aa
	ld a,(ix+000h)		;14ab
	rrca			;14ae
	rrca			;14af
	rrca			;14b0
	and 007h		;14b1
	ld (0c85ch),a		;14b3
	ld a,(0c85dh)		;14b6
	xor (ix+000h)		;14b9
	and 0c0h		;14bc
	ld (0c85dh),a		;14be
	ld h,000h		;14c1
	ld l,(ix+001h)		;14c3
	add hl,hl			;14c6
	ld de,(0aff1h)		;14c7
	add hl,de			;14cb
	ld e,(hl)			;14cc
	inc hl			;14cd
	ld d,(hl)			;14ce
	push de			;14cf
	pop ix		;14d0
	jp 0c35eh		;14d2
	ld a,(ix+000h)		;14d5
	call 0c3cfh		;14d8
	ld a,(ix+001h)		;14db
	ld l,a			;14de
	ld h,000h		;14df
	add hl,hl			;14e1
	add hl,hl			;14e2
	add hl,hl			;14e3
	ld de,(0aff7h)		;14e4
	add hl,de			;14e8
	ex de,hl			;14e9
	ld a,(ix+003h)		;14ea
	ld h,a			;14ed
	ld a,(ix+002h)		;14ee
	sla a		;14f1
	sla a		;14f3
	sla a		;14f5
	ld l,a			;14f7
	ld a,(0d04dh)		;14f8
	ld b,0ffh		;14fb
	rra			;14fd
	jr c,$+3		;14fe
	inc b			;1500
	rra			;1501
	rra			;1502
	sbc a,a			;1503
	ld c,a			;1504
	call 0ce04h		;1505
	push hl			;1508
	ld a,008h		;1509
	ld hl,0d570h		;150b
	ex de,hl			;150e
	ex af,af'			;150f
	in a,(098h)		;1510
	and b			;1512
	xor (hl)			;1513
	xor c			;1514
	ld (de),a			;1515
	inc hl			;1516
	inc de			;1517
	ex af,af'			;1518
	dec a			;1519
	jr nz,$-11		;151a
	pop hl			;151c
	push hl			;151d
	call 0cdf5h		;151e
	ld hl,0d570h		;1521
	ld b,008h		;1524
	ld a,(hl)			;1526
	out (098h),a		;1527
	nop			;1529
	nop			;152a
	nop			;152b
	inc hl			;152c
	djnz $-7		;152d
	pop hl			;152f
	set 5,h		;1530
	push hl			;1532
	ld de,(0d050h)		;1533
	call 0ce04h		;1537
	ld hl,0d570h		;153a
	ld b,008h		;153d
	in a,(098h)		;153f
	xor e			;1541
	and d			;1542
	xor e			;1543
	ld (hl),a			;1544
	inc hl			;1545
	djnz $-7		;1546
	pop hl			;1548
	pop bc			;1549
	push bc			;154a
	call 0cdf5h		;154b
	ld hl,0d570h		;154e
	ld b,008h		;1551
	ld a,(hl)			;1553
	out (098h),a		;1554
	nop			;1556
	nop			;1557
	nop			;1558
	inc hl			;1559
	djnz $-7		;155a
	ld e,004h		;155c
	jp 0c35ah		;155e
	ld hl,0d050h		;1561
	ld a,(ix+000h)		;1564
	srl a		;1567
	srl a		;1569
	srl a		;156b
	ld b,00fh		;156d
	bit 4,a		;156f
	jr z,$+15		;1571
	jr $+19		;1573
	ld hl,0d050h		;1575
	ld a,(ix+000h)		;1578
	rlca			;157b
	ld b,0f0h		;157c
	jr c,$+8		;157e
	call 0c595h		;1580
	xor a			;1583
	jr $+8		;1584
	ld a,(hl)			;1586
	call 0c595h		;1587
	ld a,0ffh		;158a
	call 0c595h		;158c
	ld de,00001h		;158f
	jp 0c35ah		;1592
	xor (hl)			;1595
	and b			;1596
	xor (hl)			;1597
	ld (hl),a			;1598
	inc hl			;1599
	ld a,b			;159a
	ret			;159b
	ld a,(0d04ch)		;159c
	or a			;159f
	ret z			;15a0
	pop bc			;15a1
	ld a,b			;15a2
	and 007h		;15a3
	ld (0c85ch),a		;15a5
	ld a,b			;15a8
	and 0c0h		;15a9
	ld (0c85dh),a		;15ab
	pop ix		;15ae
	dec (iy+001h)		;15b0
	ld e,002h		;15b3
	jp 0c35ah		;15b5
	call 0c5e6h		;15b8
	ld bc,(0d04eh)		;15bb
	ld a,c			;15bf
	bit 6,(iy+000h)		;15c0
	jr z,$+6		;15c4
	sub e			;15c6
	jp 0c5cbh		;15c7
	add a,e			;15ca
	ld c,a			;15cb
	ld a,b			;15cc
	bit 7,(iy+000h)		;15cd
	jr z,$+10		;15d1
	sub d			;15d3
	jr nc,$+16		;15d4
	sub 050h		;15d6
	jp 0c5e4h		;15d8
	add a,d			;15db
	jr c,$+6		;15dc
	cp 0b0h		;15de
	jr c,$+4		;15e0
	sub 0b0h		;15e2
	ld b,a			;15e4
	ret			;15e5
	ld a,(0c85ch)		;15e6
	and a			;15e9
	jp nz,0c5f6h		;15ea
	ld e,(ix+001h)		;15ed
	ld d,(ix+002h)		;15f0
	jp 0c607h		;15f3
	ld d,000h		;15f6
	ld e,(ix+002h)		;15f8
	call 0c611h		;15fb
	ld c,l			;15fe
	ld e,(ix+001h)		;15ff
	call 0c611h		;1602
	ld d,c			;1605
	ld e,l			;1606
	ld a,(0c85dh)		;1607
	xor (ix+000h)		;160a
	ld (0d04bh),a		;160d
	ret			;1610
	ld hl,00000h		;1611
	ld b,a			;1614
	add hl,de			;1615
	djnz $-1		;1616
	srl h		;1618
	rr l		;161a
	srl h		;161c
	rr l		;161e
	srl h		;1620
	rr l		;1622
	ret			;1624
	push iy		;1625
	push ix		;1627
	ld (0c860h),sp		;1629
	ld ix,0d568h		;162d
	push ix		;1631
	ld (0c85eh),bc		;1633
	ld c,a			;1637
	ld b,008h		;1638
	ld a,(de)			;163a
	or (hl)			;163b
	xor c			;163c
	inc hl			;163d
	inc de			;163e
	ld (ix+000h),a		;163f
	inc ix		;1642
	djnz $-10		;1644
	pop ix		;1646
	ld hl,0d570h		;1648
	ld (hl),0ffh		;164b
	inc hl			;164d
	ld b,0b0h		;164e
	ld (hl),000h		;1650
	inc hl			;1652
	djnz $-3		;1653
	ld (hl),0ffh		;1655
	ld bc,(0c85eh)		;1657
	ld d,000h		;165b
	call 0c7a3h		;165d
	call 0ce04h		;1660
	in a,(098h)		;1663
	and e			;1665
	jp nz,0c79ah		;1666
	ld a,01eh		;1669
	ex af,af'			;166b
	ld hl,0ffffh		;166c
	push hl			;166f
	ld a,d			;1670
	and 0fch		;1671
	ld d,a			;1673
	call 0c7a3h		;1674
	ld a,b			;1677
	bit 7,d		;1678
	jr z,$+6		;167a
	inc b			;167c
	jp 0c681h		;167d
	dec b			;1680
	push bc			;1681
	exx			;1682
	ld l,a			;1683
	ld h,000h		;1684
	ld de,0d571h		;1686
	add hl,de			;1689
	push hl			;168a
	pop iy		;168b
	and 007h		;168d
	ld (0c694h),a		;168f
	ld d,(ix+000h)		;1692
	pop bc			;1695
	ld a,b			;1696
	cp 0b0h		;1697
	call c,0c7a3h		;1699
	exx			;169c
	bit 7,d		;169d
	exx			;169f
	jr z,$+8		;16a0
	ld c,(iy+001h)		;16a2
	jp 0c6abh		;16a5
	ld c,(iy-001h)		;16a8
	exx			;16ab
	srl e		;16ac
	jr nc,$+22		;16ae
	ld e,080h		;16b0
	inc hl			;16b2
	inc hl			;16b3
	inc hl			;16b4
	inc hl			;16b5
	inc hl			;16b6
	inc hl			;16b7
	inc hl			;16b8
	inc hl			;16b9
	exx			;16ba
	inc hl			;16bb
	inc hl			;16bc
	inc hl			;16bd
	inc hl			;16be
	inc hl			;16bf
	inc hl			;16c0
	inc hl			;16c1
	inc hl			;16c2
	exx			;16c3
	inc c			;16c4
	jr z,$+10		;16c5
	call 0ce04h		;16c7
	in a,(098h)		;16ca
	and e			;16cc
	jr z,$-33		;16cd
	dec c			;16cf
	ld (iy+000h),c		;16d0
	jp 0c731h		;16d3
	call 0ce04h		;16d6
	in a,(098h)		;16d9
	ld (0d567h),a		;16db
	and e			;16de
	jp nz,0c780h		;16df
	call 0cdf5h		;16e2
	exx			;16e5
	ld a,d			;16e6
	exx			;16e7
	and e			;16e8
	or (ix-001h)		;16e9
	out (098h),a		;16ec
	push de			;16ee
	call 0c825h		;16ef
	pop de			;16f2
	ld a,c			;16f3
	or a			;16f4
	jp z,0c780h		;16f5
	bit 1,d		;16f8
	jr nz,$+54		;16fa
	exx			;16fc
	ld e,a			;16fd
	ld a,c			;16fe
	or a			;16ff
	jp z,0c70fh		;1700
	cp e			;1703
	jp c,0c70fh		;1704
	exx			;1707
	ld a,d			;1708
	or 003h		;1709
	ld d,a			;170b
	jp 0c730h		;170c
	call 0ce04h		;170f
	in a,(098h)		;1712
	exx			;1714
	and e			;1715
	jr nz,$+3		;1716
	scf			;1718
	bit 0,d		;1719
	jp nz,0c72bh		;171b
	jr nc,$+18		;171e
	ex af,af'			;1720
	dec a			;1721
	jr z,$+103		;1722
	ex af,af'			;1724
	push bc			;1725
	set 0,d		;1726
	jp 0c730h		;1728
	jp c,0c730h		;172b
	res 0,d		;172e
	dec c			;1730
	sla e		;1731
	jp nc,0c6d6h		;1733
	ld e,001h		;1736
	dec hl			;1738
	dec hl			;1739
	dec hl			;173a
	dec hl			;173b
	dec hl			;173c
	dec hl			;173d
	dec hl			;173e
	dec hl			;173f
	call 0ce04h		;1740
	in a,(098h)		;1743
	exx			;1745
	dec hl			;1746
	dec hl			;1747
	dec hl			;1748
	dec hl			;1749
	dec hl			;174a
	dec hl			;174b
	dec hl			;174c
	dec hl			;174d
	or a			;174e
	jr nz,$+45		;174f
	exx			;1751
	bit 1,d		;1752
	exx			;1754
	jr nz,$+16		;1755
	call 0ce04h		;1757
	in a,(098h)		;175a
	or a			;175c
	jr nz,$+31		;175d
	exx			;175f
	bit 0,d		;1760
	exx			;1762
	jr z,$+25		;1763
	exx			;1765
	call 0cdf5h		;1766
	exx			;1769
	ld a,d			;176a
	exx			;176b
	out (098h),a		;176c
	push de			;176e
	call 0c825h		;176f
	pop de			;1772
	ld a,c			;1773
	sub 008h		;1774
	ld c,a			;1776
	jr c,$+9		;1777
	jp 0c738h		;1779
	exx			;177c
	jp 0c6d6h		;177d
	ex af,af'			;1780
	inc a			;1781
	ex af,af'			;1782
	pop bc			;1783
	ld a,b			;1784
	inc a			;1785
	jp nz,0c670h		;1786
	ld bc,(0c85eh)		;1789
	bit 7,d		;178d
	jr nz,$+11		;178f
	set 7,d		;1791
	inc b			;1793
	ld a,b			;1794
	cp 0b0h		;1795
	jp c,0c65dh		;1797
	ld sp,(0c860h)		;179a
	pop ix		;179e
	pop iy		;17a0
	ret			;17a2
	push bc			;17a3
	call 0c83dh		;17a4
	ld bc,0c7b0h		;17a7
	add a,c			;17aa
	ld c,a			;17ab
	ld a,(bc)			;17ac
	ld e,a			;17ad
	pop bc			;17ae
	ret			;17af
	add a,b			;17b0
	ld b,b			;17b1
	jr nz,$+18		;17b2
	ex af,af'			;17b4
	inc b			;17b5
	ld (bc),a			;17b6
	ld bc,0b879h		;17b7
	jr nc,$+8		;17ba
	ld l,c			;17bc
	push de			;17bd
	xor a			;17be
	ld e,a			;17bf
	jr $+9		;17c0
	or c			;17c2
	ret z			;17c3
	ld l,b			;17c4
	ld b,c			;17c5
	push de			;17c6
	ld d,000h		;17c7
	ld h,b			;17c9
	ld a,b			;17ca
	rra			;17cb
	add a,l			;17cc
	jr c,$+5		;17cd
	cp h			;17cf
	jr c,$+9		;17d0
	sub h			;17d2
	ld c,a			;17d3
	exx			;17d4
	pop bc			;17d5
	push bc			;17d6
	jr $+6		;17d7
	ld c,a			;17d9
	push de			;17da
	exx			;17db
	pop bc			;17dc
	ld hl,(0d04eh)		;17dd
	ld a,b			;17e0
	add a,h			;17e1
	ld b,a			;17e2
	ld a,c			;17e3
	inc a			;17e4
	add a,l			;17e5
	jr c,$+15		;17e6
	jr z,$+15		;17e8
	dec a			;17ea
	ld c,a			;17eb
	call 0c7fch		;17ec
	exx			;17ef
	ld a,c			;17f0
	djnz $-37		;17f1
	pop de			;17f3
	ret			;17f4
	jr z,$-11		;17f5
	ld a,067h		;17f7
	jp 0b03ah		;17f9
	ld (0d04eh),bc		;17fc
	call 0c83dh		;1800
	ld b,a			;1803
	inc b			;1804
	ld a,0feh		;1805
	rrca			;1807
	djnz $-1		;1808
	ld b,a			;180a
	call 0ce04h		;180b
	in a,(098h)		;180e
	ld c,(iy+002h)		;1810
	bit 0,c		;1813
	jr nz,$+3		;1815
	and b			;1817
	bit 2,c		;1818
	jr nz,$+4		;181a
	xor b			;181c
	cpl			;181d
	ld b,a			;181e
	call 0cdf5h		;181f
	ld a,b			;1822
	out (098h),a		;1823
	set 5,h		;1825
	ld de,(0d050h)		;1827
	call 0ce04h		;182b
	in a,(098h)		;182e
	xor e			;1830
	and d			;1831
	xor e			;1832
	push af			;1833
	call 0cdf5h		;1834
	pop af			;1837
	out (098h),a		;1838
	res 5,h		;183a
	ret			;183c
	ld a,0afh		;183d
	sub b			;183f
	jp c,0c7f7h		;1840
	ld b,a			;1843
	and 007h		;1844
	ld l,a			;1846
	ld a,c			;1847
	and 0f8h		;1848
	or l			;184a
	ld l,a			;184b
	ld a,b			;184c
	srl a		;184d
	srl a		;184f
	srl a		;1851
	ld h,a			;1853
	ld a,c			;1854
	and 007h		;1855
	ret			;1857
	nop			;1858
	nop			;1859
	nop			;185a
	nop			;185b
	nop			;185c
	nop			;185d
	nop			;185e
	nop			;185f
	nop			;1860
	nop			;1861
	call 0cae7h		;1862
	ld l,010h		;1865
	call 0bde8h		;1867
	call 0cae7h		;186a
	ld a,(0d158h)		;186d
	inc a			;1870
	call 0c8bbh		;1871
	bit 2,(ix+031h)		;1874
	jr z,$+8		;1878
	ld a,(0d197h)		;187a
	or a			;187d
	jr nz,$+14		;187e
	push de			;1880
	call 0cf84h		;1881
	halt			;1884
	call 0009ch		;1885
	jr z,$-3		;1888
	jr $+38		;188a
	push de			;188c
	ld d,(ix+030h)		;188d
	ld e,000h		;1890
	srl d		;1892
	rr e		;1894
	srl d		;1896
	rr e		;1898
	jr $+3		;189a
	push de			;189c
	call 0cf84h		;189d
	halt			;18a0
	call 0009ch		;18a1
	jr nz,$+12		;18a4
	call 0cfcdh		;18a6
	dec de			;18a9
	ld a,d			;18aa
	or e			;18ab
	jr nz,$-11		;18ac
	jr $+6		;18ae
	call 0009fh		;18b0
	scf			;18b3
	push af			;18b4
	call 0cf8fh		;18b5
	pop af			;18b8
	pop de			;18b9
	ret			;18ba
	ld (0d155h),a		;18bb
	push ix		;18be
	push bc			;18c0
	ld ix,0d0bbh		;18c1
	ld de,00010h		;18c5
	ld b,008h		;18c8
	ld a,(ix+00ah)		;18ca
	ld (ix+007h),a		;18cd
	add ix,de		;18d0
	djnz $-8		;18d2
	pop bc			;18d4
	pop ix		;18d5
	ret			;18d7
	cp (ix-01ah)		;18d8
	ret z			;18db
	push af			;18dc
	call 0cae7h		;18dd
	ld a,(0d14dh)		;18e0
	call 0c907h		;18e3
	ex de,hl			;18e6
	ld hl,0d14eh		;18e7
	ld bc,00010h		;18ea
	ldir		;18ed
	pop af			;18ef
	push af			;18f0
	ld (0d14dh),a		;18f1
	call 0c907h		;18f4
	ld de,0d14eh		;18f7
	ld bc,00010h		;18fa
	ldir		;18fd
	ld a,(0d152h)		;18ff
	ld (0d050h),a		;1902
	pop af			;1905
	ret			;1906
	ld hl,0d0bbh		;1907
	rlca			;190a
	rlca			;190b
	rlca			;190c
	rlca			;190d
	ld e,a			;190e
	ld d,000h		;190f
	add hl,de			;1911
	ret			;1912
	bit 6,(ix+01dh)		;1913
	ret nz			;1917
	push bc			;1918
	push hl			;1919
	push de			;191a
	call 0cb26h		;191b
	call 0c928h		;191e
	call 0cd40h		;1921
	pop de			;1924
	pop hl			;1925
	pop bc			;1926
	ret			;1927
	ld a,(0d159h)		;1928
	ld (0d15ch),a		;192b
	call 0cdf1h		;192e
	ld (0d153h),hl		;1931
	ld a,(0d15ah)		;1934
	ld (0d15bh),a		;1937
	ld a,(0d158h)		;193a
	ld (0d155h),a		;193d
	ret			;1940
	ld a,d			;1941
	cp (ix-00eh)		;1942
	jr c,$+27		;1945
	ld a,(0d159h)		;1947
	add a,(ix-00fh)		;194a
	cp d			;194d
	jr c,$+18		;194e
	ld a,e			;1950
	cp (ix-00dh)		;1951
	jr c,$+12		;1954
	ld a,(0d15ah)		;1956
	add a,(ix-010h)		;1959
	dec a			;195c
	cp e			;195d
	jr nc,$+8		;195e
	ld e,(ix-00dh)		;1960
	ld d,(ix-00eh)		;1963
	ld (ix-00ch),e		;1966
	ld a,d			;1969
	ld (0d15ch),a		;196a
	call 0cdf1h		;196d
	ld (0d153h),hl		;1970
	ret			;1973
	ld a,(0d159h)		;1974
	add a,(ix-00fh)		;1977
	cp 018h		;197a
	jr c,$+10		;197c
	ld a,017h		;197e
	sub (ix-00eh)		;1980
	ld (0d158h),a		;1983
	ld a,(0d15ah)		;1986
	add a,(ix-010h)		;1989
	cp 02bh		;198c
	ret c			;198e
	ld a,02ah		;198f
	sub (ix-00dh)		;1991
	ld (0d157h),a		;1994
	ret			;1997
	ld a,00dh		;1998
	push ix		;199a
	ld ix,0d167h		;199c
	cp 080h		;19a0
	jr c,$+37		;19a2
	bit 2,(ix-003h)		;19a4
	jr z,$+31		;19a8
	push hl			;19aa
	ld hl,(00108h)		;19ab
	sub 07fh		;19ae
	bit 7,(hl)		;19b0
	inc hl			;19b2
	jr z,$-3		;19b3
	dec a			;19b5
	jr nz,$-6		;19b6
	ld a,(hl)			;19b8
	add a,080h		;19b9
	jr c,$+13		;19bb
	add a,080h		;19bd
	call 0c9c5h		;19bf
	inc hl			;19c2
	jr $-11		;19c3
	push ix		;19c5
	push hl			;19c7
	push de			;19c8
	push bc			;19c9
	cp 00dh		;19ca
	jr z,$+76		;19cc
	cp 010h		;19ce
	jr c,$+92		;19d0
	bit 0,(ix-00ah)		;19d2
	jr z,$+4		;19d6
	or 080h		;19d8
	bit 6,(ix-00ah)		;19da
	jr z,$+53		;19de
	cp 020h		;19e0
	jr z,$+34		;19e2
	cp 0a0h		;19e4
	jr z,$+30		;19e6
	push af			;19e8
	ld a,(0d071h)		;19e9
	cp 028h		;19ec
	call z,0cae7h		;19ee
	pop af			;19f1
	res 5,(ix-00ah)		;19f2
	ld hl,(0d09ah)		;19f6
	ld (hl),a			;19f9
	inc hl			;19fa
	ld (0d09ah),hl		;19fb
	ld hl,0d071h		;19fe
	inc (hl)			;1a01
	jr $+36		;1a02
	bit 5,(ix-00ah)		;1a04
	jr nz,$+30		;1a08
	push af			;1a0a
	call 0cae7h		;1a0b
	pop af			;1a0e
	bit 5,(ix-00ah)		;1a0f
	call z,0cabfh		;1a13
	jr $+16		;1a16
	call 0cae7h		;1a18
	bit 7,(ix-00ah)		;1a1b
	res 7,(ix-00ah)		;1a1f
	call z,0ca5ah		;1a23
	pop bc			;1a26
	pop de			;1a27
	pop hl			;1a28
	pop ix		;1a29
	ret			;1a2b
	cp 00fh		;1a2c
	jr nz,$+8		;1a2e
	res 0,(ix-00ah)		;1a30
	jr $-14		;1a34
	cp 00eh		;1a36
	jr nz,$+8		;1a38
	set 0,(ix-00ah)		;1a3a
	jr $-24		;1a3e
	push af			;1a40
	call 0cae7h		;1a41
	pop af			;1a44
	cp 00bh		;1a45
	jr nz,$+7		;1a47
	call 0c918h		;1a49
	jr $-38		;1a4c
	cp 00ch		;1a4e
	jr nz,$-42		;1a50
	ld a,(0d158h)		;1a52
	call 0c871h		;1a55
	jr $-50		;1a58
	ld a,00dh		;1a5a
	call 0cb35h		;1a5c
	ld hl,0d155h		;1a5f
	dec (hl)			;1a62
	ret nz			;1a63
	ld a,(0d158h)		;1a64
	ld (hl),a			;1a67
	bit 6,(ix-004h)		;1a68
	ret nz			;1a6c
	bit 1,(ix-00ah)		;1a6d
	ret nz			;1a71
	call 0c8bbh		;1a72
	ld l,020h		;1a75
	call 0bd6eh		;1a77
	ld a,(0d15dh)		;1a7a
	push af			;1a7d
	ld a,002h		;1a7e
	ld (0d15dh),a		;1a80
	ld a,(hl)			;1a83
	cpl			;1a84
	cp 00ah		;1a85
	jr z,$+8		;1a87
	inc hl			;1a89
	call 0c99ah		;1a8a
	jr $-10		;1a8d
	pop af			;1a8f
	ld (0d15dh),a		;1a90
	bit 1,(ix+031h)		;1a93
	jr z,$+13		;1a97
	ld a,(0d197h)		;1a99
	or a			;1a9c
	jr z,$+7		;1a9d
	call 0c88ch		;1a9f
	jr $+5		;1aa2
	call 0c880h		;1aa4
	ld a,(0d15bh)		;1aa7
	cp (ix-00dh)		;1aaa
	ret z			;1aad
	ld a,008h		;1aae
	call 0cb35h		;1ab0
	ld a,020h		;1ab3
	call 0cb35h		;1ab5
	ld a,008h		;1ab8
	call 0cb35h		;1aba
	jr $-22		;1abd
	ld e,a			;1abf
	push de			;1ac0
	call 0cb35h		;1ac1
	pop de			;1ac4
	res 7,(ix-00ah)		;1ac5
	ld a,(0d15bh)		;1ac9
	cp (ix-00dh)		;1acc
	ret nz			;1acf
	ld a,e			;1ad0
	cp 020h		;1ad1
	jr nz,$+12		;1ad3
	bit 6,(ix-00ah)		;1ad5
	jr z,$+6		;1ad9
	set 5,(ix-00ah)		;1adb
	call 0ca5fh		;1adf
	set 7,(ix-00ah)		;1ae2
	ret			;1ae6
	bit 6,(ix-00ah)		;1ae7
	ret z			;1aeb
	ld a,(0d071h)		;1aec
	or a			;1aef
	ret z			;1af0
	ld a,(0d15ah)		;1af1
	add a,(ix-010h)		;1af4
	ld hl,0d15bh		;1af7
	sub (hl)			;1afa
	ld hl,0d071h		;1afb
	cp (hl)			;1afe
	jr z,$+15		;1aff
	jr nc,$+17		;1b01
	ld a,(0d15bh)		;1b03
	cp (ix-00dh)		;1b06
	call nz,0ca5ah		;1b09
	jr $+6		;1b0c
	set 5,(ix-00ah)		;1b0e
	ld bc,(0d070h)		;1b12
	ld hl,0d072h		;1b16
	ld a,(hl)			;1b19
	push hl			;1b1a
	push bc			;1b1b
	call 0cabfh		;1b1c
	pop bc			;1b1f
	pop hl			;1b20
	inc hl			;1b21
	djnz $-9		;1b22
	jr $+6		;1b24
	res 5,(ix-00ah)		;1b26
	ld hl,0d072h		;1b2a
	ld (0d09ah),hl		;1b2d
	xor a			;1b30
	ld (0d071h),a		;1b31
	ret			;1b34
	push hl			;1b35
	push de			;1b36
	push bc			;1b37
	push af			;1b38
	call 0cb52h		;1b39
	pop af			;1b3c
	cp 008h		;1b3d
	call z,0cb64h		;1b3f
	cp 00dh		;1b42
	call z,0cb8fh		;1b44
	or a			;1b47
	call nz,0cbc0h		;1b48
	call 0cb52h		;1b4b
	pop bc			;1b4e
	pop de			;1b4f
	pop hl			;1b50
	ret			;1b51
	bit 3,(ix-00ah)		;1b52
	ret z			;1b56
	set 4,(ix-00ah)		;1b57
	ld a,05fh		;1b5b
	call 0cbc0h		;1b5d
	res 4,(ix-00ah)		;1b60
	ld hl,0d15bh		;1b64
	ld a,(0d15ah)		;1b67
	cp (hl)			;1b6a
	jr z,$+5		;1b6b
	dec (hl)			;1b6d
	jr $+31		;1b6e
	ld hl,0d159h		;1b70
	ld a,(0d15ch)		;1b73
	cp (hl)			;1b76
	jr z,$+22		;1b77
	dec a			;1b79
	ld (0d15ch),a		;1b7a
	call 0cdf1h		;1b7d
	ld (0d153h),hl		;1b80
	ld a,(0d15ah)		;1b83
	add a,(ix-010h)		;1b86
	dec a			;1b89
	ld (0d15bh),a		;1b8a
	xor a			;1b8d
	ret			;1b8e
	ld a,(0d15ah)		;1b8f
	add a,(ix-010h)		;1b92
	cp (ix-00ch)		;1b95
	jr z,$+9		;1b98
	ld a,020h		;1b9a
	call 0cbc0h		;1b9c
	jr $-16		;1b9f
	ld hl,0d15ch		;1ba1
	ld a,(0d159h)		;1ba4
	add a,(ix-00fh)		;1ba7
	cp (hl)			;1baa
	jr z,$+10		;1bab
	inc (hl)			;1bad
	ld a,(hl)			;1bae
	call 0cdf1h		;1baf
	ld (0d153h),hl		;1bb2
	call z,0cccfh		;1bb5
	ld a,(0d15ah)		;1bb8
	ld (0d15bh),a		;1bbb
	xor a			;1bbe
	ret			;1bbf
	push af			;1bc0
	ld a,(0d15ah)		;1bc1
	add a,(ix-010h)		;1bc4
	cp (ix-00ch)		;1bc7
	jp nz,0cbd0h		;1bca
	call 0cb8fh		;1bcd
	ld a,(0d15bh)		;1bd0
	call 0cde2h		;1bd3
	and 003h		;1bd6
	bit 1,a		;1bd8
	jr z,$+3		;1bda
	inc hl			;1bdc
	sla l		;1bdd
	rl h		;1bdf
	sla l		;1be1
	rl h		;1be3
	sla l		;1be5
	rl h		;1be7
	ld de,(0d153h)		;1be9
	add hl,de			;1bed
	push af			;1bee
	set 7,(ix-003h)		;1bef
	call 0ce04h		;1bf3
	ld b,010h		;1bf6
	ld de,0d054h		;1bf8
	in a,(098h)		;1bfb
	nop			;1bfd
	nop			;1bfe
	nop			;1bff
	ld (de),a			;1c00
	inc de			;1c01
	djnz $-7		;1c02
	res 7,(ix-003h)		;1c04
	pop af			;1c08
	ex (sp),hl			;1c09
	push hl			;1c0a
	rlca			;1c0b
	ld d,000h		;1c0c
	ld e,a			;1c0e
	ld bc,0ffffh		;1c0f
	bit 4,(ix-00ah)		;1c12
	jr nz,$+9		;1c16
	ld hl,0ccadh		;1c18
	add hl,de			;1c1b
	ld c,(hl)			;1c1c
	inc hl			;1c1d
	ld b,(hl)			;1c1e
	rlc e		;1c1f
	rlc e		;1c21
	ld hl,0cc8dh		;1c23
	add hl,de			;1c26
	ld (0cc3eh),hl		;1c27
	ld de,(0aff7h)		;1c2a
	pop af			;1c2e
	ld l,a			;1c2f
	ld h,000h		;1c30
	add hl,hl			;1c32
	add hl,hl			;1c33
	add hl,hl			;1c34
	add hl,de			;1c35
	ld de,0d054h		;1c36
	ld a,008h		;1c39
	ex af,af'			;1c3b
	push hl			;1c3c
	call 0cc8dh		;1c3d
	ld a,(de)			;1c40
	and b			;1c41
	xor h			;1c42
	ld (de),a			;1c43
	inc de			;1c44
	inc de			;1c45
	inc de			;1c46
	inc de			;1c47
	inc de			;1c48
	inc de			;1c49
	inc de			;1c4a
	inc de			;1c4b
	ld a,(de)			;1c4c
	and c			;1c4d
	xor l			;1c4e
	ld (de),a			;1c4f
	dec de			;1c50
	dec de			;1c51
	dec de			;1c52
	dec de			;1c53
	dec de			;1c54
	dec de			;1c55
	dec de			;1c56
	pop hl			;1c57
	inc hl			;1c58
	ex af,af'			;1c59
	dec a			;1c5a
	jr nz,$-32		;1c5b
	pop hl			;1c5d
	set 7,(ix-003h)		;1c5e
	call 0cdf5h		;1c62
	ld de,0d054h		;1c65
	ld b,010h		;1c68
	ld a,(de)			;1c6a
	out (098h),a		;1c6b
	nop			;1c6d
	nop			;1c6e
	nop			;1c6f
	inc de			;1c70
	djnz $-7		;1c71
	set 5,h		;1c73
	call 0cdf5h		;1c75
	ld a,(0d050h)		;1c78
	ld b,010h		;1c7b
	out (098h),a		;1c7d
	nop			;1c7f
	nop			;1c80
	nop			;1c81
	djnz $-5		;1c82
	res 7,(ix-003h)		;1c84
	ld hl,0d15bh		;1c88
	inc (hl)			;1c8b
	ret			;1c8c
	ld h,(hl)			;1c8d
	ld l,000h		;1c8e
	ret			;1c90
	nop			;1c91
	nop			;1c92
	nop			;1c93
	nop			;1c94
	ld l,(hl)			;1c95
	ld h,000h		;1c96
	add hl,hl			;1c98
	add hl,hl			;1c99
	ret			;1c9a
	nop			;1c9b
	nop			;1c9c
	ld l,(hl)			;1c9d
	ld h,000h		;1c9e
	add hl,hl			;1ca0
	add hl,hl			;1ca1
	add hl,hl			;1ca2
	add hl,hl			;1ca3
	ret			;1ca4
	ld l,(hl)			;1ca5
	ld h,000h		;1ca6
	srl l		;1ca8
	srl l		;1caa
	ret			;1cac
	rst 38h			;1cad
	inc bc			;1cae
	rrca			;1caf
	call m,0f03fh		;1cb0
	ret nz			;1cb3
	rst 38h			;1cb4
	push hl			;1cb5
	bit 3,(ix-00ah)		;1cb6
	jr nz,$+19		;1cba
	set 3,(ix-00ah)		;1cbc
	call 0cb57h		;1cc0
	jr $+10		;1cc3
	push hl			;1cc5
	call 0cb52h		;1cc6
	res 3,(ix-00ah)		;1cc9
	pop hl			;1ccd
	ret			;1cce
	call 0cd87h		;1ccf
	ld a,(0d158h)		;1cd2
	ld c,(ix-00eh)		;1cd5
	or a			;1cd8
	jr z,$+99		;1cd9
	ld b,a			;1cdb
	ld a,(0d156h)		;1cdc
	sla a		;1cdf
	sla a		;1ce1
	sla a		;1ce3
	set 7,(ix-003h)		;1ce5
	push bc			;1ce9
	push af			;1cea
	ld a,c			;1ceb
	inc a			;1cec
	call 0cdc2h		;1ced
	call 0ce04h		;1cf0
	pop af			;1cf3
	push af			;1cf4
	ld b,a			;1cf5
	ld de,0d570h		;1cf6
	in a,(098h)		;1cf9
	nop			;1cfb
	nop			;1cfc
	nop			;1cfd
	ld (de),a			;1cfe
	inc de			;1cff
	djnz $-7		;1d00
	set 5,h		;1d02
	call 0ce04h		;1d04
	pop af			;1d07
	push af			;1d08
	ld b,a			;1d09
	in a,(098h)		;1d0a
	ld (de),a			;1d0c
	inc de			;1d0d
	djnz $-4		;1d0e
	ld a,c			;1d10
	call 0cdc2h		;1d11
	call 0cdf5h		;1d14
	pop af			;1d17
	push af			;1d18
	ld b,a			;1d19
	ld de,0d570h		;1d1a
	ld a,(de)			;1d1d
	out (098h),a		;1d1e
	nop			;1d20
	nop			;1d21
	nop			;1d22
	inc de			;1d23
	djnz $-7		;1d24
	set 5,h		;1d26
	call 0cdf5h		;1d28
	pop af			;1d2b
	push af			;1d2c
	ld b,a			;1d2d
	ld a,(de)			;1d2e
	out (098h),a		;1d2f
	nop			;1d31
	nop			;1d32
	nop			;1d33
	inc de			;1d34
	djnz $-7		;1d35
	pop af			;1d37
	pop bc			;1d38
	inc c			;1d39
	djnz $-81		;1d3a
	ld b,001h		;1d3c
	jr $+9		;1d3e
	ld b,(ix-00fh)		;1d40
	inc b			;1d43
	ld c,(ix-00eh)		;1d44
	call 0cd87h		;1d47
	ld (0d156h),a		;1d4a
	sla a		;1d4d
	sla a		;1d4f
	sla a		;1d51
	set 7,(ix-003h)		;1d53
	push bc			;1d57
	push af			;1d58
	ld a,c			;1d59
	call 0cdc2h		;1d5a
	call 0cdf5h		;1d5d
	pop af			;1d60
	push af			;1d61
	ld b,a			;1d62
	xor a			;1d63
	out (098h),a		;1d64
	nop			;1d66
	nop			;1d67
	nop			;1d68
	djnz $-5		;1d69
	set 5,h		;1d6b
	call 0cdf5h		;1d6d
	pop af			;1d70
	push af			;1d71
	ld b,a			;1d72
	ld a,(0d050h)		;1d73
	out (098h),a		;1d76
	nop			;1d78
	nop			;1d79
	nop			;1d7a
	djnz $-5		;1d7b
	pop af			;1d7d
	pop bc			;1d7e
	inc c			;1d7f
	djnz $-41		;1d80
	res 7,(ix-003h)		;1d82
	ret			;1d86
	push bc			;1d87
	push hl			;1d88
	ld a,(0d157h)		;1d89
	ld b,a			;1d8c
	srl a		;1d8d
	srl a		;1d8f
	ld c,a			;1d91
	add a,a			;1d92
	add a,c			;1d93
	ld c,a			;1d94
	ld a,(0d15ah)		;1d95
	and 003h		;1d98
	sla a		;1d9a
	sla a		;1d9c
	ld e,a			;1d9e
	ld a,b			;1d9f
	and 003h		;1da0
	add a,e			;1da2
	ld e,a			;1da3
	ld d,000h		;1da4
	ld hl,0cdb2h		;1da6
	add hl,de			;1da9
	ld a,(hl)			;1daa
	add a,c			;1dab
	ld (0d156h),a		;1dac
	pop hl			;1daf
	pop bc			;1db0
	ret			;1db1
	nop			;1db2
	ld bc,00302h		;1db3
	ld bc,00302h		;1db6
	inc bc			;1db9
	ld bc,00202h		;1dba
	inc bc			;1dbd
	ld bc,00201h		;1dbe
	inc bc			;1dc1
	call 0cdf1h		;1dc2
	push hl			;1dc5
	ld a,(0d15ah)		;1dc6
	call 0cde2h		;1dc9
	and 003h		;1dcc
	jr z,$+5		;1dce
	dec a			;1dd0
	ld e,a			;1dd1
	add hl,de			;1dd2
	sla l		;1dd3
	rl h		;1dd5
	sla l		;1dd7
	rl h		;1dd9
	sla l		;1ddb
	rl h		;1ddd
	pop de			;1ddf
	add hl,de			;1de0
	ret			;1de1
	ld e,a			;1de2
	srl e		;1de3
	srl e		;1de5
	ld d,000h		;1de7
	ld hl,00000h		;1de9
	add hl,de			;1dec
	add hl,de			;1ded
	add hl,de			;1dee
	ret			;1def
	ret			;1df0
	ld h,a			;1df1
	ld l,000h		;1df2
	ret			;1df4
	ld a,l			;1df5
	di			;1df6
	out (099h),a		;1df7
	ld a,h			;1df9
	and 03fh		;1dfa
	or 040h		;1dfc
	out (099h),a		;1dfe
	ei			;1e00
	ex (sp),hl			;1e01
	ex (sp),hl			;1e02
	ret			;1e03
	ld a,l			;1e04
	di			;1e05
	out (099h),a		;1e06
	ld a,h			;1e08
	and 03fh		;1e09
	out (099h),a		;1e0b
	ei			;1e0d
	ex (sp),hl			;1e0e
	ex (sp),hl			;1e0f
	ret			;1e10
	ld (0f3ebh),a		;1e11
	call 0cf84h		;1e14
	call 00062h		;1e17
	jp 0cf8fh		;1e1a
	push iy		;1e1d
	call 0cf84h		;1e1f
	di			;1e22
	pop de			;1e23
	ld bc,0000fh		;1e24
	xor a			;1e27
	call 0ce3fh		;1e28
	ld c,(iy+003h)		;1e2b
	ld b,(iy+004h)		;1e2e
	ld e,(iy+005h)		;1e31
	ld d,(iy+006h)		;1e34
	ld a,0ffh		;1e37
	call 0ce3fh		;1e39
	jp 0cf8fh		;1e3c
	push bc			;1e3f
	push de			;1e40
	push af			;1e41
	call 000eah		;1e42
	pop af			;1e45
	call 000edh		;1e46
	ld hl,00000h		;1e49
	pop de			;1e4c
	pop bc			;1e4d
	push bc			;1e4e
	push de			;1e4f
	ld a,(de)			;1e50
	ld e,a			;1e51
	ld d,000h		;1e52
	add hl,de			;1e54
	push hl			;1e55
	call 000edh		;1e56
	call 000b7h		;1e59
	jr c,$+24		;1e5c
	pop hl			;1e5e
	pop de			;1e5f
	pop bc			;1e60
	inc de			;1e61
	dec bc			;1e62
	ld a,b			;1e63
	or c			;1e64
	jr nz,$-23		;1e65
	ld a,l			;1e67
	call 000edh		;1e68
	call 000f0h		;1e6b
	ret			;1e6e
	call 000e7h		;1e6f
	jr $+5		;1e72
	call 000f0h		;1e74
	call 0cf8fh		;1e77
	ld a,065h		;1e7a
	jp 0b03ah		;1e7c
	call 0cf84h		;1e7f
	di			;1e82
	ld de,0d770h		;1e83
	ld bc,0000fh		;1e86
	xor a			;1e89
	call 0cf01h		;1e8a
	jr nz,$-10		;1e8d
	set 6,(ix-004h)		;1e8f
	ld hl,0cf60h		;1e93
	ld b,008h		;1e96
	call 0cf58h		;1e98
	ld hl,0d777h		;1e9b
	ld b,008h		;1e9e
	call 0cf58h		;1ea0
	call 0c998h		;1ea3
	res 6,(ix-004h)		;1ea6
	push iy		;1eaa
	pop hl			;1eac
	ld de,00007h		;1ead
	add hl,de			;1eb0
	ld de,0d777h		;1eb1
	ld b,008h		;1eb4
	ld a,(de)			;1eb6
	cp (hl)			;1eb7
	inc hl			;1eb8
	inc de			;1eb9
	jr nz,$-55		;1eba
	djnz $-6		;1ebc
	set 6,(ix-004h)		;1ebe
	ld hl,0cf68h		;1ec2
	ld b,00ch		;1ec5
	call 0cf58h		;1ec7
	res 6,(ix-004h)		;1eca
	ld a,(iy+003h)		;1ece
	ld b,(iy+004h)		;1ed1
	ld c,a			;1ed4
	or b			;1ed5
	jr nz,$+8		;1ed6
	ld bc,(0d773h)		;1ed8
	jr $+12		;1edc
	ld hl,(0d773h)		;1ede
	or a			;1ee1
	sbc hl,bc		;1ee2
	jr nc,$+4		;1ee4
	ld c,l			;1ee6
	ld b,h			;1ee7
	ld a,(iy+005h)		;1ee8
	ld d,(iy+006h)		;1eeb
	ld e,a			;1eee
	or d			;1eef
	jr nz,$+6		;1ef0
	ld de,(0d775h)		;1ef2
	ld a,0ffh		;1ef6
	call 0cf01h		;1ef8
	jp nz,0cf50h		;1efb
	jp 0cf8fh		;1efe
	push de			;1f01
	push bc			;1f02
	push af			;1f03
	call 000e1h		;1f04
	jp c,0ce6fh		;1f07
	call 000e4h		;1f0a
	jp c,0cf4dh		;1f0d
	ld l,a			;1f10
	pop af			;1f11
	cp l			;1f12
	ld hl,00000h		;1f13
	pop bc			;1f16
	pop de			;1f17
	jr z,$+9		;1f18
	call 000e7h		;1f1a
	ld a,0ffh		;1f1d
	or a			;1f1f
	ret			;1f20
	push bc			;1f21
	push de			;1f22
	push hl			;1f23
	call 000e4h		;1f24
	jp c,0cf4dh		;1f27
	pop hl			;1f2a
	pop de			;1f2b
	ld (de),a			;1f2c
	ld c,a			;1f2d
	ld b,000h		;1f2e
	add hl,bc			;1f30
	call 000b7h		;1f31
	jp c,0ce6fh		;1f34
	pop bc			;1f37
	inc de			;1f38
	dec bc			;1f39
	ld a,b			;1f3a
	or c			;1f3b
	jr nz,$-27		;1f3c
	push hl			;1f3e
	call 000e4h		;1f3f
	jp c,0cf4dh		;1f42
	call 000e7h		;1f45
	pop hl			;1f48
	cp l			;1f49
	jr nz,$+6		;1f4a
	ret			;1f4c
	call 000e7h		;1f4d
	call 0cf8fh		;1f50
	ld a,066h		;1f53
	jp 0b03ah		;1f55
	ld a,(hl)			;1f58
	call 0c99ah		;1f59
	inc hl			;1f5c
	djnz $-5		;1f5d
	ret			;1f5f
	ld c,(hl)			;1f60
	ld l,a			;1f61
	ld l,l			;1f62
	ld h,d			;1f63
	ld (hl),d			;1f64
	ld h,l			;1f65
	ld a,(04320h)		;1f66
	ld h,c			;1f69
	ld (hl),d			;1f6a
	ld h,a			;1f6b
	ld h,c			;1f6c
	ld l,(hl)			;1f6d
	ld h,h			;1f6e
	ld l,a			;1f6f
	ld l,02eh		;1f70
	ld l,00dh		;1f72
	ld e,a			;1f74
	ld a,(bc)			;1f75
	cp 00eh		;1f76
	ret nc			;1f78
	push af			;1f79
	call 0cf84h		;1f7a
	pop af			;1f7d
	call 00093h		;1f7e
	jp 0cf8fh		;1f81
	di			;1f84
	ld a,(0b008h)		;1f85
	out (0a8h),a		;1f88
	ld (0d053h),a		;1f8a
	ei			;1f8d
	ret			;1f8e
	di			;1f8f
	ld a,(0b009h)		;1f90
	out (0a8h),a		;1f93
	ld (0d053h),a		;1f95
	ei			;1f98
	ret			;1f99
	di			;1f9a
	push af			;1f9b
	push hl			;1f9c
	push ix		;1f9d
	ld a,(0d053h)		;1f9f
	push af			;1fa2
	ld a,(0b009h)		;1fa3
	out (0a8h),a		;1fa6
	ld (0d053h),a		;1fa8
	ld hl,(0b011h)		;1fab
	ld ix,0d167h		;1fae
	call 0b010h		;1fb2
	ld a,(0b008h)		;1fb5
	out (0a8h),a		;1fb8
	ld (0d053h),a		;1fba
	nop			;1fbd
	nop			;1fbe
	rst 38h			;1fbf
	di			;1fc0
	pop af			;1fc1
	out (0a8h),a		;1fc2
	ld (0d053h),a		;1fc4
	pop ix		;1fc7
	pop hl			;1fc9
	pop af			;1fca
	ei			;1fcb
	ret			;1fcc
	halt			;1fcd
	ret			;1fce
	di			;1fcf
	ld a,(0b009h)		;1fd0
	out (0a8h),a		;1fd3
	ld (0d053h),a		;1fd5
	ld b,000h		;1fd8
	ld hl,0d900h		;1fda
	ld a,0d8h		;1fdd
	ld (hl),a			;1fdf
	inc hl			;1fe0
	djnz $-2		;1fe1
	ld (hl),a			;1fe3
	ld a,0d9h		;1fe4
	ld i,a		;1fe6
	ld a,0c3h		;1fe8
	ld (0d8d8h),a		;1fea
	ld hl,0cf9ah		;1fed
	ld (0d8d9h),hl		;1ff0
	ld hl,00122h		;1ff3
	ld de,0b00ah		;1ff6
	ld a,003h		;1ff9
	ex af,af'			;1ffb
	ld c,(hl)			;1ffc
	inc hl			;1ffd
	ld b,(hl)			;1ffe
	inc hl			;1fff
	ld a,b			;2000
	or c			;2001
	jr z,$+14		;2002
	ex de,hl			;2004
	ld a,0c3h		;2005
	ld (hl),a			;2007
	inc hl			;2008
	ld (hl),c			;2009
	inc hl			;200a
	ld (hl),b			;200b
	inc hl			;200c
	ex de,hl			;200d
	jr $+5		;200e
	inc de			;2010
	inc de			;2011
	inc de			;2012
	ex af,af'			;2013
	dec a			;2014
	jr nz,$-26		;2015
	ld a,(00102h)		;2017
	ld (0d161h),a		;201a
	ld (0d06ah),sp		;201d
	ld ix,0d167h		;2021
	im 2		;2025
	ei			;2027
	res 2,(ix-003h)		;2028
	ld hl,(00108h)		;202c
	ld a,l			;202f
	or h			;2030
	jp z,0b05ch		;2031
	set 2,(ix-003h)		;2034
	jp 0b05ch		;2038
	rst 38h			;203b
	rst 38h			;203c
	rst 38h			;203d
	rst 38h			;203e
	rst 38h			;203f
	rst 38h			;2040
	rst 38h			;2041
	rst 38h			;2042
	rst 38h			;2043
	rst 38h			;2044
	rst 38h			;2045
	rst 38h			;2046
	rst 38h			;2047
	rst 8			;2048
	rst 8			;2049
	nop			;204a
	nop			;204b
	nop			;204c
	nop			;204d
	nop			;204e
	nop			;204f
	pop af			;2050
	nop			;2051
	nop			;2052
	nop			;2053
	rst 38h			;2054
	rst 38h			;2055
	rst 38h			;2056
	rst 38h			;2057
	rst 38h			;2058
	rst 38h			;2059
	rst 38h			;205a
	rst 38h			;205b
	rst 38h			;205c
	rst 38h			;205d
	rst 38h			;205e
	rst 38h			;205f
	rst 38h			;2060
	rst 38h			;2061
	rst 38h			;2062
	rst 38h			;2063
	rst 38h			;2064
	rst 38h			;2065
	rst 38h			;2066
	rst 38h			;2067
	rst 38h			;2068
	nop			;2069
	nop			;206a
	nop			;206b
	nop			;206c
	nop			;206d
	nop			;206e
	nop			;206f
	nop			;2070
	nop			;2071
	rst 38h			;2072
	rst 38h			;2073
	rst 38h			;2074
	rst 38h			;2075
	rst 38h			;2076
	rst 38h			;2077
	rst 38h			;2078
	rst 38h			;2079
	rst 38h			;207a
	rst 38h			;207b
	rst 38h			;207c
	rst 38h			;207d
	rst 38h			;207e
	rst 38h			;207f
	rst 38h			;2080
	rst 38h			;2081
	rst 38h			;2082
	rst 38h			;2083
	rst 38h			;2084
	rst 38h			;2085
	rst 38h			;2086
	rst 38h			;2087
	rst 38h			;2088
	rst 38h			;2089
	rst 38h			;208a
	rst 38h			;208b
	rst 38h			;208c
	rst 38h			;208d
	rst 38h			;208e
	rst 38h			;208f
	rst 38h			;2090
	rst 38h			;2091
	rst 38h			;2092
	rst 38h			;2093
	rst 38h			;2094
	rst 38h			;2095
	rst 38h			;2096
	rst 38h			;2097
	rst 38h			;2098
	rst 38h			;2099
	ld (hl),d			;209a
	ret nc			;209b
	nop			;209c
	nop			;209d
	nop			;209e
	nop			;209f
	nop			;20a0
	nop			;20a1
	nop			;20a2
	nop			;20a3
	nop			;20a4
	nop			;20a5
	nop			;20a6
	nop			;20a7
	nop			;20a8
	nop			;20a9
	nop			;20aa
	nop			;20ab
	nop			;20ac
	nop			;20ad
	nop			;20ae
	pop af			;20af
	nop			;20b0
	nop			;20b1
	rla			;20b2
	jr nz,$+44		;20b3
	rla			;20b5
	nop			;20b6
	nop			;20b7
	nop			;20b8
	nop			;20b9
	ld b,b			;20ba
	nop			;20bb
	nop			;20bc
	nop			;20bd
	nop			;20be
	pop af			;20bf
	nop			;20c0
	nop			;20c1
	rla			;20c2
	jr nz,$+44		;20c3
	rla			;20c5
	nop			;20c6
	nop			;20c7
	nop			;20c8
	nop			;20c9
	ld b,b			;20ca
	nop			;20cb
	nop			;20cc
	nop			;20cd
	nop			;20ce
	pop af			;20cf
	nop			;20d0
	nop			;20d1
	rla			;20d2
	jr nz,$+44		;20d3
	rla			;20d5
	nop			;20d6
	nop			;20d7
	nop			;20d8
	nop			;20d9
	ld b,b			;20da
	nop			;20db
	nop			;20dc
	nop			;20dd
	nop			;20de
	pop af			;20df
	nop			;20e0
	nop			;20e1
	rla			;20e2
	jr nz,$+44		;20e3
	rla			;20e5
	nop			;20e6
	nop			;20e7
	nop			;20e8
	nop			;20e9
	ld b,b			;20ea
	nop			;20eb
	nop			;20ec
	nop			;20ed
	nop			;20ee
	pop af			;20ef
	nop			;20f0
	nop			;20f1
	rla			;20f2
	jr nz,$+44		;20f3
	rla			;20f5
	nop			;20f6
	nop			;20f7
	nop			;20f8
	nop			;20f9
	ld b,b			;20fa
	nop			;20fb
	nop			;20fc
	nop			;20fd
	nop			;20fe
	pop af			;20ff
	nop			;2100
	nop			;2101
	rla			;2102
	jr nz,$+44		;2103
	rla			;2105
	nop			;2106
	nop			;2107
	nop			;2108
	nop			;2109
	ld b,b			;210a
	nop			;210b
	nop			;210c
	nop			;210d
	nop			;210e
	pop af			;210f
	nop			;2110
	nop			;2111
	rla			;2112
	jr nz,$+44		;2113
	rla			;2115
	nop			;2116
	nop			;2117
	nop			;2118
	nop			;2119
	ld b,b			;211a
	nop			;211b
	nop			;211c
	nop			;211d
	nop			;211e
	pop af			;211f
	nop			;2120
	nop			;2121
	rla			;2122
	jr nz,$+44		;2123
	rla			;2125
	nop			;2126
	nop			;2127
	nop			;2128
	nop			;2129
	ld b,b			;212a
	nop			;212b
	nop			;212c
	nop			;212d
	nop			;212e
	pop af			;212f
	nop			;2130
	nop			;2131
	rla			;2132
	jr nz,$+44		;2133
	rla			;2135
	nop			;2136
	nop			;2137
	nop			;2138
	nop			;2139
	ld b,b			;213a
	nop			;213b
	nop			;213c
	nop			;213d
	nop			;213e
	ld sp,hl			;213f
	nop			;2140
	nop			;2141
	inc bc			;2142
	dec c			;2143
	djnz $+5		;2144
	nop			;2146
	nop			;2147
	nop			;2148
	nop			;2149
	ld b,b			;214a
	nop			;214b
	nop			;214c
	nop			;214d
	rst 38h			;214e
	rst 38h			;214f
	nop			;2150
	nop			;2151
	pop af			;2152
	nop			;2153
	nop			;2154
	rla			;2155
	jr nz,$+44		;2156
	rla			;2158
	nop			;2159
	nop			;215a
	nop			;215b
	nop			;215c
	ld b,b			;215d
	nop			;215e
	nop			;215f
	ld a,b			;2160
	nop			;2161
	nop			;2162
	nop			;2163
	nop			;2164
	nop			;2165
	nop			;2166

[stderr]
Warning: Code might not be 8080 compatible!
