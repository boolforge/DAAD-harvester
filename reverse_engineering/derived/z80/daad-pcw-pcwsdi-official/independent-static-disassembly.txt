; z80dasm 1.1.6
; command line: z80dasm -a -g 0 /home/ubuntu/DAAD-harvester/reverse_engineering/artifacts/original/pcw/daad-pcw-pcwsdi-official/PCWSDI.BIN

	org	00000h

	ld hl,(00001h)		;0000
	ld de,0ead9h		;0003
	or a			;0006
	sbc hl,de		;0007
	jr nc,$+13		;0009
	ld de,00124h		;000b
	ld c,009h		;000e
	call 00005h		;0010
	jp 00000h		;0013
	ld de,0c080h		;0016
	ld bc,02257h		;0019
	ld hl,00180h		;001c
	ldir		;001f
	jp 0dfeeh		;0021
	ld d,h			;0024
	ld d,b			;0025
	ld b,c			;0026
	jr nz,$+102		;0027
	ld h,l			;0029
	ld l,l			;002a
	ld h,c			;002b
	ld (hl),e			;002c
	ld l,c			;002d
	ld h,c			;002e
	ld h,h			;002f
	ld l,a			;0030
	jr nz,$+114		;0031
	ld h,l			;0033
	ld (hl),c			;0034
	ld (hl),l			;0035
	ld h,l			;0036
	ld e,h			;0037
	ld l,a			;0038
	inc l			;0039
	jr nz,$+115		;003a
	ld (hl),l			;003c
	ld l,c			;003d
	ld (hl),h			;003e
	ld h,c			;003f
	jr nz,$+118		;0040
	ld l,a			;0042
	ld h,h			;0043
	ld l,a			;0044
	ld (hl),e			;0045
	jr nz,$+110		;0046
	ld l,a			;0048
	ld (hl),e			;0049
	jr nz,$+84		;004a
	ld d,e			;004c
	ld e,b			;004d
	jr nz,$+101		;004e
	ld h,c			;0050
	ld (hl),d			;0051
	ld h,a			;0052
	ld h,c			;0053
	ld h,h			;0054
	ld l,a			;0055
	ld (hl),e			;0056
	ld l,00dh		;0057
	inc h			;0059
	rst 38h			;005a
	rst 38h			;005b
	rst 38h			;005c
	rst 38h			;005d
	rst 38h			;005e
	rst 38h			;005f
	rst 38h			;0060
	rst 38h			;0061
	rst 38h			;0062
	rst 38h			;0063
	rst 38h			;0064
	rst 38h			;0065
	rst 38h			;0066
	rst 38h			;0067
	rst 38h			;0068
	rst 38h			;0069
	rst 38h			;006a
	rst 38h			;006b
	rst 38h			;006c
	rst 38h			;006d
	rst 38h			;006e
	rst 38h			;006f
	rst 38h			;0070
	rst 38h			;0071
	rst 38h			;0072
	rst 38h			;0073
	rst 38h			;0074
	rst 38h			;0075
	rst 38h			;0076
	rst 38h			;0077
	rst 38h			;0078
	rst 38h			;0079
	rst 38h			;007a
	rst 38h			;007b
	rst 38h			;007c
	rst 38h			;007d
	rst 38h			;007e
	rst 38h			;007f
	nop			;0080
	nop			;0081
	nop			;0082
	ld d,a			;0083
	ld (02b00h),hl		;0084
	nop			;0087
	ld e,d			;0088
	nop			;0089
	ret			;008a
	nop			;008b
	nop			;008c
	ret			;008d
	nop			;008e
	nop			;008f
	ret			;0090
	nop			;0091
	nop			;0092
	ret			;0093
	nop			;0094
	nop			;0095
	jp 0cedfh		;0096
	jp 0d4deh		;0099
	jp 0d590h		;009c
	jp 0d6f1h		;009f
	jp 0d45ch		;00a2
	jp 0d47ah		;00a5
	jp 0c103h		;00a8
	ld d,b			;00ab
	ld b,c			;00ac
	ld d,d			;00ad
	ld d,h			;00ae
	ld b,l			;00af
	ld (03231h),a		;00b0
	rst 38h			;00b3
	rst 38h			;00b4
	rst 38h			;00b5
	rst 38h			;00b6
	rst 38h			;00b7
	rst 38h			;00b8
	rst 38h			;00b9
	rst 38h			;00ba
	nop			;00bb
	nop			;00bc
	ld sp,03141h		;00bd
	ld b,d			;00c0
	ld sp,00042h		;00c1
	nop			;00c4
	ld sp,03141h		;00c5
	ld b,c			;00c8
	ld sp,0ff41h		;00c9
	rst 38h			;00cc
	rst 38h			;00cd
	rst 38h			;00ce
	rst 38h			;00cf
	rst 38h			;00d0
	rst 38h			;00d1
	rst 38h			;00d2
	rst 38h			;00d3
	rst 38h			;00d4
	rst 38h			;00d5
	rst 38h			;00d6
	rst 38h			;00d7
	rst 38h			;00d8
	rst 38h			;00d9
	rst 38h			;00da
	rst 38h			;00db
	rst 38h			;00dc
	rst 38h			;00dd
	rst 38h			;00de
	rst 38h			;00df
	rst 38h			;00e0
	rst 38h			;00e1
	rst 38h			;00e2
	rst 38h			;00e3
	rst 38h			;00e4
	rst 38h			;00e5
	rst 38h			;00e6
	rst 38h			;00e7
	rst 38h			;00e8
	rst 38h			;00e9
	rst 38h			;00ea
	rst 38h			;00eb
	rst 38h			;00ec
	rst 38h			;00ed
	rst 38h			;00ee
	rst 38h			;00ef
	rst 38h			;00f0
	rst 38h			;00f1
	rst 38h			;00f2
	rst 38h			;00f3
	rst 38h			;00f4
	rst 38h			;00f5
	rst 38h			;00f6
	rst 38h			;00f7
	rst 38h			;00f8
	rst 38h			;00f9
	rst 38h			;00fa
	rst 38h			;00fb
	rst 38h			;00fc
	rst 38h			;00fd
	rst 38h			;00fe
	rst 38h			;00ff
	rst 38h			;0100
	rst 38h			;0101
	rst 38h			;0102
	set 0,(ix-001h)		;0103
	pop hl			;0107
	dec hl			;0108
	dec hl			;0109
	dec hl			;010a
	dec hl			;010b
	ld (0e1d7h),hl		;010c
	ld ix,0e2d7h		;010f
	res 6,(ix-001h)		;0113
	ld a,(0e19fh)		;0117
	or a			;011a
	jr nz,$+7		;011b
	ld a,008h		;011d
	call 0d4deh		;011f
	call 0c14eh		;0122
	call 0d6f1h		;0125
	call 0d45ch		;0128
	ld a,(0e19fh)		;012b
	or a			;012e
	call z,0d4cfh		;012f
	res 2,(ix-001h)		;0132
	call 0d730h		;0136
	ld hl,0dfeeh		;0139
	ld (hl),00dh		;013c
	ld (0e196h),hl		;013e
	ld (ix-007h),078h		;0141
	ld hl,(0e19eh)		;0145
	ld a,h			;0148
	or a			;0149
	jp z,0c1a1h		;014a
	jp (hl)			;014d
	call 0d58eh		;014e
	ld a,(0e1dbh)		;0151
	ld hl,0c195h		;0154
	bit 0,(ix-001h)		;0157
	jr nz,$+20		;015b
	inc a			;015d
	ld hl,0c184h		;015e
	cp 00dh		;0161
	jr z,$+30		;0163
	ld hl,0c18ah		;0165
	cp 01bh		;0168
	jr z,$+23		;016a
	ld hl,0c18eh		;016c
	ld e,a			;016f
	push de			;0170
	call 0ceaah		;0171
	pop de			;0174
	call 0ce59h		;0175
	bit 0,(ix-001h)		;0178
	res 0,(ix-001h)		;017c
	ret			;0180
	jp 0ceaah		;0181
	cp l			;0184
	xor l			;0185
	cp d			;0186
	cp (hl)			;0187
	or h			;0188
	push af			;0189
	or (hl)			;018a
	ret nc			;018b
	or b			;018c
	rst 18h			;018d
	cp d			;018e
	adc a,l			;018f
	adc a,l			;0190
	sub b			;0191
	adc a,l			;0192
	rst 18h			;0193
	push af			;0194
	cp b			;0195
	sbc a,(hl)			;0196
	sub d			;0197
	sbc a,d			;0198
	rst 18h			;0199
	cp d			;019a
	adc a,l			;019b
	adc a,l			;019c
	sub b			;019d
	adc a,l			;019e
	rst 18h			;019f
	push af			;01a0
	ld sp,0ead7h		;01a1
	ld de,0e22ch		;01a4
	ld a,008h		;01a7
	ld hl,0e21ch		;01a9
	ld bc,00010h		;01ac
	ldir		;01af
	dec a			;01b1
	jr nz,$-9		;01b2
	ld (0e2bdh),a		;01b4
	ld de,0e2beh		;01b7
	ld hl,0e21ch		;01ba
	ld bc,00010h		;01bd
	ldir		;01c0
	ld hl,0e2d7h		;01c2
	xor a			;01c5
	ld b,a			;01c6
	ld (hl),a			;01c7
	inc hl			;01c8
	djnz $-2		;01c9
	set 7,(ix+01dh)		;01cb
	call 0c1e6h		;01cf
	jr z,$+13		;01d2
	ld hl,0e1deh		;01d4
	cp (hl)			;01d7
	jr z,$-9		;01d8
	call 0d7b4h		;01da
	jr $-14		;01dd
	ld a,001h		;01df
	out (0f8h),a		;01e1
	jp 00000h		;01e3
	ld (0e1d3h),sp		;01e6
	xor a			;01ea
	ld (0e1d6h),a		;01eb
	call 0ce17h		;01ee
	xor a			;01f1
	push af			;01f2
	ld (0e1d9h),a		;01f3
	ld (0e1d5h),a		;01f6
	xor a			;01f9
	cp (hl)			;01fa
	jr nz,$+26		;01fb
	pop de			;01fd
	ld a,d			;01fe
	or a			;01ff
	jr z,$+7		;0200
	pop hl			;0202
	pop bc			;0203
	jp 0c6a1h		;0204
	ld hl,0e1d5h		;0207
	cp (hl)			;020a
	ret z			;020b
	ld a,e			;020c
	ld (0e1d6h),a		;020d
	dec (hl)			;0210
	pop bc			;0211
	pop hl			;0212
	jr $+34		;0213
	ld a,(hl)			;0215
	inc hl			;0216
	cp 0ffh		;0217
	jr z,$+7		;0219
	cp (ix+021h)		;021b
	jr nz,$+12		;021e
	ld a,(hl)			;0220
	cp 0ffh		;0221
	jr z,$+13		;0223
	cp (ix+022h)		;0225
	jr z,$+8		;0228
	inc hl			;022a
	inc hl			;022b
	inc hl			;022c
	jp 0c1f9h		;022d
	inc hl			;0230
	ld c,(hl)			;0231
	inc hl			;0232
	ld b,(hl)			;0233
	inc hl			;0234
	ld a,(bc)			;0235
	cp 0ffh		;0236
	jr z,$-63		;0238
	ld (0e2d2h),a		;023a
	push hl			;023d
	ld d,000h		;023e
	sla a		;0240
	ld e,a			;0242
	inc bc			;0243
	ld a,(bc)			;0244
	ld hl,0c845h		;0245
	add hl,de			;0248
	ld e,(hl)			;0249
	inc hl			;024a
	ld d,(hl)			;024b
	push de			;024c
	ld d,000h		;024d
	ld e,a			;024f
	bit 7,(ix-005h)		;0250
	jr z,$+8		;0254
	ld hl,0e2d7h		;0256
	add hl,de			;0259
	ld a,(hl)			;025a
	ld e,a			;025b
	ld hl,0e3d7h		;025c
	add hl,de			;025f
	push hl			;0260
	ld hl,0e2d7h		;0261
	add hl,de			;0264
	pop de			;0265
	ret			;0266
	cp (ix+026h)		;0267
	jr z,$+99		;026a
	jr $+14		;026c
	cp (ix+026h)		;026e
	jr nz,$+92		;0271
	jr $+7		;0273
	cp (ix+026h)		;0275
	jr c,$+85		;0278
	pop hl			;027a
	jp 0c1f9h		;027b
	cp (ix+026h)		;027e
	jr c,$-7		;0281
	jr z,$-9		;0283
	jr $+72		;0285
	ld a,(de)			;0287
	cp (ix+026h)		;0288
	jr z,$+66		;028b
	cp 0fdh		;028d
	jr nc,$+62		;028f
	jr $-23		;0291
	ld a,(de)			;0293
	cp (ix+026h)		;0294
	jr z,$-29		;0297
	cp 0fdh		;0299
	jr nc,$-33		;029b
	jr $+48		;029d
	ld a,(de)			;029f
	cp 0fdh		;02a0
	jr z,$+43		;02a2
	jr $-42		;02a4
	ld a,(de)			;02a6
	cp 0fdh		;02a7
	jr nz,$+36		;02a9
	jr $-49		;02ab
	ld a,(de)			;02ad
	cp 0feh		;02ae
	jr nz,$-54		;02b0
	jr $+27		;02b2
	ld a,(de)			;02b4
	cp 0feh		;02b5
	jr z,$-61		;02b7
	jr $+20		;02b9
	call 0ce36h		;02bb
	cp h			;02be
	jr nc,$+14		;02bf
	jr $-71		;02c1
	ld a,(hl)			;02c3
	or a			;02c4
	jr nz,$-75		;02c5
	jr $+6		;02c7
	ld a,(hl)			;02c9
	or a			;02ca
	jr z,$-81		;02cb
	inc bc			;02cd
	pop hl			;02ce
	jp 0c235h		;02cf
	inc bc			;02d2
	ld a,(bc)			;02d3
	cp (hl)			;02d4
	jr nz,$-91		;02d5
	jr $-10		;02d7
	call 0c5c6h		;02d9
	jr $-8		;02dc
	inc bc			;02de
	ld a,(bc)			;02df
	cp (hl)			;02e0
	jr z,$-103		;02e1
	jr $-22		;02e3
	call 0c5c6h		;02e5
	jr $-8		;02e8
	call 0c5c6h		;02ea
	jr $+4		;02ed
	inc bc			;02ef
	ld a,(bc)			;02f0
	cp (hl)			;02f1
	jr c,$-37		;02f2
	jr $-122		;02f4
	call 0c5c6h		;02f6
	jr $+4		;02f9
	inc bc			;02fb
	ld a,(bc)			;02fc
	cp (hl)			;02fd
	jp c,0c27ah		;02fe
	jp z,0c27ah		;0301
	jp 0c2cdh		;0304
	cp (ix+023h)		;0307
	jr z,$-61		;030a
	jp 0c27ah		;030c
	cp (ix+024h)		;030f
	jr $-8		;0312
	cp (ix+02bh)		;0314
	jr $-13		;0317
	cp (ix+02ch)		;0319
	jr $-18		;031c
	cp (ix+02dh)		;031e
	jr $-23		;0321
	ld l,00ch		;0323
	push bc			;0325
	call 0d14dh		;0326
	pop bc			;0329
	ld a,(hl)			;032a
	ld l,01eh		;032b
	call 0ce27h		;032d
	cpl			;0330
	cp (hl)			;0331
	jp nz,0c27ah		;0332
	jp 0c510h		;0335
	ld l,00dh		;0338
	call 0d14dh		;033a
	ld a,(hl)			;033d
	ld l,01fh		;033e
	call 0ce27h		;0340
	cpl			;0343
	sub (hl)			;0344
	jr z,$+13		;0345
	xor a			;0347
	ld (0e2fdh),a		;0348
	ld sp,(0e1d3h)		;034b
	jp 0c1eah		;034f
	ld sp,(0e1d3h)		;0352
	or a			;0356
	ret			;0357
	res 2,(ix-001h)		;0358
	call 0cea1h		;035c
	pop hl			;035f
	set 4,(ix-001h)		;0360
	jp 0c1fdh		;0364
	ld l,00fh		;0367
	jr $-13		;0369
	push bc			;036b
	call 0d43eh		;036c
	pop bc			;036f
	jp 0c510h		;0370
	inc bc			;0373
	call 0c08ah		;0374
	jp 0c50fh		;0377
	inc bc			;037a
	call 0c08dh		;037b
	jp 0c50fh		;037e
	push bc			;0381
	call 0d518h		;0382
	pop bc			;0385
	jp 0c510h		;0386
	ld (ix+001h),000h		;0389
	ld hl,0e3d7h		;038d
	ld a,(hl)			;0390
	cp 0ffh		;0391
	jp z,0c510h		;0393
	cp 0fdh		;0396
	jr c,$+6		;0398
	ld a,(0e2fdh)		;039a
	ld (hl),a			;039d
	inc hl			;039e
	jr $-15		;039f
	push bc			;03a1
	ld a,(0e2fdh)		;03a2
	ld bc,0fefdh		;03a5
	call 0ca0fh		;03a8
	pop bc			;03ab
	ld l,01ah		;03ac
	jp z,0c43dh		;03ae
	dec bc			;03b1
	call 0ca5ch		;03b2
	ld a,(de)			;03b5
	ld l,019h		;03b6
	cp 0fdh		;03b8
	jr nc,$+74		;03ba
	inc l			;03bc
	cp (ix+026h)		;03bd
	jr nz,$+129		;03c0
	ld a,(0e30ah)		;03c2
	call 0c988h		;03c5
	call 0c962h		;03c8
	ld l,02bh		;03cb
	add a,h			;03cd
	jr c,$+115		;03ce
	ld hl,0e30bh		;03d0
	cp (hl)			;03d3
	jr z,$+6		;03d4
	ld l,02bh		;03d6
	jr nc,$+105		;03d8
	ld l,024h		;03da
	ld h,01bh		;03dc
	push hl			;03de
	ld a,(0e2d8h)		;03df
	ld hl,0e2fch		;03e2
	cp (hl)			;03e5
	jr nc,$+15		;03e6
	ex de,hl			;03e8
	ld (hl),0feh		;03e9
	inc (ix+001h)		;03eb
	pop hl			;03ee
	call 0cea1h		;03ef
	jp 0c50fh		;03f2
	pop hl			;03f5
	ld l,h			;03f6
	ld a,(0e1d9h)		;03f7
	or a			;03fa
	jp z,0c358h		;03fb
	ld a,(00103h)		;03fe
	ld (0e1d9h),a		;0401
	jp 0c358h		;0404
	call 0ca00h		;0407
	jr z,$+51		;040a
	dec bc			;040c
	call 0ca5ch		;040d
	ld a,(de)			;0410
	cp 0fdh		;0411
	ld l,018h		;0413
	jr z,$-17		;0415
	ld l,031h		;0417
	cp (ix+026h)		;0419
	jr z,$-24		;041c
	ld l,01ch		;041e
	cp 0feh		;0420
	jr nz,$-30		;0422
	ld a,(0e2fdh)		;0424
	ld (de),a			;0427
	dec (ix+001h)		;0428
	ld l,027h		;042b
	jr $-62		;042d
	push bc			;042f
	ld a,0fdh		;0430
	ld b,0feh		;0432
	call 0ca0ch		;0434
	pop bc			;0437
	dec bc			;0438
	jr nz,$+14		;0439
	ld l,017h		;043b
	jr c,$+4		;043d
	ld l,008h		;043f
	jp 0c358h		;0441
	call 0ca5ch		;0444
	ld a,(de)			;0447
	ld l,032h		;0448
	cp 0feh		;044a
	jr z,$-11		;044c
	cp (ix+026h)		;044e
	jr z,$-16		;0451
	ld l,017h		;0453
	cp 0fdh		;0455
	jr nz,$-22		;0457
	call 0c9e3h		;0459
	inc l			;045c
	jr nc,$-28		;045d
	ld l,026h		;045f
	ld h,02ah		;0461
	jp 0c3deh		;0463
	push bc			;0466
	ld b,0fdh		;0467
	call 0ca0ah		;0469
	pop bc			;046c
	ld l,01ch		;046d
	jr z,$-50		;046f
	dec bc			;0471
	call 0ca5ch		;0472
	ld a,(de)			;0475
	ld l,031h		;0476
	cp (ix+026h)		;0478
	jr z,$-58		;047b
	ld l,01dh		;047d
	cp 0fdh		;047f
	jr z,$-64		;0481
	dec l			;0483
	jr c,$-67		;0484
	call 0c9e3h		;0486
	jr nc,$-72		;0489
	ex de,hl			;048b
	ld (hl),0fdh		;048c
	dec (ix+001h)		;048e
	ld l,025h		;0491
	jp 0c3efh		;0493
	ld e,a			;0496
	ld d,000h		;0497
	push bc			;0499
	call 0d470h		;049a
	pop bc			;049d
	jr $+113		;049e
	ld (0e2fdh),a		;04a0
	jr $+108		;04a3
	ld de,(0010eh)		;04a5
	ld hl,00104h		;04a9
	jr $+22		;04ac
	set 6,(ix-001h)		;04ae
	ld de,(00110h)		;04b2
	ld hl,00105h		;04b6
	jr $+9		;04b9
	ld de,(00112h)		;04bb
	ld hl,00106h		;04bf
	cp (hl)			;04c2
	jr nc,$+17		;04c3
	ex de,hl			;04c5
	ld d,000h		;04c6
	ld e,a			;04c8
	add hl,de			;04c9
	add hl,de			;04ca
	ld e,(hl)			;04cb
	inc hl			;04cc
	ld d,(hl)			;04cd
	ex de,hl			;04ce
	call 0ceaah		;04cf
	jr $+61		;04d2
	ld d,007h		;04d4
	jp 0c103h		;04d6
	call 0ca5ch		;04d9
	ex de,hl			;04dc
	ld a,(hl)			;04dd
	cp 0feh		;04de
	ld (hl),0fch		;04e0
	jr nz,$+45		;04e2
	dec (ix+001h)		;04e4
	jr $+40		;04e7
	call 0ca5ch		;04e9
	ld a,(de)			;04ec
	cp 0feh		;04ed
	ld a,(0e2fdh)		;04ef
	ld (de),a			;04f2
	jr $-17		;04f3
	inc bc			;04f5
	ld a,(bc)			;04f6
	call 0ca5ch		;04f7
	ld l,a			;04fa
	ld h,000h		;04fb
	ld a,(de)			;04fd
	push de			;04fe
	ld de,0e3d7h		;04ff
	add hl,de			;0502
	ld d,(hl)			;0503
	ld (hl),a			;0504
	pop hl			;0505
	ld (hl),d			;0506
	jr $+8		;0507
	ld (hl),0ffh		;0509
	jr $+4		;050b
	ld (hl),000h		;050d
	inc bc			;050f
	pop hl			;0510
	set 4,(ix-001h)		;0511
	jp 0c235h		;0515
	inc bc			;0518
	ld d,a			;0519
	ld a,(bc)			;051a
	add a,(hl)			;051b
	jr nc,$+36		;051c
	ld a,0ffh		;051e
	jr $+32		;0520
	inc bc			;0522
	ld a,(bc)			;0523
	ld d,a			;0524
	ld a,(hl)			;0525
	sub d			;0526
	jr nc,$+3		;0527
	xor a			;0529
	ld (hl),a			;052a
	jr $-28		;052b
	inc bc			;052d
	push af			;052e
	ld a,(bc)			;052f
	push hl			;0530
	ld e,a			;0531
	ld d,000h		;0532
	ld hl,0e2d7h		;0534
	add hl,de			;0537
	ld e,(hl)			;0538
	pop hl			;0539
	pop af			;053a
	jr $+7		;053b
	inc bc			;053d
	ld d,a			;053e
	ld a,(bc)			;053f
	ld e,a			;0540
	ld a,d			;0541
	cp 026h		;0542
	jr nz,$+14		;0544
	ld a,(00104h)		;0546
	cp e			;0549
	ld d,001h		;054a
	jp c,0c103h		;054c
	jp z,0c103h		;054f
	ld (hl),e			;0552
	jr $-68		;0553
	call 0d58eh		;0555
	jr $-72		;0558
	ld e,(hl)			;055a
	inc hl			;055b
	ld d,(hl)			;055c
	call 0ce5bh		;055d
	jr $-81		;0560
	ld e,(hl)			;0562
	call 0ce59h		;0563
	jr $-87		;0566
	inc bc			;0568
	ex de,hl			;0569
	ld a,(bc)			;056a
	call 0c9dch		;056b
	cp (hl)			;056e
	jp nz,0c27ah		;056f
	jp 0c2cdh		;0572
	inc bc			;0575
	ex de,hl			;0576
	ld a,(bc)			;0577
	call 0c9dch		;0578
	cp (hl)			;057b
	jp nz,0c2cdh		;057c
	jp 0c27ah		;057f
	call 0ca5ch		;0582
	inc bc			;0585
	ex de,hl			;0586
	ld a,(bc)			;0587
	call 0c9dch		;0588
	push af			;058b
	ld a,(hl)			;058c
	jr $+31		;058d
	call 0c9dch		;058f
	push af			;0592
	ld a,(0e30ah)		;0593
	jr $+19		;0596
	ld a,(hl)			;0598
	cp 0ffh		;0599
	jr nz,$+8		;059b
	ld d,002h		;059d
	jp 0c103h		;059f
	ld a,(de)			;05a2
	push af			;05a3
	inc bc			;05a4
	ld a,(bc)			;05a5
	call 0ca5ch		;05a6
	call 0c9f7h		;05a9
	cp 0feh		;05ac
	jr nz,$+5		;05ae
	dec (ix+001h)		;05b0
	pop af			;05b3
	cp 0feh		;05b4
	jr nz,$+5		;05b6
	inc (ix+001h)		;05b8
	ld (hl),a			;05bb
	jp 0c50fh		;05bc
	ex de,hl			;05bf
	call 0c5c7h		;05c0
	jp 0c540h		;05c3
	ex de,hl			;05c6
	inc bc			;05c7
	ld a,(bc)			;05c8
	push bc			;05c9
	ld c,a			;05ca
	ld b,000h		;05cb
	ld hl,0e2d7h		;05cd
	add hl,bc			;05d0
	ld a,(de)			;05d1
	ld e,a			;05d2
	ld d,c			;05d3
	pop bc			;05d4
	ret			;05d5
	call 0c5c6h		;05d6
	jp 0c51bh		;05d9
	call 0c5c6h		;05dc
	jp 0c524h		;05df
	ld d,(ix+026h)		;05e2
	set 1,(ix-001h)		;05e5
	call 0cad9h		;05e9
	jp 0c510h		;05ec
	push bc			;05ef
	or a			;05f0
	jr nz,$+7		;05f1
	call 0cb8dh		;05f3
	jr $+5		;05f6
	call 0cb81h		;05f8
	pop bc			;05fb
	jp c,0c2cdh		;05fc
	set 4,(ix-001h)		;05ff
	jp 0c27ah		;0603
	call 0c9dch		;0606
	ld d,a			;0609
	res 1,(ix-001h)		;060a
	call 0cad9h		;060e
	jp 0c50fh		;0611
	inc bc			;0614
	ld hl,00107h		;0615
	cp (hl)			;0618
	jr nc,$+40		;0619
	or a			;061b
	jr z,$+37		;061c
	call 0ce17h		;061e
	push af			;0621
	ld a,(0e1d5h)		;0622
	inc a			;0625
	ld (0e1d5h),a		;0626
	cp 00ah		;0629
	jp z,0c646h		;062b
	pop af			;062e
	push bc			;062f
	ld de,(0e1d6h)		;0630
	ld d,000h		;0634
	push de			;0636
	ld (0e1d6h),a		;0637
	res 4,(ix-001h)		;063a
	jp 0c1f9h		;063e
	ld d,006h		;0641
	jp 0c103h		;0643
	pop af			;0646
	ld d,003h		;0647
	jp 0c103h		;0649
	pop hl			;064c
	ld a,(0e1d6h)		;064d
	call 0ce17h		;0650
	jp 0c1f9h		;0653
	ld (0e307h),a		;0656
	inc bc			;0659
	ld a,(bc)			;065a
	ld e,a			;065b
	ld d,0f8h		;065c
	jr $+28		;065e
	inc bc			;0660
	jp 0c50fh		;0661
	ld (0e2fch),a		;0664
	inc bc			;0667
	ld a,(bc)			;0668
	ld (0e30bh),a		;0669
	jp 0c50fh		;066c
	ld (0e300h),a		;066f
	inc bc			;0672
	ld a,(bc)			;0673
	rlca			;0674
	rlca			;0675
	rlca			;0676
	ld e,a			;0677
	ld d,0c7h		;0678
	ld a,(0e308h)		;067a
	xor e			;067d
	and d			;067e
	xor e			;067f
	ld (0e308h),a		;0680
	jp 0c50fh		;0683
	res 2,(ix-001h)		;0686
	jp 0c510h		;068a
	call 0c9dch		;068d
	ld (0e309h),a		;0690
	ld a,(0e1d9h)		;0693
	or a			;0696
	ld d,004h		;0697
	jp nz,0c103h		;0699
	pop hl			;069c
	res 4,(ix-001h)		;069d
	push bc			;06a1
	push hl			;06a2
	ld de,0ff00h		;06a3
	push de			;06a6
	push hl			;06a7
	push bc			;06a8
	ld a,(0e1d9h)		;06a9
	ld e,a			;06ac
	ld a,(0e309h)		;06ad
	call 0cab1h		;06b0
	jr nc,$+41		;06b3
	ld (0e2f9h),a		;06b5
	cp (ix+02ch)		;06b8
	inc hl			;06bb
	ld a,(hl)			;06bc
	ld (0e2fah),a		;06bd
	jr nz,$+15		;06c0
	ld a,(0e304h)		;06c2
	cp 0ffh		;06c5
	jr z,$+5		;06c7
	cp (hl)			;06c9
	jr nz,$+5		;06ca
	pop hl			;06cc
	jr $-32		;06cd
	ld a,e			;06cf
	ld (0e1d9h),a		;06d0
	dec a			;06d3
	call 0ca5ch		;06d4
	pop de			;06d7
	pop bc			;06d8
	jp 0c50fh		;06d9
	xor a			;06dc
	ld (0e1d9h),a		;06dd
	pop bc			;06e0
	pop hl			;06e1
	pop hl			;06e2
	pop hl			;06e3
	pop hl			;06e4
	jp 0c1fdh		;06e5
	push af			;06e8
	call 0c5c7h		;06e9
	pop af			;06ec
	push hl			;06ed
	call 0c988h		;06ee
	ld a,h			;06f1
	pop hl			;06f2
	jp 0c540h		;06f3
	ld d,a			;06f6
	call 0c962h		;06f7
	jp 0c540h		;06fa
	push hl			;06fd
	call 0ce36h		;06fe
	ld d,a			;0701
	ld a,h			;0702
	pop hl			;0703
	jp 0c540h		;0704
	ld (0e1cch),a		;0707
	call 0ca00h		;070a
	jp z,0c43dh		;070d
	jr $+10		;0710
	call 0ca5ch		;0712
	inc bc			;0715
	ld a,(bc)			;0716
	ld (0e1cch),a		;0717
	ld a,(de)			;071a
	cp 0fdh		;071b
	ld l,018h		;071d
	jp z,0c358h		;071f
	ld l,031h		;0722
	cp (ix+026h)		;0724
	jp z,0c358h		;0727
	cp 0feh		;072a
	ld l,01ch		;072c
	jp nz,0c358h		;072e
	ld a,(0e1cch)		;0731
	ld (de),a			;0734
	dec (ix+001h)		;0735
	ld l,02ch		;0738
	call 0c792h		;073a
	jp 0c3efh		;073d
	ld (0e1cch),a		;0740
	push bc			;0743
	ld bc,0fefdh		;0744
	call 0ca0fh		;0747
	jr nz,$+9		;074a
	ld a,(0e2fdh)		;074c
	ld b,a			;074f
	call 0ca0ch		;0750
	pop bc			;0753
	jr nz,$+10		;0754
	ld a,(0e1cch)		;0756
	jr c,$+49		;0759
	jp 0c43fh		;075b
	ld a,(0e1cch)		;075e
	jr $+7		;0761
	call 0ca5ch		;0763
	inc bc			;0766
	ld a,(bc)			;0767
	ld h,a			;0768
	ld a,(de)			;0769
	ld l,019h		;076a
	cp 0fdh		;076c
	jp nc,0c358h		;076e
	cp h			;0771
	jr nz,$+16		;0772
	ld a,h			;0774
	push de			;0775
	call 0c9f7h		;0776
	pop de			;0779
	cp 0fdh		;077a
	jp nc,0c3dah		;077c
	jp 0c3c2h		;077f
	ld l,02dh		;0782
	cp (ix+026h)		;0784
	ld a,h			;0787
	jr z,$+4		;0788
	ld l,034h		;078a
	call 0c792h		;078c
	jp 0c358h		;078f
	push af			;0792
	call 0cea1h		;0793
	pop af			;0796
	set 4,(ix-003h)		;0797
	set 5,(ix-003h)		;079b
	call 0cdb6h		;079f
	ld l,033h		;07a2
	ret			;07a4
	push bc			;07a5
	ld b,0fdh		;07a6
	call 0ca0ah		;07a8
	pop bc			;07ab
	jp 0c510h		;07ac
	push bc			;07af
	ld hl,(00118h)		;07b0
	ld de,0e3d7h		;07b3
	ld a,(hl)			;07b6
	ldi		;07b7
	cp 0feh		;07b9
	jr c,$-5		;07bb
	jr nz,$-18		;07bd
	inc (ix+001h)		;07bf
	jr $-12		;07c2
	res 4,(ix-001h)		;07c4
	pop hl			;07c8
	jp 0c1fdh		;07c9
	push bc			;07cc
	ld a,(hl)			;07cd
	push hl			;07ce
	ld hl,00104h		;07cf
	cp (hl)			;07d2
	call c,0cb67h		;07d3
	pop hl			;07d6
	pop bc			;07d7
	jp nc,0c27ah		;07d8
	ld (hl),a			;07db
	jp 0c50fh		;07dc
	bit 4,(ix-001h)		;07df
	jp nz,0c2ceh		;07e3
	jp 0c27ah		;07e6
	bit 4,(ix-001h)		;07e9
	jp nz,0c27ah		;07ed
	jp 0c2ceh		;07f0
	ld h,000h		;07f3
	ld l,a			;07f5
	sla l		;07f6
	jr nc,$+3		;07f8
	dec h			;07fa
	add hl,hl			;07fb
	pop de			;07fc
	add hl,de			;07fd
	jp 0c1f9h		;07fe
	cp 0ffh		;0801
	jr z,$+5		;0803
	ld (0e2f8h),a		;0805
	inc bc			;0808
	ld a,(bc)			;0809
	cp 0ffh		;080a
	jr z,$+5		;080c
	ld (0e2f9h),a		;080e
	jp 0c50fh		;0811
	call 0ca5ch		;0814
	jp 0c50fh		;0817
	ld a,020h		;081a
	call 0d590h		;081c
	jp 0c510h		;081f
	call 0c945h		;0822
	jp nz,0c2cdh		;0825
	jp 0c27ah		;0828
	call 0c945h		;082b
	jp nz,0c27ah		;082e
	jp 0c2cdh		;0831
	ld l,a			;0834
	inc bc			;0835
	ld a,(bc)			;0836
	inc bc			;0837
	ld h,a			;0838
	call 0c83fh		;0839
	jp 0c510h		;083c
	jp (hl)			;083f
	ld d,005h		;0840
	jp 0c103h		;0842
	ld h,a			;0845
	jp nz,0c26eh		;0846
	ld (hl),l			;0849
	jp nz,0c27eh		;084a
	add a,a			;084d
	jp nz,0c293h		;084e
	sbc a,a			;0851
	jp nz,0c2a6h		;0852
	xor l			;0855
	jp nz,0c2b4h		;0856
	cp e			;0859
	jp nz,0c2c3h		;085a
	ret			;085d
	jp nz,0c2d2h		;085e
	rst 28h			;0861
	jp nz,0c2fbh		;0862
	rlca			;0865
	jp 0c30fh		;0866
	ld a,d			;0869
	jp 0c4a5h		;086a
	inc hl			;086d
	jp 0c338h		;086e
	ld e,a			;0871
	jp 0c367h		;0872
	ld l,e			;0875
	jp 0d03bh		;0876
	jp c,05ad0h		;0879
	push bc			;087c
	dec bc			;087d
	rst 8			;087e
	add a,c			;087f
	jp 0c389h		;0880
	and c			;0883
	jp 0c407h		;0884
	ld h,(hl)			;0887
	call nz,0c42fh		;0888
	sub (hl)			;088b
	call nz,0c801h		;088c
	and b			;088f
	call nz,0c4aeh		;0890
	ld b,h			;0893
	call nz,0c3b2h		;0894
	dec c			;0897
	call nz,0c472h		;0898
	exx			;089b
	call nz,0c4e9h		;089c
	push af			;089f
	call nz,0c582h		;08a0
	add hl,bc			;08a3
	push bc			;08a4
	dec c			;08a5
	push bc			;08a6
	jr $-57		;08a7
	ld (03dc5h),hl		;08a9
	push bc			;08ac
	ld d,l			;08ad
	push bc			;08ae
	ld h,d			;08af
	push bc			;08b0
	cp e			;08b1
	call nz,0c568h		;08b2
	inc d			;08b5
	ret z			;08b6
	ld a,(de)			;08b7
	ret z			;08b8
	ld (02bc8h),hl		;08b9
	ret z			;08bc
	jp po,073c5h		;08bd
	jp 0d001h		;08c0
	ld de,0d2d0h		;08c3
	rst 8			;08c6
	sub 0cfh		;08c7
	sbc a,0cfh		;08c9
	pop hl			;08cb
	rst 8			;08cc
	inc d			;08cd
	jp 0c319h		;08ce
	ld e,0c3h		;08d1
	sub 0c5h		;08d3
	call c,0efc5h		;08d5
	push bc			;08d8
	ld b,0c6h		;08d9
	inc d			;08db
	add a,0d9h		;08dc
	jp nz,0c4b2h		;08de
	ld a,(hl)			;08e1
	rst 8			;08e2
	sbc a,0c2h		;08e3
	push hl			;08e5
	jp nz,0cef1h		;08e6
	sub (hl)			;08e9
	rst 8			;08ea
	ld d,(hl)			;08eb
	add a,000h		;08ec
	rst 8			;08ee
	adc a,l			;08ef
	add a,060h		;08f0
	add a,060h		;08f2
	add a,075h		;08f4
	push bc			;08f6
	ret pe			;08f7
	add a,012h		;08f8
	rst 0			;08fa
	ld h,e			;08fb
	rst 0			;08fc
	add a,(hl)			;08fd
	add a,064h		;08fe
	add a,0f6h		;0900
	add a,0fdh		;0902
	add a,06fh		;0904
	add a,023h		;0906
	rst 8			;0908
	scf			;0909
	rst 8			;090a
	ld e,e			;090b
	rst 8			;090c
	and l			;090d
	rst 0			;090e
	inc (hl)			;090f
	ret z			;0910
	adc a,a			;0911
	push bc			;0912
	call nz,007c7h		;0913
	rst 0			;0916
	ld b,b			;0917
	rst 0			;0918
	call z,0b2c7h		;0919
	rst 8			;091c
	ld c,h			;091d
	add a,086h		;091e
	rst 8			;0920
	ld d,d			;0921
	jp 0cfe4h		;0922
	or 0c2h		;0925
	jp pe,0dfc2h		;0927
	rst 0			;092a
	jp (hl)			;092b
	rst 0			;092c
	di			;092d
	rst 0			;092e
	ld c,e			;092f
	jp 0cf4eh		;0930
	ret nz			;0933
	push bc			;0934
	ld b,b			;0935
	ret z			;0936
	and d			;0937
	push bc			;0938
	ld b,b			;0939
	ret z			;093a
	sbc a,b			;093b
	push bc			;093c
	ld b,b			;093d
	ret z			;093e
	cp a			;093f
	push bc			;0940
	dec l			;0941
	push bc			;0942
	xor a			;0943
	rst 0			;0944
	ld h,a			;0945
	rrca			;0946
	rrca			;0947
	rrca			;0948
	and 01fh		;0949
	ld l,a			;094b
	ld a,03bh		;094c
	sub l			;094e
	ld (0c95fh),a		;094f
	ld a,h			;0952
	rlca			;0953
	rlca			;0954
	rlca			;0955
	and 038h		;0956
	or 046h		;0958
	ld (0c960h),a		;095a
	defb 0ddh,0cbh,000h,000h	;rlc (ix+000h) & ld b,(ix+000h)		;095d
	ret			;0961
	xor a			;0962
	push hl			;0963
	push de			;0964
	push bc			;0965
	ld bc,(00102h)		;0966
	ld c,a			;096a
	ld de,0e3d7h		;096b
	push af			;096e
	ld a,(de)			;096f
	cp 0fdh		;0970
	ld l,c			;0972
	ld h,000h		;0973
	call nc,0c989h		;0975
	pop af			;0978
	add a,h			;0979
	jr nc,$+6		;097a
	ld a,0ffh		;097c
	jr $+6		;097e
	inc c			;0980
	inc de			;0981
	djnz $-20		;0982
	pop bc			;0984
	pop de			;0985
	pop hl			;0986
	ret			;0987
	ld l,a			;0988
	ex de,hl			;0989
	xor a			;098a
	ld d,a			;098b
	ld (0e1dah),a		;098c
	call 0c995h		;098f
	ex de,hl			;0992
	ld a,l			;0993
	ret			;0994
	push hl			;0995
	push de			;0996
	push bc			;0997
	call 0c9efh		;0998
	and 07fh		;099b
	add a,0c0h		;099d
	res 6,a		;099f
	res 7,a		;09a1
	jr nc,$+45		;09a3
	and 03fh		;09a5
	jr z,$+40		;09a7
	ld c,a			;09a9
	ld a,(0e1dah)		;09aa
	inc a			;09ad
	ld (0e1dah),a		;09ae
	cp 00ah		;09b1
	jr z,$+23		;09b3
	ld a,c			;09b5
	ld bc,(00102h)		;09b6
	ld c,e			;09ba
	ld e,000h		;09bb
	ld d,a			;09bd
	ld hl,0e3d7h		;09be
	ld a,(hl)			;09c1
	cp c			;09c2
	call z,0c995h		;09c3
	inc hl			;09c6
	inc e			;09c7
	djnz $-7		;09c8
	ld hl,0e1dah		;09ca
	dec (hl)			;09cd
	ld a,d			;09ce
	scf			;09cf
	pop bc			;09d0
	pop de			;09d1
	pop hl			;09d2
	push af			;09d3
	add a,d			;09d4
	ld d,a			;09d5
	jr nc,$+4		;09d6
	ld d,0ffh		;09d8
	pop af			;09da
	ret			;09db
	cp 0ffh		;09dc
	ret nz			;09de
	ld a,(0e2fdh)		;09df
	ret			;09e2
	push de			;09e3
	ld e,(ix+033h)		;09e4
	call 0c9efh		;09e7
	pop de			;09ea
	ld l,028h		;09eb
	rlca			;09ed
	ret			;09ee
	ld d,000h		;09ef
	ld hl,(0011ch)		;09f1
	add hl,de			;09f4
	ld a,(hl)			;09f5
	ret			;09f6
	ld e,a			;09f7
	ld d,000h		;09f8
	ld hl,0e3d7h		;09fa
	add hl,de			;09fd
	ld a,(hl)			;09fe
	ret			;09ff
	push bc			;0a00
	ld b,0fdh		;0a01
	call 0ca0ah		;0a03
	pop bc			;0a06
	ld l,01ch		;0a07
	ret			;0a09
	ld a,0feh		;0a0a
	ld c,(ix+026h)		;0a0c
	ld (0e30ah),a		;0a0f
	ld a,(0e2f9h)		;0a12
	inc a			;0a15
	scf			;0a16
	jr z,$+30		;0a17
	push bc			;0a19
	ld b,(ix+033h)		;0a1a
	call 0ca41h		;0a1d
	jr c,$+25		;0a20
	pop bc			;0a22
	call 0ca41h		;0a23
	jr c,$+20		;0a26
	ld b,c			;0a28
	call 0ca41h		;0a29
	jr c,$+14		;0a2c
	ld b,0ffh		;0a2e
	call 0ca41h		;0a30
	jr c,$+8		;0a33
	ld a,0ffh		;0a35
	jr $+37		;0a37
	pop bc			;0a39
	or a			;0a3a
	dec hl			;0a3b
	ex de,hl			;0a3c
	dec hl			;0a3d
	ld a,l			;0a3e
	jr $+29		;0a3f
	ld e,000h		;0a41
	call 0cab2h		;0a43
	ret nc			;0a46
	call 0ca4fh		;0a47
	pop hl			;0a4a
	jr nz,$-8		;0a4b
	scf			;0a4d
	ret			;0a4e
	cp (ix+022h)		;0a4f
	ret nz			;0a52
	ld a,(0e2fah)		;0a53
	cp 0ffh		;0a56
	ret z			;0a58
	inc hl			;0a59
	cp (hl)			;0a5a
	ret			;0a5b
	push af			;0a5c
	push hl			;0a5d
	push de			;0a5e
	ld hl,00000h		;0a5f
	ld (0e30dh),hl		;0a62
	ld (0e30fh),hl		;0a65
	ld (0e311h),hl		;0a68
	ld e,a			;0a6b
	ld (0e30ah),a		;0a6c
	cp 0ffh		;0a6f
	jr z,$+51		;0a71
	ld hl,00103h		;0a73
	cp (hl)			;0a76
	jr nc,$+49		;0a77
	call 0c9efh		;0a79
	rla			;0a7c
	rr (ix+039h)		;0a7d
	rla			;0a81
	rr (ix+038h)		;0a82
	call 0c9fah		;0a86
	ld (0e30dh),a		;0a89
	ex de,hl			;0a8c
	call 0c989h		;0a8d
	ld (ix+037h),h		;0a90
	ld h,000h		;0a93
	ex de,hl			;0a95
	ld hl,(0011eh)		;0a96
	add hl,de			;0a99
	add hl,de			;0a9a
	ld a,(hl)			;0a9b
	ld (0e312h),a		;0a9c
	inc hl			;0a9f
	ld a,(hl)			;0aa0
	ld (0e311h),a		;0aa1
	pop de			;0aa4
	pop hl			;0aa5
	pop af			;0aa6
	ret			;0aa7
	pop de			;0aa8
	pop hl			;0aa9
	pop af			;0aaa
	pop af			;0aab
	ld d,000h		;0aac
	jp 0c103h		;0aae
	ld b,a			;0ab1
	ld hl,0e3d7h		;0ab2
	ld d,000h		;0ab5
	add hl,de			;0ab7
	ld a,(00103h)		;0ab8
	cp e			;0abb
	ret z			;0abc
	ld a,b			;0abd
	inc de			;0abe
	cp 0ffh		;0abf
	jr z,$+3		;0ac1
	cp (hl)			;0ac3
	inc hl			;0ac4
	jr nz,$-13		;0ac5
	ex (sp),hl			;0ac7
	push hl			;0ac8
	push de			;0ac9
	dec e			;0aca
	ld hl,(0011ah)		;0acb
	add hl,de			;0ace
	add hl,de			;0acf
	ld a,(hl)			;0ad0
	cp 0ffh		;0ad1
	pop de			;0ad3
	ret c			;0ad4
	pop hl			;0ad5
	ex (sp),hl			;0ad6
	jr $-31		;0ad7
	push bc			;0ad9
	ld bc,00000h		;0ada
	push bc			;0add
	res 7,(ix+035h)		;0ade
	res 4,(ix-003h)		;0ae2
	set 5,(ix-003h)		;0ae6
	ld hl,0e3d7h		;0aea
	add hl,bc			;0aed
	ld a,(hl)			;0aee
	cp 0ffh		;0aef
	jr z,$+76		;0af1
	cp d			;0af3
	jr nz,$+45		;0af4
	ld h,c			;0af6
	ld l,000h		;0af7
	ex (sp),hl			;0af9
	push hl			;0afa
	pop af			;0afb
	bit 7,(ix+035h)		;0afc
	jr nz,$+30		;0b00
	set 7,(ix+035h)		;0b02
	bit 1,(ix-001h)		;0b06
	jr z,$+14		;0b0a
	ld l,001h		;0b0c
	call 0cea1h		;0b0e
	bit 6,(ix+035h)		;0b11
	call z,0d58eh		;0b15
	ex (sp),hl			;0b18
	ld l,001h		;0b19
	ex (sp),hl			;0b1b
	jr $+5		;0b1c
	call 0cb24h		;0b1e
	inc bc			;0b21
	jr $-56		;0b22
	bit 6,(ix+035h)		;0b24
	jr nz,$+9		;0b28
	ld l,a			;0b2a
	call 0ce20h		;0b2b
	jp 0cea6h		;0b2e
	jr c,$+9		;0b31
	push af			;0b33
	ld l,02eh		;0b34
	call 0cea1h		;0b36
	pop af			;0b39
	jp 0cdb6h		;0b3a
	pop af			;0b3d
	pop bc			;0b3e
	bit 7,(ix+035h)		;0b3f
	jr z,$+26		;0b43
	bit 6,(ix+035h)		;0b45
	jr z,$-37		;0b49
	jr c,$+10		;0b4b
	push af			;0b4d
	ld l,02fh		;0b4e
	call 0cea1h		;0b50
	pop af			;0b53
	scf			;0b54
	call 0cb24h		;0b55
	ld l,030h		;0b58
	jp 0cea1h		;0b5a
	bit 1,(ix-001h)		;0b5d
	ret nz			;0b61
	ld l,035h		;0b62
	jp 0cea1h		;0b64
	ld hl,(00114h)		;0b67
	ld c,a			;0b6a
	ld b,000h		;0b6b
	add hl,bc			;0b6d
	add hl,bc			;0b6e
	ld e,(hl)			;0b6f
	inc hl			;0b70
	ld d,(hl)			;0b71
	ex de,hl			;0b72
	ld a,(hl)			;0b73
	cp 0ffh		;0b74
	ret z			;0b76
	cp (ix+021h)		;0b77
	inc hl			;0b7a
	ld a,(hl)			;0b7b
	scf			;0b7c
	ret z			;0b7d
	inc hl			;0b7e
	jr $-12		;0b7f
	ld hl,(0e1d1h)		;0b81
	ld a,l			;0b84
	or h			;0b85
	jr z,$+101		;0b86
	ld d,001h		;0b88
	xor a			;0b8a
	jr $+32		;0b8b
	bit 2,(ix-001h)		;0b8d
	jr z,$+8		;0b91
	ld hl,(0e1cfh)		;0b93
	ld a,(hl)			;0b96
	cp 00dh		;0b97
	push af			;0b99
	scf			;0b9a
	call z,0d123h		;0b9b
	jr nc,$+41		;0b9e
	ld (0e1cfh),hl		;0ba0
	pop af			;0ba3
	ld de,00000h		;0ba4
	ld (0e1d1h),de		;0ba7
	ld (ix-002h),d		;0bab
	ld de,0ffffh		;0bae
	ld a,e			;0bb1
	jr nz,$+8		;0bb2
	ld (0e2f8h),a		;0bb4
	ld (0e2fbh),a		;0bb7
	ld (0e2f9h),de		;0bba
	ld (0e302h),de		;0bbe
	ld (0e304h),a		;0bc2
	jr $+10		;0bc5
	pop af			;0bc7
	set 7,(ix+031h)		;0bc8
	jr $+31		;0bcc
	inc hl			;0bce
	call 0cd94h		;0bcf
	cp 022h		;0bd2
	jr z,$+23		;0bd4
	call 0cd9ch		;0bd6
	jr c,$+25		;0bd9
	cp 00dh		;0bdb
	jr nz,$-15		;0bdd
	bit 2,(ix-001h)		;0bdf
	jr z,$+8		;0be3
	res 2,(ix-001h)		;0be5
	jr $-92		;0be9
	res 2,(ix-001h)		;0beb
	scf			;0bef
	ret			;0bf0
	pop hl			;0bf1
	call 0cd41h		;0bf2
	or a			;0bf5
	jp nz,0cc78h		;0bf6
	call 0cd9bh		;0bf9
	jr c,$-10		;0bfc
	cp 022h		;0bfe
	jr nz,$+35		;0c00
	bit 0,(ix-002h)		;0c02
	jr nz,$+29		;0c06
	ld de,(0e1d1h)		;0c08
	ld a,d			;0c0c
	or e			;0c0d
	jr nz,$-35		;0c0e
	inc hl			;0c10
	ld (0e1d1h),hl		;0c11
	ld a,(hl)			;0c14
	cp 00dh		;0c15
	jr z,$-44		;0c17
	inc hl			;0c19
	cp 022h		;0c1a
	jr nz,$-8		;0c1c
	call 0cd94h		;0c1e
	jr $-40		;0c21
	set 2,(ix-001h)		;0c23
	bit 0,(ix-002h)		;0c27
	jr nz,$+7		;0c2b
	ld (0e1cfh),hl		;0c2d
	jr $+5		;0c30
	ld (0e1d1h),hl		;0c32
	ld a,(hl)			;0c35
	cp 00dh		;0c36
	jr nz,$+6		;0c38
	res 2,(ix-001h)		;0c3a
	bit 1,(ix-002h)		;0c3e
	jr nz,$+17		;0c42
	ld a,(0e2f9h)		;0c44
	cp 0ffh		;0c47
	jr z,$-94		;0c49
	cp 028h		;0c4b
	ret nc			;0c4d
	ld (0e2f8h),a		;0c4e
	or a			;0c51
	ret			;0c52
	bit 5,(ix-002h)		;0c53
	jr nz,$+7		;0c57
	ld a,0ffh		;0c59
	ld (0e2fbh),a		;0c5b
	ld de,(0e303h)		;0c5e
	call 0cc69h		;0c62
	ld de,(0e2f9h)		;0c65
	ld a,e			;0c69
	cp 0ffh		;0c6a
	jr z,$+10		;0c6c
	cp 032h		;0c6e
	jr c,$+6		;0c70
	ld (0e305h),de		;0c72
	xor a			;0c76
	ret			;0c77
	ld e,a			;0c78
	ld a,(bc)			;0c79
	cp 005h		;0c7a
	jr z,$-89		;0c7c
	or a			;0c7e
	jr nz,$+48		;0c7f
	bit 1,(ix-002h)		;0c81
	jp nz,0cbf2h		;0c85
	ld (ix+021h),e		;0c88
	set 1,(ix-002h)		;0c8b
	push hl			;0c8f
	ld hl,(0e1e1h)		;0c90
	dec hl			;0c93
	ld a,(hl)			;0c94
	cp 053h		;0c95
	jr nz,$+4		;0c97
	dec hl			;0c99
	ld a,(hl)			;0c9a
	cp 041h		;0c9b
	jr z,$+6		;0c9d
	cp 04fh		;0c9f
	jr nz,$+8		;0ca1
	dec hl			;0ca3
	ld a,(hl)			;0ca4
	cp 04ch		;0ca5
	ld a,006h		;0ca7
	pop hl			;0ca9
	jr z,$+44		;0caa
	jp 0cbf2h		;0cac
	cp 001h		;0caf
	jr nz,$+19		;0cb1
	bit 5,(ix-002h)		;0cb3
	jp nz,0cbf2h		;0cb7
	ld (ix+024h),e		;0cba
	set 5,(ix-002h)		;0cbd
	jp 0cbf2h		;0cc1
	cp 004h		;0cc4
	jr nz,$+16		;0cc6
	ld a,(0e302h)		;0cc8
	cp 0ffh		;0ccb
	jp nz,0cbf2h		;0ccd
	ld (ix+02bh),e		;0cd0
	jp 0cbf2h		;0cd3
	push hl			;0cd6
	ld hl,0e2d5h		;0cd7
	bit 2,(hl)		;0cda
	jr nz,$+35		;0cdc
	cp 003h		;0cde
	jr z,$+28		;0ce0
	cp 002h		;0ce2
	jr z,$+18		;0ce4
	ld a,(0e305h)		;0ce6
	cp 0ffh		;0ce9
	jr z,$+17		;0ceb
	ld e,a			;0ced
	ld a,(0e306h)		;0cee
	ld (0e2fah),a		;0cf1
	set 6,(hl)		;0cf4
	ld a,e			;0cf6
	ld (0e2f9h),a		;0cf7
	set 2,(hl)		;0cfa
	jp 0cbf1h		;0cfc
	bit 6,(hl)		;0cff
	jr nz,$+13		;0d01
	set 6,(hl)		;0d03
	cp 003h		;0d05
	jr nz,$+7		;0d07
	ld (ix+023h),e		;0d09
	jr $-16		;0d0c
	bit 3,(hl)		;0d0e
	jr nz,$+34		;0d10
	cp 003h		;0d12
	jr z,$-24		;0d14
	cp 002h		;0d16
	jr z,$+18		;0d18
	ld a,(0e305h)		;0d1a
	cp 0ffh		;0d1d
	jr z,$-35		;0d1f
	ld e,a			;0d21
	ld a,(0e306h)		;0d22
	ld (0e304h),a		;0d25
	set 7,(hl)		;0d28
	ld a,e			;0d2a
	ld (0e303h),a		;0d2b
	set 3,(hl)		;0d2e
	jr $-52		;0d30
	bit 7,(hl)		;0d32
	jr nz,$-56		;0d34
	set 7,(hl)		;0d36
	cp 003h		;0d38
	jr nz,$-62		;0d3a
	ld (ix+02dh),e		;0d3c
	jr $-67		;0d3f
	ld a,0dfh		;0d41
	ld de,0e19eh		;0d43
	ld b,005h		;0d46
	dec de			;0d48
	ld (de),a			;0d49
	djnz $-2		;0d4a
	ld b,005h		;0d4c
	call 0cd9bh		;0d4e
	jr nc,$+15		;0d51
	cpl			;0d53
	ld (de),a			;0d54
	inc de			;0d55
	inc hl			;0d56
	djnz $-9		;0d57
	call 0cd9bh		;0d59
	inc hl			;0d5c
	jr c,$-4		;0d5d
	dec hl			;0d5f
	ld (0e1e1h),hl		;0d60
	call 0cd94h		;0d63
	ld bc,(00116h)		;0d66
	push hl			;0d6a
	ld hl,0e199h		;0d6b
	res 5,(ix-001h)		;0d6e
	ld d,005h		;0d72
	ld a,(bc)			;0d74
	or a			;0d75
	jr nz,$+4		;0d76
	pop hl			;0d78
	ret			;0d79
	cp (hl)			;0d7a
	jr z,$+6		;0d7b
	set 5,(ix-001h)		;0d7d
	inc bc			;0d81
	inc hl			;0d82
	dec d			;0d83
	jr nz,$-16		;0d84
	bit 5,(ix-001h)		;0d86
	jr nz,$+6		;0d8a
	ld a,(bc)			;0d8c
	inc bc			;0d8d
	pop hl			;0d8e
	ret			;0d8f
	inc bc			;0d90
	inc bc			;0d91
	jr $-39		;0d92
	ld a,(hl)			;0d94
	cp 020h		;0d95
	ret nz			;0d97
	inc hl			;0d98
	jr $-5		;0d99
	ld a,(hl)			;0d9b
	call 0cdafh		;0d9c
	ccf			;0d9f
	ret c			;0da0
	cp 015h		;0da1
	ccf			;0da3
	ret nc			;0da4
	cp 020h		;0da5
	ret c			;0da7
	cp 041h		;0da8
	ccf			;0daa
	ret nc			;0dab
	cp 05bh		;0dac
	ret			;0dae
	cp 030h		;0daf
	ret c			;0db1
	cp 03ah		;0db2
	ccf			;0db4
	ret			;0db5
	push hl			;0db6
	ld hl,00103h		;0db7
	cp (hl)			;0dba
	jr nc,$+90		;0dbb
	call 0ce1fh		;0dbd
	call 0ce07h		;0dc0
	jr z,$-3		;0dc3
	bit 4,(ix-003h)		;0dc5
	jr nz,$+6		;0dc9
	and 0dfh		;0dcb
	jr $+49		;0dcd
	cp 0aah		;0dcf
	jr z,$+6		;0dd1
	cp 08ah		;0dd3
	jr nz,$+41		;0dd5
	call 0ce07h		;0dd7
	call 0ce07h		;0dda
	jr nz,$+26		;0ddd
	push af			;0ddf
	ld a,065h		;0de0
	call 0cdech		;0de2
	ld a,06ch		;0de5
	call 0d590h		;0de7
	jr $+19		;0dea
	bit 5,(ix-003h)		;0dec
	jr nz,$+4		;0df0
	and 0dfh		;0df2
	jp 0d590h		;0df4
	push af			;0df7
	ld a,06ch		;0df8
	call 0cdech		;0dfa
	pop af			;0dfd
	cpl			;0dfe
	call 0d590h		;0dff
	call 0ce07h		;0e02
	jr $-7		;0e05
	ld a,(hl)			;0e07
	cp 0f5h		;0e08
	jr z,$+10		;0e0a
	cp 0d1h		;0e0c
	jr z,$+6		;0e0e
	inc hl			;0e10
	cp 0dfh		;0e11
	ret			;0e13
	pop hl			;0e14
	pop hl			;0e15
	ret			;0e16
	ld l,a			;0e17
	push de			;0e18
	ld de,(0010ah)		;0e19
	jr $+15		;0e1d
	ld l,a			;0e1f
	push de			;0e20
	ld de,(0010ch)		;0e21
	jr $+7		;0e25
	push de			;0e27
	ld de,(00112h)		;0e28
	ld h,000h		;0e2c
	add hl,hl			;0e2e
	add hl,de			;0e2f
	ld e,(hl)			;0e30
	inc hl			;0e31
	ld d,(hl)			;0e32
	ex de,hl			;0e33
	pop de			;0e34
	ret			;0e35
	push af			;0e36
	push bc			;0e37
	ld bc,00865h		;0e38
	ld hl,00000h		;0e3b
	ld de,(0e1dch)		;0e3e
	inc e			;0e42
	ld d,h			;0e43
	srl c		;0e44
	jr nc,$+3		;0e46
	add hl,de			;0e48
	sla e		;0e49
	rl d		;0e4b
	djnz $-9		;0e4d
	ld (0e1dch),hl		;0e4f
	ld a,h			;0e52
	or a			;0e53
	jr z,$-28		;0e54
	pop bc			;0e56
	pop af			;0e57
	ret			;0e58
	ld d,000h		;0e59
	push hl			;0e5b
	push de			;0e5c
	res 1,(ix-003h)		;0e5d
	ex de,hl			;0e61
	ld de,02710h		;0e62
	call 0ce83h		;0e65
	ld de,003e8h		;0e68
	call 0ce83h		;0e6b
	ld de,00064h		;0e6e
	call 0ce83h		;0e71
	ld de,0000ah		;0e74
	call 0ce83h		;0e77
	ld a,l			;0e7a
	add a,030h		;0e7b
	call 0d590h		;0e7d
	pop de			;0e80
	pop hl			;0e81
	ret			;0e82
	xor a			;0e83
	inc a			;0e84
	or a			;0e85
	sbc hl,de		;0e86
	jr nc,$-4		;0e88
	add hl,de			;0e8a
	dec a			;0e8b
	jr nz,$+7		;0e8c
	bit 1,(ix-003h)		;0e8e
	ret z			;0e92
	set 1,(ix-003h)		;0e93
	add a,030h		;0e97
	jp 0d590h		;0e99
	call 0ce27h		;0e9c
	jr $+7		;0e9f
	call 0ce27h		;0ea1
	jr $+6		;0ea4
	set 6,(ix-001h)		;0ea6
	ld a,(hl)			;0eaa
	cpl			;0eab
	cp 00ah		;0eac
	jr z,$+37		;0eae
	set 5,(ix-003h)		;0eb0
	cp (ix-006h)		;0eb4
	jr z,$+10		;0eb7
	cp 040h		;0eb9
	jr nz,$+18		;0ebb
	res 5,(ix-003h)		;0ebd
	set 4,(ix-003h)		;0ec1
	ld a,(0e30ah)		;0ec5
	call 0cdb6h		;0ec8
	jr $+5		;0ecb
	call 0d590h		;0ecd
	inc hl			;0ed0
	jr $-39		;0ed1
	bit 6,(ix-001h)		;0ed3
	ret z			;0ed7
	res 6,(ix-001h)		;0ed8
	jp 0d58eh		;0edc
	ex (sp),hl			;0edf
	ld (0ceeeh),de		;0ee0
	ld e,(hl)			;0ee4
	inc hl			;0ee5
	ld d,(hl)			;0ee6
	inc hl			;0ee7
	ex de,hl			;0ee8
	add hl,de			;0ee9
	ex de,hl			;0eea
	ex (sp),hl			;0eeb
	push de			;0eec
	ld de,00000h		;0eed
	ret			;0ef0
	and 007h		;0ef1
	ld e,a			;0ef3
	ld a,(0e2cdh)		;0ef4
	and 0f8h		;0ef7
	or e			;0ef9
	ld (0e2cdh),a		;0efa
	jp 0c50fh		;0efd
	push bc			;0f00
	call 0d979h		;0f01
	pop bc			;0f04
	jp nc,0c2cdh		;0f05
	jp 0c27ah		;0f08
	res 7,(ix-004h)		;0f0b
	or a			;0f0f
	jr z,$+6		;0f10
	set 7,(ix-004h)		;0f12
	push bc			;0f16
	ld bc,0debch		;0f17
	call 0dd49h		;0f1a
	jp (hl)			;0f1d
	nop			;0f1e
	pop bc			;0f1f
	jp 0c50fh		;0f20
	push bc			;0f23
	call 0d6f1h		;0f24
	res 5,(ix-00ah)		;0f27
	res 7,(ix-00ah)		;0f2b
	ld hl,(0e2cbh)		;0f2f
	ld (0e2c0h),hl		;0f32
	jr $+21		;0f35
	push bc			;0f37
	call 0d6f1h		;0f38
	res 5,(ix-00ah)		;0f3b
	res 7,(ix-00ah)		;0f3f
	ld de,(0e2c0h)		;0f43
	call 0d532h		;0f47
	pop bc			;0f4a
	jp 0c510h		;0f4b
	push bc			;0f4e
	push af			;0f4f
	call 0d6f1h		;0f50
	ld a,(0e2cch)		;0f53
	ld d,a			;0f56
	pop af			;0f57
	ld e,a			;0f58
	jr $+17		;0f59
	ld d,a			;0f5b
	inc bc			;0f5c
	ld a,(bc)			;0f5d
	ld e,a			;0f5e
	push bc			;0f5f
	push de			;0f60
	call 0d6f1h		;0f61
	pop de			;0f64
	ld a,(0e2c9h)		;0f65
	add a,d			;0f68
	ld d,a			;0f69
	ld a,(0e2cah)		;0f6a
	add a,e			;0f6d
	ld e,a			;0f6e
	call 0d532h		;0f6f
	res 5,(ix-00ah)		;0f72
	res 7,(ix-00ah)		;0f76
	pop bc			;0f7a
	jp 0c50fh		;0f7b
	push bc			;0f7e
	call 0d4deh		;0f7f
	pop bc			;0f82
	jp 0c50fh		;0f83
	dec bc			;0f86
	push bc			;0f87
	call 0d6f1h		;0f88
	ld a,(0e2c7h)		;0f8b
	srl a		;0f8e
	ld h,a			;0f90
	ld a,02dh		;0f91
	sub h			;0f93
	jr $+25		;0f94
	push af			;0f96
	push bc			;0f97
	call 0d6f1h		;0f98
	pop bc			;0f9b
	pop af			;0f9c
	cp 020h		;0f9d
	jr c,$+3		;0f9f
	xor a			;0fa1
	ld (0e2c9h),a		;0fa2
	inc bc			;0fa5
	push bc			;0fa6
	ld a,(bc)			;0fa7
	cp 05ah		;0fa8
	jr c,$+3		;0faa
	xor a			;0fac
	ld (0e2cah),a		;0fad
	jr $+19		;0fb0
	push af			;0fb2
	push bc			;0fb3
	call 0d6f1h		;0fb4
	pop bc			;0fb7
	pop af			;0fb8
	dec a			;0fb9
	ld (0e2c8h),a		;0fba
	inc bc			;0fbd
	push bc			;0fbe
	ld a,(bc)			;0fbf
	ld (0e2c7h),a		;0fc0
	call 0d56ah		;0fc3
	ld bc,0dd4ch		;0fc6
	call 0dd49h		;0fc9
	jp (hl)			;0fcc
	nop			;0fcd
	pop bc			;0fce
	jp 0c50fh		;0fcf
	inc bc			;0fd2
	jp 0c50fh		;0fd3
	or a			;0fd6
	jr z,$+4		;0fd7
	ld a,0ffh		;0fd9
	ld (0e2c2h),a		;0fdb
	jp 0c50fh		;0fde
	jp 0c50fh		;0fe1
	push bc			;0fe4
	push de			;0fe5
	push hl			;0fe6
	ld c,006h		;0fe7
	ld e,0ffh		;0fe9
	call 0d7a2h		;0feb
	pop hl			;0fee
	pop de			;0fef
	pop bc			;0ff0
	or a			;0ff1
	jp z,0c27ah		;0ff2
	ld (0e313h),a		;0ff5
	ld a,(0e2c8h)		;0ff8
	call 0d4a6h		;0ffb
	jp 0c2ceh		;0ffe
	ld de,0e4d7h		;1001
	set 0,(ix-003h)		;1004
	push bc			;1008
	ld hl,0e2d7h		;1009
	ld bc,00200h		;100c
	jr $+38		;100f
	bit 0,(ix-003h)		;1011
	jr nz,$+9		;1015
	res 2,(ix-001h)		;1017
	jp 0c27ah		;101b
	inc bc			;101e
	push bc			;101f
	ld hl,0e4d7h		;1020
	ld de,0e2d7h		;1023
	ld c,a			;1026
	ld b,000h		;1027
	inc bc			;1029
	push hl			;102a
	ldir		;102b
	pop hl			;102d
	inc h			;102e
	ld de,0e3d7h		;102f
	ld bc,00100h		;1032
	ldir		;1035
	pop bc			;1037
	jp 0c510h		;1038
	push bc			;103b
	ld a,(00103h)		;103c
	ld (0e4d6h),a		;103f
	call 0d73fh		;1042
	jp c,0d0cbh		;1045
	ld hl,0e1fah		;1048
	ld de,00065h		;104b
	ld bc,00003h		;104e
	ldir		;1051
	ld de,0005ch		;1053
	ld c,013h		;1056
	call 0d7a2h		;1058
	ld de,0005ch		;105b
	ld c,016h		;105e
	call 0d7a2h		;1060
	ld l,039h		;1063
	inc a			;1065
	jr z,$+101		;1066
	ld b,004h		;1068
	ld de,0e2d7h		;106a
	push bc			;106d
	push de			;106e
	ld c,01ah		;106f
	call 0d7a2h		;1071
	ld de,0005ch		;1074
	ld c,015h		;1077
	call 0d7a2h		;1079
	pop de			;107c
	pop bc			;107d
	or a			;107e
	ld l,03ah		;107f
	jr nz,$+74		;1081
	ld hl,00080h		;1083
	add hl,de			;1086
	ex de,hl			;1087
	djnz $-27		;1088
	ld de,0005ch		;108a
	ld c,010h		;108d
	call 0d7a2h		;108f
	inc a			;1092
	jr z,$+54		;1093
	ld hl,0e1f7h		;1095
	ld de,00065h		;1098
	ld bc,00003h		;109b
	ldir		;109e
	ld de,0005ch		;10a0
	ld c,013h		;10a3
	call 0d7a2h		;10a5
	ld hl,0005ch		;10a8
	ld de,0006ch		;10ab
	ld bc,00010h		;10ae
	ldir		;10b1
	ld hl,0e1fah		;10b3
	ld de,00065h		;10b6
	ld bc,00003h		;10b9
	ldir		;10bc
	ld de,0005ch		;10be
	ld c,017h		;10c1
	call 0d7a2h		;10c3
	inc a			;10c6
	jr nz,$+10		;10c7
	ld l,038h		;10c9
	call 0ce9ch		;10cb
	call 0d43eh		;10ce
	pop bc			;10d1
	ld a,0ffh		;10d2
	ld (0e4d6h),a		;10d4
	jp 0c50fh		;10d7
	push bc			;10da
	call 0d73fh		;10db
	jr c,$-19		;10de
	ld de,0005ch		;10e0
	ld c,00fh		;10e3
	call 0d7a2h		;10e5
	ld l,039h		;10e8
	inc a			;10ea
	jr z,$-32		;10eb
	ld de,0e2d7h		;10ed
	push de			;10f0
	ld c,01ah		;10f1
	call 0d7a2h		;10f3
	ld de,0005ch		;10f6
	ld c,014h		;10f9
	call 0d7a2h		;10fb
	pop de			;10fe
	or a			;10ff
	jr nz,$+9		;1100
	ld hl,00080h		;1102
	add hl,de			;1105
	ex de,hl			;1106
	jr $-23		;1107
	ld a,(00103h)		;1109
	ld hl,0e4d6h		;110c
	cp (hl)			;110f
	ld (hl),0ffh		;1110
	jr nz,$+6		;1112
	pop bc			;1114
	jp 0c50fh		;1115
	ld l,039h		;1118
	call 0ce9ch		;111a
	call 0d43eh		;111d
	jp 0c132h		;1120
	res 4,(ix-002h)		;1123
	ld a,(0e301h)		;1127
	or a			;112a
	jr z,$+11		;112b
	ld l,a			;112d
	ld a,(00106h)		;112e
	cp l			;1131
	jr z,$+4		;1132
	jr nc,$+37		;1134
	call 0ce36h		;1136
	ld a,h			;1139
	ld l,002h		;113a
	cp 01eh		;113c
	jr c,$+27		;113e
	inc l			;1140
	cp 03ch		;1141
	jr c,$+22		;1143
	inc l			;1145
	cp 05ah		;1146
	jr c,$+17		;1148
	inc l			;114a
	jr $+14		;114b
	res 2,(ix-001h)		;114d
	set 4,(ix-002h)		;1151
	ld (ix-007h),00ch		;1155
	push hl			;1159
	ld a,(0e2bdh)		;115a
	ld (0e1ceh),a		;115d
	ld a,(0e300h)		;1160
	and 007h		;1163
	call nz,0d4deh		;1165
	pop hl			;1168
	call 0cea1h		;1169
	call 0d6f1h		;116c
	res 6,(ix-00ah)		;116f
	res 3,(ix-003h)		;1173
	ld a,080h		;1177
	ld (0e2c5h),a		;1179
	ld l,021h		;117c
	call 0cea1h		;117e
	call 0d416h		;1181
	ld bc,0de3eh		;1184
	call 0dd49h		;1187
	jp (hl)			;118a
	nop			;118b
	ld a,(0e2cbh)		;118c
	ld (0e1cdh),a		;118f
	bit 4,(ix-002h)		;1192
	jr nz,$+25		;1196
	ld a,(0e308h)		;1198
	rlca			;119b
	jr nc,$+19		;119c
	rlca			;119e
	jr nc,$+16		;119f
	rlca			;11a1
	jr nc,$+13		;11a2
	ld hl,0dfeeh		;11a4
	call 0d3b3h		;11a7
	call 0d3d5h		;11aa
	jr $+23		;11ad
	xor a			;11af
	ld (0e198h),a		;11b0
	ld hl,0dfeeh		;11b3
	ld a,00dh		;11b6
	ld (hl),a			;11b8
	ld (0e196h),hl		;11b9
	jr $+8		;11bc
	bit 0,(ix+031h)		;11be
	jr nz,$+36		;11c2
	bit 4,(ix-002h)		;11c4
	jr nz,$+30		;11c8
	ld a,(0e307h)		;11ca
	or a			;11cd
	jr z,$+24		;11ce
	call 0d47ah		;11d0
	jr c,$+22		;11d3
	ld hl,(0e196h)		;11d5
	call 0d3b3h		;11d8
	call 0d3f7h		;11db
	ld a,(0e308h)		;11de
	or 0c0h		;11e1
	jp 0d2d1h		;11e3
	call 0d45ch		;11e6
	cp 07fh		;11e9
	jp z,0d36ah		;11eb
	cp 001h		;11ee
	jp z,0d336h		;11f0
	cp 006h		;11f3
	jp z,0d354h		;11f5
	cp 0b3h		;11f8
	jr nz,$+9		;11fa
	set 3,(ix-003h)		;11fc
	jp 0d1beh		;1200
	cp 00dh		;1203
	jr nz,$+34		;1205
	ld a,(0e198h)		;1207
	or a			;120a
	jp nz,0d2bfh		;120b
	jp 0d1beh		;120e
	dec d			;1211
	nop			;1212
	nop			;1213
	nop			;1214
	ld d,000h		;1215
	nop			;1217
	nop			;1218
	rla			;1219
	nop			;121a
	nop			;121b
	nop			;121c
	nop			;121d
	dec de			;121e
	jr $+2		;121f
	nop			;1221
	nop			;1222
	nop			;1223
	nop			;1224
	add hl,de			;1225
	ld e,0feh		;1226
	ld a,h			;1228
	ld e,01ah		;1229
	jr z,$+73		;122b
	inc e			;122d
	cp 05ch		;122e
	jr z,$+68		;1230
	inc e			;1232
	cp 0f5h		;1233
	jr z,$+63		;1235
	cp 05bh		;1237
	ld e,011h		;1239
	jr z,$+57		;123b
	inc e			;123d
	cp 05dh		;123e
	jr z,$+52		;1240
	bit 3,(ix-003h)		;1242
	res 3,(ix-003h)		;1246
	jr z,$+41		;124a
	cp 021h		;124c
	ld e,011h		;124e
	jr z,$+36		;1250
	inc e			;1252
	cp 03fh		;1253
	jr z,$+31		;1255
	and 0dfh		;1257
	cp 041h		;1259
	jp c,0d1beh		;125b
	cp 057h		;125e
	jp nc,0d1beh		;1260
	sub 041h		;1263
	ld e,a			;1265
	ld d,000h		;1266
	push hl			;1268
	ld hl,0d211h		;1269
	add hl,de			;126c
	ld a,(hl)			;126d
	pop hl			;126e
	or a			;126f
	jp z,0d1beh		;1270
	ld e,a			;1273
	ld a,(0e198h)		;1274
	cp (ix-007h)		;1277
	jp z,0d1beh		;127a
	ld a,e			;127d
	cp 011h		;127e
	jp c,0d1beh		;1280
	cp 05bh		;1283
	jr c,$+12		;1285
	cp 061h		;1287
	jp c,0d1beh		;1289
	cp 07bh		;128c
	jp nc,0d1beh		;128e
	ld hl,(0e196h)		;1291
	push hl			;1294
	ld a,00dh		;1295
	ld b,0ffh		;1297
	cpir		;1299
	pop bc			;129b
	ld a,e			;129c
	push hl			;129d
	pop de			;129e
	or a			;129f
	sbc hl,bc		;12a0
	push hl			;12a2
	pop bc			;12a3
	push de			;12a4
	pop hl			;12a5
	dec hl			;12a6
	lddr		;12a7
	ld hl,0e198h		;12a9
	inc (hl)			;12ac
	ld hl,(0e196h)		;12ad
	ld (hl),a			;12b0
	inc hl			;12b1
	ld (0e196h),hl		;12b2
	dec hl			;12b5
	call 0d3b3h		;12b6
	call 0d3d5h		;12b9
	jp 0d1beh		;12bc
	ld hl,(0e196h)		;12bf
	call 0d3b3h		;12c2
	call 0d3f7h		;12c5
	call 0d58eh		;12c8
	ld a,(0e308h)		;12cb
	and 03fh		;12ce
	scf			;12d0
	ld (0e308h),a		;12d1
	push af			;12d4
	ld bc,0de4eh		;12d5
	call 0dd49h		;12d8
	jp (hl)			;12db
	nop			;12dc
	bit 3,(ix+031h)		;12dd
	call nz,0d518h		;12e1
	set 6,(ix-00ah)		;12e4
	ld a,(0e1ceh)		;12e8
	call 0d4deh		;12eb
	pop af			;12ee
	push af			;12ef
	jr nc,$+61		;12f0
	bit 4,(ix+031h)		;12f2
	ld l,021h		;12f6
	call nz,0cea1h		;12f8
	ld hl,0dfeeh		;12fb
	ld a,(hl)			;12fe
	cp 00dh		;12ff
	jr z,$+37		;1301
	push af			;1303
	bit 4,(ix+031h)		;1304
	call nz,0d590h		;1308
	pop af			;130b
	cp 01ah		;130c
	jr c,$+10		;130e
	cp 020h		;1310
	jr nc,$+6		;1312
	or 001h		;1314
	jr $+12		;1316
	cp 061h		;1318
	jr c,$+9		;131a
	cp 07bh		;131c
	jr nc,$+5		;131e
	and 0dfh		;1320
	ld (hl),a			;1322
	inc hl			;1323
	jr $-38		;1324
	bit 4,(ix+031h)		;1326
	call nz,0d58eh		;132a
	pop af			;132d
	ld (ix-007h),078h		;132e
	ld hl,0dfeeh		;1332
	ret			;1335
	ld de,(0e196h)		;1336
	ld hl,0dfeeh		;133a
	or a			;133d
	sbc hl,de		;133e
	jr z,$+17		;1340
	dec de			;1342
	ld (0e196h),de		;1343
	ld a,008h		;1347
	ld bc,0dd65h		;1349
	call 0dd49h		;134c
	jp (hl)			;134f
	nop			;1350
	jp 0d1beh		;1351
	ld hl,(0e196h)		;1354
	ld a,(hl)			;1357
	cp 00dh		;1358
	jr z,$-9		;135a
	inc hl			;135c
	ld (0e196h),hl		;135d
	ld bc,0dd65h		;1360
	call 0dd49h		;1363
	jp (hl)			;1366
	nop			;1367
	jr $-23		;1368
	ld de,(0e196h)		;136a
	ld hl,0dfeeh		;136e
	or a			;1371
	sbc hl,de		;1372
	jr z,$-35		;1374
	push de			;1376
	pop hl			;1377
	ld a,00dh		;1378
	ld b,0ffh		;137a
	cpir		;137c
	or a			;137e
	sbc hl,de		;137f
	push hl			;1381
	pop bc			;1382
	push de			;1383
	pop hl			;1384
	dec de			;1385
	ldir		;1386
	ld a,008h		;1388
	ld bc,0dd65h		;138a
	call 0dd49h		;138d
	jp (hl)			;1390
	nop			;1391
	ld hl,0e198h		;1392
	dec (hl)			;1395
	ld hl,(0e196h)		;1396
	dec hl			;1399
	ld (0e196h),hl		;139a
	call 0d3b3h		;139d
	push hl			;13a0
	ld a,020h		;13a1
	ld bc,0dd65h		;13a3
	call 0dd49h		;13a6
	jp (hl)			;13a9
	nop			;13aa
	pop hl			;13ab
	inc hl			;13ac
	call 0d3d5h		;13ad
	jp 0d1beh		;13b0
	ld bc,0de4eh		;13b3
	call 0dd49h		;13b6
	jp (hl)			;13b9
	nop			;13ba
	ld a,(hl)			;13bb
	cp 00dh		;13bc
	jp z,0d3cch		;13be
	ld bc,0dd65h		;13c1
	call 0dd49h		;13c4
	jp (hl)			;13c7
	nop			;13c8
	inc hl			;13c9
	jr $-15		;13ca
	ld bc,0de3eh		;13cc
	call 0dd49h		;13cf
	jp (hl)			;13d2
	nop			;13d3
	ret			;13d4
	ld de,(0e196h)		;13d5
	xor a			;13d9
	sbc hl,de		;13da
	ret z			;13dc
	ld bc,0de4eh		;13dd
	call 0dd49h		;13e0
	jp (hl)			;13e3
	nop			;13e4
	ld b,l			;13e5
	push bc			;13e6
	ld a,008h		;13e7
	ld bc,0dd65h		;13e9
	call 0dd49h		;13ec
	jp (hl)			;13ef
	nop			;13f0
	pop bc			;13f1
	djnz $-12		;13f2
	jp 0d3cch		;13f4
	ld de,0dfeeh		;13f7
	or a			;13fa
	sbc hl,de		;13fb
	ld a,(0e1cdh)		;13fd
	add a,l			;1400
	sub (ix-00dh)		;1401
	ld l,(ix-00fh)		;1404
	sub (ix-010h)		;1407
	jr c,$+8		;140a
	dec l			;140c
	jr nz,$-6		;140d
	ld l,(ix-00fh)		;140f
	ld a,l			;1412
	jp 0d4a6h		;1413
	ld de,(0e2c7h)		;1416
	ld d,000h		;141a
	ld h,d			;141c
	ld l,d			;141d
	ld bc,(0e2c7h)		;141e
	inc b			;1422
	add hl,de			;1423
	djnz $-1		;1424
	ld a,(0e2cah)		;1426
	inc a			;1429
	sub (ix-00dh)		;142a
	ld e,a			;142d
	or a			;142e
	sbc hl,de		;142f
	ld e,(ix-007h)		;1431
	push hl			;1434
	or a			;1435
	sbc hl,de		;1436
	pop hl			;1438
	ret nc			;1439
	ld (ix-007h),l		;143a
	ret			;143d
	call 0d6f1h		;143e
	ld l,010h		;1441
	call 0cea1h		;1443
	call 0d6f1h		;1446
	ld a,(0e2c8h)		;1449
	inc a			;144c
	call 0d4a6h		;144d
	bit 2,(ix+031h)		;1450
	jr z,$+8		;1454
	ld a,(0e307h)		;1456
	or a			;1459
	jr nz,$+32		;145a
	push bc			;145c
	push de			;145d
	push hl			;145e
	push de			;145f
	ld c,006h		;1460
	ld e,0ffh		;1462
	call 0d7a2h		;1464
	or a			;1467
	jr z,$-8		;1468
	pop de			;146a
	pop hl			;146b
	pop de			;146c
	pop bc			;146d
	scf			;146e
	ret			;146f
	push bc			;1470
	push de			;1471
	push hl			;1472
	ld de,(0c088h)		;1473
	ld b,a			;1477
	jr $+18		;1478
	push bc			;147a
	push de			;147b
	push hl			;147c
	ld b,(ix+030h)		;147d
	ld hl,(0c088h)		;1480
	add hl,hl			;1483
	add hl,hl			;1484
	add hl,hl			;1485
	add hl,hl			;1486
	add hl,hl			;1487
	add hl,hl			;1488
	ex de,hl			;1489
	push de			;148a
	push de			;148b
	push bc			;148c
	ld c,006h		;148d
	ld e,0ffh		;148f
	call 0d7a2h		;1491
	pop bc			;1494
	pop de			;1495
	or a			;1496
	jr nz,$-45		;1497
	dec de			;1499
	ld a,d			;149a
	or e			;149b
	jr nz,$-17		;149c
	pop de			;149e
	djnz $-21		;149f
	pop hl			;14a1
	pop de			;14a2
	pop bc			;14a3
	xor a			;14a4
	ret			;14a5
	ld (0e2c5h),a		;14a6
	push ix		;14a9
	push bc			;14ab
	ld ix,0e22ch		;14ac
	ld de,00010h		;14b0
	ld b,008h		;14b3
	ld a,(ix+00ah)		;14b5
	ld (ix+007h),a		;14b8
	add ix,de		;14bb
	djnz $-8		;14bd
	pop bc			;14bf
	pop ix		;14c0
	ret			;14c2
	ld a,(0e2bdh)		;14c3
	ld (0e2bch),a		;14c6
	ld a,0ffh		;14c9
	ld (0e2b0h),a		;14cb
	ret			;14ce
	xor a			;14cf
	ld (0e2c2h),a		;14d0
	ld bc,0de85h		;14d3
	call 0dd49h		;14d6
	jp (hl)			;14d9
	nop			;14da
	ld a,(0e2bch)		;14db
	cp (ix-01ah)		;14de
	ret z			;14e1
	push af			;14e2
	cp 008h		;14e3
	call z,0d4c3h		;14e5
	call 0d6f1h		;14e8
	ld a,(0e2bdh)		;14eb
	call 0d50ch		;14ee
	ex de,hl			;14f1
	ld hl,0e2beh		;14f2
	ld bc,00010h		;14f5
	ldir		;14f8
	pop af			;14fa
	push af			;14fb
	ld (0e2bdh),a		;14fc
	call 0d50ch		;14ff
	ld de,0e2beh		;1502
	ld bc,00010h		;1505
	ldir		;1508
	pop af			;150a
	ret			;150b
	ld hl,0e22ch		;150c
	rlca			;150f
	rlca			;1510
	rlca			;1511
	rlca			;1512
	ld e,a			;1513
	ld d,000h		;1514
	add hl,de			;1516
	ret			;1517
	push bc			;1518
	push hl			;1519
	push de			;151a
	call 0d730h		;151b
	ld bc,0dd4ch		;151e
	call 0dd49h		;1521
	jp (hl)			;1524
	nop			;1525
	ld bc,0de85h		;1526
	call 0dd49h		;1529
	jp (hl)			;152c
	nop			;152d
	pop de			;152e
	pop hl			;152f
	pop bc			;1530
	ret			;1531
	ld a,d			;1532
	cp (ix-00eh)		;1533
	jr c,$+27		;1536
	ld a,(0e2c9h)		;1538
	add a,(ix-00fh)		;153b
	cp d			;153e
	jr c,$+18		;153f
	ld a,e			;1541
	cp (ix-00dh)		;1542
	jr c,$+12		;1545
	ld a,(0e2cah)		;1547
	add a,(ix-010h)		;154a
	dec a			;154d
	cp e			;154e
	jr nc,$+8		;154f
	ld e,(ix-00dh)		;1551
	ld d,(ix-00eh)		;1554
	ld (ix-00ch),e		;1557
	ld a,d			;155a
	ld (0e2cch),a		;155b
	ld bc,0dfa1h		;155e
	call 0dd49h		;1561
	jp (hl)			;1564
	nop			;1565
	ld (0e2c3h),hl		;1566
	ret			;1569
	ld a,(0e2c9h)		;156a
	add a,(ix-00fh)		;156d
	cp 020h		;1570
	jr c,$+10		;1572
	ld a,01fh		;1574
	sub (ix-00eh)		;1576
	ld (0e2c8h),a		;1579
	ld a,(0e2cah)		;157c
	add a,(ix-010h)		;157f
	cp 05bh		;1582
	ret c			;1584
	ld a,05ah		;1585
	sub (ix-00dh)		;1587
	ld (0e2c7h),a		;158a
	ret			;158d
	ld a,00dh		;158e
	push ix		;1590
	ld ix,0e2d7h		;1592
	cp 080h		;1596
	jr c,$+37		;1598
	bit 2,(ix-003h)		;159a
	jr z,$+31		;159e
	push hl			;15a0
	ld hl,(00108h)		;15a1
	sub 07fh		;15a4
	bit 7,(hl)		;15a6
	inc hl			;15a8
	jr z,$-3		;15a9
	dec a			;15ab
	jr nz,$-6		;15ac
	ld a,(hl)			;15ae
	add a,080h		;15af
	jr c,$+13		;15b1
	add a,080h		;15b3
	call 0d5bbh		;15b5
	inc hl			;15b8
	jr $-11		;15b9
	push ix		;15bb
	push hl			;15bd
	push de			;15be
	push bc			;15bf
	cp 00dh		;15c0
	jr z,$+76		;15c2
	cp 010h		;15c4
	jr c,$+92		;15c6
	bit 0,(ix-00ah)		;15c8
	jr z,$+4		;15cc
	or 080h		;15ce
	bit 6,(ix-00ah)		;15d0
	jr z,$+53		;15d4
	cp 020h		;15d6
	jr z,$+34		;15d8
	cp 0a0h		;15da
	jr z,$+30		;15dc
	push af			;15de
	ld a,(0e1a0h)		;15df
	cp 028h		;15e2
	call z,0d6f1h		;15e4
	pop af			;15e7
	res 5,(ix-00ah)		;15e8
	ld hl,(0e1c9h)		;15ec
	ld (hl),a			;15ef
	inc hl			;15f0
	ld (0e1c9h),hl		;15f1
	ld hl,0e1a0h		;15f4
	inc (hl)			;15f7
	jr $+36		;15f8
	bit 5,(ix-00ah)		;15fa
	jr nz,$+30		;15fe
	push af			;1600
	call 0d6f1h		;1601
	pop af			;1604
	bit 5,(ix-00ah)		;1605
	call z,0d6c4h		;1609
	jr $+16		;160c
	call 0d6f1h		;160e
	bit 7,(ix-00ah)		;1611
	res 7,(ix-00ah)		;1615
	call z,0d650h		;1619
	pop bc			;161c
	pop de			;161d
	pop hl			;161e
	pop ix		;161f
	ret			;1621
	cp 00fh		;1622
	jr nz,$+8		;1624
	res 0,(ix-00ah)		;1626
	jr $-14		;162a
	cp 00eh		;162c
	jr nz,$+8		;162e
	set 0,(ix-00ah)		;1630
	jr $-24		;1634
	push af			;1636
	call 0d6f1h		;1637
	pop af			;163a
	cp 00bh		;163b
	jr nz,$+7		;163d
	call 0d518h		;163f
	jr $-38		;1642
	cp 00ch		;1644
	jr nz,$-42		;1646
	ld a,(0e2c8h)		;1648
	call 0d44dh		;164b
	jr $-50		;164e
	ld a,00dh		;1650
	ld bc,0dd65h		;1652
	call 0dd49h		;1655
	jp (hl)			;1658
	nop			;1659
	ld hl,0e2c5h		;165a
	dec (hl)			;165d
	ret nz			;165e
	ld a,(0e2c8h)		;165f
	ld (hl),a			;1662
	bit 1,(ix-00ah)		;1663
	ret nz			;1667
	call 0d4a6h		;1668
	ld l,020h		;166b
	call 0ce27h		;166d
	ld a,(0e2cdh)		;1670
	push af			;1673
	ld a,002h		;1674
	ld (0e2cdh),a		;1676
	ld a,(hl)			;1679
	cpl			;167a
	cp 00ah		;167b
	jr z,$+8		;167d
	inc hl			;167f
	call 0d590h		;1680
	jr $-10		;1683
	pop af			;1685
	ld (0e2cdh),a		;1686
	bit 1,(ix+031h)		;1689
	jr z,$+13		;168d
	ld a,(0e307h)		;168f
	or a			;1692
	jr z,$+7		;1693
	call 0d47ah		;1695
	jr $+5		;1698
	call 0d45ch		;169a
	ld a,(0e2cbh)		;169d
	cp (ix-00dh)		;16a0
	ret z			;16a3
	ld a,008h		;16a4
	ld bc,0dd65h		;16a6
	call 0dd49h		;16a9
	jp (hl)			;16ac
	nop			;16ad
	ld a,020h		;16ae
	ld bc,0dd65h		;16b0
	call 0dd49h		;16b3
	jp (hl)			;16b6
	nop			;16b7
	ld a,008h		;16b8
	ld bc,0dd65h		;16ba
	call 0dd49h		;16bd
	jp (hl)			;16c0
	nop			;16c1
	jr $-37		;16c2
	ld e,a			;16c4
	push de			;16c5
	ld bc,0dd65h		;16c6
	call 0dd49h		;16c9
	jp (hl)			;16cc
	nop			;16cd
	pop de			;16ce
	res 7,(ix-00ah)		;16cf
	ld a,(0e2cbh)		;16d3
	cp (ix-00dh)		;16d6
	ret nz			;16d9
	ld a,e			;16da
	cp 020h		;16db
	jr nz,$+12		;16dd
	bit 6,(ix-00ah)		;16df
	jr z,$+6		;16e3
	set 5,(ix-00ah)		;16e5
	call 0d65ah		;16e9
	set 7,(ix-00ah)		;16ec
	ret			;16f0
	bit 6,(ix-00ah)		;16f1
	ret z			;16f5
	ld a,(0e1a0h)		;16f6
	or a			;16f9
	ret z			;16fa
	ld a,(0e2cah)		;16fb
	add a,(ix-010h)		;16fe
	ld hl,0e2cbh		;1701
	sub (hl)			;1704
	ld hl,0e1a0h		;1705
	cp (hl)			;1708
	jr z,$+15		;1709
	jr nc,$+17		;170b
	ld a,(0e2cbh)		;170d
	cp (ix-00dh)		;1710
	call nz,0d650h		;1713
	jr $+6		;1716
	set 5,(ix-00ah)		;1718
	ld bc,(0e19fh)		;171c
	ld hl,0e1a1h		;1720
	ld a,(hl)			;1723
	push hl			;1724
	push bc			;1725
	call 0d6c4h		;1726
	pop bc			;1729
	pop hl			;172a
	inc hl			;172b
	djnz $-9		;172c
	jr $+6		;172e
	res 5,(ix-00ah)		;1730
	ld hl,0e1a1h		;1734
	ld (0e1c9h),hl		;1737
	xor a			;173a
	ld (0e1a0h),a		;173b
	ret			;173e
	ld l,03ch		;173f
	call 0d14dh		;1741
	xor a			;1744
	ld (0005ch),a		;1745
	ld (0007ch),a		;1748
	ld (00068h),a		;174b
	ld hl,0005dh		;174e
	ld a,020h		;1751
	ld b,008h		;1753
	ld (hl),a			;1755
	inc hl			;1756
	djnz $-2		;1757
	ex de,hl			;1759
	ld hl,0e1f7h		;175a
	ld bc,00003h		;175d
	ldir		;1760
	ld hl,0dfeeh		;1762
	ld a,(0dfefh)		;1765
	cp 03ah		;1768
	jr nz,$+25		;176a
	ld a,(0dff0h)		;176c
	cp 00dh		;176f
	jr z,$+43		;1771
	ld a,(hl)			;1773
	cp 041h		;1774
	jr z,$+6		;1776
	cp 042h		;1778
	jr nz,$+34		;177a
	sub 040h		;177c
	ld (0005ch),a		;177e
	inc hl			;1781
	inc hl			;1782
	ld de,0005dh		;1783
	ld bc,00008h		;1786
	res 7,(hl)		;1789
	ld a,(hl)			;178b
	cp 00dh		;178c
	jr z,$+12		;178e
	call 0cd9ch		;1790
	jr nc,$+9		;1793
	ldi		;1795
	jp pe,0d789h		;1797
	xor a			;179a
	ret			;179b
	ld l,03bh		;179c
	scf			;179e
	ret			;179f
	ld c,002h		;17a0
	push ix		;17a2
	call 00005h		;17a4
	pop ix		;17a7
	ret			;17a9
	ld a,(hl)			;17aa
	cp 024h		;17ab
	ret z			;17ad
	inc hl			;17ae
	call 0d590h		;17af
	jr $-8		;17b2
	ld (0e1deh),a		;17b4
	push af			;17b7
	call 0d6f1h		;17b8
	ld e,011h		;17bb
	ld c,02ch		;17bd
	call 0d7a2h		;17bf
	pop af			;17c2
	push af			;17c3
	ld hl,0e1e7h		;17c4
	call 0dbfah		;17c7
	ld de,00100h		;17ca
	ld c,01ah		;17cd
	call 0d7a2h		;17cf
	ld de,0005ch		;17d2
	ld c,014h		;17d5
	call 0d7a2h		;17d7
	or a			;17da
	jp nz,0d88eh		;17db
	ld de,0005ch		;17de
	ld c,010h		;17e1
	call 0d7a2h		;17e3
	ld bc,0dfbah		;17e6
	call 0dd49h		;17e9
	jp (hl)			;17ec
	nop			;17ed
	ld a,08bh		;17ee
	ld (0e1fdh),a		;17f0
	ld e,080h		;17f3
	ld c,02ch		;17f5
	call 0d7a2h		;17f7
	pop af			;17fa
	push af			;17fb
	ld hl,0e1f3h		;17fc
	call 0dbfah		;17ff
	ld de,04000h		;1802
	ld c,01ah		;1805
	call 0d7a2h		;1807
	ld de,0005ch		;180a
	ld c,014h		;180d
	call 0d7a2h		;180f
	cp 001h		;1812
	jr z,$+26		;1814
	or a			;1816
	jr nz,$+119		;1817
	ld a,(0e1fdh)		;1819
	push af			;181c
	ld bc,04000h		;181d
	call 0dfd0h		;1820
	pop af			;1823
	inc a			;1824
	cp 090h		;1825
	jr z,$+28		;1827
	ld (0e1fdh),a		;1829
	jr $-42		;182c
	ld a,(0e1fdh)		;182e
	cp 08bh		;1831
	jr nz,$+10		;1833
	ld a,h			;1835
	cp 015h		;1836
	jr c,$+86		;1838
	ld a,(0e1fdh)		;183a
	ld bc,04000h		;183d
	call 0dfd0h		;1840
	ld de,0005ch		;1843
	ld c,010h		;1846
	call 0d7a2h		;1848
	call 0d91dh		;184b
	ld e,001h		;184e
	ld c,02ch		;1850
	call 0d7a2h		;1852
	pop af			;1855
	ld hl,0e1efh		;1856
	call 0dbfah		;1859
	ld a,0ffh		;185c
	ld (00100h),a		;185e
	ld de,00100h		;1861
	push de			;1864
	ld c,01ah		;1865
	call 0d7a2h		;1867
	ld de,0005ch		;186a
	ld c,014h		;186d
	call 0d7a2h		;186f
	pop de			;1872
	or a			;1873
	jr nz,$+94		;1874
	ld hl,00100h		;1876
	xor a			;1879
	sbc hl,de		;187a
	ld a,h			;187c
	or l			;187d
	jr nz,$+77		;187e
	ld a,(00100h)		;1880
	cp 002h		;1883
	jr nz,$+9		;1885
	ld a,(00101h)		;1887
	cp 071h		;188a
	jr z,$+19		;188c
	ld a,008h		;188e
	call 0d4deh		;1890
	ld hl,0dd17h		;1893
	call 0d7aah		;1896
	call 0d45ch		;1899
	jp 0c1dfh		;189c
	ld bc,(00120h)		;189f
	ld hl,0c000h		;18a3
	ld a,l			;18a6
	and 080h		;18a7
	ld l,a			;18a9
	sbc hl,bc		;18aa
	jr nc,$+31		;18ac
	push hl			;18ae
	ld a,008h		;18af
	call 0d4deh		;18b1
	ld hl,0dcfbh		;18b4
	call 0d7aah		;18b7
	ld hl,00000h		;18ba
	pop de			;18bd
	xor a			;18be
	sbc hl,de		;18bf
	ex de,hl			;18c1
	call 0ce5bh		;18c2
	call 0d45ch		;18c5
	jp 0c1dfh		;18c8
	ld hl,00080h		;18cb
	add hl,de			;18ce
	ex de,hl			;18cf
	jr $-108		;18d0
	ld de,0005ch		;18d2
	ld c,010h		;18d5
	call 0d7a2h		;18d7
	ld a,089h		;18da
	ld bc,04000h		;18dc
	call 0dfd0h		;18df
	ld hl,00122h		;18e2
	ld de,0c08ah		;18e5
	ld a,003h		;18e8
	ex af,af'			;18ea
	ld c,(hl)			;18eb
	inc hl			;18ec
	ld b,(hl)			;18ed
	inc hl			;18ee
	ex de,hl			;18ef
	ld a,b			;18f0
	or c			;18f1
	jr z,$+11		;18f2
	ld a,0c3h		;18f4
	ld (hl),a			;18f6
	inc hl			;18f7
	ld (hl),c			;18f8
	inc hl			;18f9
	ld (hl),b			;18fa
	jr $+7		;18fb
	ld a,0c9h		;18fd
	ld (hl),a			;18ff
	inc hl			;1900
	inc hl			;1901
	inc hl			;1902
	ex de,hl			;1903
	ex af,af'			;1904
	dec a			;1905
	jr nz,$-28		;1906
	ld a,(00102h)		;1908
	ld (0e2d1h),a		;190b
	res 2,(ix-003h)		;190e
	ld hl,(00108h)		;1912
	ld a,h			;1915
	or l			;1916
	ret z			;1917
	set 2,(ix-003h)		;1918
	ret			;191c
	di			;191d
	ld a,08bh		;191e
	out (0f0h),a		;1920
	ex af,af'			;1922
	ld hl,00000h		;1923
	ld (0e200h),hl		;1926
	ld hl,00a06h		;1929
	ld (0e1feh),hl		;192c
	ld de,00004h		;192f
	call 0d964h		;1932
	jr c,$+40		;1935
	ld c,(hl)			;1937
	ld de,00001h		;1938
	call 0d964h		;193b
	jr c,$+31		;193e
	ld d,(hl)			;1940
	ld e,c			;1941
	inc de			;1942
	call 0d964h		;1943
	jr c,$+23		;1946
	push hl			;1948
	ld hl,(0e1feh)		;1949
	add hl,de			;194c
	ld (0e1feh),hl		;194d
	jr nc,$+9		;1950
	ld hl,(0e200h)		;1952
	inc hl			;1955
	ld (0e200h),hl		;1956
	pop hl			;1959
	jp 0d92fh		;195a
	ex af,af'			;195d
	ld a,084h		;195e
	out (0f0h),a		;1960
	ei			;1962
	ret			;1963
	add hl,de			;1964
	bit 6,h		;1965
	ret z			;1967
	ex af,af'			;1968
	inc a			;1969
	cp 090h		;196a
	jr nc,$+8		;196c
	out (0f0h),a		;196e
	ex af,af'			;1970
	res 6,h		;1971
	ret			;1973
	ex af,af'			;1974
	sbc hl,de		;1975
	scf			;1977
	ret			;1978
	bit 5,(ix+01dh)		;1979
	jp nz,0d9b5h		;197d
	ld hl,0e202h		;1980
	cp (hl)			;1983
	jp nz,0d990h		;1984
	call 0dacch		;1987
	jp z,0d9b5h		;198a
	jp 0dacah		;198d
	ld (hl),a			;1990
	ld h,000h		;1991
	ld l,a			;1993
	add hl,hl			;1994
	ld d,h			;1995
	ld e,l			;1996
	add hl,hl			;1997
	add hl,hl			;1998
	add hl,de			;1999
	ld de,00006h		;199a
	add hl,de			;199d
	ld de,0e207h		;199e
	ld bc,0000ah		;19a1
	di			;19a4
	ld a,08bh		;19a5
	out (0f0h),a		;19a7
	ldir		;19a9
	ld a,084h		;19ab
	out (0f0h),a		;19ad
	ei			;19af
	call 0dacch		;19b0
	jr nz,$+4		;19b3
	scf			;19b5
	ret			;19b6
	ld hl,(0e207h)		;19b7
	ld de,(0e203h)		;19ba
	or a			;19be
	sbc hl,de		;19bf
	ld a,h			;19c1
	or l			;19c2
	jr nz,$+16		;19c3
	ld hl,(0e209h)		;19c5
	ld de,(0e205h)		;19c8
	sbc hl,de		;19cc
	ld a,h			;19ce
	or l			;19cf
	jp z,0dacah		;19d0
	call 0dacch		;19d3
	ld (0e203h),hl		;19d6
	ld (0e205h),de		;19d9
	ex de,hl			;19dd
	ld bc,(0e200h)		;19de
	or a			;19e2
	sbc hl,bc		;19e3
	jr z,$+7		;19e5
	jr nc,$+80		;19e7
	jp 0d9f6h		;19e9
	ex de,hl			;19ec
	ld bc,(0e1feh)		;19ed
	or a			;19f1
	sbc hl,bc		;19f2
	jr nc,$+67		;19f4
	call 0dacch		;19f6
	ld a,h			;19f9
	rlca			;19fa
	rl e		;19fb
	rlca			;19fd
	rl e		;19fe
	ld a,h			;1a00
	and 03fh		;1a01
	ld h,a			;1a03
	di			;1a04
	ld a,08bh		;1a05
	add a,e			;1a07
	out (0f0h),a		;1a08
	ex af,af'			;1a0a
	ld de,0e211h		;1a0b
	ld bc,00006h		;1a0e
	call 0db08h		;1a11
	ld bc,(0e215h)		;1a14
	ld a,(0e212h)		;1a18
	bit 7,a		;1a1b
	jr z,$+17		;1a1d
	ld bc,00005h		;1a1f
	call 0db08h		;1a22
	ld bc,(0e215h)		;1a25
	dec bc			;1a29
	dec bc			;1a2a
	dec bc			;1a2b
	dec bc			;1a2c
	dec bc			;1a2d
	ld de,04000h		;1a2e
	call 0db08h		;1a31
	jp 0dab0h		;1a34
	ld a,(0e1deh)		;1a37
	ld hl,0e1f3h		;1a3a
	call 0dbfah		;1a3d
	ld e,001h		;1a40
	ld c,02ch		;1a42
	call 0d7a2h		;1a44
	ld de,00080h		;1a47
	ld c,01ah		;1a4a
	call 0d7a2h		;1a4c
	call 0dacch		;1a4f
	ld a,l			;1a52
	ld l,h			;1a53
	ld h,e			;1a54
	ld e,d			;1a55
	rlca			;1a56
	rl l		;1a57
	rl h		;1a59
	rl e		;1a5b
	ld a,e			;1a5d
	ld (0007dh),hl		;1a5e
	ld (0007fh),a		;1a61
	ld de,0005ch		;1a64
	ld c,021h		;1a67
	call 0d7a2h		;1a69
	or a			;1a6c
	jp nz,0d88eh		;1a6d
	call 0dad8h		;1a70
	ld hl,00080h		;1a73
	ld a,(0e203h)		;1a76
	and 07fh		;1a79
	ld e,a			;1a7b
	ld d,000h		;1a7c
	add hl,de			;1a7e
	ld de,0e211h		;1a7f
	ld bc,00006h		;1a82
	call 0dae7h		;1a85
	ld bc,(0e215h)		;1a88
	ld a,(0e212h)		;1a8c
	bit 7,a		;1a8f
	jr z,$+17		;1a91
	ld bc,00005h		;1a93
	call 0dae7h		;1a96
	ld bc,(0e215h)		;1a99
	dec bc			;1a9d
	dec bc			;1a9e
	dec bc			;1a9f
	dec bc			;1aa0
	dec bc			;1aa1
	ld de,04000h		;1aa2
	call 0dae7h		;1aa5
	ld de,0005ch		;1aa8
	ld c,010h		;1aab
	call 0d7a2h		;1aad
	ld bc,(0e215h)		;1ab0
	ld a,(0e212h)		;1ab4
	rlca			;1ab7
	srl a		;1ab8
	jr nc,$+8		;1aba
	ld (0e212h),a		;1abc
	call 0db1bh		;1abf
	ld a,08ah		;1ac2
	call 0dfd0h		;1ac4
	call 0dfe1h		;1ac7
	or a			;1aca
	ret			;1acb
	ld hl,(0e207h)		;1acc
	ld de,(0e209h)		;1acf
	ld a,e			;1ad3
	or d			;1ad4
	or h			;1ad5
	or l			;1ad6
	ret			;1ad7
	ld hl,(0007dh)		;1ad8
	inc hl			;1adb
	ld (0007dh),hl		;1adc
	ld a,l			;1adf
	or h			;1ae0
	ret nz			;1ae1
	ld hl,0007fh		;1ae2
	inc (hl)			;1ae5
	ret			;1ae6
	ldi		;1ae7
	push af			;1ae9
	ld a,l			;1aea
	or a			;1aeb
	jr nz,$+24		;1aec
	push de			;1aee
	push bc			;1aef
	ld de,0005ch		;1af0
	ld c,021h		;1af3
	call 0d7a2h		;1af5
	or a			;1af8
	jp nz,0d88eh		;1af9
	call 0dad8h		;1afc
	pop bc			;1aff
	pop de			;1b00
	ld hl,00080h		;1b01
	pop af			;1b04
	ret po			;1b05
	jr $-31		;1b06
	ldi		;1b08
	push af			;1b0a
	bit 6,h		;1b0b
	jr z,$+10		;1b0d
	ex af,af'			;1b0f
	inc a			;1b10
	out (0f0h),a		;1b11
	ex af,af'			;1b13
	ld hl,00000h		;1b14
	pop af			;1b17
	ret po			;1b18
	jr $-17		;1b19
	push ix		;1b1b
	ld de,(0e211h)		;1b1d
	srl d		;1b21
	rr e		;1b23
	srl d		;1b25
	rr e		;1b27
	srl d		;1b29
	rr e		;1b2b
	ld a,(0e213h)		;1b2d
	ld hl,00000h		;1b30
	ld b,008h		;1b33
	rrca			;1b35
	jr nc,$+3		;1b36
	add hl,de			;1b38
	ex de,hl			;1b39
	add hl,hl			;1b3a
	ex de,hl			;1b3b
	djnz $-7		;1b3c
	ld a,l			;1b3e
	ld (0db5ch),a		;1b3f
	ld a,h			;1b42
	ld (0db57h),a		;1b43
	ld hl,04000h		;1b46
	ld de,00000h		;1b49
	ld ix,0e217h		;1b4c
	di			;1b50
	ld a,08ah		;1b51
	out (0f0h),a		;1b53
	ld a,d			;1b55
	cp 000h		;1b56
	jr nz,$+7		;1b58
	ld a,e			;1b5a
	cp 000h		;1b5b
	jr z,$+34		;1b5d
	ld a,(hl)			;1b5f
	inc hl			;1b60
	ld (de),a			;1b61
	inc de			;1b62
	ld b,(ix+000h)		;1b63
	ex af,af'			;1b66
	ld a,b			;1b67
	ld (0db6eh),a		;1b68
	ex af,af'			;1b6b
	cp (ix+001h)		;1b6c
	jr z,$+6		;1b6f
	djnz $-11		;1b71
	jr $-30		;1b73
	ld b,(hl)			;1b75
	inc hl			;1b76
	jr $+4		;1b77
	ld (de),a			;1b79
	inc de			;1b7a
	djnz $-2		;1b7b
	jr $-40		;1b7d
	push de			;1b7f
	exx			;1b80
	push hl			;1b81
	exx			;1b82
	ld hl,(0e211h)		;1b83
	push hl			;1b86
	srl h		;1b87
	rr l		;1b89
	srl h		;1b8b
	rr l		;1b8d
	ld (0dbd9h),hl		;1b8f
	dec hl			;1b92
	ld (0dbb9h),hl		;1b93
	inc hl			;1b96
	srl h		;1b97
	rr l		;1b99
	ld a,l			;1b9b
	ld (0dbc1h),a		;1b9c
	pop hl			;1b9f
	ld de,00008h		;1ba0
	or a			;1ba3
	sbc hl,de		;1ba4
	ld (0dbe6h),hl		;1ba6
	ld hl,00000h		;1ba9
	ld a,(0e213h)		;1bac
	srl a		;1baf
	ld de,04000h		;1bb1
	push af			;1bb4
	push hl			;1bb5
	push de			;1bb6
	push hl			;1bb7
	ld bc,00000h		;1bb8
	add hl,bc			;1bbb
	ex (sp),hl			;1bbc
	exx			;1bbd
	pop hl			;1bbe
	exx			;1bbf
	ld b,000h		;1bc0
	ld a,(hl)			;1bc2
	inc hl			;1bc3
	ld (de),a			;1bc4
	inc de			;1bc5
	exx			;1bc6
	ld a,(hl)			;1bc7
	dec hl			;1bc8
	exx			;1bc9
	ld (de),a			;1bca
	inc de			;1bcb
	inc de			;1bcc
	inc de			;1bcd
	inc de			;1bce
	inc de			;1bcf
	inc de			;1bd0
	inc de			;1bd1
	djnz $-16		;1bd2
	pop de			;1bd4
	inc de			;1bd5
	inc de			;1bd6
	pop hl			;1bd7
	ld bc,00000h		;1bd8
	add hl,bc			;1bdb
	pop af			;1bdc
	dec a			;1bdd
	jr z,$+16		;1bde
	push af			;1be0
	and 003h		;1be1
	jr nz,$-46		;1be3
	ld bc,00000h		;1be5
	ex de,hl			;1be8
	add hl,bc			;1be9
	ex de,hl			;1bea
	jp 0dbb5h		;1beb
	exx			;1bee
	pop hl			;1bef
	exx			;1bf0
	ld a,084h		;1bf1
	out (0f0h),a		;1bf3
	ei			;1bf5
	pop bc			;1bf6
	pop ix		;1bf7
	ret			;1bf9
	push af			;1bfa
	ld a,(hl)			;1bfb
	ld (0e1e0h),a		;1bfc
	inc hl			;1bff
	pop af			;1c00
	ld (0e1dfh),a		;1c01
	ld de,00065h		;1c04
	ld bc,00003h		;1c07
	ldir		;1c0a
	ld hl,0c0abh		;1c0c
	ld de,0005dh		;1c0f
	ld bc,00005h		;1c12
	ldir		;1c15
	ld hl,00062h		;1c17
	ld e,064h		;1c1a
	call 0dc9fh		;1c1c
	ld e,00ah		;1c1f
	call 0dc9fh		;1c21
	add a,030h		;1c24
	ld (hl),a			;1c26
	xor a			;1c27
	ld (0007ch),a		;1c28
	ld (00068h),a		;1c2b
	ld (0005ch),a		;1c2e
	ld de,0005ch		;1c31
	ld c,00fh		;1c34
	call 0d7a2h		;1c36
	cp 0ffh		;1c39
	ret nz			;1c3b
	ld a,(0e1e0h)		;1c3c
	or a			;1c3f
	ret z			;1c40
	ld a,008h		;1c41
	call 0d4deh		;1c43
	ld hl,(0e1dfh)		;1c46
	ld h,000h		;1c49
	add hl,hl			;1c4b
	add hl,hl			;1c4c
	add hl,hl			;1c4d
	ld de,(0e1e0h)		;1c4e
	ld d,000h		;1c52
	add hl,de			;1c54
	ld de,0c0b3h		;1c55
	add hl,de			;1c58
	push hl			;1c59
	ld hl,0dcaah		;1c5a
	call 0d7aah		;1c5d
	ld a,(0c0b1h)		;1c60
	cp 032h		;1c63
	jr c,$+13		;1c65
	pop hl			;1c67
	push hl			;1c68
	ld a,(hl)			;1c69
	call 0d590h		;1c6a
	ld a,020h		;1c6d
	call 0d590h		;1c6f
	ld hl,0dcbdh		;1c72
	call 0d7aah		;1c75
	pop hl			;1c78
	ld a,(0c0b2h)		;1c79
	cp 032h		;1c7c
	jr c,$+13		;1c7e
	inc hl			;1c80
	ld a,(hl)			;1c81
	ld (0dcd5h),a		;1c82
	ld hl,0dcc9h		;1c85
	call 0d7aah		;1c88
	ld hl,0dce8h		;1c8b
	call 0d7aah		;1c8e
	call 0d45ch		;1c91
	ld a,(0e2c8h)		;1c94
	call 0d4a6h		;1c97
	call 0d4cfh		;1c9a
	jr $-108		;1c9d
	ld d,030h		;1c9f
	inc d			;1ca1
	sub e			;1ca2
	jr nc,$-2		;1ca3
	add a,e			;1ca5
	dec d			;1ca6
	ld (hl),d			;1ca7
	inc hl			;1ca8
	ret			;1ca9
	dec c			;1caa
	ld c,c			;1cab
	ld l,(hl)			;1cac
	ld (hl),e			;1cad
	ld h,l			;1cae
	ld (hl),d			;1caf
	ld (hl),h			;1cb0
	ld h,c			;1cb1
	jr nz,$+103		;1cb2
	ld l,h			;1cb4
	jr nz,$+102		;1cb5
	ld l,c			;1cb7
	ld (hl),e			;1cb8
	ld h,e			;1cb9
	ld l,a			;1cba
	jr nz,$+38		;1cbb
	ld h,h			;1cbd
	ld h,l			;1cbe
	ld l,h			;1cbf
	jr nz,$+108		;1cc0
	ld (hl),l			;1cc2
	ld h,l			;1cc3
	ld h,a			;1cc4
	ld l,a			;1cc5
	inc l			;1cc6
	jr nz,$+38		;1cc7
	ld (hl),b			;1cc9
	ld l,a			;1cca
	ld (hl),d			;1ccb
	jr nz,$+110		;1ccc
	ld h,c			;1cce
	jr nz,$+101		;1ccf
	ld h,c			;1cd1
	ld (hl),d			;1cd2
	ld h,c			;1cd3
	jr nz,$+50		;1cd4
	jr nz,$+99		;1cd6
	jr nz,$+118		;1cd8
	ld (hl),l			;1cda
	jr nz,$+107		;1cdb
	ld a,d			;1cdd
	ld (hl),c			;1cde
	ld (hl),l			;1cdf
	ld l,c			;1ce0
	ld h,l			;1ce1
	ld (hl),d			;1ce2
	ld h,h			;1ce3
	ld h,c			;1ce4
	inc l			;1ce5
	jr nz,$+38		;1ce6
	ld l,h			;1ce8
	ld (hl),l			;1ce9
	ld h,l			;1cea
	ld h,a			;1ceb
	ld l,a			;1cec
	jr nz,$+114		;1ced
	ld (hl),l			;1cef
	ld l,h			;1cf0
	ld (hl),e			;1cf1
	ld h,c			;1cf2
	jr nz,$+84		;1cf3
	ld b,l			;1cf5
	ld d,h			;1cf6
	ld d,l			;1cf7
	ld d,d			;1cf8
	ld c,(hl)			;1cf9
	inc h			;1cfa
	dec c			;1cfb
	ld b,(hl)			;1cfc
	ld l,c			;1cfd
	ld h,e			;1cfe
	ld l,b			;1cff
	ld h,l			;1d00
	ld (hl),d			;1d01
	ld l,a			;1d02
	jr nz,$+102		;1d03
	ld h,l			;1d05
	ld l,l			;1d06
	ld h,c			;1d07
	ld (hl),e			;1d08
	ld l,c			;1d09
	ld h,c			;1d0a
	ld h,h			;1d0b
	ld l,a			;1d0c
	jr nz,$+105		;1d0d
	ld (hl),d			;1d0f
	ld h,c			;1d10
	ld l,(hl)			;1d11
	ld h,h			;1d12
	ld h,l			;1d13
	ld l,020h		;1d14
	inc h			;1d16
	dec c			;1d17
	ld b,(hl)			;1d18
	ld l,c			;1d19
	ld h,e			;1d1a
	ld l,b			;1d1b
	ld h,l			;1d1c
	ld (hl),d			;1d1d
	ld l,a			;1d1e
	jr nz,$+102		;1d1f
	ld h,c			;1d21
	ld a,(de)			;1d22
	ld h,c			;1d23
	ld h,h			;1d24
	ld l,a			;1d25
	jr nz,$+113		;1d26
	jr nz,$+107		;1d28
	ld l,(hl)			;1d2a
	ld h,e			;1d2b
	ld l,a			;1d2c
	ld l,l			;1d2d
	ld (hl),b			;1d2e
	ld h,c			;1d2f
	ld (hl),h			;1d30
	ld l,c			;1d31
	ld h,d			;1d32
	ld l,h			;1d33
	ld h,l			;1d34
	jr nz,$+101		;1d35
	ld l,a			;1d37
	ld l,(hl)			;1d38
	jr nz,$+103		;1d39
	ld (hl),e			;1d3b
	ld (hl),h			;1d3c
	ld h,l			;1d3d
	jr nz,$+114		;1d3e
	ld (hl),d			;1d40
	ld l,a			;1d41
	ld h,a			;1d42
	ld (hl),d			;1d43
	ld h,c			;1d44
	ld l,l			;1d45
	ld h,c			;1d46
	ld l,024h		;1d47
	jp 00000h		;1d49
	ld a,(0e2c9h)		;1d4c
	ld (0e2cch),a		;1d4f
	call 0dfa1h		;1d52
	ld (0e2c3h),hl		;1d55
	ld a,(0e2cah)		;1d58
	ld (0e2cbh),a		;1d5b
	ld a,(0e2c8h)		;1d5e
	ld (0e2c5h),a		;1d61
	ret			;1d64
	push hl			;1d65
	push de			;1d66
	push bc			;1d67
	push af			;1d68
	call 0dd82h		;1d69
	pop af			;1d6c
	cp 008h		;1d6d
	call z,0dd94h		;1d6f
	cp 00dh		;1d72
	call z,0ddbfh		;1d74
	or a			;1d77
	call nz,0ddf2h		;1d78
	call 0dd82h		;1d7b
	pop bc			;1d7e
	pop de			;1d7f
	pop hl			;1d80
	ret			;1d81
	bit 3,(ix-00ah)		;1d82
	ret z			;1d86
	set 4,(ix-00ah)		;1d87
	ld a,05fh		;1d8b
	call 0ddf2h		;1d8d
	res 4,(ix-00ah)		;1d90
	ld hl,0e2cbh		;1d94
	ld a,(0e2cah)		;1d97
	cp (hl)			;1d9a
	jr z,$+5		;1d9b
	dec (hl)			;1d9d
	jr $+31		;1d9e
	ld hl,0e2c9h		;1da0
	ld a,(0e2cch)		;1da3
	cp (hl)			;1da6
	jr z,$+22		;1da7
	dec a			;1da9
	ld (0e2cch),a		;1daa
	call 0dfa1h		;1dad
	ld (0e2c3h),hl		;1db0
	ld a,(0e2cah)		;1db3
	add a,(ix-010h)		;1db6
	dec a			;1db9
	ld (0e2cbh),a		;1dba
	xor a			;1dbd
	ret			;1dbe
	ld a,(0e2cah)		;1dbf
	add a,(ix-010h)		;1dc2
	cp (ix-00ch)		;1dc5
	jr z,$+9		;1dc8
	ld a,020h		;1dca
	call 0ddf2h		;1dcc
	jr $-16		;1dcf
	ld hl,0e2cch		;1dd1
	ld a,(0e2c9h)		;1dd4
	add a,(ix-00fh)		;1dd7
	cp (hl)			;1dda
	jr z,$+12		;1ddb
	inc (hl)			;1ddd
	ld a,(hl)			;1dde
	call 0dfa1h		;1ddf
	ld (0e2c3h),hl		;1de2
	jr $+5		;1de5
	call 0de58h		;1de7
	ld a,(0e2cah)		;1dea
	ld (0e2cbh),a		;1ded
	xor a			;1df0
	ret			;1df1
	push af			;1df2
	ld a,(0e2cah)		;1df3
	add a,(ix-010h)		;1df6
	cp (ix-00ch)		;1df9
	jp nz,0de02h		;1dfc
	call 0ddbfh		;1dff
	ld a,(0e2cbh)		;1e02
	ld l,a			;1e05
	ld h,000h		;1e06
	add hl,hl			;1e08
	add hl,hl			;1e09
	add hl,hl			;1e0a
	ld de,(0e2c3h)		;1e0b
	add hl,de			;1e0f
	ex (sp),hl			;1e10
	push hl			;1e11
	ld b,0ffh		;1e12
	bit 4,(ix-00ah)		;1e14
	jr nz,$+3		;1e18
	inc b			;1e1a
	ld de,0b800h		;1e1b
	pop af			;1e1e
	ld l,a			;1e1f
	ld h,000h		;1e20
	add hl,hl			;1e22
	add hl,hl			;1e23
	add hl,hl			;1e24
	add hl,de			;1e25
	pop de			;1e26
	ld a,008h		;1e27
	ex af,af'			;1e29
	ld a,(hl)			;1e2a
	xor (ix-015h)		;1e2b
	ld c,a			;1e2e
	ld a,(de)			;1e2f
	and b			;1e30
	xor c			;1e31
	ld (de),a			;1e32
	inc de			;1e33
	inc hl			;1e34
	ex af,af'			;1e35
	dec a			;1e36
	jr nz,$-14		;1e37
	ld hl,0e2cbh		;1e39
	inc (hl)			;1e3c
	ret			;1e3d
	push hl			;1e3e
	bit 3,(ix-00ah)		;1e3f
	jr nz,$+19		;1e43
	set 3,(ix-00ah)		;1e45
	call 0dd87h		;1e49
	jr $+10		;1e4c
	push hl			;1e4e
	call 0dd82h		;1e4f
	res 3,(ix-00ah)		;1e52
	pop hl			;1e56
	ret			;1e57
	ld a,(0e2c8h)		;1e58
	ld c,(ix-00eh)		;1e5b
	or a			;1e5e
	jr z,$+34		;1e5f
	ld b,a			;1e61
	ld a,(0e2c7h)		;1e62
	ld l,a			;1e65
	ld h,000h		;1e66
	add hl,hl			;1e68
	add hl,hl			;1e69
	add hl,hl			;1e6a
	push bc			;1e6b
	push hl			;1e6c
	ld a,c			;1e6d
	call 0deadh		;1e6e
	push hl			;1e71
	ld a,c			;1e72
	inc a			;1e73
	call 0deadh		;1e74
	pop de			;1e77
	pop bc			;1e78
	push bc			;1e79
	ldir		;1e7a
	pop hl			;1e7c
	pop bc			;1e7d
	inc c			;1e7e
	djnz $-20		;1e7f
	ld b,001h		;1e81
	jr $+9		;1e83
	ld b,(ix-00fh)		;1e85
	inc b			;1e88
	ld c,(ix-00eh)		;1e89
	ld a,(0e2c7h)		;1e8c
	ld l,a			;1e8f
	ld h,000h		;1e90
	add hl,hl			;1e92
	add hl,hl			;1e93
	add hl,hl			;1e94
	dec hl			;1e95
	push bc			;1e96
	push hl			;1e97
	ld a,c			;1e98
	call 0deadh		;1e99
	ld e,l			;1e9c
	ld d,h			;1e9d
	inc de			;1e9e
	pop bc			;1e9f
	push bc			;1ea0
	ld a,(0e2c2h)		;1ea1
	ld (hl),a			;1ea4
	ldir		;1ea5
	pop hl			;1ea7
	pop bc			;1ea8
	inc c			;1ea9
	djnz $-20		;1eaa
	ret			;1eac
	call 0dfa1h		;1ead
	ex de,hl			;1eb0
	ld a,(0e2cah)		;1eb1
	ld l,a			;1eb4
	ld h,000h		;1eb5
	add hl,hl			;1eb7
	add hl,hl			;1eb8
	add hl,hl			;1eb9
	add hl,de			;1eba
	ret			;1ebb
	ld a,(0e20bh)		;1ebc
	ld hl,(0e2c9h)		;1ebf
	bit 0,a		;1ec2
	jr nz,$+38		;1ec4
	ld hl,(0e20fh)		;1ec6
	srl h		;1ec9
	rr l		;1ecb
	srl h		;1ecd
	rr l		;1ecf
	srl h		;1ed1
	rr l		;1ed3
	ld e,l			;1ed5
	ld hl,(0e20dh)		;1ed6
	srl h		;1ed9
	rr l		;1edb
	srl h		;1edd
	rr l		;1edf
	srl h		;1ee1
	rr l		;1ee3
	ld d,l			;1ee5
	ex de,hl			;1ee6
	ld (0e2c9h),hl		;1ee7
	ld (ix-00ch),h		;1eea
	ld a,l			;1eed
	ld (0e2cch),a		;1eee
	call 0deadh		;1ef1
	ld (0e2c3h),hl		;1ef4
	ld hl,(0e211h)		;1ef7
	srl h		;1efa
	rr l		;1efc
	srl h		;1efe
	rr l		;1f00
	srl h		;1f02
	rr l		;1f04
	ld e,l			;1f06
	ld hl,(0e213h)		;1f07
	srl h		;1f0a
	rr l		;1f0c
	srl h		;1f0e
	rr l		;1f10
	srl h		;1f12
	rr l		;1f14
	dec l			;1f16
	ld d,l			;1f17
	ld bc,00000h		;1f18
	ld a,(0e20bh)		;1f1b
	bit 0,a		;1f1e
	jr z,$+43		;1f20
	ld a,d			;1f22
	cp (ix-00fh)		;1f23
	jr c,$+7		;1f26
	jr z,$+5		;1f28
	ld d,(ix-00fh)		;1f2a
	ld a,e			;1f2d
	cp (ix-010h)		;1f2e
	jr z,$+30		;1f31
	jr c,$+28		;1f33
	sub (ix-010h)		;1f35
	sla a		;1f38
	rl b		;1f3a
	sla a		;1f3c
	rl b		;1f3e
	sla a		;1f40
	rl b		;1f42
	ld c,a			;1f44
	ld e,(ix-010h)		;1f45
	jp 0df4fh		;1f48
	ld (0e2c7h),de		;1f4b
	ld a,(0e2c8h)		;1f4f
	ld (0e2c5h),a		;1f52
	bit 7,(ix-004h)		;1f55
	jr z,$+5		;1f59
	jp 0de85h		;1f5b
	ld hl,(0e2cah)		;1f5e
	ld h,000h		;1f61
	add hl,hl			;1f63
	add hl,hl			;1f64
	add hl,hl			;1f65
	ex de,hl			;1f66
	ld a,(0e2c9h)		;1f67
	add a,h			;1f6a
	inc a			;1f6b
	ld (0df98h),a		;1f6c
	ld h,000h		;1f6f
	add hl,hl			;1f71
	add hl,hl			;1f72
	add hl,hl			;1f73
	ld (0df8eh),hl		;1f74
	di			;1f77
	ld a,08ah		;1f78
	out (0f0h),a		;1f7a
	ld a,(0e2c9h)		;1f7c
	ld hl,00000h		;1f7f
	push af			;1f82
	push hl			;1f83
	push de			;1f84
	call 0dfa1h		;1f85
	pop de			;1f88
	add hl,de			;1f89
	ex de,hl			;1f8a
	ex (sp),hl			;1f8b
	push bc			;1f8c
	ld bc,00000h		;1f8d
	ldir		;1f90
	pop bc			;1f92
	add hl,bc			;1f93
	pop de			;1f94
	pop af			;1f95
	inc a			;1f96
	cp 000h		;1f97
	jr nz,$-23		;1f99
	ld a,080h		;1f9b
	out (0f0h),a		;1f9d
	ei			;1f9f
	ret			;1fa0
	ld l,a			;1fa1
	ld h,000h		;1fa2
	add hl,hl			;1fa4
	add hl,hl			;1fa5
	add hl,hl			;1fa6
	add hl,hl			;1fa7
	ld de,0b600h		;1fa8
	add hl,de			;1fab
	ld e,(hl)			;1fac
	inc hl			;1fad
	ld d,(hl)			;1fae
	ld a,e			;1faf
	ex de,hl			;1fb0
	add hl,hl			;1fb1
	and 007h		;1fb2
	or l			;1fb4
	ld l,a			;1fb5
	ret			;1fb6
	jp 00000h		;1fb7
	ld hl,00180h		;1fba
	ld de,0b800h		;1fbd
	ld bc,00800h		;1fc0
	di			;1fc3
	ld a,084h		;1fc4
	out (0f0h),a		;1fc6
	ldir		;1fc8
	ld a,080h		;1fca
	out (0f0h),a		;1fcc
	ei			;1fce
	ret			;1fcf
	ld de,00000h		;1fd0
	ld hl,04000h		;1fd3
	di			;1fd6
	out (0f0h),a		;1fd7
	ldir		;1fd9
	ld a,084h		;1fdb
	out (0f0h),a		;1fdd
	ei			;1fdf
	ret			;1fe0
	ld a,089h		;1fe1
	ld hl,00000h		;1fe3
	ld de,04000h		;1fe6
	ld bc,04000h		;1fe9
	jr $-22		;1fec
	ld sp,0ead7h		;1fee
	ld hl,(00001h)		;1ff1
	push hl			;1ff4
	ld de,00057h		;1ff5
	add hl,de			;1ff8
	ld (0dd4ah),hl		;1ff9
	pop hl			;1ffc
	ld de,0004eh		;1ffd
	add hl,de			;2000
	ld (0dfb8h),hl		;2001
	ld a,r		;2004
	ld (0e1dch),a		;2006
	xor a			;2009
	ld bc,0dfa1h		;200a
	call 0dd49h		;200d
	jp (hl)			;2010
	nop			;2011
	ld (0e221h),hl		;2012
	ld a,01fh		;2015
	ld bc,0dfa1h		;2017
	call 0dd49h		;201a
	jp (hl)			;201d
	nop			;201e
	ld (0e2b1h),hl		;201f
	ld ix,0e2d7h		;2022
	ld e,011h		;2026
	ld c,02ch		;2028
	call 0d7a2h		;202a
	ld a,001h		;202d
	ld hl,0e1ebh		;202f
	call 0dbfah		;2032
	jr nz,$+41		;2035
	ld c,009h		;2037
	ld de,0e040h		;2039
	call 00005h		;203c
	rst 0			;203f
	ld b,(hl)			;2040
	ld h,c			;2041
	ld l,h			;2042
	ld (hl),h			;2043
	ld h,c			;2044
	jr nz,$+103		;2045
	ld l,h			;2047
	jr nz,$+108		;2048
	ld (hl),l			;204a
	ld h,l			;204b
	ld h,a			;204c
	ld l,a			;204d
	jr nz,$+102		;204e
	ld h,l			;2050
	jr nz,$+101		;2051
	ld h,c			;2053
	ld (hl),d			;2054
	ld h,c			;2055
	ld h,e			;2056
	ld (hl),h			;2057
	ld h,l			;2058
	ld (hl),d			;2059
	ld (hl),e			;205a
	ld l,00dh		;205b
	inc h			;205d
	ld de,00100h		;205e
	ld c,01ah		;2061
	call 0d7a2h		;2063
	ld de,0005ch		;2066
	ld c,014h		;2069
	call 0d7a2h		;206b
	or a			;206e
	jr nz,$-56		;206f
	ld de,0005ch		;2071
	ld c,010h		;2074
	call 0d7a2h		;2076
	ld bc,0dfbah		;2079
	call 0dd49h		;207c
	jp (hl)			;207f
	nop			;2080
	ld e,07dh		;2081
	ld c,02ch		;2083
	call 0d7a2h		;2085
	xor a			;2088
	ld hl,0e1e3h		;2089
	call 0dbfah		;208c
	jr z,$+68		;208f
	ld de,00100h		;2091
	ld c,01ah		;2094
	call 0d7a2h		;2096
	ld de,0005ch		;2099
	ld c,014h		;209c
	call 0d7a2h		;209e
	or a			;20a1
	jp nz,0d88eh		;20a2
	ld de,0005ch		;20a5
	ld c,010h		;20a8
	call 0d7a2h		;20aa
	ld bc,0e11ah		;20ad
	call 0dd49h		;20b0
	jp (hl)			;20b3
	nop			;20b4
	ld bc,0e0f4h		;20b5
	call 0dd49h		;20b8
	jp (hl)			;20bb
	nop			;20bc
	call 0e130h		;20bd
	jr c,$+14		;20c0
	push af			;20c2
	ld bc,0e0f4h		;20c3
	call 0dd49h		;20c6
	jp (hl)			;20c9
	nop			;20ca
	pop af			;20cb
	jr $+34		;20cc
	call 0d45ch		;20ce
	jr $+27		;20d1
	ld bc,0e11ah		;20d3
	call 0dd49h		;20d6
	jp (hl)			;20d9
	nop			;20da
	call 0e130h		;20db
	jr c,$+14		;20de
	push af			;20e0
	ld bc,0e11ah		;20e1
	call 0dd49h		;20e4
	jp (hl)			;20e7
	nop			;20e8
	pop af			;20e9
	jr $+4		;20ea
	ld a,001h		;20ec
	call 0d7b4h		;20ee
	jp 0c132h		;20f1
	di			;20f4
	ld a,084h		;20f5
	out (0f0h),a		;20f7
	ld hl,00100h		;20f9
	ld a,003h		;20fc
	push af			;20fe
	push hl			;20ff
	call 0dfa1h		;2100
	ld de,00028h		;2103
	add hl,de			;2106
	ex de,hl			;2107
	pop hl			;2108
	ld bc,00280h		;2109
	ldir		;210c
	pop af			;210e
	inc a			;210f
	cp 01ch		;2110
	jr nz,$-20		;2112
	ld a,080h		;2114
	out (0f0h),a		;2116
	ei			;2118
	ret			;2119
	xor a			;211a
	push af			;211b
	call 0dfa1h		;211c
	ld e,l			;211f
	ld d,h			;2120
	inc de			;2121
	ld bc,002cfh		;2122
	ld (hl),000h		;2125
	ldir		;2127
	pop af			;2129
	inc a			;212a
	cp 020h		;212b
	jr nz,$-18		;212d
	ret			;212f
	ld a,(0c0b0h)		;2130
	cp 032h		;2133
	ret c			;2135
	ld (0e192h),a		;2136
	ld a,(0e2c9h)		;2139
	ld bc,0dfa1h		;213c
	call 0dd49h		;213f
	jp (hl)			;2142
	nop			;2143
	ld (0e2c3h),hl		;2144
	ld bc,0de85h		;2147
	call 0dd49h		;214a
	jp (hl)			;214d
	nop			;214e
	ld hl,0e16ah		;214f
	call 0d7aah		;2152
	call 0d6f1h		;2155
	call 0d45ch		;2158
	cp 031h		;215b
	jr c,$-5		;215d
	ld hl,0e192h		;215f
	cp (hl)			;2162
	jr z,$+4		;2163
	jr nc,$-13		;2165
	sub 030h		;2167
	ret			;2169
	dec c			;216a
	jr nz,$+34		;216b
	jr nz,$+20		;216d
	ld d,c			;216f
	ld (hl),l			;2170
	ld d,020h		;2171
	ld (hl),b			;2173
	ld h,c			;2174
	ld (hl),d			;2175
	ld (hl),h			;2176
	ld h,l			;2177
	jr nz,$+115		;2178
	ld (hl),l			;217a
	ld l,c			;217b
	ld h,l			;217c
	ld (hl),d			;217d
	ld h,l			;217e
	ld (hl),e			;217f
	dec c			;2180
	dec c			;2181
	jr nz,$+34		;2182
	jr nz,$+34		;2184
	jr nz,$+101		;2186
	ld h,c			;2188
	ld (hl),d			;2189
	ld h,a			;218a
	ld h,c			;218b
	ld (hl),d			;218c
	ccf			;218d
	jr nz,$+42		;218e
	ld sp,0322dh		;2190
	add hl,hl			;2193
	ccf			;2194
	inc h			;2195
	xor 0dfh		;2196
	nop			;2198
	rst 38h			;2199
	rst 38h			;219a
	rst 38h			;219b
	rst 38h			;219c
	rst 38h			;219d
	nop			;219e
	nop			;219f
	nop			;21a0
	rst 38h			;21a1
	rst 38h			;21a2
	rst 38h			;21a3
	rst 38h			;21a4
	rst 38h			;21a5
	rst 38h			;21a6
	rst 38h			;21a7
	rst 38h			;21a8
	rst 38h			;21a9
	rst 38h			;21aa
	rst 38h			;21ab
	rst 38h			;21ac
	rst 38h			;21ad
	rst 38h			;21ae
	rst 38h			;21af
	rst 38h			;21b0
	rst 38h			;21b1
	rst 38h			;21b2
	rst 38h			;21b3
	rst 38h			;21b4
	rst 38h			;21b5
	rst 38h			;21b6
	rst 38h			;21b7
	rst 38h			;21b8
	rst 38h			;21b9
	rst 38h			;21ba
	rst 38h			;21bb
	rst 38h			;21bc
	rst 38h			;21bd
	rst 38h			;21be
	rst 38h			;21bf
	rst 38h			;21c0
	rst 38h			;21c1
	rst 38h			;21c2
	rst 38h			;21c3
	rst 38h			;21c4
	rst 38h			;21c5
	rst 38h			;21c6
	rst 38h			;21c7
	rst 38h			;21c8
	and c			;21c9
	pop hl			;21ca
	nop			;21cb
	nop			;21cc
	nop			;21cd
	nop			;21ce
	nop			;21cf
	nop			;21d0
	nop			;21d1
	nop			;21d2
	nop			;21d3
	nop			;21d4
	nop			;21d5
	nop			;21d6
	nop			;21d7
	nop			;21d8
	nop			;21d9
	nop			;21da
	nop			;21db
	nop			;21dc
	nop			;21dd
	ld bc,00000h		;21de
	nop			;21e1
	nop			;21e2
	nop			;21e3
	ld d,b			;21e4
	ld c,c			;21e5
	ld b,e			;21e6
	ld (bc),a			;21e7
	ld b,e			;21e8
	ld c,b			;21e9
	ld d,d			;21ea
	nop			;21eb
	ld b,e			;21ec
	ld c,b			;21ed
	ld d,d			;21ee
	inc b			;21ef
	ld b,h			;21f0
	ld b,h			;21f1
	ld b,d			;21f2
	ld b,044h		;21f3
	ld b,c			;21f5
	ld d,h			;21f6
	ld b,c			;21f7
	ld b,a			;21f8
	ld d,b			;21f9
	inc h			;21fa
	inc h			;21fb
	inc h			;21fc
	nop			;21fd
	nop			;21fe
	nop			;21ff
	nop			;2200
	nop			;2201
	nop			;2202
	nop			;2203
	nop			;2204
	nop			;2205
	nop			;2206
	nop			;2207
	nop			;2208
	nop			;2209
	nop			;220a
	nop			;220b
	nop			;220c
	nop			;220d
	nop			;220e
	nop			;220f
	nop			;2210
	nop			;2211
	nop			;2212
	nop			;2213
	nop			;2214
	nop			;2215
	nop			;2216
	nop			;2217
	nop			;2218
	nop			;2219
	nop			;221a
	nop			;221b
	nop			;221c
	nop			;221d
	nop			;221e
	nop			;221f
	nop			;2220
	nop			;2221
	ld b,b			;2222
	rra			;2223
	ld e,d			;2224
	ld e,d			;2225
	rra			;2226
	nop			;2227
	nop			;2228
	nop			;2229
	nop			;222a
	ld b,b			;222b
	nop			;222c
	nop			;222d
	nop			;222e
	nop			;222f
	nop			;2230
	nop			;2231
	ld b,b			;2232
	rra			;2233
	ld e,d			;2234
	ld e,d			;2235
	rra			;2236
	nop			;2237
	nop			;2238
	nop			;2239
	nop			;223a
	ld b,b			;223b
	nop			;223c
	nop			;223d
	nop			;223e
	nop			;223f
	nop			;2240
	nop			;2241
	ld b,b			;2242
	rra			;2243
	ld e,d			;2244
	ld e,d			;2245
	rra			;2246
	nop			;2247
	nop			;2248
	nop			;2249
	nop			;224a
	ld b,b			;224b
	nop			;224c
	nop			;224d
	nop			;224e
	nop			;224f
	nop			;2250
	nop			;2251
	ld b,b			;2252
	rra			;2253
	ld e,d			;2254
	ld e,d			;2255
	rra			;2256
	nop			;2257
	nop			;2258
	nop			;2259
	nop			;225a
	ld b,b			;225b
	nop			;225c
	nop			;225d
	nop			;225e
	nop			;225f
	nop			;2260
	nop			;2261
	ld b,b			;2262
	rra			;2263
	ld e,d			;2264
	ld e,d			;2265
	rra			;2266
	nop			;2267
	nop			;2268
	nop			;2269
	nop			;226a
	ld b,b			;226b
	nop			;226c
	nop			;226d
	nop			;226e
	nop			;226f
	nop			;2270
	nop			;2271
	ld b,b			;2272
	rra			;2273
	ld e,d			;2274
	ld e,d			;2275
	rra			;2276
	nop			;2277
	nop			;2278
	nop			;2279
	nop			;227a
	ld b,b			;227b
	nop			;227c
	nop			;227d
	nop			;227e
	nop			;227f
	nop			;2280
	nop			;2281
	ld b,b			;2282
	rra			;2283
	ld e,d			;2284
	ld e,d			;2285
	rra			;2286
	nop			;2287
	nop			;2288
	nop			;2289
	nop			;228a
	ld b,b			;228b
	nop			;228c
	nop			;228d
	nop			;228e
	nop			;228f
	nop			;2290
	nop			;2291
	ld b,b			;2292
	rra			;2293
	ld e,d			;2294
	ld e,d			;2295
	rra			;2296
	nop			;2297
	nop			;2298
	nop			;2299
	nop			;229a
	ld b,b			;229b
	nop			;229c
	nop			;229d
	nop			;229e
	nop			;229f
	nop			;22a0
	nop			;22a1
	ld b,b			;22a2
	rra			;22a3
	ld e,d			;22a4
	ld e,d			;22a5
	rra			;22a6
	nop			;22a7
	nop			;22a8
	nop			;22a9
	nop			;22aa
	ld b,b			;22ab
	nop			;22ac
	nop			;22ad
	nop			;22ae
	nop			;22af
	rst 38h			;22b0
	nop			;22b1
	ld b,b			;22b2
	nop			;22b3
	nop			;22b4
	ld e,d			;22b5
	nop			;22b6
	rra			;22b7
	nop			;22b8
	nop			;22b9
	rra			;22ba
	ld (bc),a			;22bb
	nop			;22bc
	nop			;22bd
	rst 38h			;22be
	rst 38h			;22bf
	nop			;22c0
	nop			;22c1
	rst 38h			;22c2
	nop			;22c3
	nop			;22c4
	dec b			;22c5
	nop			;22c6
	jr $+7		;22c7
	inc c			;22c9
	ld hl,00c21h		;22ca
	ld b,d			;22cd
	nop			;22ce
	nop			;22cf
	ld a,b			;22d0
	nop			;22d1
	nop			;22d2
	nop			;22d3
	nop			;22d4
	nop			;22d5
	nop			;22d6

[stderr]
Warning: Code might not be 8080 compatible!
